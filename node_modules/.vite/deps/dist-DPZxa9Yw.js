import { r as __export } from "./chunk-CBgdiQcn.js";
import { Fi as decodeErrorResult, do as BaseError, sa as getAddress } from "./ccip-D4p2e5rB.js";
import { J as getSelector$1, K as zeroAddress, Kn as waitForCallsStatus, On as getEip712Domain, U as verifyHash, Un as createClient, Yr as getContractError, Yt as hashTypedData, _ as fallback, b as createTransport, h as http, m as webSocket, mn as defineChain, q as from$12, rn as hashMessage, sr as withCache } from "./_esm-CRgaECpT.js";
import "./utils-Dngmz2iM.js";
import "./sha3-BaTSXYLF.js";
import "./hmac-C6LUpY3l.js";
import "./ripemd160-CjKhWecw.js";
import "./utils-DTJIMQ4r.js";
import "./secp256k1-BcZDTLZ3.js";
import { n as import_eventemitter3 } from "./eventemitter3-Dkd_1R2c.js";
import { n as toAccount } from "./accounts-CykT4LI_.js";
import "./esm-BYcMNodf.js";
import { i as get, n as createStore$1, o as set, r as del } from "./dist-BAiCoMBQ.js";
import { t as announceProvider } from "./utils-CwSQ_cAc.js";
import { C as baseSepolia, D as arbitrumSepolia, O as arbitrum, S as berachain, _ as gnosis, b as bsc, c as polygon, d as optimism, f as mainnet, g as hoodi, i as sepolia, k as anvil, p as katana, u as optimismSepolia, w as base, x as berachainBepolia, y as celo } from "./chains-c39jFqiu.js";
import { $ as optional, A as fromHex$1, At as slice, B as any, C as getSignPayload$2, Ct as toHex$1, D as assert$1, Dt as fromNumber, E as create$2, Et as fromBytes, F as recoverAddress, Ft as parse$1, G as date, H as bigint$1, I as sign$1, It as stringify, J as nullable, K as discriminatedUnion, M as toTuple$1, Mt as toNumber, N as getPublicKey, Nt as validate$1, O as extract, Ot as fromString, P as randomPrivateKey, Pt as BaseError$2, Q as omit, R as _null, S as getSelector$2, St as fromHex$2, T as from$17, Tt as from$16, U as boolean, V as array, W as codec, X as number$1, Y as nullish, Z as object, _ as encodeData, _t as isEqual, a as createWebCryptoP256, at as templateLiteral, b as from$13, bt as keccak256, c as fromWebAuthnP256, ct as unknown, d as sign$2, dt as _regex, et as partial, f as toRelay$1, ft as decode, g as from$19, gt as fromPublicKey, h as sign$3, ht as from$15, i as createWebAuthnP256, it as string, j as toHex, jt as toBigInt, k as from$18, kt as size, l as resolvePermissions, lt as _gte, mt as safeParse, n as createHeadlessWebAuthnP256, nt as readonly, o as from$14, ot as tuple, p as wrapSignature, pt as encode$1, q as literal, r as createP256, rt as record, s as fromRelay, st as union, t as Key_exports, tt as pick, u as serializeWebAuthnSignature, ut as _minLength, v as fromAbi$2, vt as validate$2, w as encode$2, wt as concat, x as fromAbi$1, xt as from$20, y as NotFoundError, yt as fromHex$3, z as _undefined } from "./Key-qH_k6-uB.js";

//#region node_modules/viem/_esm/experimental/erc7821/errors.js
var FunctionSelectorNotRecognizedError = class extends BaseError {
	constructor() {
		super("Function is not recognized.", {
			metaMessages: [
				"This could be due to any of the following:",
				"  - The contract does not have the function,",
				"  - The address is not a contract."
			],
			name: "FunctionSelectorNotRecognizedError"
		});
	}
};

//#endregion
//#region node_modules/viem/node_modules/ox/_esm/core/AbiError.js
/**
* Parses an arbitrary **JSON ABI Error** or **Human Readable ABI Error** into a typed {@link ox#AbiError.AbiError}.
*
* @example
* ### JSON ABIs
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from({
*   inputs: [{ name: 'v', type: 'uint8' }],
*   name: 'BadSignatureV',
*   type: 'error',
* })
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
* @example
* ### Human Readable ABIs
*
* A Human Readable ABI can be parsed into a typed ABI object:
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from(
*   'error BadSignatureV(uint8 v)' // [!code hl]
* )
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
* @example
* It is possible to specify `struct`s along with your definitions:
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from([
*   'struct Signature { uint8 v; }', // [!code hl]
*   'error BadSignatureV(Signature signature)',
* ])
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
*
*
* @param abiError - The ABI Error to parse.
* @returns Typed ABI Error.
*/
function from$11(abiError, options = {}) {
	return from$12(abiError, options);
}
/**
* Computes the [4-byte selector](https://solidity-by-example.org/function-selector/) for an {@link ox#AbiError.AbiError}.
*
* @example
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const selector = AbiError.getSelector('error BadSignatureV(uint8 v)')
* // @log: '0x6352211e'
* ```
*
* @example
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const selector = AbiError.getSelector({
*   inputs: [{ name: 'v', type: 'uint8' }],
*   name: 'BadSignatureV',
*   type: 'error'
* })
* // @log: '0x6352211e'
* ```
*
* @param abiItem - The ABI item to compute the selector for.
* @returns The first 4 bytes of the {@link ox#Hash.(keccak256:function)} hash of the error signature.
*/
function getSelector(abiItem) {
	return getSelector$1(abiItem);
}

//#endregion
//#region node_modules/viem/_esm/experimental/erc7821/utils/getExecuteError.js
function getExecuteError(e, parameters) {
	const error = e.walk((e$1) => "data" in e$1);
	if (!error?.data) return e;
	if (error.data === getSelector(from$11("error FnSelectorNotRecognized()"))) return new FunctionSelectorNotRecognizedError();
	let matched = null;
	for (const c of parameters.calls) {
		const call$1 = c;
		if (!call$1.abi) continue;
		try {
			if (!Boolean(decodeErrorResult({
				abi: call$1.abi,
				data: error.data
			}))) continue;
			matched = call$1;
		} catch {}
	}
	if (matched) return getContractError(error, {
		abi: matched.abi,
		address: matched.to,
		args: matched.args,
		functionName: matched.functionName
	});
	return e;
}

//#endregion
//#region node_modules/porto/dist/core/internal/_generated/chains.js
var chains_exports = /* @__PURE__ */ __export({
	arbitrum: () => arbitrum,
	arbitrumSepolia: () => arbitrumSepolia,
	base: () => base,
	baseSepolia: () => baseSepolia,
	berachain: () => berachain,
	berachainBepolia: () => berachainBepolia,
	bsc: () => bsc,
	celo: () => celo,
	gnosis: () => gnosis,
	hoodi: () => hoodi,
	katana: () => katana,
	mainnet: () => mainnet,
	optimism: () => optimism,
	optimismSepolia: () => optimismSepolia,
	polygon: () => polygon,
	sepolia: () => sepolia
});

//#endregion
//#region node_modules/porto/dist/core/Chains.js
var Chains_exports = /* @__PURE__ */ __export({
	all: () => all,
	anvil: () => anvil$1,
	anvil2: () => anvil2,
	anvil3: () => anvil3,
	arbitrum: () => arbitrum,
	arbitrumSepolia: () => arbitrumSepolia,
	base: () => base,
	baseSepolia: () => baseSepolia,
	berachain: () => berachain,
	berachainBepolia: () => berachainBepolia,
	bsc: () => bsc,
	celo: () => celo,
	gnosis: () => gnosis,
	hoodi: () => hoodi,
	katana: () => katana,
	mainnet: () => mainnet,
	optimism: () => optimism,
	optimismSepolia: () => optimismSepolia,
	polygon: () => polygon,
	sepolia: () => sepolia
});
const all = [base, ...Object.values(chains_exports).filter((c) => c && c.id !== base.id)];
const anvil$1 = anvil;
/** Additional Anvil environment, purposed for interop. */
const anvil2 = /* @__PURE__ */ defineChain({
	...anvil$1,
	id: 31338
});
/** Additional Anvil environment, purposed for interop. */
const anvil3 = /* @__PURE__ */ defineChain({
	...anvil$1,
	id: 31339
});

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/RpcResponse.js
/**
* A type-safe interface to parse a JSON-RPC response object as per the [JSON-RPC 2.0 specification](https://www.jsonrpc.org/specification#response_object), and extract the result.
*
* @example
* ```ts twoslash
* import { RpcRequest, RpcResponse } from 'ox'
*
* // 1. Create a request store.
* const store = RpcRequest.createStore()
*
* // 2. Get a request object.
* const request = store.prepare({
*   method: 'eth_getBlockByNumber',
*   params: ['0x1', false],
* })
*
* // 3. Send the JSON-RPC request via HTTP.
* const block = await fetch('https://1.rpc.thirdweb.com', {
*   body: JSON.stringify(request),
*   headers: {
*     'Content-Type': 'application/json',
*   },
*   method: 'POST',
* })
*  .then((response) => response.json())
*  // 4. Parse the JSON-RPC response into a type-safe result. // [!code focus]
*  .then((response) => RpcResponse.parse(response, { request })) // [!code focus]
*
* block // [!code focus]
* // ^?
*
*
*
*
*
*
*
*
*
*
*
* ```
*
* :::tip
*
* If you don't need the return type, you can omit the options entirely.
*
* ```ts twoslash
* // @noErrors
* import { RpcResponse } from 'ox'
*
* const block = await fetch('https://1.rpc.thirdweb.com', {})
*  .then((response) => response.json())
*  .then((response) => RpcResponse.parse(response, { request })) // [!code --]
*  .then(RpcResponse.parse) // [!code ++]
* ```
* :::
*
* @example
* ### Raw Mode
*
* If `raw` is `true`, the response will be returned as an object with `result` and `error` properties instead of returning the `result` directly and throwing errors.
*
* ```ts twoslash
* import { RpcRequest, RpcResponse } from 'ox'
*
* const store = RpcRequest.createStore()
*
* const request = store.prepare({
*   method: 'eth_blockNumber',
* })
*
* const response = RpcResponse.parse({}, {
*   request,
*   raw: true, // [!code hl]
* })
*
* response.result
* //       ^?
*
*
* response.error
* //       ^?
*
*
* ```
*
* @param response - Opaque JSON-RPC response object.
* @param options - Parsing options.
* @returns Typed JSON-RPC result, or response object (if `raw` is `true`).
*/
function parse(response, options = {}) {
	const { raw = false } = options;
	const response_ = response;
	if (raw) return response;
	if (response_.error) throw parseError$1(response_.error);
	return response_.result;
}
/**
* Parses an error into a RPC Error instance.
*
* @example
* ```ts twoslash
* import { RpcResponse } from 'ox'
*
* const error = RpcResponse.parseError({ code: -32000, message: 'unsupported method' })
*
* error
* // ^?
*
* ```
*
* @param error - Error.
* @returns RPC Error instance.
*/
function parseError$1(error) {
	const error_ = error;
	if (error_ instanceof Error && !("code" in error_)) return new InternalError({
		cause: error_,
		data: error_,
		message: error_.message,
		stack: error_.stack
	});
	const { code } = error_;
	if (code === InternalError.code) return new InternalError(error_);
	if (code === InvalidInputError.code) return new InvalidInputError(error_);
	if (code === InvalidParamsError.code) return new InvalidParamsError(error_);
	if (code === InvalidRequestError.code) return new InvalidRequestError(error_);
	if (code === LimitExceededError.code) return new LimitExceededError(error_);
	if (code === MethodNotFoundError.code) return new MethodNotFoundError(error_);
	if (code === MethodNotSupportedError.code) return new MethodNotSupportedError(error_);
	if (code === ParseError.code) return new ParseError(error_);
	if (code === ResourceNotFoundError.code) return new ResourceNotFoundError(error_);
	if (code === ResourceUnavailableError.code) return new ResourceUnavailableError(error_);
	if (code === TransactionRejectedError.code) return new TransactionRejectedError(error_);
	if (code === VersionNotSupportedError.code) return new VersionNotSupportedError(error_);
	return new InternalError({
		cause: error_ instanceof Error ? error_ : void 0,
		data: error_,
		message: error_.message,
		stack: error_ instanceof Error ? error_.stack : void 0
	});
}
/** Thrown when a JSON-RPC error has occurred. */
var BaseError$1 = class extends Error {
	constructor(errorObject) {
		const { cause, code, message, data, stack } = errorObject;
		super(message, { cause });
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.BaseError"
		});
		Object.defineProperty(this, "cause", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		Object.defineProperty(this, "stack", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		Object.defineProperty(this, "data", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		this.cause = cause;
		this.code = code;
		this.data = data;
		this.stack = stack ?? "";
	}
};
/** Thrown when the input to a JSON-RPC method is invalid. */
var InvalidInputError = class InvalidInputError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: InvalidInputError.code,
			data: parameters.data,
			message: parameters.message ?? "Missing or invalid parameters."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32e3
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.InvalidInputError"
		});
	}
};
Object.defineProperty(InvalidInputError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32e3
});
/** Thrown when a JSON-RPC resource is not found. */
var ResourceNotFoundError = class ResourceNotFoundError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: ResourceNotFoundError.code,
			data: parameters.data,
			message: parameters.message ?? "Requested resource not found."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32001
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.ResourceNotFoundError"
		});
	}
};
Object.defineProperty(ResourceNotFoundError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32001
});
/** Thrown when a JSON-RPC resource is unavailable. */
var ResourceUnavailableError = class ResourceUnavailableError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: ResourceUnavailableError.code,
			data: parameters.data,
			message: parameters.message ?? "Requested resource not available."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32002
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.ResourceUnavailableError"
		});
	}
};
Object.defineProperty(ResourceUnavailableError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32002
});
/** Thrown when a JSON-RPC transaction is rejected. */
var TransactionRejectedError = class TransactionRejectedError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: TransactionRejectedError.code,
			data: parameters.data,
			message: parameters.message ?? "Transaction creation failed."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32003
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.TransactionRejectedError"
		});
	}
};
Object.defineProperty(TransactionRejectedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32003
});
/** Thrown when a JSON-RPC method is not supported. */
var MethodNotSupportedError = class MethodNotSupportedError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: MethodNotSupportedError.code,
			data: parameters.data,
			message: parameters.message ?? "Method is not implemented."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32004
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.MethodNotSupportedError"
		});
	}
};
Object.defineProperty(MethodNotSupportedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32004
});
/** Thrown when a rate-limit is exceeded. */
var LimitExceededError = class LimitExceededError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: LimitExceededError.code,
			data: parameters.data,
			message: parameters.message ?? "Rate limit exceeded."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32005
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.LimitExceededError"
		});
	}
};
Object.defineProperty(LimitExceededError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32005
});
/** Thrown when a JSON-RPC version is not supported. */
var VersionNotSupportedError = class VersionNotSupportedError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: VersionNotSupportedError.code,
			data: parameters.data,
			message: parameters.message ?? "JSON-RPC version not supported."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32006
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.VersionNotSupportedError"
		});
	}
};
Object.defineProperty(VersionNotSupportedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32006
});
/** Thrown when a JSON-RPC request is invalid. */
var InvalidRequestError = class InvalidRequestError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: InvalidRequestError.code,
			data: parameters.data,
			message: parameters.message ?? "Input is not a valid JSON-RPC request."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32600
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.InvalidRequestError"
		});
	}
};
Object.defineProperty(InvalidRequestError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32600
});
/** Thrown when a JSON-RPC method is not found. */
var MethodNotFoundError = class MethodNotFoundError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: MethodNotFoundError.code,
			data: parameters.data,
			message: parameters.message ?? "Method does not exist."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32601
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.MethodNotFoundError"
		});
	}
};
Object.defineProperty(MethodNotFoundError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32601
});
/** Thrown when the parameters to a JSON-RPC method are invalid. */
var InvalidParamsError = class InvalidParamsError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: InvalidParamsError.code,
			data: parameters.data,
			message: parameters.message ?? "Invalid method parameters."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32602
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.InvalidParamsError"
		});
	}
};
Object.defineProperty(InvalidParamsError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32602
});
/** Thrown when an internal JSON-RPC error has occurred. */
var InternalError = class InternalError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			cause: parameters.cause,
			code: InternalError.code,
			data: parameters.data,
			message: parameters.message ?? "Internal JSON-RPC error.",
			stack: parameters.stack
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32603
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.InternalError"
		});
	}
};
Object.defineProperty(InternalError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32603
});
/** Thrown when a JSON-RPC response is invalid. */
var ParseError = class ParseError extends BaseError$1 {
	constructor(parameters = {}) {
		super({
			code: ParseError.code,
			data: parameters.data,
			message: parameters.message ?? "Failed to parse JSON-RPC response."
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: -32700
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "RpcResponse.ParseError"
		});
	}
};
Object.defineProperty(ParseError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: -32700
});

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/Provider.js
var ProviderRpcError = class extends Error {
	constructor(code, message) {
		super(message);
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "ProviderRpcError"
		});
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		Object.defineProperty(this, "details", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		this.code = code;
		this.details = message;
	}
};
/** The user rejected the request. */
var UserRejectedRequestError = class extends ProviderRpcError {
	constructor({ message = "The user rejected the request." } = {}) {
		super(4001, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4001
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UserRejectedRequestError"
		});
	}
};
Object.defineProperty(UserRejectedRequestError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4001
});
/** The requested method and/or account has not been authorized by the user. */
var UnauthorizedError = class extends ProviderRpcError {
	constructor({ message = "The requested method and/or account has not been authorized by the user." } = {}) {
		super(4100, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4100
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UnauthorizedError"
		});
	}
};
Object.defineProperty(UnauthorizedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4100
});
/** The provider does not support the requested method. */
var UnsupportedMethodError = class extends ProviderRpcError {
	constructor({ message = "The provider does not support the requested method." } = {}) {
		super(4200, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4200
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UnsupportedMethodError"
		});
	}
};
Object.defineProperty(UnsupportedMethodError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4200
});
/** The provider is disconnected from all chains. */
var DisconnectedError = class extends ProviderRpcError {
	constructor({ message = "The provider is disconnected from all chains." } = {}) {
		super(4900, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4900
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.DisconnectedError"
		});
	}
};
Object.defineProperty(DisconnectedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4900
});
/** The provider is not connected to the requested chain. */
var ChainDisconnectedError = class extends ProviderRpcError {
	constructor({ message = "The provider is not connected to the requested chain." } = {}) {
		super(4901, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4901
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.ChainDisconnectedError"
		});
	}
};
Object.defineProperty(ChainDisconnectedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4901
});
/** An error occurred when attempting to switch chain. */
var SwitchChainError = class extends ProviderRpcError {
	constructor({ message = "An error occurred when attempting to switch chain." } = {}) {
		super(4902, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 4902
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.SwitchChainError"
		});
	}
};
Object.defineProperty(SwitchChainError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 4902
});
/** This Wallet does not support a capability that was not marked as optional. */
var UnsupportedNonOptionalCapabilityError = class extends ProviderRpcError {
	constructor({ message = "This Wallet does not support a capability that was not marked as optional." } = {}) {
		super(5700, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5700
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UnsupportedNonOptionalCapabilityError"
		});
	}
};
Object.defineProperty(UnsupportedNonOptionalCapabilityError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5700
});
/** This Wallet does not support the requested chain ID. */
var UnsupportedChainIdError = class extends ProviderRpcError {
	constructor({ message = "This Wallet does not support the requested chain ID." } = {}) {
		super(5710, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5710
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UnsupportedChainIdError"
		});
	}
};
Object.defineProperty(UnsupportedChainIdError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5710
});
/** There is already a bundle submitted with this ID. */
var DuplicateIdError = class extends ProviderRpcError {
	constructor({ message = "There is already a bundle submitted with this ID." } = {}) {
		super(5720, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5720
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.DuplicateIdError"
		});
	}
};
Object.defineProperty(DuplicateIdError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5720
});
/** This bundle id is unknown / has not been submitted. */
var UnknownBundleIdError = class extends ProviderRpcError {
	constructor({ message = "This bundle id is unknown / has not been submitted." } = {}) {
		super(5730, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5730
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.UnknownBundleIdError"
		});
	}
};
Object.defineProperty(UnknownBundleIdError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5730
});
/** The call bundle is too large for the Wallet to process. */
var BundleTooLargeError = class extends ProviderRpcError {
	constructor({ message = "The call bundle is too large for the Wallet to process." } = {}) {
		super(5740, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5740
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.BundleTooLargeError"
		});
	}
};
Object.defineProperty(BundleTooLargeError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5740
});
/** The Wallet can support atomicity after an upgrade, but the user rejected the upgrade. */
var AtomicReadyWalletRejectedUpgradeError = class extends ProviderRpcError {
	constructor({ message = "The Wallet can support atomicity after an upgrade, but the user rejected the upgrade." } = {}) {
		super(5750, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5750
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.AtomicReadyWalletRejectedUpgradeError"
		});
	}
};
Object.defineProperty(AtomicReadyWalletRejectedUpgradeError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5750
});
/** The wallet does not support atomic execution but the request requires it. */
var AtomicityNotSupportedError = class extends ProviderRpcError {
	constructor({ message = "The wallet does not support atomic execution but the request requires it." } = {}) {
		super(5760, message);
		Object.defineProperty(this, "code", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: 5760
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.AtomicityNotSupportedError"
		});
	}
};
Object.defineProperty(AtomicityNotSupportedError, "code", {
	enumerable: true,
	configurable: true,
	writable: true,
	value: 5760
});
/**
* Creates an EIP-1193 flavored event emitter to be injected onto a Provider.
*
* @example
* ```ts twoslash
* // @noErrors
* import { Provider, RpcRequest, RpcResponse } from 'ox' // [!code focus]
*
* // 1. Instantiate a Provider Emitter. // [!code focus]
* const emitter = Provider.createEmitter() // [!code focus]
*
* const store = RpcRequest.createStore()
*
* const provider = Provider.from({
*   // 2. Pass the Emitter to the Provider. // [!code focus]
*   ...emitter, // [!code focus]
*   async request(args) {
*     return await fetch('https://1.rpc.thirdweb.com', {
*       body: JSON.stringify(store.prepare(args)),
*       method: 'POST',
*       headers: {
*         'Content-Type': 'application/json',
*       },
*     })
*       .then((res) => res.json())
*       .then(RpcResponse.parse)
*   },
* })
*
* // 3. Emit Provider Events. // [!code focus]
* emitter.emit('accountsChanged', ['0x...']) // [!code focus]
* ```
*
* @returns An event emitter.
*/
function createEmitter() {
	const emitter = new import_eventemitter3.default();
	return {
		get eventNames() {
			return emitter.eventNames.bind(emitter);
		},
		get listenerCount() {
			return emitter.listenerCount.bind(emitter);
		},
		get listeners() {
			return emitter.listeners.bind(emitter);
		},
		addListener: emitter.addListener.bind(emitter),
		emit: emitter.emit.bind(emitter),
		off: emitter.off.bind(emitter),
		on: emitter.on.bind(emitter),
		once: emitter.once.bind(emitter),
		removeAllListeners: emitter.removeAllListeners.bind(emitter),
		removeListener: emitter.removeListener.bind(emitter)
	};
}
function from$10(provider, _options = {}) {
	if (!provider) throw new IsUndefinedError();
	return {
		...provider,
		async request(args) {
			try {
				const result = await provider.request(args);
				if (result && typeof result === "object" && "jsonrpc" in result) return parse(result);
				return result;
			} catch (error) {
				throw parseError(error);
			}
		}
	};
}
/**
* Parses an error into a Provider error instance.
*
* @example
* ```ts twoslash
* import { Provider } from 'ox'
*
* const error = Provider.parseError({ code: 4200, message: 'foo' })
*
* error
* // ^?
*
* ```
*
* @param error - The error object to parse.
* @returns An error instance.
*/
function parseError(error) {
	const error_ = parseError$1(error);
	if (error_ instanceof InternalError) {
		if (!error_.data) return error_;
		const { code } = error_.data;
		if (code === DisconnectedError.code) return new DisconnectedError(error_);
		if (code === ChainDisconnectedError.code) return new ChainDisconnectedError(error_);
		if (code === UserRejectedRequestError.code) return new UserRejectedRequestError(error_);
		if (code === UnauthorizedError.code) return new UnauthorizedError(error_);
		if (code === UnsupportedMethodError.code) return new UnsupportedMethodError(error_);
		if (code === SwitchChainError.code) return new SwitchChainError(error_);
		if (code === AtomicReadyWalletRejectedUpgradeError.code) return new AtomicReadyWalletRejectedUpgradeError(error_);
		if (code === AtomicityNotSupportedError.code) return new AtomicityNotSupportedError(error_);
		if (code === BundleTooLargeError.code) return new BundleTooLargeError(error_);
		if (code === UnknownBundleIdError.code) return new UnknownBundleIdError(error_);
		if (code === DuplicateIdError.code) return new DuplicateIdError(error_);
		if (code === UnsupportedChainIdError.code) return new UnsupportedChainIdError(error_);
		if (code === UnsupportedNonOptionalCapabilityError.code) return new UnsupportedNonOptionalCapabilityError(error_);
	}
	return error_;
}
/** Thrown when the provider is undefined. */
var IsUndefinedError = class extends BaseError$2 {
	constructor() {
		super("`provider` is undefined.");
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Provider.IsUndefinedError"
		});
	}
};

//#endregion
//#region node_modules/porto/dist/core/internal/intersectionObserver.js
const supported = () => "IntersectionObserver" in window && "IntersectionObserverEntry" in window && "intersectionRatio" in IntersectionObserverEntry.prototype && "isVisible" in IntersectionObserverEntry.prototype;

//#endregion
//#region node_modules/porto/dist/core/internal/logger.js
function create$1(options = {}) {
	const { prefix = "[Porto]" } = options;
	const memo = /* @__PURE__ */ new Set();
	return {
		error: createLogFn(console.error, { prefix }),
		errorOnce: createLogFn(console.error, {
			memo,
			prefix
		}),
		log: createLogFn(console.log, { prefix }),
		logOnce: createLogFn(console.log, {
			memo,
			prefix
		}),
		warn: createLogFn(console.warn, { prefix }),
		warnOnce: createLogFn(console.warn, {
			memo,
			prefix
		})
	};
}
const logger = create$1();
function createLogFn(fn, options = {}) {
	const { memo, prefix } = options;
	return (...messages) => {
		const message = messages.join(" ");
		if (memo?.has(message)) return;
		memo?.add(message);
		fn(`${prefix} ${message}`);
	};
}

//#endregion
//#region node_modules/porto/dist/core/internal/userAgent.js
function isSafari() {
	const ua = navigator.userAgent.toLowerCase();
	return ua.includes("safari") && !ua.includes("chrome");
}
function isFirefox() {
	const ua = navigator.userAgent.toLowerCase();
	return (ua.includes("firefox") || ua.includes("fxios")) && !ua.includes("seamonkey");
}
function isMobile() {
	if (window.navigator?.userAgentData?.mobile) return true;
	return navigator.maxTouchPoints > 1 || /(android|bb\d+|meego).+mobile|avantgo|bada\/|blackberry|blazer|compal|elaine|fennec|hiptop|iemobile|ip(hone|od)|iris|kindle|lge |maemo|midp|mmp|mobile.+firefox|netfront|opera m(ob|in)i|palm( os)?|phone|p(ixi|re)\/|plucker|pocket|psp|series(4|6)0|symbian|treo|up\.(browser|link)|vodafone|wap|windows ce|xda|xiino/i.test(navigator.userAgent) || /1207|6310|6590|3gso|4thp|50[1-6]i|770s|802s|a wa|abac|ac(er|oo|s-)|ai(ko|rn)|al(av|ca|co)|amoi|an(ex|ny|yw)|aptu|ar(ch|go)|as(te|us)|attw|au(di|-m|r |s )|avan|be(ck|ll|nq)|bi(lb|rd)|bl(ac|az)|br(e|v)w|bumb|bw-(n|u)|c55\/|capi|ccwa|cdm-|cell|chtm|cldc|cmd-|co(mp|nd)|craw|da(it|ll|ng)|dbte|dc-s|devi|dica|dmob|do(c|p)o|ds(12|-d)|el(49|ai)|em(l2|ul)|er(ic|k0)|esl8|ez([4-7]0|os|wa|ze)|fetc|fly(-|_)|g1 u|g560|gene|gf-5|g-mo|go(\.w|od)|gr(ad|un)|haie|hcit|hd-(m|p|t)|hei-|hi(pt|ta)|hp( i|ip)|hs-c|ht(c(-| |_|a|g|p|s|t)|tp)|hu(aw|tc)|i-(20|go|ma)|i230|iac( |-|\/)|ibro|idea|ig01|ikom|im1k|inno|ipaq|iris|ja(t|v)a|jbro|jemu|jigs|kddi|keji|kgt( |\/)|klon|kpt |kwc-|kyo(c|k)|le(no|xi)|lg( g|\/(k|l|u)|50|54|-[a-w])|libw|lynx|m1-w|m3ga|m50\/|ma(te|ui|xo)|mc(01|21|ca)|m-cr|me(rc|ri)|mi(o8|oa|ts)|mmef|mo(01|02|bi|de|do|t(-| |o|v)|zz)|mt(50|p1|v )|mwbp|mywa|n10[0-2]|n20[2-3]|n30(0|2)|n50(0|2|5)|n7(0(0|1)|10)|ne((c|m)-|on|tf|wf|wg|wt)|nok(6|i)|nzph|o2im|op(ti|wv)|oran|owg1|p800|pan(a|d|t)|pdxg|pg(13|-([1-8]|c))|phil|pire|pl(ay|uc)|pn-2|po(ck|rt|se)|prox|psio|pt-g|qa-a|qc(07|12|21|32|60|-[2-7]|i-)|qtek|r380|r600|raks|rim9|ro(ve|zo)|s55\/|sa(ge|ma|mm|ms|ny|va)|sc(01|h-|oo|p-)|sdk\/|se(c(-|0|1)|47|mc|nd|ri)|sgh-|shar|sie(-|m)|sk-0|sl(45|id)|sm(al|ar|b3|it|t5)|so(ft|ny)|sp(01|h-|v-|v )|sy(01|mb)|t2(18|50)|t6(00|10|18)|ta(gt|lk)|tcl-|tdg-|tel(i|m)|tim-|t-mo|to(pl|sh)|ts(70|m-|m3|m5)|tx-9|up(\.b|g1|si)|utst|v400|v750|veri|vi(rg|te)|vk(40|5[0-3]|-v)|vm40|voda|vulc|vx(52|53|60|61|70|80|81|83|85|98)|w3c(-| )|webc|whit|wi(g |nc|nw)|wmlb|wonu|x700|yas-|your|zeto|zte-/i.test(navigator.userAgent.slice(0, 4));
}

//#endregion
//#region node_modules/porto/dist/core/internal/promise.js
/** @internal */
function withResolvers() {
	let resolve = () => void 0;
	let reject = () => void 0;
	return {
		promise: new Promise((resolve_, reject_) => {
			resolve = resolve_;
			reject = reject_;
		}),
		reject,
		resolve
	};
}

//#endregion
//#region node_modules/porto/dist/core/internal/utils.js
/**
* Normalizes a value into a structured-clone compatible format.
*
* @see https://developer.mozilla.org/en-US/docs/Web/API/Window/structuredClone
*/
function normalizeValue(value) {
	if (Array.isArray(value)) return value.map(normalizeValue);
	if (typeof value === "function") return void 0;
	if (typeof value !== "object" || value === null) return value;
	if (Object.getPrototypeOf(value) !== Object.prototype) try {
		return structuredClone(value);
	} catch {
		return;
	}
	const normalized = {};
	for (const [k, v] of Object.entries(value)) normalized[k] = normalizeValue(v);
	return normalized;
}
/**
* Returns a new array containing only one copy of each element in the original
* list transformed by a function.
*
* @param data - Array.
* @param fn - Extracts a value to be used to compare elements.
*/
function uniqBy(data, fn) {
	const result = [];
	const seen = /* @__PURE__ */ new Set();
	for (const item of data) {
		const key = fn(item);
		if (!seen.has(key)) {
			seen.add(key);
			result.push(item);
		}
	}
	return result;
}
function uuidv4() {
	if (typeof globalThis !== "undefined" && "crypto" in globalThis) return globalThis.crypto.randomUUID();
	return crypto.randomUUID();
}
/** Deduplicates in-flight promises. */
function withDedupe(fn, { enabled = true, id }) {
	if (!enabled || !id) return fn();
	if (withDedupe.cache.get(id)) return withDedupe.cache.get(id);
	const promise = fn().finally(() => withDedupe.cache.delete(id));
	withDedupe.cache.set(id, promise);
	return promise;
}
(function(withDedupe$1) {
	withDedupe$1.cache = /* @__PURE__ */ new Map();
})(withDedupe || (withDedupe = {}));

//#endregion
//#region node_modules/porto/dist/core/Messenger.js
var Messenger_exports = /* @__PURE__ */ __export({
	bridge: () => bridge,
	cliRelay: () => cliRelay,
	from: () => from$9,
	fromWindow: () => fromWindow,
	noop: () => noop$1
});
/**
* Instantiates a messenger.
*
* @param messenger - Messenger.
* @returns Instantiated messenger.
*/
function from$9(messenger) {
	return messenger;
}
/**
* Instantiates a messenger from a window instance.
*
* @param w - Window.
* @param options - Options.
* @returns Instantiated messenger.
*/
function fromWindow(w, options = {}) {
	const { targetOrigin } = options;
	const listeners = /* @__PURE__ */ new Map();
	return from$9({
		destroy() {
			for (const listener of listeners.values()) w.removeEventListener("message", listener);
		},
		on(topic, listener, id) {
			function handler(event) {
				if (event.data.topic !== topic) return;
				if (id && event.data.id !== id) return;
				if (targetOrigin && event.origin !== targetOrigin) return;
				listener(event.data.payload, event);
			}
			w.addEventListener("message", handler);
			listeners.set(topic, handler);
			return () => w.removeEventListener("message", handler);
		},
		async send(topic, payload, target) {
			const id = uuidv4();
			w.postMessage(normalizeValue({
				id,
				payload,
				topic
			}), target ?? targetOrigin ?? "*");
			return {
				id,
				payload,
				topic
			};
		},
		async sendAsync(topic, payload, target) {
			const { id } = await this.send(topic, payload, target);
			return new Promise((resolve) => this.on(topic, resolve, id));
		}
	});
}
/**
* Bridges two messengers for cross-window (e.g. parent to iframe) communication.
*
* @param parameters - Parameters.
* @returns Instantiated messenger.
*/
function bridge(parameters) {
	const { from: from_, to, waitForReady = false } = parameters;
	let pending = false;
	const ready = withResolvers();
	from_.on("ready", ready.resolve);
	const messenger = from$9({
		destroy() {
			from_.destroy();
			to.destroy();
			if (pending) ready.reject();
		},
		on(topic, listener, id) {
			return from_.on(topic, listener, id);
		},
		async send(topic, payload) {
			pending = true;
			if (waitForReady) await ready.promise.finally(() => pending = false);
			return to.send(topic, payload);
		},
		async sendAsync(topic, payload) {
			pending = true;
			if (waitForReady) await ready.promise.finally(() => pending = false);
			return to.sendAsync(topic, payload);
		}
	});
	return {
		...messenger,
		ready(options) {
			messenger.send("ready", options);
		},
		waitForReady() {
			return ready.promise;
		}
	};
}
function noop$1() {
	return {
		destroy() {},
		on() {
			return () => {};
		},
		ready() {},
		send() {
			return Promise.resolve(void 0);
		},
		sendAsync() {
			return Promise.resolve(void 0);
		},
		waitForReady() {
			return Promise.resolve(void 0);
		}
	};
}
/**
* Creates a CLI relay messenger that sends messages via fetch to a relay URL
* and receives events via server-sent events.
*
* @param options - Options.
* @returns Local relay messenger.
*/
function cliRelay(options) {
	const { relayUrl } = options;
	let eventSource = null;
	const listenerSets = /* @__PURE__ */ new Map();
	function connect() {
		if (!relayUrl || eventSource) return;
		eventSource = new EventSource(relayUrl);
		eventSource.onmessage = (event) => {
			try {
				const data = JSON.parse(event.data);
				if (!data.topic) return;
				if (!data.payload) return;
				const listeners = listenerSets.get(data.topic);
				if (!listeners) return;
				for (const listener of listeners) listener(data.payload, {
					data,
					origin: relayUrl
				});
			} catch (error) {
				console.error("Error parsing SSE message:", error);
			}
		};
		eventSource.onerror = (error) => {
			console.error("SSE connection error:", error);
			eventSource?.close();
			eventSource = null;
			setTimeout(connect, 1e3);
		};
	}
	connect();
	async function request(topic, payload) {
		const id = uuidv4();
		const data = {
			id,
			payload,
			topic
		};
		return {
			id,
			payload,
			response: await fetch(relayUrl, {
				body: stringify(data),
				headers: { "Content-Type": "application/json" },
				method: "POST"
			}),
			topic
		};
	}
	return {
		destroy() {
			eventSource?.close();
			eventSource = null;
			listenerSets.clear();
		},
		on(topic, listener) {
			if (!listenerSets.has(topic)) listenerSets.set(topic, /* @__PURE__ */ new Set());
			listenerSets.get(topic).add(listener);
			return () => {
				const listeners = listenerSets.get(topic);
				if (!listeners) return;
				listeners.delete(listener);
				if (listeners.size === 0) listenerSets.delete(topic);
			};
		},
		async ready(options$1) {
			await new Promise((resolve) => setTimeout(resolve, 32));
			this.send("ready", options$1);
		},
		async send(topic, payload) {
			const { id } = await request(topic, payload);
			return {
				id,
				payload,
				topic
			};
		},
		async sendAsync(topic, payload) {
			const { response } = await request(topic, payload);
			if (!response.ok) throw new Error(`HTTP ${response.status}: ${response.statusText}`);
			if (response.headers.get("content-type")?.includes("application/json")) return await response.json();
		}
	};
}

//#endregion
//#region node_modules/porto/dist/core/react-native/environment.js
var environment;
const reactNative$1 = {
	get environment() {
		if (!environment) throw new Error("React Native environment is not configured");
		return environment;
	},
	set environment(env) {
		environment = env;
	}
};

//#endregion
//#region node_modules/porto/dist/core/react-native/utils.js
function isReactNative() {
	if (typeof navigator !== "undefined" && navigator.product === "ReactNative") return true;
	if (typeof window !== "undefined" && Object.hasOwn(window, "ReactNativeWebView")) return true;
	if (typeof globalThis !== "undefined" && Object.hasOwn(globalThis, "HermesEngine")) return true;
	return false;
}

//#endregion
//#region node_modules/porto/dist/core/Dialog.js
var Dialog_exports = /* @__PURE__ */ __export({
	authSession: () => authSession,
	createThemeController: () => createThemeController,
	defaultSize: () => defaultSize,
	experimental_inline: () => experimental_inline,
	from: () => from$8,
	getDialogUrl: () => getDialogUrl,
	getReferrer: () => getReferrer,
	handleBlur: () => handleBlur,
	handleResponse: () => handleResponse,
	hostUrls: () => hostUrls,
	iframe: () => iframe,
	noop: () => noop,
	popup: () => popup,
	requiresConfirmation: () => requiresConfirmation
});
var AuthSessionStatus = {
	cancel: "cancel",
	error: "error",
	success: "success",
	unknown: "unknown"
};
const hostUrls = {
	local: "http://localhost:5175/dialog/",
	prod: "https://id.porto.sh/dialog",
	stg: "https://stg.id.porto.sh/dialog"
};
/**
* Instantiates a dialog.
*
* @param dialog - Dialog.
* @returns Instantiated dialog.
*/
function from$8(dialog$1) {
	return dialog$1;
}
/**
* Instantiates an iframe dialog.
*
* @returns iframe dialog.
*/
function iframe(options = {}) {
	const { skipProtocolCheck, skipUnsupported } = options;
	const includesUnsupported = (requests) => !skipUnsupported && isSafari() && requests?.some((x) => ["wallet_connect", "eth_requestAccounts"].includes(x.method));
	if (typeof window === "undefined") return noop();
	return from$8({
		name: "iframe",
		setup(parameters) {
			const { host, internal, theme, themeController } = parameters;
			const { store } = internal;
			const fallback$1 = popup().setup(parameters);
			let open = false;
			const hostUrl = new URL(host);
			const root = document.createElement("dialog");
			root.dataset.porto = "";
			root.setAttribute("role", "dialog");
			root.setAttribute("aria-closed", "true");
			root.setAttribute("aria-label", "Porto Wallet");
			root.setAttribute("hidden", "until-found");
			Object.assign(root.style, {
				background: "transparent",
				border: "0",
				outline: "0",
				padding: "0",
				position: "fixed"
			});
			document.body.appendChild(root);
			const iframe$1 = document.createElement("iframe");
			iframe$1.setAttribute("data-testid", "porto");
			const iframeAllow = [
				"payment",
				`publickey-credentials-get ${hostUrl.origin}`,
				`publickey-credentials-create ${hostUrl.origin}`
			];
			if (!isFirefox()) iframeAllow.push("clipboard-write");
			iframe$1.setAttribute("allow", iframeAllow.join("; "));
			iframe$1.setAttribute("tabindex", "0");
			iframe$1.setAttribute("sandbox", "allow-forms allow-scripts allow-same-origin allow-popups allow-popups-to-escape-sandbox");
			iframe$1.setAttribute("src", getDialogUrl(host));
			iframe$1.setAttribute("title", "Porto");
			Object.assign(iframe$1.style, {
				backgroundColor: "transparent",
				border: "0",
				colorScheme: "light dark",
				height: "100%",
				left: "0",
				position: "fixed",
				top: "0",
				width: "100%"
			});
			const style = document.createElement("style");
			style.innerHTML = `
        dialog[data-porto]::backdrop {
          background: transparent!important;
        }
      `;
			root.appendChild(style);
			root.appendChild(iframe$1);
			const messenger = bridge({
				from: fromWindow(window, { targetOrigin: hostUrl.origin }),
				to: fromWindow(iframe$1.contentWindow, { targetOrigin: hostUrl.origin }),
				waitForReady: true
			});
			themeController?._setup(messenger, true);
			const drawerModeQuery = window.matchMedia("(max-width: 460px)");
			const onDrawerModeChange = () => {
				messenger.send("__internal", {
					type: "resize",
					width: drawerModeQuery.matches ? 460 : 461
				});
			};
			drawerModeQuery.addEventListener("change", onDrawerModeChange);
			messenger.on("ready", (options$1) => {
				let compatibleChainIds = parameters.internal.store.getState().chainIds.filter((id) => options$1.chainIds.includes(id));
				if (compatibleChainIds.length === 0) compatibleChainIds = options$1.chainIds;
				store.setState((x) => ({
					...x,
					chainIds: compatibleChainIds
				}));
				messenger.send("__internal", {
					chainIds: compatibleChainIds,
					mode: "iframe",
					referrer: getReferrer(),
					theme,
					type: "init"
				});
				onDrawerModeChange();
			});
			messenger.on("rpc-response", (response) => {
				if (includesUnsupported([response._request])) iframe$1.src = iframe$1.src;
				handleResponse(store, response);
			});
			messenger.on("__internal", (payload) => {
				if (payload.type === "switch" && payload.mode === "popup") {
					fallback$1.open();
					fallback$1.syncRequests(store.getState().requestQueue);
				}
			});
			let bodyStyle = null;
			let opener = null;
			const onBlur = () => handleBlur(store);
			const onEscape = (event) => {
				if (event.key === "Escape") handleBlur(store);
			};
			const inertObserver = new MutationObserver((mutations) => {
				for (const mutation of mutations) {
					if (mutation.type !== "attributes") continue;
					const name = mutation.attributeName;
					if (!name) continue;
					if (name !== "inert") continue;
					root.removeAttribute(name);
				}
			});
			inertObserver.observe(root, {
				attributeOldValue: true,
				attributes: true
			});
			let dialogActive = false;
			const activatePage = () => {
				if (!dialogActive) return;
				dialogActive = false;
				root.removeEventListener("click", onBlur);
				document.removeEventListener("keydown", onEscape);
				root.style.pointerEvents = "none";
				opener?.focus();
				opener = null;
				Object.assign(document.body.style, bodyStyle ?? "");
				document.body.style.overflow = bodyStyle?.overflow ?? "";
			};
			const activateDialog = () => {
				if (dialogActive) return;
				dialogActive = true;
				root.addEventListener("click", onBlur);
				document.addEventListener("keydown", onEscape);
				iframe$1.focus();
				root.style.pointerEvents = "auto";
				bodyStyle = Object.assign({}, document.body.style);
				document.body.style.overflow = "hidden";
			};
			let visible = false;
			const showDialog = () => {
				if (visible) return;
				visible = true;
				if (document.activeElement instanceof HTMLElement) opener = document.activeElement;
				root.removeAttribute("hidden");
				root.removeAttribute("aria-closed");
				root.showModal();
			};
			const hideDialog = () => {
				if (!visible) return;
				visible = false;
				root.setAttribute("hidden", "true");
				root.setAttribute("aria-closed", "true");
				root.close();
				for (const sibling of root.parentNode ? Array.from(root.parentNode.children) : []) {
					if (sibling === root) continue;
					if (!sibling.hasAttribute("inert")) continue;
					sibling.removeAttribute("inert");
				}
			};
			return {
				close() {
					fallback$1.close();
					open = false;
					messenger.send("__internal", {
						mode: "iframe",
						referrer: getReferrer(),
						type: "init"
					});
					hideDialog();
					activatePage();
				},
				destroy() {
					fallback$1.close();
					open = false;
					activatePage();
					hideDialog();
					fallback$1.destroy();
					messenger.destroy();
					root.remove();
					inertObserver.disconnect();
					drawerModeQuery.removeEventListener("change", onDrawerModeChange);
				},
				open() {
					if (open) return;
					open = true;
					showDialog();
					activateDialog();
					messenger.send("__internal", {
						mode: "iframe",
						referrer: getReferrer(),
						type: "init"
					});
				},
				async secure() {
					const { trustedHosts } = await messenger.waitForReady();
					const secureProtocol = (() => {
						if (skipProtocolCheck) return true;
						const secure = window.location.protocol.startsWith("https");
						if (!secure) logger.warnOnce("Detected insecure protocol (HTTP).", `\n\nThe Porto iframe is not supported on HTTP origins (${window.location.origin})`, "due to lack of WebAuthn support.", "See https://porto.sh/sdk#secure-origins-https for more information.");
						return secure;
					})();
					const intersectionObserverSupported = supported();
					const trustedHost = Boolean(trustedHosts?.includes(window.location.hostname));
					const secureFrame = Boolean(intersectionObserverSupported || trustedHost);
					if (!secureFrame) logger.warnOnce([
						`Warning: Browser does not support IntersectionObserver v2 or host "${hostUrl.hostname}" is not trusted by Porto.`,
						"This may result in the dialog falling back to a popup.",
						"",
						`Add "${hostUrl.hostname}" to the trusted hosts list to enable iframe dialog: https://github.com/ithacaxyz/porto/edit/main/src/trusted-hosts.ts`
					].join("\n"));
					return {
						frame: secureFrame,
						host: trustedHost,
						protocol: secureProtocol
					};
				},
				async syncRequests(requests) {
					const { methodPolicies } = await messenger.waitForReady();
					const secure = await this.secure();
					const headless = requests?.every((request) => methodPolicies?.find((policy) => policy.method === request.request.method)?.modes?.headless === true);
					const unsupported = includesUnsupported(requests.map((x) => x.request));
					if (!headless && (unsupported || !secure.protocol || !secure.frame)) fallback$1.syncRequests(requests);
					else {
						const requiresConfirm = requests.some((x) => requiresConfirmation(x.request, {
							methodPolicies,
							targetOrigin: hostUrl.origin
						}));
						if (!open && requiresConfirm) this.open();
						messenger.send("rpc-requests", requests);
					}
				}
			};
		},
		supportsHeadless: true
	});
}
/**
* Instantiates a popup dialog.
*
* @returns Popup dialog.
*/
function popup(options = {}) {
	if (typeof window === "undefined") return noop();
	const { type = "auto", size: size$1 = defaultSize } = options;
	return from$8({
		name: "popup",
		setup(parameters) {
			const { host, internal, themeController } = parameters;
			const { store } = internal;
			const hostUrl = new URL(host);
			let popup$1 = null;
			const resolvedType = type === "page" || type === "auto" && isMobile() ? "page" : "popup";
			function onBlur() {
				if (popup$1) handleBlur(store);
			}
			const offDetectClosed = (() => {
				const timer = setInterval(() => {
					if (popup$1?.closed) handleBlur(store);
				}, 100);
				return () => clearInterval(timer);
			})();
			let messenger;
			themeController?._setup(null, true);
			return {
				close() {
					if (!popup$1) return;
					popup$1.close();
					popup$1 = null;
				},
				destroy() {
					this.close();
					window.removeEventListener("focus", onBlur);
					messenger?.destroy();
					offDetectClosed();
				},
				open() {
					if (resolvedType === "popup") {
						const left = (window.innerWidth - size$1.width) / 2 + window.screenX;
						const top = window.screenY + 100;
						popup$1 = window.open(getDialogUrl(host), "_blank", `width=${size$1.width},height=${size$1.height},left=${left},top=${top}`);
					} else popup$1 = window.open(getDialogUrl(host), "_blank");
					if (!popup$1) throw new Error("Failed to open popup");
					messenger = bridge({
						from: fromWindow(window, { targetOrigin: hostUrl.origin }),
						to: fromWindow(popup$1, { targetOrigin: hostUrl.origin }),
						waitForReady: true
					});
					themeController?._setup(messenger, false);
					messenger.send("__internal", {
						mode: resolvedType === "page" ? "page" : "popup",
						referrer: getReferrer(),
						theme: themeController?.getTheme() ?? parameters.theme,
						type: "init"
					});
					messenger.on("rpc-response", (response) => handleResponse(store, response));
					window.removeEventListener("focus", onBlur);
					window.addEventListener("focus", onBlur);
				},
				async secure() {
					return {
						frame: true,
						host: true,
						protocol: true
					};
				},
				async syncRequests(requests) {
					if (requests.some((x) => requiresConfirmation(x.request))) {
						if (!popup$1 || popup$1.closed) this.open();
						popup$1?.focus();
					}
					messenger?.send("rpc-requests", requests);
				}
			};
		},
		supportsHeadless: false
	});
}
/**
* Instantiates a authSession dialog for React Native.
* @param options - Options.
*/
function authSession(options = {}) {
	if (!isReactNative()) return noop();
	const { requestOptions = { showTitle: true } } = options;
	return from$8({
		name: "authSession",
		setup(parameters) {
			const { host, internal } = parameters;
			const { store } = internal;
			const environment$1 = reactNative$1.environment;
			environment$1.maybeCompleteAuthSession?.();
			let processing = false;
			let inFlightId = null;
			async function handle(request) {
				const { request: rpcRequest } = request;
				const rpcMethod = {
					account_verifyEmail: "account_verifyEmail",
					eth_requestAccounts: "eth_requestAccounts",
					eth_sendTransaction: "eth_sendTransaction",
					eth_signTypedData_v4: "eth_signTypedData_v4",
					personal_sign: "personal_sign",
					wallet_addFunds: "wallet_addFunds",
					wallet_connect: "wallet_connect",
					wallet_getAccountVersion: "wallet_getAccountVersion",
					wallet_getAssets: "wallet_getAssets",
					wallet_getCallsStatus: "wallet_getCallsStatus",
					wallet_getCapabilities: "wallet_getCapabilities",
					wallet_getKeys: "wallet_getKeys",
					wallet_getPermissions: "wallet_getPermissions",
					wallet_grantAdmin: "wallet_grantAdmin",
					wallet_grantPermissions: "wallet_grantPermissions",
					wallet_prepareCalls: "wallet_prepareCalls",
					wallet_prepareUpgradeAccount: "wallet_prepareUpgradeAccount",
					wallet_revokeAdmin: "wallet_revokeAdmin",
					wallet_revokePermissions: "wallet_revokePermissions",
					wallet_sendCalls: "wallet_sendCalls",
					wallet_sendPreparedCalls: "wallet_sendPreparedCalls",
					wallet_switchEthereumChain: "wallet_switchEthereumChain",
					wallet_verifySignature: "wallet_verifySignature"
				}[rpcRequest.method];
				if (!rpcMethod) throw new UnsupportedMethodError({ message: `Method not supported in Mode.reactNative(): ${rpcRequest.method}` });
				const redirectUri = environment$1.makeRedirectUri({
					...options.redirectUri ? { scheme: options.redirectUri.scheme } : {},
					path: options.redirectUri?.path ?? "/",
					preferLocalhost: typeof __DEV__ === "boolean" && __DEV__
				});
				const url = new URL(host);
				url.pathname = `${url.pathname.replace(/\/$/, "")}/${rpcMethod}`;
				const searchParams = new URLSearchParams({
					id: String(rpcRequest.id),
					jsonrpc: "2.0",
					method: rpcRequest.method,
					redirectUri
				});
				const params = rpcRequest.params ?? [];
				if (params.length > 0) searchParams.set("params", stringify(params));
				const decodedParams = rpcRequest._decoded?.params;
				if (decodedParams) searchParams.set("_decoded", stringify(decodedParams));
				url.search = searchParams.toString();
				const result = await environment$1.openAuthSessionAsync(url.toString(), redirectUri, requestOptions);
				handleResponse(store, (() => {
					if (result.type === "success" && result.url) {
						const resolved = new URL(result.url);
						const status = resolved.searchParams.get("status") ?? AuthSessionStatus.unknown;
						const message = resolved.searchParams.get("message") ?? void 0;
						const payload = resolved.searchParams.get("payload") ?? void 0;
						if (status === AuthSessionStatus.success) try {
							return {
								id: rpcRequest.id,
								jsonrpc: "2.0",
								result: payload ? parse$1(payload) : void 0
							};
						} catch (error$1) {
							return {
								error: {
									code: -32603,
									message: error$1 instanceof Error ? error$1.message : "Failed to parse redirect payload"
								},
								id: rpcRequest.id,
								jsonrpc: "2.0"
							};
						}
						const error = status === AuthSessionStatus.cancel ? new UserRejectedRequestError({ message: message ?? "User rejected request" }) : new ProviderRpcError(-32603, message ?? status ?? "Request failed");
						return {
							error: {
								code: error.code,
								message: error.message
							},
							id: rpcRequest.id,
							jsonrpc: "2.0"
						};
					}
					if (result.type === "cancel" || result.type === "dismiss") return {
						error: {
							code: UserRejectedRequestError.code,
							message: "User rejected request"
						},
						id: rpcRequest.id,
						jsonrpc: "2.0"
					};
					return {
						error: {
							code: -32603,
							message: result.error ?? "Request failed"
						},
						id: rpcRequest.id,
						jsonrpc: "2.0"
					};
				})());
				environment$1.dismissAuthSession?.();
			}
			return {
				close() {
					environment$1.dismissAuthSession?.();
				},
				destroy() {
					environment$1.dismissAuthSession?.();
				},
				open() {},
				async secure() {
					return {
						frame: false,
						host: true,
						protocol: true
					};
				},
				async syncRequests(requests) {
					if (processing) return;
					const [request] = requests;
					if (!request) return;
					if (inFlightId === request.request.id) return;
					inFlightId = request.request.id;
					processing = true;
					try {
						await handle(request);
					} catch (error) {
						handleResponse(store, {
							error: {
								code: -32603,
								message: error instanceof Error ? error.message : "Request failed"
							},
							id: request.request.id,
							jsonrpc: "2.0"
						});
						environment$1.dismissAuthSession?.();
					} finally {
						inFlightId = null;
						processing = false;
					}
				}
			};
		},
		supportsHeadless: false
	});
}
/**
* Instantiates a noop dialog.
*
* @returns Noop dialog.
*/
function noop() {
	return from$8({
		name: "noop",
		setup() {
			return {
				close() {},
				destroy() {},
				open() {},
				async secure() {
					return {
						frame: true,
						host: true,
						protocol: true
					};
				},
				async syncRequests() {}
			};
		},
		supportsHeadless: true
	});
}
/**
* Instantiates an inline iframe dialog rendered on a provided `element`.
*
* @param options - Options.
* @returns Inline iframe dialog.
*/
function experimental_inline(options) {
	const { element } = options;
	if (typeof window === "undefined") return noop();
	return from$8({
		name: "inline",
		setup(parameters) {
			const { host, internal, theme, themeController } = parameters;
			const { store } = internal;
			let open = false;
			const hostUrl = new URL(host);
			const root = document.createElement("div");
			root.dataset.porto = "";
			root.style.height = "100%";
			element().appendChild(root);
			const iframe$1 = document.createElement("iframe");
			iframe$1.setAttribute("allow", `payment; publickey-credentials-get ${hostUrl.origin}; publickey-credentials-create ${hostUrl.origin}`);
			iframe$1.setAttribute("aria-label", "Porto Wallet");
			iframe$1.setAttribute("tabindex", "0");
			iframe$1.setAttribute("sandbox", "allow-forms allow-scripts allow-same-origin allow-popups allow-popups-to-escape-sandbox");
			iframe$1.setAttribute("src", getDialogUrl(host));
			iframe$1.setAttribute("title", "Porto");
			Object.assign(iframe$1.style, {
				border: "0",
				height: "100%",
				width: "100%"
			});
			root.appendChild(iframe$1);
			const messenger = bridge({
				from: fromWindow(window, { targetOrigin: hostUrl.origin }),
				to: fromWindow(iframe$1.contentWindow, { targetOrigin: hostUrl.origin }),
				waitForReady: true
			});
			themeController?._setup(messenger, true);
			messenger.on("ready", () => {
				messenger.send("__internal", {
					mode: "inline-iframe",
					referrer: getReferrer(),
					theme,
					type: "init"
				});
			});
			messenger.on("rpc-response", (response) => handleResponse(store, response));
			return {
				close() {},
				destroy() {
					messenger.destroy();
					root.remove();
				},
				open() {
					if (open) return;
					open = true;
					messenger.send("__internal", {
						mode: "iframe",
						referrer: getReferrer(),
						type: "init"
					});
				},
				async secure() {
					return {
						frame: true,
						host: true,
						protocol: true
					};
				},
				async syncRequests(requests) {
					messenger.send("rpc-requests", requests);
				}
			};
		},
		supportsHeadless: true
	});
}
/**
* A controller to update the dialog theme.
*/
function createThemeController() {
	let lastTheme = null;
	let messenger = null;
	return {
		_setup(messenger_, resetTheme = false) {
			if (resetTheme) lastTheme = null;
			messenger = messenger_;
		},
		getTheme() {
			return lastTheme;
		},
		setTheme(theme) {
			lastTheme = theme;
			messenger?.send("__internal", {
				theme,
				type: "set-theme"
			}).catch(() => {});
		}
	};
}
const defaultSize = {
	height: 282,
	width: 360
};
function requiresConfirmation(request, options = {}) {
	const { methodPolicies, targetOrigin } = options;
	const policy = methodPolicies?.find((x) => x.method === request.method);
	if (!policy) return true;
	if (policy.modes?.headless) {
		if (typeof policy.modes.headless === "object" && policy.modes.headless.sameOrigin && targetOrigin !== window.location.origin) return true;
		return false;
	}
	return true;
}
function getReferrer() {
	return {
		icon: (() => {
			const dark = document.querySelector("link[rel=\"icon\"][media=\"(prefers-color-scheme: dark)\"]")?.href;
			const light = document.querySelector("link[rel=\"icon\"][media=\"(prefers-color-scheme: light)\"]")?.href ?? document.querySelector("link[rel=\"icon\"]")?.href;
			if (dark && light && dark !== light) return {
				dark,
				light
			};
			if (window.matchMedia("(prefers-color-scheme: dark)").matches) return dark;
			return light;
		})(),
		title: document.title
	};
}
function handleBlur(store) {
	store.setState((x) => ({
		...x,
		requestQueue: x.requestQueue.map((x$1) => ({
			account: x$1.account,
			error: new UserRejectedRequestError(),
			request: x$1.request,
			status: "error"
		}))
	}));
}
function handleResponse(store, response) {
	store.setState((x) => ({
		...x,
		requestQueue: x.requestQueue.map((queued) => {
			if (queued.request.id !== response.id) return queued;
			if (response.error) return {
				account: queued.account,
				error: response.error,
				request: queued.request,
				status: "error"
			};
			return {
				account: queued.account,
				request: queued.request,
				result: response.result,
				status: "success"
			};
		})
	}));
}
function getDialogUrl(host) {
	const url = new URL(host);
	const parentParams = new URLSearchParams(window.location.search);
	const prefix = "porto.";
	for (const [key, value] of parentParams.entries()) if (key.startsWith(prefix)) url.searchParams.set(key.slice(6), value);
	return url.toString();
}

//#endregion
//#region node_modules/porto/dist/viem/Account.js
var Account_exports = /* @__PURE__ */ __export({
	from: () => from$7,
	fromPrivateKey: () => fromPrivateKey,
	getKey: () => getKey,
	sign: () => sign
});
/**
* Instantiates a delegated account.
*
* @param account - Account to instantiate.
* @returns An instantiated delegated account.
*/
function from$7(parameters) {
	const account = typeof parameters === "string" ? { address: parameters } : parameters;
	const source = account.sign ? "privateKey" : "porto";
	const { address: address$1, sign: sign_, signMessage, signTransaction, signTypedData, type } = toAccount({
		address: account.address,
		sign({ hash: hash$1 }) {
			if (source === "porto") throw new Error("`sign` not supported on porto accounts.");
			if (!account.sign) throw new Error("`sign` not supported.");
			return account.sign({ hash: hash$1 });
		},
		signMessage({ message }) {
			return this.sign({ hash: hashMessage(message) });
		},
		signTransaction() {
			throw new Error("`signTransaction` not supported on porto accounts.");
		},
		signTypedData(typedData) {
			return this.sign({ hash: hashTypedData(typedData) });
		}
	});
	return {
		address: address$1,
		keys: account.keys ?? void 0,
		sign: sign_,
		signMessage,
		signTransaction,
		signTypedData,
		source,
		type
	};
}
/**
* Instantiates a delegated account from a private key.
*
* @param privateKey - Private key.
* @param options - Options.
* @returns An instantiated delegated account.
*/
function fromPrivateKey(privateKey, options = {}) {
	const { keys } = options;
	return from$7({
		address: fromPublicKey(getPublicKey({ privateKey })),
		keys,
		async sign({ hash: hash$1 }) {
			return toHex(sign$1({
				payload: hash$1,
				privateKey
			}));
		},
		source: "privateKey"
	});
}
function getKey(account, parameters = {}) {
	const { key, role } = parameters;
	if (key === null) return void 0;
	if (typeof key === "object") return key;
	if (account.keys && account.keys.length > 0) {
		if (typeof key === "number") return account.keys[key];
		return account.keys.find((key$1) => key$1.privateKey && (!role || key$1.role === role));
	}
}
/**
* Extracts a signing key from a delegated account and signs a payload.
*
* @example
* TODO
*
* @param parameters - Parameters.
* @returns Signature.
*/
async function sign(account, parameters) {
	const { storage, replaySafe = true, wrap: wrap$2 = true, webAuthn } = parameters;
	const key = getKey(account, parameters);
	const payload = (() => {
		if (!replaySafe) return parameters.payload;
		return getSignPayload$2({
			domain: { verifyingContract: account.address },
			message: { digest: parameters.payload },
			primaryType: "ERC1271Sign",
			types: { ERC1271Sign: [{
				name: "digest",
				type: "bytes32"
			}] }
		});
	})();
	const sign$4 = (() => {
		if (!key) {
			if (account.source === "privateKey") return account.sign;
			return;
		}
		return ({ hash: hash$1 }) => sign$2(key, {
			address: null,
			payload: hash$1,
			storage,
			webAuthn,
			wrap: wrap$2
		});
	})();
	if (!sign$4) throw new Error("cannot find key to sign with.");
	return await sign$4({ hash: payload });
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/AbiError.js
/**
* Parses an arbitrary **JSON ABI Error** or **Human Readable ABI Error** into a typed {@link ox#AbiError.AbiError}.
*
* @example
* ### JSON ABIs
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from({
*   inputs: [{ name: 'v', type: 'uint8' }],
*   name: 'BadSignatureV',
*   type: 'error',
* })
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
* @example
* ### Human Readable ABIs
*
* A Human Readable ABI can be parsed into a typed ABI object:
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from(
*   'error BadSignatureV(uint8 v)' // [!code hl]
* )
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
* @example
* It is possible to specify `struct`s along with your definitions:
*
* ```ts twoslash
* import { AbiError } from 'ox'
*
* const badSignatureVError = AbiError.from([
*   'struct Signature { uint8 v; }', // [!code hl]
*   'error BadSignatureV(Signature signature)',
* ])
*
* badSignatureVError
* //^?
*
*
*
*
*
*
*
*
*
*
*
*
* ```
*
*
*
* @param abiError - The ABI Error to parse.
* @returns Typed ABI Error.
*/
function from$6(abiError, options = {}) {
	return from$13(abiError, options);
}
/**
* Extracts an {@link ox#AbiError.AbiError} from an {@link ox#Abi.Abi} given a name and optional arguments.
*
* @example
* ### Extracting by Name
*
* ABI Errors can be extracted by their name using the `name` option:
*
* ```ts twoslash
* import { Abi, AbiError } from 'ox'
*
* const abi = Abi.from([
*   'function foo()',
*   'error BadSignatureV(uint8 v)',
*   'function bar(string a) returns (uint256 x)',
* ])
*
* const item = AbiError.fromAbi(abi, 'BadSignatureV') // [!code focus]
* //    ^?
*
*
*
*
*
*
* ```
*
* @example
* ### Extracting by Selector
*
* ABI Errors can be extract by their selector when {@link ox#Hex.Hex} is provided to `name`.
*
* ```ts twoslash
* import { Abi, AbiError } from 'ox'
*
* const abi = Abi.from([
*   'function foo()',
*   'error BadSignatureV(uint8 v)',
*   'function bar(string a) returns (uint256 x)',
* ])
* const item = AbiError.fromAbi(abi, '0x095ea7b3') // [!code focus]
* //    ^?
*
*
*
*
*
*
*
*
*
* ```
*
* :::note
*
* Extracting via a hex selector is useful when extracting an ABI Error from JSON-RPC error data.
*
* :::
*
* @param abi - The ABI to extract from.
* @param name - The name (or selector) of the ABI item to extract.
* @param options - Extraction options.
* @returns The ABI item.
*/
function fromAbi(abi, name, options) {
	if (name === "Error") return solidityError;
	if (name === "Panic") return solidityPanic;
	if (validate$1(name, { strict: false })) {
		const selector = slice(name, 0, 4);
		if (selector === solidityErrorSelector) return solidityError;
		if (selector === solidityPanicSelector) return solidityPanic;
	}
	const item = fromAbi$1(abi, name, options);
	if (item.type !== "error") throw new NotFoundError({
		name,
		type: "error"
	});
	return item;
}
const solidityError = /* @__PURE__ */ from$6({
	inputs: [{
		name: "message",
		type: "string"
	}],
	name: "Error",
	type: "error"
});
const solidityErrorSelector = "0x08c379a0";
const solidityPanic = /* @__PURE__ */ from$6({
	inputs: [{
		name: "reason",
		type: "uint8"
	}],
	name: "Panic",
	type: "error"
});
const solidityPanicSelector = "0x4e487b71";

//#endregion
//#region node_modules/porto/dist/core/internal/schema/utils.js
const address = () => templateLiteral(["0x", string()], { message: "Needs string in format ^0x[A-Fa-f0-9]{40}$." });
const hex = () => templateLiteral(["0x", string()], { message: "Needs string in format ^0x[A-Fa-f0-9]+$." });
const number = () => codec(hex(), number$1(), {
	decode: (value) => toNumber(value),
	encode: (value) => fromNumber(value)
});
const bigint = () => codec(hex(), bigint$1({ message: "Required bigint" }), {
	decode: (value) => toBigInt(value),
	encode: (value) => fromNumber(value)
});
function oneOf(options) {
	return union(options);
}
var ValidationError = class extends BaseError$2 {
	constructor() {
		super(...arguments);
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Schema.ValidationError"
		});
	}
};
function toValidationError(e) {
	const error = e;
	let message = `Validation failed with ${error.issues.length} error${error.issues.length === 1 ? "" : "s"}:`;
	message += "\n";
	for (const issue of error.issues) {
		if (!issue) continue;
		message += "\n";
		message += formatIssue(issue);
	}
	return new ValidationError(message);
}
function formatIssue(issue, level = 0) {
	const path = formatPath(issue.path);
	const prefix = `- ${path ? `${path}: ` : ""}`;
	const indent = "  ".repeat(level + 1);
	let message = prefix;
	switch (issue.code) {
		case "invalid_type": {
			const expected = issue.expected;
			const received = issue.input ? formatReceivedType(issue) : "undefined";
			message += `Expected ${expected}. ${issue.message !== "Invalid input" ? issue.message : ""}`;
			if (received !== "undefined") message += `but received ${received}`;
			break;
		}
		case "too_big": {
			const max = issue.maximum;
			const inclusive = issue.inclusive ?? true;
			if (issue.exact ?? false) message += `${issue.origin} must be exactly ${max}`;
			else message += `${issue.origin} must be ${inclusive ? "at most" : "less than"} ${max}`;
			break;
		}
		case "too_small": {
			const min = issue.minimum;
			const inclusive = issue.inclusive ?? true;
			if (issue.exact ?? false) message += `${issue.origin} must be exactly ${min}`;
			else message += `${issue.origin} must be ${inclusive ? "at least" : "greater than"} ${min}`;
			break;
		}
		case "invalid_format":
			switch (issue.format) {
				case "regex":
					message += `Must match pattern: ${issue.pattern}`;
					break;
				case "starts_with":
					message += `Must start with "${issue.prefix}"`;
					break;
				case "ends_with":
					message += `Must end with "${issue.suffix}"`;
					break;
				case "includes":
					message += `Must include "${issue.includes}"`;
					break;
				case "template_literal":
					message += `Must match pattern: ${issue.pattern}`;
					break;
				default: message += `Invalid ${issue.format} format`;
			}
			break;
		case "not_multiple_of":
			message += `Number must be a multiple of ${issue.divisor}`;
			break;
		case "unrecognized_keys": {
			const keys = issue.keys.map((k) => `"${k}"`).join(", ");
			message += `Unrecognized key${issue.keys.length > 1 ? "s" : ""}: ${keys}`;
			break;
		}
		case "invalid_union": {
			const hasErrors = issue.errors && issue.errors.length > 0;
			message += "Invalid union value.";
			if (hasErrors) issue.errors.forEach((optionErrors) => {
				if (optionErrors.length > 0) optionErrors.forEach((subIssue) => {
					message += "\n";
					message += indent;
					message += formatIssue(subIssue, level + 1);
				});
			});
			break;
		}
		case "invalid_key":
			message += `Invalid ${issue.origin} key`;
			if (issue.issues && issue.issues.length > 0) issue.issues.forEach((subIssue) => {
				message += "\n";
				message += indent;
				message += formatIssue(subIssue, level + 1);
			});
			break;
		case "invalid_element":
			message += `Invalid ${issue.origin} element at key "${issue.key}"`;
			if (issue.issues && issue.issues.length > 0) issue.issues.forEach((subIssue) => {
				message += "\n";
				message += indent;
				message += formatIssue(subIssue, level + 1);
			});
			break;
		case "invalid_value": {
			const values = issue.values.map((v) => JSON.stringify(v)).join(", ");
			if (issue.values.length > 1) message += `Expected one of: ${values}`;
			else message += `Expected ${values}`;
			break;
		}
		case "custom":
			message += issue.message || "Custom validation failed";
			break;
		default: message += issue.message || "Validation failed";
	}
	return message;
}
function formatPath(path) {
	if (path.length === 0) return "";
	return "at `" + path.map((key, index) => {
		if (typeof key === "number") return `[${key}]`;
		if (typeof key === "symbol") return `[${key.toString()}]`;
		if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key) && index > 0) return `.${key}`;
		if (index === 0 && /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) return key;
		return `["${key}"]`;
	}).join("") + "`";
}
function formatReceivedType(issue) {
	const value = issue.input;
	if (value === void 0) return "undefined";
	if (value === null) return "null";
	const type = typeof value;
	if (type === "object") {
		if (Array.isArray(value)) return "array";
		if (value instanceof Date) return "date";
		if (value instanceof Map) return "map";
		if (value instanceof Set) return "set";
		return "object";
	}
	return type;
}

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/permission.js
const CallPermission = object({
	selector: hex(),
	to: address(),
	type: literal("call")
});
const SpendPermission = object({
	limit: bigint(),
	period: union([
		literal("minute"),
		literal("hour"),
		literal("day"),
		literal("week"),
		literal("month"),
		literal("year")
	]),
	token: optional(union([address(), _null()])),
	type: literal("spend")
});
const Permission = union([CallPermission, SpendPermission]);

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/key.js
/**
* RPC account key.
*
* @see https://github.com/ithacaxyz/relay/blob/main/src/types/key.rs
*/
const Key = object({
	expiry: number(),
	prehash: optional(boolean()),
	publicKey: hex(),
	role: union([literal("admin"), literal("normal")]),
	type: union([
		literal("p256"),
		literal("secp256k1"),
		literal("webauthnp256")
	])
});
const WithPermissions$1 = object({
	...Key.shape,
	permissions: readonly(array(Permission))
});

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/capabilities.js
/**
* RPC capabilities.
*
* @see https://github.com/ithacaxyz/relay/blob/main/src/types/capabilities.rs
*/
var assetDiffs;
(function(assetDiffs$1) {
	assetDiffs$1.AssetDiffAsset = union([
		object({
			address: optional(union([address(), _null()])),
			decimals: optional(union([number$1(), _null()])),
			direction: union([literal("incoming"), literal("outgoing")]),
			fiat: optional(object({
				currency: string(),
				value: codec(string(), number$1(), {
					decode: (value) => Number(value),
					encode: (value) => String(value)
				})
			})),
			name: optional(union([string(), _null()])),
			symbol: string(),
			type: literal("erc20"),
			value: bigint()
		}),
		object({
			address: optional(union([address(), _null()])),
			direction: union([literal("incoming"), literal("outgoing")]),
			fiat: optional(object({
				currency: string(),
				value: codec(string(), number$1(), {
					decode: (value) => Number(value),
					encode: (value) => String(value)
				})
			})),
			name: optional(union([string(), _null()])),
			symbol: string(),
			type: literal("erc721"),
			uri: string(),
			value: bigint()
		}),
		object({
			address: _null(),
			decimals: optional(union([number$1(), _null()])),
			direction: union([literal("incoming"), literal("outgoing")]),
			fiat: optional(object({
				currency: string(),
				value: codec(string(), number$1(), {
					decode: (value) => Number(value),
					encode: (value) => String(value)
				})
			})),
			symbol: string(),
			type: _null(),
			value: bigint()
		})
	]);
	assetDiffs$1.Response = record(hex(), readonly(array(readonly(tuple([address(), readonly(array(assetDiffs$1.AssetDiffAsset))])))));
})(assetDiffs || (assetDiffs = {}));
var authorizeKeys;
(function(authorizeKeys$1) {
	/** Represents a key authorization request. */
	authorizeKeys$1.Request = readonly(array(WithPermissions$1));
	/** Represents a key authorization response. */
	authorizeKeys$1.Response = readonly(array(object({
		...WithPermissions$1.shape,
		hash: hex()
	})));
})(authorizeKeys || (authorizeKeys = {}));
var feeTotals;
(function(feeTotals$1) {
	feeTotals$1.Response = record(hex(), object({
		currency: string(),
		value: string()
	}));
})(feeTotals || (feeTotals = {}));
var meta;
(function(meta$1) {
	/** Represents metadata for a call bundle. */
	meta$1.Request = object({
		feePayer: optional(address()),
		feeToken: optional(address()),
		nonce: optional(bigint())
	});
})(meta || (meta = {}));
var requiredFunds$1;
(function(requiredFunds$2) {
	requiredFunds$2.Request = readonly(array(object({
		address: address(),
		value: bigint()
	})));
})(requiredFunds$1 || (requiredFunds$1 = {}));
var revokeKeys;
(function(revokeKeys$1) {
	/** Represents a key revocation request. */
	revokeKeys$1.Request = readonly(array(object({ hash: hex() })));
	/** Represents a key revocation response. */
	revokeKeys$1.Response = readonly(array(object({ hash: hex() })));
})(revokeKeys || (revokeKeys = {}));

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/preCall.js
const PreCall = object({
	eoa: address(),
	executionData: hex(),
	nonce: hex(),
	signature: hex()
});
const Context = object({
	...PreCall.shape,
	chainId: number()
});

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/intent.js
/**
* Intent.
*
* @see https://github.com/ithacaxyz/relay/blob/main/src/types/intent.rs
*/
const Intent = union([object({
	combinedGas: bigint(),
	encodedFundTransfers: readonly(array(hex())),
	encodedPreCalls: readonly(array(hex())),
	eoa: address(),
	executionData: hex(),
	expiry: bigint(),
	funder: address(),
	funderSignature: hex(),
	isMultichain: boolean(),
	nonce: bigint(),
	payer: address(),
	paymentAmount: bigint(),
	paymentMaxAmount: bigint(),
	paymentRecipient: address(),
	paymentSignature: hex(),
	paymentToken: address(),
	settler: address(),
	settlerContext: hex(),
	signature: hex(),
	supportedAccountImplementation: address()
}), object({
	combinedGas: bigint(),
	encodedFundTransfers: readonly(array(hex())),
	encodedPreCalls: readonly(array(hex())),
	eoa: address(),
	executionData: hex(),
	expiry: bigint(),
	funder: address(),
	funderSignature: hex(),
	isMultichain: boolean(),
	nonce: bigint(),
	payer: address(),
	paymentRecipient: address(),
	paymentSignature: hex(),
	paymentToken: address(),
	prePaymentAmount: bigint(),
	prePaymentMaxAmount: bigint(),
	settler: address(),
	settlerContext: hex(),
	signature: hex(),
	supportedAccountImplementation: address(),
	totalPaymentAmount: bigint(),
	totalPaymentMaxAmount: bigint()
})]);
const Partial = object({
	eoa: address(),
	executionData: hex(),
	nonce: bigint()
});

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/quotes.js
/**
* RPC quote.
*
* @see https://github.com/ithacaxyz/relay/blob/main/src/types/quote.rs
*/
const AssetDeficit = object({
	address: union([address(), _null()]),
	decimals: optional(number$1()),
	deficit: bigint(),
	fiat: optional(object({
		currency: string(),
		value: string()
	})),
	name: optional(string()),
	required: bigint(),
	symbol: optional(string())
});
/** A quote from the RPC for a given `Intent`. */
const Quote = object({
	additionalAuthorization: nullish(object({
		address: address(),
		chainId: number(),
		nonce: number(),
		r: hex(),
		s: hex(),
		yParity: number()
	})),
	assetDeficits: optional(array(AssetDeficit)),
	authorizationAddress: optional(union([address(), _null()])),
	chainId: number(),
	ethPrice: bigint(),
	extraPayment: bigint(),
	feeTokenDeficit: bigint(),
	intent: Intent,
	nativeFeeEstimate: object({
		maxFeePerGas: bigint(),
		maxPriorityFeePerGas: bigint()
	}),
	orchestrator: address(),
	paymentTokenDecimals: number$1(),
	txGas: bigint()
});
const Quotes = object({
	multiChainRoot: optional(union([hex(), _null()])),
	quotes: readonly(array(Quote)).check(_minLength(1)),
	ttl: number$1()
});
const Signed = object({
	...Quotes.shape,
	hash: hex(),
	r: hex(),
	s: hex(),
	v: optional(hex()),
	yParity: optional(hex())
});

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/token.js
const Token = object({
	address: address(),
	decimals: number$1(),
	feeToken: optional(boolean()),
	interop: optional(boolean()),
	nativeRate: optional(bigint()),
	symbol: string(),
	uid: string()
});
const Symbol = string().check(_regex(/^[A-Z0-9]+$/));

//#endregion
//#region node_modules/porto/dist/core/internal/relay/schema/rpc.js
/**
* JSON-RPC Schemas.
*
* @see https://github.com/ithacaxyz/relay/tree/main/src/types/rpc
*/
var Authorization = object({
	address: address(),
	chainId: number(),
	nonce: number()
});
var SignedAuthorization = object({
	...Authorization.shape,
	r: hex(),
	s: hex(),
	yParity: number()
});
var Call = object({
	data: optional(hex()),
	to: address(),
	value: optional(bigint())
});
var account_getOnrampContactInfo$1;
(function(account_getOnrampContactInfo$2) {
	/** Parameters for `account_getOnrampContactInfo` request. */
	account_getOnrampContactInfo$2.Parameters = object({
		address: address(),
		secret: string()
	});
	/** Request for `account_getOnrampContactInfo`. */
	account_getOnrampContactInfo$2.Request = object({
		method: literal("account_getOnrampContactInfo"),
		params: readonly(tuple([account_getOnrampContactInfo$2.Parameters]))
	});
	/** Response for `account_getOnrampContactInfo`. */
	account_getOnrampContactInfo$2.Response = object({
		email: optional(string()),
		phone: optional(string()),
		phoneVerifiedAt: optional(number$1())
	});
})(account_getOnrampContactInfo$1 || (account_getOnrampContactInfo$1 = {}));
var account_onrampStatus$1;
(function(account_onrampStatus$2) {
	/** Parameters for `account_onrampStatus` request. */
	account_onrampStatus$2.Parameters = object({ address: address() });
	/** Request for `account_onrampStatus`. */
	account_onrampStatus$2.Request = object({
		method: literal("account_onrampStatus"),
		params: readonly(tuple([account_onrampStatus$2.Parameters]))
	});
	/** Response for `account_onrampStatus`. */
	account_onrampStatus$2.Response = object({
		email: optional(number$1()),
		phone: optional(number$1())
	});
})(account_onrampStatus$1 || (account_onrampStatus$1 = {}));
var account_resendVerifyPhone$1;
(function(account_resendVerifyPhone$2) {
	/** Parameters for `account_resendVerifyPhone` request. */
	account_resendVerifyPhone$2.Parameters = object({
		phone: string(),
		walletAddress: address()
	});
	/** Request for `account_resendVerifyPhone`. */
	account_resendVerifyPhone$2.Request = object({
		method: literal("account_resendVerifyPhone"),
		params: readonly(tuple([account_resendVerifyPhone$2.Parameters]))
	});
	/** Response for `account_resendVerifyPhone`. */
	account_resendVerifyPhone$2.Response = _null();
})(account_resendVerifyPhone$1 || (account_resendVerifyPhone$1 = {}));
var account_setEmail$1;
(function(account_setEmail$2) {
	/** Parameters for `account_setEmail` request. */
	account_setEmail$2.Parameters = object({
		email: string().check(_regex(/^.*@.*$/)),
		walletAddress: address()
	});
	/** Request for `account_setEmail`. */
	account_setEmail$2.Request = object({
		method: literal("account_setEmail"),
		params: readonly(tuple([account_setEmail$2.Parameters]))
	});
	/** Response for `account_setEmail`. */
	account_setEmail$2.Response = _null();
})(account_setEmail$1 || (account_setEmail$1 = {}));
var account_setPhone$1;
(function(account_setPhone$2) {
	/** Parameters for `account_setPhone` request. */
	account_setPhone$2.Parameters = object({
		phone: string(),
		walletAddress: address()
	});
	/** Request for `account_setPhone`. */
	account_setPhone$2.Request = object({
		method: literal("account_setPhone"),
		params: readonly(tuple([account_setPhone$2.Parameters]))
	});
	/** Response for `account_setPhone`. */
	account_setPhone$2.Response = _null();
})(account_setPhone$1 || (account_setPhone$1 = {}));
var account_verifyEmail$1;
(function(account_verifyEmail$2) {
	/** Parameters for `account_verifyEmail` request. */
	account_verifyEmail$2.Parameters = object({
		chainId: number(),
		email: string(),
		signature: hex(),
		token: string(),
		walletAddress: address()
	});
	/** Request for `account_verifyEmail`. */
	account_verifyEmail$2.Request = object({
		method: literal("account_verifyEmail"),
		params: readonly(tuple([account_verifyEmail$2.Parameters]))
	});
	/** Response for `account_verifyEmail`. */
	account_verifyEmail$2.Response = _null();
})(account_verifyEmail$1 || (account_verifyEmail$1 = {}));
var account_verifyPhone$1;
(function(account_verifyPhone$2) {
	/** Parameters for `account_verifyPhone` request. */
	account_verifyPhone$2.Parameters = object({
		code: string(),
		phone: string(),
		walletAddress: address()
	});
	/** Request for `account_verifyPhone`. */
	account_verifyPhone$2.Request = object({
		method: literal("account_verifyPhone"),
		params: readonly(tuple([account_verifyPhone$2.Parameters]))
	});
	/** Response for `account_verifyPhone`. */
	account_verifyPhone$2.Response = _null();
})(account_verifyPhone$1 || (account_verifyPhone$1 = {}));
var health$1;
(function(health$2) {
	health$2.Request = object({
		method: literal("health"),
		params: _undefined()
	});
	health$2.Response = object({
		quoteSigner: address(),
		status: string(),
		version: string()
	});
})(health$1 || (health$1 = {}));
var wallet_addFaucetFunds;
(function(wallet_addFaucetFunds$1) {
	wallet_addFaucetFunds$1.Parameters = object({
		address: address(),
		chainId: number(),
		tokenAddress: address(),
		value: bigint()
	});
	wallet_addFaucetFunds$1.Request = object({
		method: literal("wallet_addFaucetFunds"),
		params: readonly(tuple([wallet_addFaucetFunds$1.Parameters]))
	});
	wallet_addFaucetFunds$1.Response = object({
		message: optional(string()),
		transactionHash: hex()
	});
})(wallet_addFaucetFunds || (wallet_addFaucetFunds = {}));
var wallet_getAccounts;
(function(wallet_getAccounts$1) {
	/** Parameters for `wallet_getAccounts` request. */
	wallet_getAccounts$1.Parameters = object({
		chainId: number(),
		id: hex()
	});
	/** Request for `wallet_getAccounts`. */
	wallet_getAccounts$1.Request = object({
		method: literal("wallet_getAccounts"),
		params: readonly(tuple([wallet_getAccounts$1.Parameters]))
	});
	/** Response for `wallet_getAccounts`. */
	wallet_getAccounts$1.Response = readonly(array(object({
		address: address(),
		keys: authorizeKeys.Response
	})));
})(wallet_getAccounts || (wallet_getAccounts = {}));
var wallet_getAuthorization;
(function(wallet_getAuthorization$1) {
	wallet_getAuthorization$1.Parameters = object({ address: address() });
	wallet_getAuthorization$1.Request = object({
		method: literal("wallet_getAuthorization"),
		params: readonly(tuple([wallet_getAuthorization$1.Parameters]))
	});
	wallet_getAuthorization$1.Response = object({
		authorization: SignedAuthorization,
		data: hex(),
		to: address()
	});
})(wallet_getAuthorization || (wallet_getAuthorization = {}));
var wallet_getCapabilities$1;
(function(wallet_getCapabilities$2) {
	/** Request for `wallet_getCapabilities`. */
	wallet_getCapabilities$2.Request = object({
		method: literal("wallet_getCapabilities"),
		params: optional(tuple([readonly(array(number$1()))]))
	});
	const VersionedContract = object({
		address: address(),
		version: optional(union([string(), _null()]))
	});
	wallet_getCapabilities$2.Response = record(hex(), object({
		contracts: object({
			accountImplementation: VersionedContract,
			accountProxy: VersionedContract,
			legacyAccountImplementations: readonly(array(VersionedContract)),
			legacyOrchestrators: readonly(array(union([object({
				orchestrator: VersionedContract,
				simulator: VersionedContract
			}), VersionedContract]))),
			orchestrator: VersionedContract,
			simulator: VersionedContract
		}),
		fees: object({
			quoteConfig: object({
				constantRate: optional(union([number$1(), _null()])),
				gas: optional(object({
					intentBuffer: optional(number$1()),
					txBuffer: optional(number$1())
				})),
				rateTtl: number$1(),
				ttl: number$1()
			}),
			recipient: address(),
			tokens: readonly(array(Token))
		})
	}));
})(wallet_getCapabilities$1 || (wallet_getCapabilities$1 = {}));
var wallet_getAssets$1;
(function(wallet_getAssets$2) {
	/** Parameters  */
	const AssetType = union([
		literal("native"),
		literal("erc20"),
		literal("erc721"),
		string()
	]);
	wallet_getAssets$2.Parameters = object({
		account: address(),
		assetFilter: optional(record(hex(), readonly(array(object({
			address: union([address(), literal("native")]),
			type: AssetType
		}))))),
		assetTypeFilter: optional(readonly(array(AssetType))),
		chainFilter: optional(readonly(array(number())))
	});
	/** Request for `wallet_getAssets`. */
	wallet_getAssets$2.Request = object({
		method: literal("wallet_getAssets"),
		params: readonly(tuple([wallet_getAssets$2.Parameters]))
	});
	wallet_getAssets$2.Price = object({
		currency: string(),
		value: codec(string(), number$1(), {
			decode: (value) => Number(value),
			encode: (value) => String(value)
		})
	});
	/** Response for `wallet_getAssets`. */
	wallet_getAssets$2.Response = record(string(), readonly(array(oneOf([object({
		address: address(),
		balance: bigint(),
		metadata: nullable(object({
			decimals: number$1(),
			fiat: nullish(wallet_getAssets$2.Price),
			name: string(),
			symbol: string()
		})),
		type: literal("erc20")
	}), object({
		address: nullable(literal("native")),
		balance: bigint(),
		metadata: nullable(object({
			decimals: number$1(),
			fiat: nullish(wallet_getAssets$2.Price),
			name: optional(string()),
			symbol: optional(string())
		})),
		type: literal("native")
	})]))));
})(wallet_getAssets$1 || (wallet_getAssets$1 = {}));
var wallet_getCallsStatus$1;
(function(wallet_getCallsStatus$2) {
	wallet_getCallsStatus$2.Request = object({
		method: literal("wallet_getCallsStatus"),
		params: readonly(tuple([hex()]))
	});
	wallet_getCallsStatus$2.Response = object({
		id: string(),
		receipts: optional(readonly(array(object({
			blockHash: hex(),
			blockNumber: number(),
			chainId: number(),
			gasUsed: number(),
			logs: readonly(array(object({
				address: address(),
				data: hex(),
				topics: readonly(array(hex()))
			}))),
			status: hex(),
			transactionHash: hex()
		})))),
		status: number$1()
	});
})(wallet_getCallsStatus$1 || (wallet_getCallsStatus$1 = {}));
var wallet_getKeys$1;
(function(wallet_getKeys$2) {
	/** Parameters for `wallet_getKeys` request. */
	wallet_getKeys$2.Parameters = object({
		address: address(),
		chainIds: optional(readonly(array(number())))
	});
	/** Request for `wallet_getKeys`. */
	wallet_getKeys$2.Request = object({
		method: literal("wallet_getKeys"),
		params: readonly(tuple([wallet_getKeys$2.Parameters]))
	});
	/** Response for `wallet_getKeys`. */
	wallet_getKeys$2.Response = record(hex(), authorizeKeys.Response);
})(wallet_getKeys$1 || (wallet_getKeys$1 = {}));
var wallet_prepareCalls$1;
(function(wallet_prepareCalls$2) {
	/** Capabilities for `wallet_prepareCalls` request. */
	wallet_prepareCalls$2.Capabilities = object({
		authorizeKeys: optional(authorizeKeys.Request),
		meta: meta.Request,
		preCall: optional(boolean()),
		preCalls: optional(readonly(array(PreCall))),
		requiredFunds: optional(requiredFunds$1.Request),
		revokeKeys: optional(revokeKeys.Request)
	});
	/** Capabilities for `wallet_prepareCalls` response. */
	wallet_prepareCalls$2.ResponseCapabilities = object({
		assetDiffs: optional(assetDiffs.Response),
		authorizeKeys: nullish(authorizeKeys.Response),
		feePayerDigest: optional(hex()),
		feeSignature: optional(hex()),
		feeTotals: optional(feeTotals.Response),
		revokeKeys: nullish(revokeKeys.Response)
	});
	/** Parameters for `wallet_prepareCalls` request. */
	wallet_prepareCalls$2.Parameters = object({
		calls: readonly(array(Call)),
		capabilities: wallet_prepareCalls$2.Capabilities,
		chainId: number(),
		from: optional(address()),
		key: optional(object({
			prehash: boolean(),
			publicKey: hex(),
			type: Key.shape.type
		}))
	});
	/** Request for `wallet_prepareCalls`. */
	wallet_prepareCalls$2.Request = object({
		method: literal("wallet_prepareCalls"),
		params: readonly(tuple([wallet_prepareCalls$2.Parameters]))
	});
	/** Response for `wallet_prepareCalls`. */
	wallet_prepareCalls$2.Response = object({
		capabilities: wallet_prepareCalls$2.ResponseCapabilities,
		context: object({
			preCall: optional(partial(Context)),
			quote: optional(partial(Signed))
		}),
		digest: hex(),
		key: nullish(object({
			prehash: boolean(),
			publicKey: hex(),
			type: Key.shape.type
		})),
		signature: hex(),
		typedData: object({
			domain: union([object({
				chainId: union([number(), number$1()]),
				name: string(),
				verifyingContract: address(),
				version: string()
			}), object({})]),
			message: record(string(), unknown()),
			primaryType: string(),
			types: record(string(), unknown())
		})
	});
})(wallet_prepareCalls$1 || (wallet_prepareCalls$1 = {}));
var wallet_prepareUpgradeAccount$1;
(function(wallet_prepareUpgradeAccount$2) {
	/** Capabilities for `wallet_prepareUpgradeAccount` request. */
	wallet_prepareUpgradeAccount$2.Capabilities = object({ authorizeKeys: authorizeKeys.Request });
	/** Parameters for `wallet_prepareUpgradeAccount` request. */
	wallet_prepareUpgradeAccount$2.Parameters = object({
		address: address(),
		capabilities: wallet_prepareUpgradeAccount$2.Capabilities,
		chainId: optional(number$1()),
		delegation: address()
	});
	/** Request for `wallet_prepareUpgradeAccount`. */
	wallet_prepareUpgradeAccount$2.Request = object({
		method: literal("wallet_prepareUpgradeAccount"),
		params: readonly(tuple([wallet_prepareUpgradeAccount$2.Parameters]))
	});
	/** Response for `wallet_prepareUpgradeAccount`. */
	wallet_prepareUpgradeAccount$2.Response = object({
		capabilities: wallet_prepareUpgradeAccount$2.Capabilities,
		chainId: number(),
		context: object({
			address: address(),
			authorization: Authorization,
			chainId: number(),
			preCall: PreCall
		}),
		digests: object({
			auth: hex(),
			exec: hex()
		}),
		typedData: object({
			domain: union([object({
				chainId: union([number(), number$1()]),
				name: string(),
				verifyingContract: address(),
				version: string()
			}), object({})]),
			message: record(string(), unknown()),
			primaryType: string(),
			types: record(string(), unknown())
		})
	});
})(wallet_prepareUpgradeAccount$1 || (wallet_prepareUpgradeAccount$1 = {}));
var wallet_feeTokens;
(function(wallet_feeTokens$1) {
	/** Request for `wallet_feeTokens`. */
	wallet_feeTokens$1.Request = object({
		method: literal("wallet_feeTokens"),
		params: optional(_undefined())
	});
	/** Response for `wallet_feeTokens`. */
	wallet_feeTokens$1.Response = record(hex(), readonly(array(object({
		address: address(),
		decimals: number$1(),
		nativeRate: optional(bigint()),
		symbol: string()
	}))));
})(wallet_feeTokens || (wallet_feeTokens = {}));
var wallet_sendPreparedCalls$1;
(function(wallet_sendPreparedCalls$2) {
	/** Parameters for `wallet_sendPreparedCalls` request. */
	wallet_sendPreparedCalls$2.Parameters = object({
		capabilities: optional(object({ feeSignature: optional(hex()) })),
		context: object({
			preCall: optional(partial(Context)),
			quote: optional(partial(Signed))
		}),
		key: optional(object({
			prehash: boolean(),
			publicKey: hex(),
			type: Key.shape.type
		})),
		signature: hex()
	});
	/** Request for `wallet_sendPreparedCalls`. */
	wallet_sendPreparedCalls$2.Request = object({
		method: literal("wallet_sendPreparedCalls"),
		params: readonly(tuple([wallet_sendPreparedCalls$2.Parameters]))
	});
	/** Response for `wallet_sendPreparedCalls`. */
	wallet_sendPreparedCalls$2.Response = object({ id: hex() });
})(wallet_sendPreparedCalls$1 || (wallet_sendPreparedCalls$1 = {}));
var wallet_upgradeAccount$1;
(function(wallet_upgradeAccount$2) {
	wallet_upgradeAccount$2.Parameters = object({
		context: object({
			address: address(),
			authorization: Authorization,
			chainId: number(),
			preCall: PreCall
		}),
		signatures: object({
			auth: hex(),
			exec: hex()
		})
	});
	/** Request for `wallet_sendPreparedCalls`. */
	wallet_upgradeAccount$2.Request = object({
		method: literal("wallet_upgradeAccount"),
		params: readonly(tuple([wallet_upgradeAccount$2.Parameters]))
	});
	wallet_upgradeAccount$2.Response = _undefined();
})(wallet_upgradeAccount$1 || (wallet_upgradeAccount$1 = {}));
var wallet_verifySignature$1;
(function(wallet_verifySignature$2) {
	wallet_verifySignature$2.Parameters = object({
		address: hex(),
		chainId: number(),
		digest: hex(),
		signature: hex()
	});
	/** Request for `wallet_verifySignature`. */
	wallet_verifySignature$2.Request = object({
		method: literal("wallet_verifySignature"),
		params: readonly(tuple([wallet_verifySignature$2.Parameters]))
	});
	/** Response for `wallet_verifySignature`. */
	wallet_verifySignature$2.Response = object({
		proof: nullish(object({
			account: address(),
			initPreCall: nullish(PreCall),
			keyHash: hex()
		})),
		valid: boolean()
	});
})(wallet_verifySignature$1 || (wallet_verifySignature$1 = {}));

//#endregion
//#region node_modules/porto/dist/viem/internal/relayActions.js
/**
* Actions for Porto Relay.
*
* @see https://porto.sh/relay
*/
/**
* Gets the authorization for a given address.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function getAuthorization(client, parameters) {
	try {
		const method = "wallet_getAuthorization";
		const result = await withCache(() => client.request({
			method,
			params: [encode$1(wallet_getAuthorization.Parameters, parameters)]
		}), { cacheKey: `${client.uid}.${method}.${parameters.address}` });
		return decode(wallet_getAuthorization.Response, result);
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Gets the capabilities for a given chain ID.
*
* @example
* TODO
*
* @param client - The client to use.
* @param options - Options.
* @returns Result.
*/
async function getCapabilities(client, options = {}) {
	const chainIds = (() => {
		if (options.chainId) return [options.chainId];
		if (options.chainIds === "all") return void 0;
		if (options.chainIds) return options.chainIds;
		return [client.chain.id];
	})();
	try {
		const method = "wallet_getCapabilities";
		const result = await withCache(() => client.request({
			method,
			params: chainIds ? [chainIds] : void 0
		}, { retryCount: 0 }), { cacheKey: `${client.uid}.${method}.${chainIds?.join(",")}` });
		const parsed = (() => {
			if (options.raw) return result;
			return decode(wallet_getCapabilities$1.Response, result);
		})();
		if (options.chainIds) return parsed;
		return Object.values(parsed)[0];
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Get assets owned by user in given chain IDs.
*/
async function getAssets(client, parameters) {
	const { account, assetFilter, assetTypeFilter, chainFilter } = parameters;
	try {
		const result = await client.request({
			method: "wallet_getAssets",
			params: [encode$1(wallet_getAssets$1.Parameters, {
				account,
				assetFilter,
				assetTypeFilter,
				chainFilter
			})]
		});
		const value = decode(wallet_getAssets$1.Response, result);
		const decoded = Object.entries(value).reduce((acc, [key, value$1]) => {
			acc[toNumber(key)] = value$1;
			return acc;
		}, {});
		const aggregated = {};
		for (const value$1 of Object.values(decoded)) for (const item of value$1) {
			const key = JSON.stringify(item.metadata);
			aggregated[key] = {
				...item,
				balance: item.balance + (aggregated[key]?.balance ?? 0n)
			};
		}
		return {
			...decoded,
			"0": Object.values(aggregated)
		};
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Requests faucet funds to be sent to an address on the Relay.
*/
async function addFaucetFunds(client, parameters) {
	const { address: address$1, chain = client.chain, tokenAddress, value } = parameters;
	try {
		const result = await client.request({
			method: "wallet_addFaucetFunds",
			params: [encode$1(wallet_addFaucetFunds.Parameters, {
				address: address$1,
				chainId: chain?.id,
				tokenAddress,
				value
			})]
		}, { retryCount: 0 });
		await new Promise((resolve) => setTimeout(resolve, 2e3));
		return result;
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Gets the status of a call bundle.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function getCallsStatus(client, parameters) {
	const { id } = parameters;
	try {
		const result = await client.request({
			method: "wallet_getCallsStatus",
			params: [id]
		});
		return decode(wallet_getCallsStatus$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Gets the keys for a given account.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function getKeys$1(client, parameters) {
	const { address: address$1, chainIds } = parameters;
	try {
		const result = await client.request({
			method: "wallet_getKeys",
			params: [encode$1(wallet_getKeys$1.Parameters, {
				address: address$1,
				chainIds
			})]
		});
		return decode(wallet_getKeys$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
/**
* Gets the health of the RPC.
*
* @example
* TODO
*
* @param client - The client to use.
* @returns Result.
*/
async function health(client) {
	const method = "health";
	const result = await withCache(() => client.request({ method }), { cacheKey: `${client.uid}.${method}` });
	return decode(health$1.Response, result);
}
/**
* Prepares a call bundle.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function prepareCalls$1(client, parameters) {
	const { address: address$1, capabilities, chain = client.chain, key } = parameters;
	const calls = parameters.calls.map((call$1) => {
		return {
			data: call$1.abi ? encodeData(fromAbi$2(call$1.abi, call$1.functionName), call$1.args) : call$1.data ?? "0x",
			to: call$1.to,
			value: call$1.value ?? 0n
		};
	});
	try {
		const result = await client.request({
			method: "wallet_prepareCalls",
			params: [encode$1(wallet_prepareCalls$1.Parameters, {
				calls,
				capabilities: {
					...capabilities,
					meta: { ...capabilities?.meta }
				},
				chainId: chain?.id,
				from: address$1,
				key: key ? {
					prehash: key.prehash,
					publicKey: key.publicKey,
					type: key.type
				} : void 0
			})]
		}, { retryCount: 0 });
		return Object.assign(decode(wallet_prepareCalls$1.Response, result), { _raw: result });
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error, { calls: parameters.calls });
		throw error;
	}
}
/**
* Prepares an account upgrade.
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function prepareUpgradeAccount$1(client, parameters) {
	const { address: address$1, chain = client.chain, delegation, ...capabilities } = parameters;
	try {
		const result = await client.request({
			method: "wallet_prepareUpgradeAccount",
			params: [encode$1(wallet_prepareUpgradeAccount$1.Parameters, normalizeValue({
				address: address$1,
				capabilities,
				chainId: chain?.id,
				delegation
			}))]
		}, { retryCount: 0 });
		return decode(wallet_prepareUpgradeAccount$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Broadcasts a signed call bundle.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function sendPreparedCalls$1(client, parameters) {
	const { capabilities, context, key, signature } = parameters;
	try {
		const result = await client.request({
			method: "wallet_sendPreparedCalls",
			params: [encode$1(wallet_sendPreparedCalls$1.Parameters, {
				capabilities,
				context: {
					preCall: context.preCall,
					quote: context.quote
				},
				key: key ? {
					prehash: key.prehash,
					publicKey: key.publicKey,
					type: key.type
				} : void 0,
				signature
			})]
		}, { retryCount: 0 });
		return decode(wallet_sendPreparedCalls$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* NOTE: SHOULD ONLY BE USED ON SERVER.
*
* Gets onramp contact info for address.
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function getOnrampContactInfo$1(client, parameters) {
	const { address: address$1, secret } = parameters;
	try {
		const result = await client.request({
			method: "account_getOnrampContactInfo",
			params: [encode$1(account_getOnrampContactInfo$1.Parameters, {
				address: address$1,
				secret
			})]
		}, { retryCount: 0 });
		return decode(account_getOnrampContactInfo$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Gets onramp status for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function onrampStatus$1(client, parameters) {
	const { address: address$1 } = parameters;
	try {
		const result = await client.request({
			method: "account_onrampStatus",
			params: [encode$1(account_onrampStatus$1.Parameters, { address: address$1 })]
		}, { retryCount: 0 });
		return decode(account_onrampStatus$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Resends phone verification for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function resendVerifyPhone$1(client, parameters) {
	const { phone, walletAddress } = parameters;
	try {
		const result = await client.request({
			method: "account_resendVerifyPhone",
			params: [encode$1(account_resendVerifyPhone$1.Parameters, {
				phone,
				walletAddress
			})]
		}, { retryCount: 0 });
		return decode(account_resendVerifyPhone$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Sets email for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function setEmail$1(client, parameters) {
	const { email, walletAddress } = parameters;
	try {
		const result = await client.request({
			method: "account_setEmail",
			params: [encode$1(account_setEmail$1.Parameters, {
				email,
				walletAddress
			})]
		}, { retryCount: 0 });
		return decode(account_setEmail$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Sets phone for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function setPhone$1(client, parameters) {
	const { phone, walletAddress } = parameters;
	try {
		const result = await client.request({
			method: "account_setPhone",
			params: [encode$1(account_setPhone$1.Parameters, {
				phone,
				walletAddress
			})]
		}, { retryCount: 0 });
		return decode(account_setPhone$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Submits an account upgrade to the Relay.
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function upgradeAccount$1(client, parameters) {
	const { context, signatures } = parameters;
	try {
		await client.request({
			method: "wallet_upgradeAccount",
			params: [encode$1(wallet_upgradeAccount$1.Parameters, {
				context,
				signatures
			})]
		}, { retryCount: 0 });
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Verifies email for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function verifyEmail$1(client, parameters) {
	const { chainId, email, signature, token, walletAddress } = parameters;
	try {
		const result = await client.request({
			method: "account_verifyEmail",
			params: [encode$1(account_verifyEmail$1.Parameters, {
				chainId,
				email,
				signature,
				token,
				walletAddress
			})]
		}, { retryCount: 0 });
		return decode(account_verifyEmail$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Verifies email for address
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function verifyPhone$1(client, parameters) {
	const { code, phone, walletAddress } = parameters;
	try {
		const result = await client.request({
			method: "account_verifyPhone",
			params: [encode$1(account_verifyPhone$1.Parameters, {
				code,
				phone,
				walletAddress
			})]
		}, { retryCount: 0 });
		return decode(account_verifyPhone$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		parseExecutionError(error);
		throw error;
	}
}
/**
* Verifies a prepare calls response.
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Whether or not the response is valid.
*/
async function verifyPrepareCallsResponse(client, parameters) {
	const { signature } = parameters;
	const { signature: _, capabilities: { feeSignature: __, ...capabilities }, ...response } = parameters.response;
	const sorted = sortKeys({
		capabilities,
		...response
	});
	const payload = keccak256(fromString(JSON.stringify(sorted)));
	const address$1 = recoverAddress({
		payload,
		signature: fromHex$1(signature)
	});
	const { quoteSigner } = await health(client);
	return address$1 === quoteSigner;
}
/**
* Verifies a signature.
*
* @example
* TODO
*
* @param client - The client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function verifySignature(client, parameters) {
	const { address: address$1, chain = client.chain, digest, signature } = parameters;
	try {
		async function fallback$1() {
			return {
				proof: null,
				valid: await verifyHash(client, {
					address: address$1,
					hash: digest,
					signature
				})
			};
		}
		const method = "wallet_verifySignature";
		const result = await (async () => {
			const result$1 = await client.request({
				method,
				params: [encode$1(wallet_verifySignature$1.Parameters, {
					address: address$1,
					chainId: chain?.id,
					digest,
					signature
				})]
			}, { retryCount: 0 }).catch(fallback$1);
			if (result$1.valid) return result$1;
			return fallback$1();
		})();
		return decode(wallet_verifySignature$1.Response, result);
	} catch (error) {
		parseSchemaError(error);
		throw error;
	}
}
function parseExecutionError(e, { calls } = {}) {
	if (!(e instanceof BaseError)) return;
	const getAbiError = (error$1) => {
		try {
			if (error$1.name === "ContractFunctionExecutionError") {
				const data$1 = error$1.cause.name === "ContractFunctionRevertedError" ? error$1.cause.data : void 0;
				if (data$1) return fromAbi([data$1.abiItem], data$1.errorName);
			}
			const cause = error$1.walk((e$1) => !(e$1 instanceof Error) && e$1.code === 3);
			if (!cause) return void 0;
			const { data, message } = cause;
			if (data === "0xd0d5039b") return from$6("error Unauthorized()");
			return {
				inputs: [],
				name: (message ?? data).split("(")[0],
				type: "error"
			};
		} catch {
			return;
		}
	};
	const error = getExecuteError(e, { calls: calls ?? [] });
	const abiError = getAbiError(error);
	if (error === e && !abiError) return;
	throw new ExecutionError(Object.assign(error, { abiError }));
}
function sortKeys(value) {
	if (typeof value === "object" && value !== null) {
		if (Array.isArray(value)) return value.map(sortKeys);
		const result = {};
		for (const key of Object.keys(value).sort()) result[key] = sortKeys(value[key]);
		return result;
	}
	return value;
}
/** Thrown when schema validation fails. */
function parseSchemaError(e) {
	if (e.name === "$ZodError") throw toValidationError(e);
}
/** Thrown when the execution fails. */
var ExecutionError = class extends BaseError$2 {
	constructor(cause) {
		super("An error occurred while executing calls.", {
			cause,
			metaMessages: [cause.abiError && "Reason: " + cause.abiError.name].filter(Boolean)
		});
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Rpc.ExecutionError"
		});
		Object.defineProperty(this, "abiError", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: void 0
		});
		this.abiError = cause.abiError;
	}
};

//#endregion
//#region node_modules/porto/dist/core/Transport.js
var Transport_exports = /* @__PURE__ */ __export({
	fallback: () => fallback,
	http: () => http,
	relayProxy: () => relayProxy,
	relayUrls: () => relayUrls,
	webSocket: () => webSocket
});
const relayUrls = {
	anvil: { http: "http://localhost:9119" },
	prod: { http: "https://rpc.porto.sh" },
	stg: { http: "https://stg-rpc.porto.sh" }
};
function relayProxy(transports) {
	return (config) => {
		const transport_public = transports.public(config);
		const transport_relay = transports.relay(config);
		return createTransport({
			key: relayProxy.type,
			name: "Relay Proxy",
			async request({ method, params }, options) {
				if (isRelay(method)) return transport_relay.request({
					method,
					params
				}, options);
				return transport_public.request({
					method,
					params
				}, options);
			},
			type: relayProxy.type
		});
	};
}
(function(relayProxy$1) {
	relayProxy$1.type = "relayProxy";
})(relayProxy || (relayProxy = {}));
/** @internal */
function isRelay(method) {
	if (method.startsWith("wallet_")) return true;
	if (method.startsWith("account_")) return true;
	if (method === "health") return true;
	return false;
}

//#endregion
//#region node_modules/porto/dist/viem/RelayClient.js
var clientCache = /* @__PURE__ */ new Map();
/**
* Extracts a Viem Client from a Porto instance, and an optional chain ID.
* By default, the Client for the current chain ID will be extracted.
*
* @param porto - Porto instance.
* @param parameters - Parameters.
* @returns Client.
*/
function fromPorto(porto, config = {}) {
	const { config: config_, id, store } = porto._internal;
	const { chains, relay: relay$1 } = config_;
	const state = store.getState();
	const chainId = config.chainId ?? state.chainIds[0];
	const chain = chains.find((chain$1) => chain$1.id === chainId);
	if (!chain) throw new Error([
		"Could not find a compatible Porto chain on the given chain configuration.",
		"",
		`Provided chains: [${chains.map((chain$1) => `${chain$1.name} (id: ${chain$1.id})`).join(", ")}]`,
		`Needed chain (id): ${chainId}`,
		"Please add this chain (id) to your chain configuration."
	].join("\n"));
	const transport = relayProxy({
		public: config_.transports[chain.id] ?? fallback(chain.rpcUrls.default.http.map((url) => http(url))),
		relay: relay$1
	});
	const key = [id, stringify(chain)].filter(Boolean).join(":");
	if (clientCache.has(key)) return clientCache.get(key);
	const client = createClient({
		...config,
		chain,
		pollingInterval: 1e3,
		transport
	});
	clientCache.set(key, client);
	return client;
}

//#endregion
//#region node_modules/porto/dist/core/internal/schema/key.js
const Base = object({
	chainId: optional(number()),
	expiry: number(),
	hash: hex(),
	id: hex(),
	prehash: optional(boolean()),
	publicKey: hex(),
	role: union([literal("admin"), literal("session")]),
	type: union([
		literal("address"),
		literal("p256"),
		literal("secp256k1"),
		literal("webauthn-p256")
	])
});
const CallPermissions = readonly(array(oneOf([
	object({
		signature: string(),
		to: address()
	}),
	object({ signature: string() }),
	object({ to: address() })
])).check(_minLength(1)));
const FeeToken = object({
	limit: union([templateLiteral([
		number$1(),
		".",
		number$1()
	]), templateLiteral([number$1()])]).check(_regex(/^\d+(\.\d+)?$/)),
	symbol: optional(union([literal("native"), Symbol]))
});
const SignatureVerificationPermission = object({ addresses: readonly(array(address())) });
const SpendPermissions = readonly(array(object({
	limit: bigint(),
	period: union([
		literal("minute"),
		literal("hour"),
		literal("day"),
		literal("week"),
		literal("month"),
		literal("year")
	]),
	token: optional(address())
})));
const Permissions$1 = object({
	calls: optional(CallPermissions),
	signatureVerification: optional(SignatureVerificationPermission),
	spend: optional(SpendPermissions)
});
const WithPermissions = object({
	...Base.shape,
	feeToken: optional(nullable(FeeToken)),
	permissions: optional(Permissions$1)
});

//#endregion
//#region node_modules/porto/dist/core/internal/schema/permissions.js
const Permissions = object({
	address: address(),
	chainId: optional(number()),
	expiry: number$1(),
	id: hex(),
	key: pick(Base, {
		publicKey: true,
		type: true
	}),
	permissions: object({
		calls: CallPermissions,
		signatureVerification: optional(SignatureVerificationPermission),
		spend: optional(SpendPermissions)
	})
});
const Request$1 = object({
	address: optional(address()),
	chainId: optional(number()),
	expiry: number$1().check(_gte(1)),
	feeToken: nullable(FeeToken),
	key: optional(pick(Base, {
		publicKey: true,
		type: true
	})),
	permissions: object({
		calls: CallPermissions,
		signatureVerification: optional(SignatureVerificationPermission),
		spend: optional(SpendPermissions)
	})
});

//#endregion
//#region node_modules/porto/dist/core/internal/permissions.js
const Schema$1 = Permissions;
function fromKey$1(key, options) {
	const { chainId, expiry, permissions: permissions$1, id, publicKey, type } = key;
	const { address: address$1 } = options;
	return {
		address: address$1,
		chainId,
		expiry,
		id,
		key: {
			publicKey,
			type
		},
		permissions: permissions$1 ?? {}
	};
}
function toKey$1(permissions$1) {
	const { chainId, expiry, key } = permissions$1;
	return from$14({
		chainId,
		expiry,
		permissions: permissions$1.permissions ?? {},
		publicKey: key.publicKey,
		role: "session",
		type: key.type
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/schema/capabilities.js
var atomic;
(function(atomic$1) {
	atomic$1.GetCapabilitiesResponse = object({ status: union([literal("supported"), literal("unsupported")]) });
})(atomic || (atomic = {}));
var createAccount$1;
(function(createAccount$2) {
	createAccount$2.Request = union([boolean(), object({
		chainId: optional(number()),
		label: optional(string())
	})]);
})(createAccount$1 || (createAccount$1 = {}));
var signInWithEthereum;
(function(signInWithEthereum$1) {
	signInWithEthereum$1.Request = oneOf([object({
		chainId: optional(number$1()),
		domain: optional(string()),
		expirationTime: optional(date()),
		issuedAt: optional(date()),
		nonce: string(),
		notBefore: optional(date()),
		requestId: optional(string()),
		resources: optional(readonly(array(string()))),
		scheme: optional(string()),
		statement: optional(string()),
		uri: optional(string()),
		version: optional(literal("1"))
	}), object({
		authUrl: union([string(), object({
			logout: string(),
			nonce: string(),
			verify: string()
		})]),
		chainId: optional(number()),
		domain: optional(string()),
		expirationTime: optional(date()),
		issuedAt: optional(date()),
		notBefore: optional(date()),
		requestId: optional(string()),
		resources: optional(readonly(array(string()))),
		scheme: optional(string()),
		statement: optional(string()),
		uri: optional(string()),
		version: optional(literal("1"))
	})]);
	signInWithEthereum$1.Response = object({
		message: string(),
		signature: hex(),
		token: optional(string())
	});
})(signInWithEthereum || (signInWithEthereum = {}));
var feeToken;
(function(feeToken$1) {
	feeToken$1.GetCapabilitiesResponse = object({
		supported: boolean(),
		tokens: readonly(array(Token))
	});
	feeToken$1.Request = union([Symbol, address()]);
})(feeToken || (feeToken = {}));
var grantPermissions;
(function(grantPermissions$1) {
	grantPermissions$1.Request = Request$1;
})(grantPermissions || (grantPermissions = {}));
var merchant;
(function(merchant$1) {
	merchant$1.GetCapabilitiesResponse = object({ supported: boolean() });
})(merchant || (merchant = {}));
var permissions;
(function(permissions$1) {
	permissions$1.GetCapabilitiesResponse = object({ supported: boolean() });
	permissions$1.Request = object({ id: optional(union([hex(), _null()])) });
	permissions$1.Response = readonly(array(Permissions));
})(permissions || (permissions = {}));
var preCalls;
(function(preCalls$1) {
	preCalls$1.Request = readonly(array(object({
		context: unknown(),
		signature: hex()
	})));
	preCalls$1.Response = preCalls$1.Request;
})(preCalls || (preCalls = {}));
var merchantUrl;
(function(merchantUrl$1) {
	merchantUrl$1.Request = string();
})(merchantUrl || (merchantUrl = {}));
var requiredFunds;
(function(requiredFunds$2) {
	requiredFunds$2.GetCapabilitiesResponse = object({
		supported: boolean(),
		tokens: readonly(array(Token))
	});
	requiredFunds$2.Request = readonly(array(oneOf([object({
		address: address(),
		value: bigint()
	}), object({
		symbol: Symbol,
		value: union([templateLiteral([
			number$1(),
			".",
			number$1()
		]), templateLiteral([number$1()])]).check(_regex(/^\d+(\.\d+)?$/))
	})])));
})(requiredFunds || (requiredFunds = {}));

//#endregion
//#region node_modules/porto/dist/core/internal/schema/rpc.js
var KeyWithCredentialId = object({
	...pick(Base, {
		id: true,
		publicKey: true,
		type: true
	}).shape,
	credentialId: optional(string()),
	privateKey: optional(any())
});
var account_getOnrampContactInfo;
(function(account_getOnrampContactInfo$2) {
	account_getOnrampContactInfo$2.Parameters = object({
		address: address(),
		secret: string()
	});
	account_getOnrampContactInfo$2.Request = object({
		method: literal("account_getOnrampContactInfo"),
		params: readonly(tuple([account_getOnrampContactInfo$2.Parameters]))
	});
	account_getOnrampContactInfo$2.Response = object({
		email: optional(string()),
		phone: optional(string()),
		phoneVerifiedAt: optional(number$1())
	});
})(account_getOnrampContactInfo || (account_getOnrampContactInfo = {}));
var account_onrampStatus;
(function(account_onrampStatus$2) {
	account_onrampStatus$2.Parameters = object({ address: address() });
	account_onrampStatus$2.Request = object({
		method: literal("account_onrampStatus"),
		params: readonly(tuple([account_onrampStatus$2.Parameters]))
	});
	account_onrampStatus$2.Response = object({
		email: optional(number$1()),
		phone: optional(number$1())
	});
})(account_onrampStatus || (account_onrampStatus = {}));
var account_resendVerifyPhone;
(function(account_resendVerifyPhone$2) {
	account_resendVerifyPhone$2.Parameters = object({
		email: string(),
		walletAddress: address()
	});
	account_resendVerifyPhone$2.Request = object({
		method: literal("account_resendVerifyPhone"),
		params: readonly(tuple([account_resendVerifyPhone$2.Parameters]))
	});
	account_resendVerifyPhone$2.Response = _null();
})(account_resendVerifyPhone || (account_resendVerifyPhone = {}));
var account_setEmail;
(function(account_setEmail$2) {
	account_setEmail$2.Parameters = object({
		email: string(),
		walletAddress: address()
	});
	account_setEmail$2.Request = object({
		method: literal("account_setEmail"),
		params: readonly(tuple([account_setEmail$2.Parameters]))
	});
	account_setEmail$2.Response = _null();
})(account_setEmail || (account_setEmail = {}));
var account_setPhone;
(function(account_setPhone$2) {
	account_setPhone$2.Parameters = object({
		email: string(),
		walletAddress: address()
	});
	account_setPhone$2.Request = object({
		method: literal("account_setPhone"),
		params: readonly(tuple([account_setPhone$2.Parameters]))
	});
	account_setPhone$2.Response = _null();
})(account_setPhone || (account_setPhone = {}));
var account_verifyEmail;
(function(account_verifyEmail$2) {
	account_verifyEmail$2.Parameters = object({
		chainId: number(),
		email: string(),
		token: string(),
		walletAddress: address()
	});
	account_verifyEmail$2.Request = object({
		method: literal("account_verifyEmail"),
		params: readonly(tuple([account_verifyEmail$2.Parameters]))
	});
	account_verifyEmail$2.Response = _null();
})(account_verifyEmail || (account_verifyEmail = {}));
var account_verifyPhone;
(function(account_verifyPhone$2) {
	account_verifyPhone$2.Parameters = object({
		code: string(),
		phone: string(),
		walletAddress: address()
	});
	account_verifyPhone$2.Request = object({
		method: literal("account_verifyPhone"),
		params: readonly(tuple([account_verifyPhone$2.Parameters]))
	});
	account_verifyPhone$2.Response = _null();
})(account_verifyPhone || (account_verifyPhone = {}));
var wallet_addFunds;
(function(wallet_addFunds$1) {
	wallet_addFunds$1.Parameters = object({
		address: optional(address()),
		chainId: optional(number()),
		token: optional(address()),
		value: optional(string())
	});
	wallet_addFunds$1.Request = object({
		method: literal("wallet_addFunds"),
		params: readonly(tuple([wallet_addFunds$1.Parameters]))
	});
	wallet_addFunds$1.Response = object({ id: hex() });
})(wallet_addFunds || (wallet_addFunds = {}));
var eth_accounts;
(function(eth_accounts$1) {
	eth_accounts$1.Request = object({
		method: literal("eth_accounts"),
		params: optional(unknown())
	});
	eth_accounts$1.Response = readonly(array(address()));
})(eth_accounts || (eth_accounts = {}));
var eth_chainId;
(function(eth_chainId$1) {
	eth_chainId$1.Request = object({
		method: literal("eth_chainId"),
		params: optional(unknown())
	});
	eth_chainId$1.Response = hex();
})(eth_chainId || (eth_chainId = {}));
var eth_requestAccounts;
(function(eth_requestAccounts$1) {
	eth_requestAccounts$1.Request = object({
		method: literal("eth_requestAccounts"),
		params: optional(unknown())
	});
	eth_requestAccounts$1.Response = readonly(array(address()));
})(eth_requestAccounts || (eth_requestAccounts = {}));
var eth_sendTransaction;
(function(eth_sendTransaction$1) {
	eth_sendTransaction$1.Request = object({
		method: literal("eth_sendTransaction"),
		params: readonly(tuple([object({
			capabilities: optional(object({
				feeToken: optional(feeToken.Request),
				merchantUrl: optional(merchantUrl.Request),
				preCalls: optional(preCalls.Request)
			})),
			chainId: optional(number()),
			data: optional(hex()),
			from: optional(address()),
			to: address(),
			value: optional(bigint())
		})]))
	});
	eth_sendTransaction$1.Response = hex();
})(eth_sendTransaction || (eth_sendTransaction = {}));
var eth_signTypedData_v4;
(function(eth_signTypedData_v4$1) {
	eth_signTypedData_v4$1.Request = object({
		method: literal("eth_signTypedData_v4"),
		params: readonly(tuple([address(), string()]))
	});
	eth_signTypedData_v4$1.Response = hex();
})(eth_signTypedData_v4 || (eth_signTypedData_v4 = {}));
var wallet_getAdmins;
(function(wallet_getAdmins$1) {
	wallet_getAdmins$1.Parameters = object({
		address: optional(address()),
		chainId: optional(number())
	});
	wallet_getAdmins$1.Request = object({
		method: literal("wallet_getAdmins"),
		params: optional(readonly(tuple([wallet_getAdmins$1.Parameters])))
	});
	wallet_getAdmins$1.Key = KeyWithCredentialId;
	wallet_getAdmins$1.Response = object({
		address: address(),
		chainId: number(),
		keys: readonly(array(wallet_getAdmins$1.Key))
	});
})(wallet_getAdmins || (wallet_getAdmins = {}));
var wallet_grantAdmin;
(function(wallet_grantAdmin$1) {
	wallet_grantAdmin$1.Capabilities = object({ feeToken: optional(feeToken.Request) });
	wallet_grantAdmin$1.Parameters = object({
		address: optional(address()),
		capabilities: optional(wallet_grantAdmin$1.Capabilities),
		chainId: optional(number()),
		key: pick(Base, {
			publicKey: true,
			type: true
		})
	});
	wallet_grantAdmin$1.Request = object({
		method: literal("wallet_grantAdmin"),
		params: readonly(tuple([wallet_grantAdmin$1.Parameters]))
	});
	wallet_grantAdmin$1.Response = object({
		address: address(),
		chainId: number(),
		key: wallet_getAdmins.Key
	});
})(wallet_grantAdmin || (wallet_grantAdmin = {}));
var wallet_grantPermissions;
(function(wallet_grantPermissions$1) {
	wallet_grantPermissions$1.Parameters = Request$1;
	wallet_grantPermissions$1.Request = object({
		method: literal("wallet_grantPermissions"),
		params: readonly(tuple([wallet_grantPermissions$1.Parameters]))
	});
	wallet_grantPermissions$1.ResponseCapabilities = object({ preCalls: optional(preCalls.Response) });
	wallet_grantPermissions$1.Response = object({
		...Permissions.shape,
		capabilities: optional(any())
	});
})(wallet_grantPermissions || (wallet_grantPermissions = {}));
var wallet_getAccountVersion;
(function(wallet_getAccountVersion$1) {
	wallet_getAccountVersion$1.Parameters = object({ address: optional(address()) });
	wallet_getAccountVersion$1.Request = object({
		method: literal("wallet_getAccountVersion"),
		params: optional(readonly(tuple([wallet_getAccountVersion$1.Parameters])))
	});
	wallet_getAccountVersion$1.Response = object({
		current: string(),
		latest: string()
	});
})(wallet_getAccountVersion || (wallet_getAccountVersion = {}));
var wallet_getPermissions;
(function(wallet_getPermissions$1) {
	wallet_getPermissions$1.Parameters = object({
		address: optional(address()),
		chainIds: optional(readonly(array(number())))
	});
	wallet_getPermissions$1.Request = object({
		method: literal("wallet_getPermissions"),
		params: optional(readonly(tuple([wallet_getPermissions$1.Parameters])))
	});
	wallet_getPermissions$1.Response = permissions.Response;
})(wallet_getPermissions || (wallet_getPermissions = {}));
var wallet_revokeAdmin;
(function(wallet_revokeAdmin$1) {
	wallet_revokeAdmin$1.Capabilities = object({ feeToken: optional(feeToken.Request) });
	wallet_revokeAdmin$1.Parameters = object({
		address: optional(address()),
		capabilities: optional(wallet_revokeAdmin$1.Capabilities),
		chainId: optional(number()),
		id: hex()
	});
	wallet_revokeAdmin$1.Request = object({
		method: literal("wallet_revokeAdmin"),
		params: readonly(tuple([wallet_revokeAdmin$1.Parameters]))
	});
	wallet_revokeAdmin$1.Response = void 0;
})(wallet_revokeAdmin || (wallet_revokeAdmin = {}));
var wallet_revokePermissions;
(function(wallet_revokePermissions$1) {
	wallet_revokePermissions$1.Capabilities = object({ feeToken: optional(feeToken.Request) });
	wallet_revokePermissions$1.Parameters = object({
		address: optional(address()),
		capabilities: optional(wallet_revokePermissions$1.Capabilities),
		id: hex()
	});
	wallet_revokePermissions$1.Request = object({
		method: literal("wallet_revokePermissions"),
		params: readonly(tuple([wallet_revokePermissions$1.Parameters]))
	});
	wallet_revokePermissions$1.Response = void 0;
})(wallet_revokePermissions || (wallet_revokePermissions = {}));
var wallet_switchEthereumChain;
(function(wallet_switchEthereumChain$1) {
	wallet_switchEthereumChain$1.Request = object({
		method: literal("wallet_switchEthereumChain"),
		params: readonly(tuple([object({ chainId: hex() })]))
	});
})(wallet_switchEthereumChain || (wallet_switchEthereumChain = {}));
var wallet_upgradeAccount;
(function(wallet_upgradeAccount$2) {
	wallet_upgradeAccount$2.Parameters = object({
		context: unknown(),
		signatures: object({
			auth: hex(),
			exec: hex()
		})
	});
	wallet_upgradeAccount$2.Request = object({
		method: literal("wallet_upgradeAccount"),
		params: readonly(tuple([wallet_upgradeAccount$2.Parameters]))
	});
	wallet_upgradeAccount$2.ResponseCapabilities = object({
		admins: optional(readonly(array(wallet_getAdmins.Key))),
		permissions: optional(permissions.Response)
	});
	wallet_upgradeAccount$2.Response = object({
		address: address(),
		capabilities: optional(wallet_upgradeAccount$2.ResponseCapabilities)
	});
})(wallet_upgradeAccount || (wallet_upgradeAccount = {}));
var personal_sign;
(function(personal_sign$1) {
	personal_sign$1.Request = object({
		method: literal("personal_sign"),
		params: readonly(tuple([hex(), address()]))
	});
	personal_sign$1.Response = hex();
})(personal_sign || (personal_sign = {}));
var porto_ping;
(function(porto_ping$1) {
	porto_ping$1.Request = object({
		method: literal("porto_ping"),
		params: optional(_undefined())
	});
	porto_ping$1.Response = literal("pong");
})(porto_ping || (porto_ping = {}));
var wallet_connect;
(function(wallet_connect$1) {
	wallet_connect$1.Capabilities = object({
		createAccount: optional(createAccount$1.Request),
		email: optional(boolean()),
		grantAdmins: optional(readonly(array(pick(Base, {
			publicKey: true,
			type: true
		})))),
		grantPermissions: optional(grantPermissions.Request),
		preCalls: optional(preCalls.Request),
		selectAccount: optional(union([boolean(), object({
			address: address(),
			key: optional(object({
				credentialId: optional(string()),
				publicKey: hex()
			}))
		})])),
		signInWithEthereum: optional(signInWithEthereum.Request)
	});
	wallet_connect$1.Parameters = object({
		capabilities: optional(wallet_connect$1.Capabilities),
		chainIds: optional(readonly(array(number())))
	});
	wallet_connect$1.Request = object({
		method: literal("wallet_connect"),
		params: optional(readonly(tuple([wallet_connect$1.Parameters])))
	});
	wallet_connect$1.ResponseCapabilities = object({
		admins: optional(readonly(array(object({
			...pick(Base, {
				id: true,
				publicKey: true,
				type: true
			}).shape,
			credentialId: optional(string())
		})))),
		permissions: optional(permissions.Response),
		preCalls: optional(preCalls.Response),
		signInWithEthereum: optional(signInWithEthereum.Response)
	});
	wallet_connect$1.Response = object({
		accounts: readonly(array(object({
			address: address(),
			capabilities: optional(wallet_connect$1.ResponseCapabilities)
		}))),
		chainIds: readonly(array(number()))
	});
})(wallet_connect || (wallet_connect = {}));
var wallet_disconnect;
(function(wallet_disconnect$1) {
	wallet_disconnect$1.Request = object({
		method: literal("wallet_disconnect"),
		params: optional(unknown())
	});
	wallet_disconnect$1.Response = void 0;
})(wallet_disconnect || (wallet_disconnect = {}));
var wallet_getAssets;
(function(wallet_getAssets$2) {
	/** Parameters  */
	wallet_getAssets$2.Parameters = wallet_getAssets$1.Parameters;
	/** Request for `wallet_getAssets`. */
	wallet_getAssets$2.Request = wallet_getAssets$1.Request;
	/** Response for `wallet_getAssets`. */
	wallet_getAssets$2.Response = wallet_getAssets$1.Response;
})(wallet_getAssets || (wallet_getAssets = {}));
var wallet_getCallsStatus;
(function(wallet_getCallsStatus$2) {
	wallet_getCallsStatus$2.Request = object({
		method: literal("wallet_getCallsStatus"),
		params: tuple([hex()])
	});
	wallet_getCallsStatus$2.Response = object({
		atomic: boolean(),
		chainId: number(),
		id: string(),
		receipts: optional(readonly(array(object({
			blockHash: hex(),
			blockNumber: hex(),
			gasUsed: hex(),
			logs: readonly(array(object({
				address: address(),
				data: hex(),
				topics: readonly(array(hex()))
			}))),
			status: hex(),
			transactionHash: hex()
		})))),
		status: number$1(),
		version: string()
	});
})(wallet_getCallsStatus || (wallet_getCallsStatus = {}));
var wallet_getCapabilities;
(function(wallet_getCapabilities$2) {
	wallet_getCapabilities$2.Request = object({
		method: literal("wallet_getCapabilities"),
		params: optional(union([readonly(tuple([union([hex(), _undefined()])])), readonly(tuple([union([hex(), _undefined()]), readonly(array(number()))]))]))
	});
	wallet_getCapabilities$2.Response = record(hex(), object({
		atomic: atomic.GetCapabilitiesResponse,
		feeToken: feeToken.GetCapabilitiesResponse,
		merchant: merchant.GetCapabilitiesResponse,
		permissions: permissions.GetCapabilitiesResponse,
		requiredFunds: requiredFunds.GetCapabilitiesResponse
	}));
})(wallet_getCapabilities || (wallet_getCapabilities = {}));
var wallet_getKeys;
(function(wallet_getKeys$2) {
	wallet_getKeys$2.Parameters = object({
		address: address(),
		chainIds: optional(readonly(array(number())))
	});
	wallet_getKeys$2.Request = object({
		method: literal("wallet_getKeys"),
		params: readonly(tuple([wallet_getKeys$2.Parameters]))
	});
	wallet_getKeys$2.Response = readonly(array(WithPermissions));
})(wallet_getKeys || (wallet_getKeys = {}));
var wallet_prepareCalls;
(function(wallet_prepareCalls$2) {
	wallet_prepareCalls$2.Capabilities = object({
		feeToken: optional(feeToken.Request),
		merchantUrl: optional(merchantUrl.Request),
		permissions: optional(permissions.Request),
		preCalls: optional(preCalls.Request),
		requiredFunds: optional(requiredFunds.Request)
	});
	wallet_prepareCalls$2.Parameters = object({
		calls: readonly(array(object({
			data: optional(hex()),
			to: address(),
			value: optional(bigint())
		}))),
		capabilities: optional(wallet_prepareCalls$2.Capabilities),
		chainId: optional(number()),
		from: optional(address()),
		key: optional(pick(Base, {
			prehash: true,
			publicKey: true,
			type: true
		})),
		version: optional(string())
	});
	wallet_prepareCalls$2.Request = object({
		method: literal("wallet_prepareCalls"),
		params: readonly(tuple([wallet_prepareCalls$2.Parameters]))
	});
	wallet_prepareCalls$2.Response = object({
		capabilities: optional(object({
			...wallet_prepareCalls$1.ResponseCapabilities.shape,
			quote: optional(Signed)
		})),
		chainId: hex(),
		context: object({
			account: object({ address: address() }),
			calls: wallet_prepareCalls$2.Parameters.shape.calls,
			nonce: bigint(),
			quote: optional(partial(Signed))
		}),
		digest: hex(),
		key: pick(Base, {
			prehash: true,
			publicKey: true,
			type: true
		}),
		typedData: object({
			domain: union([object({
				chainId: number(),
				name: string(),
				verifyingContract: address(),
				version: string()
			}), object({})]),
			message: record(string(), unknown()),
			primaryType: string(),
			types: record(string(), unknown())
		})
	});
})(wallet_prepareCalls || (wallet_prepareCalls = {}));
var wallet_prepareUpgradeAccount;
(function(wallet_prepareUpgradeAccount$2) {
	wallet_prepareUpgradeAccount$2.Capabilities = object({
		...wallet_connect.Capabilities.shape,
		label: optional(string())
	});
	wallet_prepareUpgradeAccount$2.Parameters = object({
		address: address(),
		capabilities: optional(wallet_prepareUpgradeAccount$2.Capabilities),
		chainId: optional(number())
	});
	wallet_prepareUpgradeAccount$2.Request = object({
		method: literal("wallet_prepareUpgradeAccount"),
		params: readonly(tuple([wallet_prepareUpgradeAccount$2.Parameters]))
	});
	wallet_prepareUpgradeAccount$2.Response = object({
		context: unknown(),
		digests: object({
			auth: hex(),
			exec: hex()
		})
	});
})(wallet_prepareUpgradeAccount || (wallet_prepareUpgradeAccount = {}));
var wallet_sendCalls;
(function(wallet_sendCalls$1) {
	wallet_sendCalls$1.Capabilities = wallet_prepareCalls.Capabilities;
	wallet_sendCalls$1.Request = object({
		method: literal("wallet_sendCalls"),
		params: readonly(tuple([omit(wallet_prepareCalls.Parameters, { key: true })]))
	});
	wallet_sendCalls$1.Response = object({ id: hex() });
})(wallet_sendCalls || (wallet_sendCalls = {}));
var wallet_sendPreparedCalls;
(function(wallet_sendPreparedCalls$2) {
	wallet_sendPreparedCalls$2.Parameters = object({
		capabilities: wallet_prepareCalls.Response.shape.capabilities,
		chainId: hex(),
		context: wallet_prepareCalls.Response.shape.context,
		key: wallet_prepareCalls.Response.shape.key,
		signature: hex()
	});
	wallet_sendPreparedCalls$2.Request = object({
		method: literal("wallet_sendPreparedCalls"),
		params: readonly(tuple([wallet_sendPreparedCalls$2.Parameters]))
	});
	wallet_sendPreparedCalls$2.Response = readonly(array(object({
		capabilities: optional(record(string(), unknown())),
		id: hex()
	})));
})(wallet_sendPreparedCalls || (wallet_sendPreparedCalls = {}));
var wallet_verifySignature;
(function(wallet_verifySignature$2) {
	wallet_verifySignature$2.Parameters = object({
		address: address(),
		chainId: optional(number()),
		digest: hex(),
		signature: hex()
	});
	/** Request for `wallet_verifySignature`. */
	wallet_verifySignature$2.Request = object({
		method: literal("wallet_verifySignature"),
		params: readonly(tuple([wallet_verifySignature$2.Parameters]))
	});
	/** Response for `wallet_verifySignature`. */
	wallet_verifySignature$2.Response = object({
		address: address(),
		chainId: number(),
		proof: optional(unknown()),
		valid: boolean()
	});
})(wallet_verifySignature || (wallet_verifySignature = {}));

//#endregion
//#region node_modules/porto/dist/core/internal/schema/request.js
const Request = discriminatedUnion("method", [
	account_verifyEmail.Request,
	wallet_addFunds.Request,
	eth_accounts.Request,
	eth_chainId.Request,
	eth_requestAccounts.Request,
	eth_sendTransaction.Request,
	eth_signTypedData_v4.Request,
	wallet_getAccountVersion.Request,
	wallet_getAdmins.Request,
	wallet_getPermissions.Request,
	wallet_grantAdmin.Request,
	wallet_grantPermissions.Request,
	wallet_prepareUpgradeAccount.Request,
	wallet_revokeAdmin.Request,
	wallet_revokePermissions.Request,
	wallet_upgradeAccount.Request,
	personal_sign.Request,
	porto_ping.Request,
	wallet_connect.Request,
	wallet_disconnect.Request,
	wallet_getAssets.Request,
	wallet_getCallsStatus.Request,
	wallet_getCapabilities.Request,
	wallet_getKeys.Request,
	wallet_prepareCalls.Request,
	wallet_sendCalls.Request,
	wallet_sendPreparedCalls.Request,
	wallet_switchEthereumChain.Request,
	wallet_verifySignature.Request
]);
function validate(schema, value) {
	const result = safeParse(schema, value);
	if (result.error) {
		const issue = result.error.issues.at(0);
		if (issue?.code === "invalid_union" && issue.note === "No matching discriminator") throw new MethodNotSupportedError();
		throw new InvalidParamsError(toValidationError(result.error));
	}
	return {
		...value,
		_decoded: result.data
	};
}

//#endregion
//#region node_modules/porto/dist/core/internal/store.js
async function waitForHydration(store) {
	if (store.persist.hasHydrated()) return;
	await new Promise((resolve) => {
		store.persist.onFinishHydration(() => resolve(true));
		setTimeout(() => resolve(true), 100);
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/urlString.js
function toAbsolute(url) {
	if (!url) return void 0;
	if (url.startsWith("/")) return `${window.location.origin}${url}`;
	return url;
}

//#endregion
//#region node_modules/porto/dist/core/internal/provider.js
function from(parameters) {
	const { config, getMode, id, store } = parameters;
	const { announceProvider: announceProvider$1 } = config;
	function getCapabilities$1(parameters$1 = {}) {
		const client = getClient();
		const request = parameters$1.request ?? validate(Request, {
			method: "wallet_getCapabilities",
			params: parameters$1.chainIds ? [void 0, parameters$1.chainIds] : void 0
		});
		return withCache(() => getMode().actions.getCapabilities({
			chainIds: parameters$1.chainIds,
			internal: {
				client,
				config,
				request,
				store
			}
		}), { cacheKey: `getCapabilities.${id}.${parameters$1.chainIds?.join(",")}` });
	}
	function getClient(chainId_) {
		const chainId = typeof chainId_ === "string" ? toNumber(chainId_) : chainId_;
		return fromPorto({ _internal: parameters }, { chainId });
	}
	const lock = /* @__PURE__ */ new Map();
	const preparedAccounts_internal = [];
	const emitter = createEmitter();
	const provider = from$10({
		...emitter,
		async request(request_) {
			await waitForHydration(store);
			return withDedupe(async () => {
				let request;
				try {
					request = validate(Request, request_);
				} catch (e) {
					const error = e;
					if (!(error instanceof MethodNotSupportedError)) throw error;
					if (request_.method.startsWith("wallet_")) throw new UnsupportedMethodError();
					return getClient().request(request_);
				}
				const state = store.getState();
				switch (request.method) {
					case "account_verifyEmail": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [parameters$1] = request._decoded.params;
						const { chainId, email, token, walletAddress } = parameters$1;
						const client = getClient(chainId);
						if (chainId && chainId !== client.chain.id) throw new ChainDisconnectedError();
						const account = walletAddress ? state.accounts.find((account$1) => isEqual(account$1.address, walletAddress)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						return await getMode().actions.verifyEmail({
							account,
							chainId,
							email,
							internal: {
								client,
								config,
								request,
								store
							},
							token,
							walletAddress
						});
					}
					case "wallet_addFunds": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const { address: address$1, value, token } = request.params[0] ?? {};
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						const result = await getMode().actions.addFunds({
							address: account.address,
							internal: {
								client,
								config,
								request,
								store
							},
							token,
							value
						});
						emitter.emit("message", {
							data: null,
							type: "assetsChanged"
						});
						return result;
					}
					case "eth_accounts":
						if (state.accounts.length === 0) throw new DisconnectedError();
						return state.accounts.map(getAccountAddress);
					case "eth_chainId": return fromNumber(state.chainIds[0]);
					case "eth_requestAccounts": {
						if (state.accounts.length > 0 && lock.get("eth_requestAccounts")) return state.accounts.map(getAccountAddress);
						const client = getClient();
						const { accounts } = await getMode().actions.loadAccounts({ internal: {
							client,
							config,
							request,
							store
						} });
						store.setState((x) => ({
							...x,
							accounts
						}));
						emitter.emit("connect", { chainId: fromNumber(client.chain.id) });
						lock.set("eth_requestAccounts", true);
						setTimeout(() => lock.delete("eth_requestAccounts"), 1e3);
						return accounts.map(getAccountAddress);
					}
					case "eth_sendTransaction": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ capabilities, chainId, data = "0x", from: from$21, to, value }] = request._decoded.params;
						const client = getClient(chainId);
						if (chainId && chainId !== client.chain.id) throw new ChainDisconnectedError();
						const account = from$21 ? state.accounts.find((account$1) => isEqual(account$1.address, from$21)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const { id: id$1 } = await getMode().actions.sendCalls({
							account,
							asTxHash: true,
							calls: [{
								data,
								to,
								value
							}],
							chainId: client.chain.id,
							internal: {
								client,
								config,
								request,
								store
							},
							merchantUrl: toAbsolute(config.merchantUrl ?? capabilities?.merchantUrl)
						});
						return id$1;
					}
					case "eth_signTypedData_v4": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [address$1, data] = request._decoded.params;
						const account = state.accounts.find((account$1) => isEqual(account$1.address, address$1));
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						return await getMode().actions.signTypedData({
							account,
							data,
							internal: {
								client,
								config,
								request,
								store
							}
						});
					}
					case "wallet_grantAdmin": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, capabilities, chainId, key: keyToAuthorize }] = request._decoded.params ?? [{}];
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient(chainId);
						if (getAdmins([...account.keys ?? []])?.some((key$1) => key$1.publicKey?.toLowerCase() === keyToAuthorize.publicKey.toLowerCase())) throw new InvalidParamsError({ message: "Key already granted as admin." });
						const { key } = await getMode().actions.grantAdmin({
							account,
							feeToken: capabilities?.feeToken,
							internal: {
								client,
								config,
								request,
								store
							},
							key: keyToAuthorize
						});
						store.setState((x) => {
							const index = x.accounts.findIndex((x$1) => account ? isEqual(x$1.address, account.address) : true);
							if (index === -1) return x;
							return {
								...x,
								accounts: x.accounts.map((account$1, i) => i === index ? {
									...account$1,
									keys: [...account$1.keys ?? [], key]
								} : account$1)
							};
						});
						const admins = getAdmins([...account.keys ?? [], key]);
						emitter.emit("message", {
							data: null,
							type: "adminsChanged"
						});
						return encode$1(wallet_grantAdmin.Response, {
							address: account.address,
							chainId: client.chain.id,
							key: admins.at(-1)
						});
					}
					case "wallet_grantPermissions": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, chainId, ...permissions$1 }] = request._decoded.params ?? [{}];
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient(chainId);
						const { key } = await getMode().actions.grantPermissions({
							account,
							internal: {
								client,
								config,
								request,
								store
							},
							permissions: permissions$1
						});
						store.setState((x) => {
							const index = x.accounts.findIndex((x$1) => account ? isEqual(x$1.address, account.address) : true);
							if (index === -1) return x;
							return {
								...x,
								accounts: x.accounts.map((account$1, i) => i === index ? {
									...account$1,
									keys: [...account$1.keys ?? [], key]
								} : account$1)
							};
						});
						emitter.emit("message", {
							data: null,
							type: "permissionsChanged"
						});
						return encode$1(wallet_grantPermissions.Response, { ...fromKey$1(key, { address: account.address }) });
					}
					case "wallet_getAdmins": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, chainId }] = request._decoded.params ?? [{}];
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient(chainId);
						const admins = getAdmins(await getMode().actions.getKeys({
							account,
							internal: {
								client,
								config,
								request,
								store
							}
						}));
						return encode$1(wallet_getAdmins.Response, {
							address: account.address,
							chainId: client.chain.id,
							keys: admins
						});
					}
					case "wallet_prepareUpgradeAccount": {
						const [{ address: address$1, capabilities, chainId }] = request._decoded.params ?? [{}];
						const { email, label, grantPermissions: permissions$1 } = capabilities ?? {};
						const client = getClient(chainId);
						const { context, digests } = await getMode().actions.prepareUpgradeAccount({
							address: address$1,
							email,
							internal: {
								client,
								config,
								request,
								store
							},
							label,
							permissions: permissions$1
						});
						preparedAccounts_internal.push(context.account);
						return {
							context,
							digests
						};
					}
					case "wallet_getAccountVersion": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1 }] = request._decoded.params ?? [{}];
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						const { current, latest } = await getMode().actions.getAccountVersion({
							address: account.address,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						return {
							current,
							latest
						};
					}
					case "wallet_getKeys": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, chainIds }] = request._decoded.params ?? [{}];
						const account = state.accounts.find((account$1) => isEqual(account$1.address, address$1));
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						const keys = await getMode().actions.getKeys({
							account,
							chainIds,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						return encode$1(wallet_getKeys.Response, keys);
					}
					case "wallet_getPermissions": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, chainIds }] = request._decoded.params ?? [{}];
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						return getActivePermissions(await getMode().actions.getKeys({
							account,
							chainIds,
							internal: {
								client,
								config,
								request,
								store
							}
						}), { address: account.address });
					}
					case "wallet_revokeAdmin": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, capabilities, id: id$1 }] = request._decoded.params;
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						await getMode().actions.revokeAdmin({
							account,
							feeToken: capabilities?.feeToken,
							id: id$1,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						const keys = account.keys?.filter((key) => key.id.toLowerCase() !== id$1.toLowerCase());
						store.setState((x) => ({
							...x,
							accounts: x.accounts.map((x$1) => isEqual(x$1.address, account.address) ? {
								...x$1,
								keys
							} : x$1)
						}));
						emitter.emit("message", {
							data: null,
							type: "adminsChanged"
						});
						return;
					}
					case "wallet_revokePermissions": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [{ address: address$1, capabilities, id: id$1 }] = request._decoded.params;
						const account = address$1 ? state.accounts.find((account$1) => isEqual(account$1.address, address$1)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						await getMode().actions.revokePermissions({
							account,
							feeToken: capabilities?.feeToken,
							id: id$1,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						const keys = account.keys?.filter((key) => key.id.toLowerCase() !== id$1.toLowerCase());
						store.setState((x) => ({
							...x,
							accounts: x.accounts.map((x$1) => isEqual(x$1.address, account.address) ? {
								...x$1,
								keys
							} : x$1)
						}));
						emitter.emit("message", {
							data: null,
							type: "permissionsChanged"
						});
						return;
					}
					case "wallet_upgradeAccount": {
						const [{ context, signatures }] = request._decoded.params ?? [{}];
						const client = getClient();
						const account_ = preparedAccounts_internal.find((account$1) => isEqual(account$1.address, context.account.address));
						if (!account_) throw new UnauthorizedError();
						const { account } = await getMode().actions.upgradeAccount({
							account: account_,
							context,
							internal: {
								client,
								config,
								request,
								store
							},
							signatures
						});
						const admins = getAdmins(account.keys ?? []);
						const permissions$1 = getActivePermissions(account.keys ?? [], { address: account.address });
						store.setState((x) => ({
							...x,
							accounts: [account]
						}));
						emitter.emit("connect", { chainId: fromNumber(client.chain.id) });
						return {
							address: account.address,
							capabilities: {
								admins,
								...permissions$1.length > 0 ? { permissions: permissions$1 } : {}
							}
						};
					}
					case "porto_ping": return "pong";
					case "personal_sign": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [data, address$1] = request._decoded.params;
						const account = state.accounts.find((account$1) => isEqual(account$1.address, address$1));
						if (!account) throw new UnauthorizedError();
						const client = getClient();
						return await getMode().actions.signPersonalMessage({
							account,
							data,
							internal: {
								client,
								config,
								request,
								store
							}
						});
					}
					case "wallet_connect": {
						const [{ capabilities, chainIds }] = request._decoded.params ?? [{}];
						const client = getClient(chainIds?.[0]);
						const chainId = client.chain.id;
						const { createAccount: createAccount$2, email, grantAdmins: admins, grantPermissions: permissions$1, selectAccount, signInWithEthereum: signInWithEthereum$1 } = capabilities ?? {};
						const internal = {
							client,
							config,
							request,
							store
						};
						const { accounts } = await (async () => {
							if (email || createAccount$2) {
								const { label = void 0 } = typeof createAccount$2 === "object" ? createAccount$2 : {};
								const { account: account$1 } = await getMode().actions.createAccount({
									admins,
									email,
									internal,
									label,
									permissions: permissions$1,
									signInWithEthereum: signInWithEthereum$1
								});
								return { accounts: [account$1] };
							}
							const account = state.accounts[0];
							const { address: address$1, key } = (() => {
								if (selectAccount) {
									if (typeof selectAccount === "object") return selectAccount;
									return {
										address: void 0,
										key: void 0
									};
								}
								for (const key$1 of account?.keys ?? []) if (key$1.type === "webauthn-p256" && key$1.role === "admin") return {
									address: account?.address,
									key: {
										credentialId: key$1.credentialId ?? key$1.privateKey?.credential?.id,
										publicKey: key$1.publicKey
									}
								};
								return {
									address: void 0,
									key: void 0
								};
							})();
							const loadAccountsParams = {
								internal,
								permissions: permissions$1,
								signInWithEthereum: signInWithEthereum$1
							};
							try {
								return await getMode().actions.loadAccounts({
									address: address$1,
									key,
									...loadAccountsParams
								});
							} catch (error) {
								if (error instanceof UserRejectedRequestError) throw error;
								if (address$1 && key) return await getMode().actions.loadAccounts(loadAccountsParams);
								throw error;
							}
						})();
						store.setState((x) => ({
							...x,
							accounts
						}));
						const chainIds_response = [chainId, ...store.getState().chainIds.filter((id$1) => id$1 !== chainId)];
						emitter.emit("connect", { chainId: fromNumber(chainIds_response[0]) });
						return {
							accounts: accounts.map((account) => ({
								address: getAccountAddress(account),
								capabilities: {
									admins: account.keys ? getAdmins(account.keys) : [],
									permissions: account.keys ? getActivePermissions(account.keys, { address: account.address }) : [],
									...account.signInWithEthereum && { signInWithEthereum: account.signInWithEthereum }
								}
							})),
							chainIds: chainIds_response.map((chainId$1) => fromNumber(chainId$1))
						};
					}
					case "wallet_disconnect": {
						const client = getClient();
						await getMode().actions.disconnect?.({ internal: {
							client,
							config,
							request,
							store
						} });
						store.setState((x) => ({
							...x,
							accounts: []
						}));
						emitter.emit("disconnect", new DisconnectedError());
						return;
					}
					case "wallet_getAssets": {
						const [parameters$1] = request._decoded.params ?? [];
						const { account, chainFilter, assetFilter, assetTypeFilter } = parameters$1;
						const client = getClient();
						const response = await getMode().actions.getAssets({
							account,
							assetFilter,
							assetTypeFilter,
							chainFilter,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						const value = Object.entries(response).reduce((acc, [key, value$1]) => {
							acc[fromNumber(Number(key))] = value$1;
							return acc;
						}, {});
						return encode$1(wallet_getAssets.Response, value);
					}
					case "wallet_getCallsStatus": {
						const [id$1] = request._decoded.params ?? [];
						const client = getClient();
						return await getMode().actions.getCallsStatus({
							id: id$1,
							internal: {
								client,
								config,
								request,
								store
							}
						});
					}
					case "wallet_getCapabilities": {
						const [_, chainIds] = request.params ?? [];
						return await getCapabilities$1({
							chainIds,
							request
						});
					}
					case "wallet_prepareCalls": {
						const [parameters$1] = request._decoded.params;
						const { calls, capabilities, chainId, key, from: from$21 } = parameters$1;
						const client = getClient(chainId);
						const account = from$21 ?? state.accounts[0];
						if (!account) throw new UnauthorizedError();
						if (chainId && chainId !== client.chain.id) throw new ChainDisconnectedError();
						const { digest, ...rest } = await getMode().actions.prepareCalls({
							account: from$7(account),
							calls,
							feeToken: capabilities?.feeToken,
							internal: {
								client,
								config,
								request,
								store
							},
							key,
							merchantUrl: toAbsolute(config.merchantUrl ?? capabilities?.merchantUrl),
							requiredFunds: capabilities?.requiredFunds
						});
						return encode$1(wallet_prepareCalls.Response, {
							capabilities: rest.capabilities,
							chainId: fromNumber(rest.chainId ?? client.chain.id),
							context: {
								...rest.context,
								account: { address: rest.account.address },
								calls: rest.context.calls ?? [],
								nonce: rest.context.nonce ?? 0n
							},
							digest,
							key: rest.key,
							typedData: rest.typedData
						});
					}
					case "wallet_sendPreparedCalls": {
						const [parameters$1] = request._decoded.params;
						const { chainId, context, key, signature } = parameters$1;
						const { account } = parameters$1.context;
						const client = getClient(chainId);
						if (chainId && toNumber(chainId) !== client.chain.id) throw new ChainDisconnectedError();
						return [{ id: await getMode().actions.sendPreparedCalls({
							account: from$7(account),
							context,
							internal: {
								client,
								config,
								request,
								store
							},
							key,
							signature
						}) }];
					}
					case "wallet_sendCalls": {
						if (state.accounts.length === 0) throw new DisconnectedError();
						const [parameters$1] = request._decoded.params;
						const { calls, capabilities, chainId, from: from$21 } = parameters$1;
						const client = getClient(chainId);
						if (chainId && chainId !== client.chain.id) throw new ChainDisconnectedError();
						const account = from$21 ? state.accounts.find((account$1) => isEqual(account$1.address, from$21)) : state.accounts[0];
						if (!account) throw new UnauthorizedError();
						const { id: id$1 } = await getMode().actions.sendCalls({
							account,
							calls,
							chainId: client.chain.id,
							feeToken: capabilities?.feeToken,
							internal: {
								client,
								config,
								request,
								store
							},
							merchantUrl: toAbsolute(config.merchantUrl ?? capabilities?.merchantUrl),
							permissionsId: capabilities?.permissions?.id,
							requiredFunds: capabilities?.requiredFunds
						});
						return { id: id$1 };
					}
					case "wallet_switchEthereumChain": {
						const [parameters$1] = request._decoded.params;
						const { chainId } = parameters$1;
						const chainId_number = toNumber(chainId);
						if (!config.chains.find((chain) => chain.id === chainId_number)) throw new UnsupportedChainIdError();
						const client = getClient(chainId);
						await getMode().actions.switchChain?.({
							chainId: client.chain.id,
							internal: {
								client,
								config,
								request,
								store
							}
						});
						store.setState((state$1) => ({
							...state$1,
							chainIds: [chainId_number, ...state$1.chainIds.filter((id$1) => id$1 !== chainId_number)]
						}));
						return;
					}
					case "wallet_verifySignature": {
						const [parameters$1] = request._decoded.params;
						const { address: address$1, chainId, digest, signature } = parameters$1;
						const client = getClient(chainId);
						return {
							...await verifySignature(client, {
								address: address$1,
								digest,
								signature
							}),
							address: address$1,
							chainId: fromNumber(client.chain.id)
						};
					}
				}
			}, {
				enabled: [
					"eth_accounts",
					"eth_chainId",
					"eth_requestAccounts",
					"wallet_getAssets",
					"wallet_getCapabilities",
					"wallet_getKeys",
					"wallet_getPermissions",
					"wallet_getAccountVersion",
					"wallet_connect"
				].includes(request_.method),
				id: stringify(request_)
			});
		}
	});
	function setup() {
		let unsubscribe_accounts = () => {};
		let unsubscribe_chain = () => {};
		waitForHydration(store).then(() => {
			getCapabilities$1().catch(() => {});
			unsubscribe_accounts();
			unsubscribe_accounts = store.subscribe((state) => state.accounts, (accounts) => {
				emitter.emit("accountsChanged", accounts.map(getAccountAddress));
			}, { equalityFn: (a, b) => a.every((a$1, index) => a$1.address === b[index]?.address) });
			unsubscribe_chain();
			unsubscribe_chain = store.subscribe((state) => state.chainIds[0], (chainId, previousChainId) => {
				if (chainId === previousChainId) return;
				emitter.emit("chainChanged", fromNumber(chainId));
			});
		});
		const unannounce = announce(provider, announceProvider$1);
		return () => {
			unsubscribe_accounts();
			unsubscribe_chain();
			unannounce();
		};
	}
	const destroy = setup();
	return Object.assign(provider, { _internal: { destroy } });
}
function announce(provider, info) {
	if (!info) return () => {};
	if (typeof window === "undefined" || !window.dispatchEvent) return () => {};
	const { icon = "data:image/svg+xml;base64,", name = "Porto", rdns = "xyz.ithaca.porto" } = typeof info === "object" ? info : {};
	return announceProvider({
		info: {
			icon,
			name,
			rdns,
			uuid: uuidv4()
		},
		provider
	});
}
function getAdmins(keys) {
	return keys.map((key) => {
		if (key.role !== "admin") return void 0;
		try {
			return encode$1(wallet_getAdmins.Key, {
				id: key.id ?? key.publicKey,
				publicKey: key.publicKey,
				type: key.type,
				...key.type === "webauthn-p256" ? {
					credentialId: key.privateKey?.credential?.id,
					privateKey: {
						credential: { id: key.privateKey?.credential?.id },
						rpId: key.privateKey?.rpId
					}
				} : {}
			});
		} catch {
			return;
		}
	}).filter(Boolean);
}
function getActivePermissions(keys, { address: address$1 }) {
	return keys.map((key) => {
		if (!key.chainId) return void 0;
		if (key.role !== "session") return void 0;
		if (key.expiry > 0 && key.expiry < BigInt(Math.floor(Date.now() / 1e3))) return void 0;
		try {
			return encode$1(Schema$1, fromKey$1(key, { address: address$1 }));
		} catch {
			return;
		}
	}).filter(Boolean);
}
function getAccountAddress(account) {
	return getAddress(account.address);
}

//#endregion
//#region node_modules/porto/dist/core/internal/mode.js
/**
* Instantiates a mode.
*
* @param mode - Mode.
* @returns Mode.
*/
function from$2(mode) {
	return {
		...mode,
		setup: mode.setup ?? (() => () => {})
	};
}
/**
*
* @param parameters
* @returns
*/
async function getAuthorizedExecuteKey(parameters) {
	const { account, calls, permissionsId } = parameters;
	if (typeof permissionsId !== "undefined") {
		if (permissionsId === null) return void 0;
		const key = account.keys?.find((key$1) => key$1.publicKey === permissionsId && key$1.privateKey);
		if (!key) throw new Error(`permission (id: ${permissionsId}) does not exist.`);
		return key;
	}
	const sessionKey = account.keys?.find((key) => {
		if (!key.privateKey) return false;
		if (key.role !== "session") return false;
		if (key.expiry < BigInt(Math.floor(Date.now() / 1e3))) return false;
		if (calls.every((call$1) => key.permissions?.calls?.some((scope) => {
			if (scope.to && scope.to !== call$1.to) return false;
			if (scope.signature) {
				if (!call$1.data) return false;
				const selector = slice(call$1.data, 0, 4);
				if (validate$1(scope.signature)) return scope.signature === selector;
				if (getSelector$2(scope.signature) !== selector) return false;
			}
			return true;
		}))) return true;
		return false;
	});
	const adminKey = account.keys?.find((key) => key.role === "admin" && key.privateKey);
	return sessionKey ?? adminKey;
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/RpcRequest.js
/**
* Creates a JSON-RPC request store to build requests with an incrementing `id`.
*
* Returns a type-safe `prepare` function to build a JSON-RPC request object as per the [JSON-RPC 2.0 specification](https://www.jsonrpc.org/specification#request_object).
*
* @example
* ```ts twoslash
* import { RpcRequest } from 'ox'
*
* const store = RpcRequest.createStore()
*
* const request_1 = store.prepare({
*   method: 'eth_blockNumber',
* })
* // @log: { id: 0, jsonrpc: '2.0', method: 'eth_blockNumber' }
*
* const request_2 = store.prepare({
*   method: 'eth_call',
*   params: [
*     {
*       to: '0x0000000000000000000000000000000000000000',
*       data: '0xdeadbeef',
*     },
*   ],
* })
* // @log: { id: 1, jsonrpc: '2.0', method: 'eth_call', params: [{ to: '0x0000000000000000000000000000000000000000', data: '0xdeadbeef' }] }
* ```
*
* @example
* ### Type-safe Custom Schemas
*
* It is possible to define your own type-safe schema by using {@link ox#RpcSchema.from}.
*
* ```ts twoslash
* import { RpcSchema, RpcRequest } from 'ox'
*
* const schema = RpcSchema.from<{ // [!code focus]
*   Request: { // [!code focus]
*     method: 'eth_foobar' // [!code focus]
*     params: [number] // [!code focus]
*   } // [!code focus]
*   ReturnType: string // [!code focus]
* } | { // [!code focus]
*   Request: { // [!code focus]
*     method: 'eth_foobaz' // [!code focus]
*     params: [string] // [!code focus]
*   } // [!code focus]
*   ReturnType: string // [!code focus]
* }>() // [!code focus]
*
* const store = RpcRequest.createStore({ schema }) // [!code focus]
*
* const request = store.prepare({
*   method: 'eth_foobar', // [!code focus]
*   // ^?
*   params: [42],
* })
* ```
*
* @param options - Request store options.
* @returns The request store
*/
function createStore$2(options = {}) {
	let id = options.id ?? 0;
	return {
		prepare(options$1) {
			return from$5({
				id: id++,
				...options$1
			});
		},
		get id() {
			return id;
		}
	};
}
/**
* A type-safe interface to build a JSON-RPC request object as per the [JSON-RPC 2.0 specification](https://www.jsonrpc.org/specification#request_object).
*
* :::warning
*
* You will likely want to use {@link ox#RpcRequest.(createStore:function)} instead as it will also manage `id`s and uses this function internally.
*
* :::
*
* @example
* ```ts twoslash
* import { RpcRequest, RpcResponse } from 'ox'
*
* // 1. Build a request object.
* const request = RpcRequest.from({ // [!code focus]
*   id: 0, // [!code focus]
*   method: 'eth_estimateGas', // [!code focus]
*   params: [ // [!code focus]
*     { // [!code focus]
*       from: '0xd2135CfB216b74109775236E36d4b433F1DF507B', // [!code focus]
*       to: '0x0D44f617435088c947F00B31160f64b074e412B4', // [!code focus]
*       value: '0x69420', // [!code focus]
*     }, // [!code focus]
*   ], // [!code focus]
* }) // [!code focus]
*
* // 2. Send the JSON-RPC request via HTTP.
* const gas = await fetch('https://1.rpc.thirdweb.com', {
*   body: JSON.stringify(request),
*   headers: {
*     'Content-Type': 'application/json',
*   },
*   method: 'POST',
* })
*  .then((response) => response.json())
*  // 3. Parse the JSON-RPC response into a type-safe result.
*  .then((response) => RpcResponse.parse(response, { request }))
* ```
*
* @param options - JSON-RPC request options.
* @returns The fully-formed JSON-RPC request object.
*/
function from$5(options) {
	return {
		...options,
		jsonrpc: "2.0"
	};
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/RpcSchema.js
/**
* Instantiates a statically typed Schema. This is a runtime-noop function, and is purposed
* to be used as a type-level tag to be used with {@link ox#Provider.(from:function)} or
* {@link ox#RpcTransport.(fromHttp:function)}.
*
* @example
* ### Using with `Provider.from`
*
* ```ts twoslash
* // @noErrors
* import 'ox/window'
* import { Provider, RpcSchema } from 'ox'
*
* const schema = RpcSchema.from<
*   | RpcSchema.Default
*   | {
*       Request: {
*         method: 'abe_foo',
*         params: [id: number],
*       }
*       ReturnType: string
*     }
*   | {
*       Request: {
*         method: 'abe_bar',
*         params: [id: string],
*       }
*       ReturnType: string
*     }
* >()
*
* const provider = Provider.from(window.ethereum, { schema })
*
* const blockNumber = await provider.request({ method: 'e' })
* //                                                    ^|
*
*
*
*
*
* ```
*/
function from$4() {
	return null;
}

//#endregion
//#region node_modules/porto/dist/core/RpcSchema.js
var RpcSchema_exports = /* @__PURE__ */ __export({
	account_getOnrampContactInfo: () => account_getOnrampContactInfo,
	account_onrampStatus: () => account_onrampStatus,
	account_resendVerifyPhone: () => account_resendVerifyPhone,
	account_setEmail: () => account_setEmail,
	account_setPhone: () => account_setPhone,
	account_verifyEmail: () => account_verifyEmail,
	account_verifyPhone: () => account_verifyPhone,
	eth_accounts: () => eth_accounts,
	eth_chainId: () => eth_chainId,
	eth_requestAccounts: () => eth_requestAccounts,
	eth_sendTransaction: () => eth_sendTransaction,
	eth_signTypedData_v4: () => eth_signTypedData_v4,
	personal_sign: () => personal_sign,
	porto_ping: () => porto_ping,
	wallet_addFunds: () => wallet_addFunds,
	wallet_connect: () => wallet_connect,
	wallet_disconnect: () => wallet_disconnect,
	wallet_getAccountVersion: () => wallet_getAccountVersion,
	wallet_getAdmins: () => wallet_getAdmins,
	wallet_getAssets: () => wallet_getAssets,
	wallet_getCallsStatus: () => wallet_getCallsStatus,
	wallet_getCapabilities: () => wallet_getCapabilities,
	wallet_getKeys: () => wallet_getKeys,
	wallet_getPermissions: () => wallet_getPermissions,
	wallet_grantAdmin: () => wallet_grantAdmin,
	wallet_grantPermissions: () => wallet_grantPermissions,
	wallet_prepareCalls: () => wallet_prepareCalls,
	wallet_prepareUpgradeAccount: () => wallet_prepareUpgradeAccount,
	wallet_revokeAdmin: () => wallet_revokeAdmin,
	wallet_revokePermissions: () => wallet_revokePermissions,
	wallet_sendCalls: () => wallet_sendCalls,
	wallet_sendPreparedCalls: () => wallet_sendPreparedCalls,
	wallet_switchEthereumChain: () => wallet_switchEthereumChain,
	wallet_upgradeAccount: () => wallet_upgradeAccount,
	wallet_verifySignature: () => wallet_verifySignature
});

//#endregion
//#region node_modules/porto/dist/core/internal/permissionsRequest.js
const Schema = Request$1;
function fromKey(key) {
	const { expiry, feeToken: feeToken$1, permissions: permissions$1, publicKey, type } = key;
	return {
		expiry,
		feeToken: feeToken$1 ?? null,
		key: {
			publicKey,
			type
		},
		permissions: permissions$1 ?? {}
	};
}
async function toKey(request, options = {}) {
	if (!request) return void 0;
	const baseParameters = {
		chainId: options.chainId ?? request.chainId,
		expiry: request.expiry ?? 0,
		feeToken: request.feeToken,
		permissions: resolvePermissions(request, { feeTokens: options.feeTokens }),
		role: "session"
	};
	if (request?.key) return from$14({
		...baseParameters,
		publicKey: request.key.publicKey,
		type: request.key.type ?? "secp256k1"
	});
	if (typeof globalThis.crypto?.subtle?.generateKey === "function") try {
		return await createWebCryptoP256(baseParameters);
	} catch (error) {
		if (!isWebCryptoUnavailable(error)) throw error;
	}
	return createP256(baseParameters);
}
function isWebCryptoUnavailable(error) {
	if (!(error instanceof Error)) return false;
	const message = error.message?.toLowerCase() ?? "";
	return error.name === "TypeError" || error.name === "ReferenceError" || message.includes("subtle") || message.includes("generatekey");
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/Siwe.js
const domainRegex = /^([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}(:[0-9]{1,5})?$/;
const ipRegex = /^(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(:[0-9]{1,5})?$/;
const localhostRegex = /^localhost(:[0-9]{1,5})?$/;
const nonceRegex = /^[a-zA-Z0-9]{8,}$/;
const schemeRegex = /^([a-zA-Z][a-zA-Z0-9+-.]*)$/;
const prefixRegex = /^(?:(?<scheme>[a-zA-Z][a-zA-Z0-9+-.]*):\/\/)?(?<domain>[a-zA-Z0-9+-.]*(?::[0-9]{1,5})?) (?:wants you to sign in with your Ethereum account:\n)(?<address>0x[a-fA-F0-9]{40})\n\n(?:(?<statement>.*)\n\n)?/;
const suffixRegex = /(?:URI: (?<uri>.+))\n(?:Version: (?<version>.+))\n(?:Chain ID: (?<chainId>\d+))\n(?:Nonce: (?<nonce>[a-zA-Z0-9]+))\n(?:Issued At: (?<issuedAt>.+))(?:\nExpiration Time: (?<expirationTime>.+))?(?:\nNot Before: (?<notBefore>.+))?(?:\nRequest ID: (?<requestId>.+))?/;
/**
* Creates [EIP-4361](https://eips.ethereum.org/EIPS/eip-4361) formatted message.
*
* @example
* ```ts twoslash
* import { Siwe } from 'ox'
*
* Siwe.createMessage({
*   address: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e',
*   chainId: 1,
*   domain: 'example.com',
*   nonce: 'foobarbaz',
*   uri: 'https://example.com/path',
*   version: '1',
* })
* // @log: "example.com wants you to sign in with your Ethereum account:
* // @log: 0xA0Cf798816D4b9b9866b5330EEa46a18382f251e
* // @log:
* // @log:
* // @log: URI: https://example.com/path
* // @log: Version: 1
* // @log: Chain ID: 1
* // @log: Nonce: foobarbaz
* // @log: Issued At: 2023-02-01T00:00:00.000Z"
* ```
*
* @param value - Values to use when creating EIP-4361 formatted message.
* @returns EIP-4361 formatted message.
*/
function createMessage(value) {
	const { chainId, domain, expirationTime, issuedAt = /* @__PURE__ */ new Date(), nonce, notBefore, requestId, resources, scheme, uri, version } = value;
	{
		if (chainId !== Math.floor(chainId)) throw new InvalidMessageFieldError({
			field: "chainId",
			metaMessages: [
				"- Chain ID must be a EIP-155 chain ID.",
				"- See https://eips.ethereum.org/EIPS/eip-155",
				"",
				`Provided value: ${chainId}`
			]
		});
		if (!(domainRegex.test(domain) || ipRegex.test(domain) || localhostRegex.test(domain))) throw new InvalidMessageFieldError({
			field: "domain",
			metaMessages: [
				"- Domain must be an RFC 3986 authority.",
				"- See https://www.rfc-editor.org/rfc/rfc3986",
				"",
				`Provided value: ${domain}`
			]
		});
		if (!nonceRegex.test(nonce)) throw new InvalidMessageFieldError({
			field: "nonce",
			metaMessages: [
				"- Nonce must be at least 8 characters.",
				"- Nonce must be alphanumeric.",
				"",
				`Provided value: ${nonce}`
			]
		});
		if (!isUri(uri)) throw new InvalidMessageFieldError({
			field: "uri",
			metaMessages: [
				"- URI must be a RFC 3986 URI referring to the resource that is the subject of the signing.",
				"- See https://www.rfc-editor.org/rfc/rfc3986",
				"",
				`Provided value: ${uri}`
			]
		});
		if (version !== "1") throw new InvalidMessageFieldError({
			field: "version",
			metaMessages: [
				"- Version must be '1'.",
				"",
				`Provided value: ${version}`
			]
		});
		if (scheme && !schemeRegex.test(scheme)) throw new InvalidMessageFieldError({
			field: "scheme",
			metaMessages: [
				"- Scheme must be an RFC 3986 URI scheme.",
				"- See https://www.rfc-editor.org/rfc/rfc3986#section-3.1",
				"",
				`Provided value: ${scheme}`
			]
		});
		const statement = value.statement;
		if (statement?.includes("\n")) throw new InvalidMessageFieldError({
			field: "statement",
			metaMessages: [
				"- Statement must not include '\\n'.",
				"",
				`Provided value: ${statement}`
			]
		});
	}
	const address$1 = from$15(value.address, { checksum: true });
	const prefix = `${(() => {
		if (scheme) return `${scheme}://${domain}`;
		return domain;
	})()} wants you to sign in with your Ethereum account:\n${address$1}\n\n${(() => {
		if (!value.statement) return "";
		return `${value.statement}\n`;
	})()}`;
	let suffix = `URI: ${uri}\nVersion: ${version}\nChain ID: ${chainId}\nNonce: ${nonce}\nIssued At: ${issuedAt.toISOString()}`;
	if (expirationTime) suffix += `\nExpiration Time: ${expirationTime.toISOString()}`;
	if (notBefore) suffix += `\nNot Before: ${notBefore.toISOString()}`;
	if (requestId) suffix += `\nRequest ID: ${requestId}`;
	if (resources) {
		let content = "\nResources:";
		for (const resource of resources) {
			if (!isUri(resource)) throw new InvalidMessageFieldError({
				field: "resources",
				metaMessages: [
					"- Every resource must be a RFC 3986 URI.",
					"- See https://www.rfc-editor.org/rfc/rfc3986",
					"",
					`Provided value: ${resource}`
				]
			});
			content += `\n- ${resource}`;
		}
		suffix += content;
	}
	return `${prefix}\n${suffix}`;
}
/**
* Check if the given URI is a valid [RFC 3986](https://www.rfc-editor.org/rfc/rfc3986) URI.
*
* @example
* ```ts twoslash
* import { Siwe } from 'ox'
*
* Siwe.isUri('https://example.com/foo')
* // @log: true
* ```
*
* @param value - Value to check.
* @returns `false` if invalid, otherwise the valid URI.
*/
function isUri(value) {
	if (/[^a-z0-9:/?#[\]@!$&'()*+,;=.\-_~%]/i.test(value)) return false;
	if (/%[^0-9a-f]/i.test(value)) return false;
	if (/%[0-9a-f](:?[^0-9a-f]|$)/i.test(value)) return false;
	const splitted = splitUri(value);
	const scheme = splitted[1];
	const authority = splitted[2];
	const path = splitted[3];
	const query = splitted[4];
	const fragment = splitted[5];
	if (!(scheme?.length && path && path.length >= 0)) return false;
	if (authority?.length) {
		if (!(path.length === 0 || /^\//.test(path))) return false;
	} else if (/^\/\//.test(path)) return false;
	if (!/^[a-z][a-z0-9+\-.]*$/.test(scheme.toLowerCase())) return false;
	let out = "";
	out += `${scheme}:`;
	if (authority?.length) out += `//${authority}`;
	out += path;
	if (query?.length) out += `?${query}`;
	if (fragment?.length) out += `#${fragment}`;
	return out;
}
function splitUri(value) {
	return value.match(/(?:([^:/?#]+):)?(?:\/\/([^/?#]*))?([^?#]*)(?:\?([^#]*))?(?:#(.*))?/);
}
/**
* [EIP-4361](https://eips.ethereum.org/EIPS/eip-4361) formatted message into message fields object.
*
* @example
* ```ts twoslash
* import { Siwe } from 'ox'
*
* Siwe.parseMessage(`example.com wants you to sign in with your Ethereum account:
* 0xA0Cf798816D4b9b9866b5330EEa46a18382f251e
*
* I accept the ExampleOrg Terms of Service: https://example.com/tos
*
* URI: https://example.com/path
* Version: 1
* Chain ID: 1
* Nonce: foobarbaz
* Issued At: 2023-02-01T00:00:00.000Z`)
* // @log: {
* // @log:   address: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e',
* // @log:   chainId: 1,
* // @log:   domain: 'example.com',
* // @log:   issuedAt: '2023-02-01T00:00:00.000Z',
* // @log:   nonce: 'foobarbaz',
* // @log:   statement: 'I accept the ExampleOrg Terms of Service: https://example.com/tos',
* // @log:   uri: 'https://example.com/path',
* // @log:   version: '1',
* // @log: }
* ```
*
* @param message - [EIP-4361](https://eips.ethereum.org/EIPS/eip-4361) formatted message.
* @returns Message fields object.
*/
function parseMessage(message) {
	const { scheme, statement, ...prefix } = message.match(prefixRegex)?.groups ?? {};
	const { chainId, expirationTime, issuedAt, notBefore, requestId, ...suffix } = message.match(suffixRegex)?.groups ?? {};
	const resources = message.split("Resources:")[1]?.split("\n- ").slice(1);
	return {
		...prefix,
		...suffix,
		...chainId ? { chainId: Number(chainId) } : {},
		...expirationTime ? { expirationTime: new Date(expirationTime) } : {},
		...issuedAt ? { issuedAt: new Date(issuedAt) } : {},
		...notBefore ? { notBefore: new Date(notBefore) } : {},
		...requestId ? { requestId } : {},
		...resources ? { resources } : {},
		...scheme ? { scheme } : {},
		...statement ? { statement } : {}
	};
}
/**
* Thrown when a field in a SIWE Message is invalid.
*
* @example
* ```ts twoslash
* import { Siwe } from 'ox'
*
* Siwe.createMessage({
*   address: '0xA0Cf798816D4b9b9866b5330EEa46a18382f251e',
*   chainId: 1.1,
*   domain: 'example.com',
*   nonce: 'foobarbaz',
*   uri: 'https://example.com/path',
*   version: '1',
* })
* // @error: Siwe.InvalidMessageFieldError: Invalid Sign-In with Ethereum message field "chainId".
* // @error: - Chain ID must be a EIP-155 chain ID.
* // @error: - See https://eips.ethereum.org/EIPS/eip-155
* // @error: Provided value: 1.1
* ```
*/
var InvalidMessageFieldError = class extends BaseError$2 {
	constructor(parameters) {
		const { field, metaMessages } = parameters;
		super(`Invalid Sign-In with Ethereum message field "${field}".`, { metaMessages });
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "Siwe.InvalidMessageFieldError"
		});
	}
};

//#endregion
//#region node_modules/porto/dist/core/internal/siwe.js
async function authenticate(parameters) {
	const { address: address$1, authUrl, message, signature, publicKey } = parameters;
	const { chainId } = parseMessage(message);
	return await fetch(authUrl.verify, {
		body: JSON.stringify({
			address: address$1,
			chainId,
			message,
			signature,
			walletAddress: address$1,
			...publicKey && { publicKey }
		}),
		credentials: "include",
		headers: { "Content-Type": "application/json" },
		method: "POST"
	}).then((res) => res.json());
}
async function buildMessage(client, siwe, options) {
	const { chainId = client.chain?.id, domain, uri, resources, version = "1" } = siwe;
	const { address: address$1 } = options;
	const authUrl = siwe.authUrl ? resolveAuthUrl(siwe.authUrl) : void 0;
	if (!chainId) throw new Error("`chainId` is required.");
	if (!domain) throw new Error("`domain` is required.");
	if (!siwe.nonce && !authUrl?.nonce) throw new Error("`nonce` or `authUrl.nonce` is required.");
	if (!uri) throw new Error("`uri` is required.");
	const nonce = await (async () => {
		if (siwe.nonce) return siwe.nonce;
		if (!authUrl?.nonce) throw new Error("`nonce` or `authUrl.nonce` is required.");
		const res = await (await fetch(authUrl.nonce, {
			body: JSON.stringify({
				address: address$1,
				chainId,
				walletAddress: address$1
			}),
			headers: { "Content-Type": "application/json" },
			method: "POST"
		})).json().catch(() => void 0);
		if (!res?.nonce) throw new Error("`nonce` or `authUrl.nonce` is required.");
		return res.nonce;
	})();
	return createMessage({
		...siwe,
		address: options.address,
		chainId,
		domain,
		nonce,
		resources,
		uri,
		version
	});
}
function resolveAuthUrl(authUrl, origin = "") {
	if (!authUrl) return void 0;
	const urls = (() => {
		if (typeof authUrl === "string") {
			const url = authUrl.replace(/\/$/, "");
			return {
				logout: url + "/logout",
				nonce: url + "/nonce",
				verify: url + "/verify"
			};
		}
		return authUrl;
	})();
	return {
		logout: resolveUrl(urls.logout, origin),
		nonce: resolveUrl(urls.nonce, origin),
		verify: resolveUrl(urls.verify, origin)
	};
}
function resolveUrl(url, origin) {
	if (!origin) return url;
	if (!url.startsWith("/")) return url;
	return origin + url;
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/PersonalMessage.js
/**
* Encodes a personal sign message in [ERC-191 format](https://eips.ethereum.org/EIPS/eip-191#version-0x45-e): `0x19 ‖ "Ethereum Signed Message:\n" + message.length ‖ message`.
*
* @example
* ```ts twoslash
* import { Hex, PersonalMessage } from 'ox'
*
* const data = PersonalMessage.encode(Hex.fromString('hello world'))
* // @log: '0x19457468657265756d205369676e6564204d6573736167653a0a313168656c6c6f20776f726c64'
* // @log: (0x19 ‖ 'Ethereum Signed Message:\n11' ‖ 'hello world')
* ```
*
* @param data - The data to encode.
* @returns The encoded personal sign message.
*/
function encode(data) {
	const message = from$16(data);
	return concat("0x19", fromString("Ethereum Signed Message:\n" + size(message)), message);
}
/**
* Gets the payload to use for signing an [ERC-191 formatted](https://eips.ethereum.org/EIPS/eip-191#version-0x45-e) personal message.
*
* @example
* ```ts twoslash
* import { Hex, PersonalMessage, Secp256k1 } from 'ox'
*
* const payload = PersonalMessage.getSignPayload(Hex.fromString('hello world')) // [!code focus]
*
* const signature = Secp256k1.sign({ payload, privateKey: '0x...' })
* ```
*
* @param data - The data to get the sign payload for.
* @returns The payload to use for signing.
*/
function getSignPayload$1(data) {
	return keccak256(encode(data));
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/Rlp.js
/**
* Encodes a {@link ox#Bytes.Bytes} or {@link ox#Hex.Hex} value into a Recursive-Length Prefix (RLP) value.
*
* @example
* ```ts twoslash
* import { Bytes, Rlp } from 'ox'
*
* Rlp.from('0x68656c6c6f20776f726c64', { as: 'Hex' })
* // @log: 0x8b68656c6c6f20776f726c64
*
* Rlp.from(Bytes.from([139, 104, 101, 108, 108, 111,  32, 119, 111, 114, 108, 100]), { as: 'Bytes' })
* // @log: Uint8Array([104, 101, 108, 108, 111,  32, 119, 111, 114, 108, 100])
* ```
*
* @param value - The {@link ox#Bytes.Bytes} or {@link ox#Hex.Hex} value to encode.
* @param options - Options.
* @returns The RLP value.
*/
function from$3(value, options) {
	const { as } = options;
	const encodable = getEncodable(value);
	const cursor = create$2(new Uint8Array(encodable.length));
	encodable.encode(cursor);
	if (as === "Hex") return fromBytes(cursor.bytes);
	return cursor.bytes;
}
/**
* Encodes a {@link ox#Hex.Hex} value into a Recursive-Length Prefix (RLP) value.
*
* @example
* ```ts twoslash
* import { Rlp } from 'ox'
*
* Rlp.fromHex('0x68656c6c6f20776f726c64')
* // @log: 0x8b68656c6c6f20776f726c64
* ```
*
* @param hex - The {@link ox#Hex.Hex} value to encode.
* @param options - Options.
* @returns The RLP value.
*/
function fromHex(hex$1, options = {}) {
	const { as = "Hex" } = options;
	return from$3(hex$1, { as });
}
function getEncodable(bytes) {
	if (Array.isArray(bytes)) return getEncodableList(bytes.map((x) => getEncodable(x)));
	return getEncodableBytes(bytes);
}
function getEncodableList(list) {
	const bodyLength = list.reduce((acc, x) => acc + x.length, 0);
	const sizeOfBodyLength = getSizeOfLength(bodyLength);
	return {
		length: (() => {
			if (bodyLength <= 55) return 1 + bodyLength;
			return 1 + sizeOfBodyLength + bodyLength;
		})(),
		encode(cursor) {
			if (bodyLength <= 55) cursor.pushByte(192 + bodyLength);
			else {
				cursor.pushByte(247 + sizeOfBodyLength);
				if (sizeOfBodyLength === 1) cursor.pushUint8(bodyLength);
				else if (sizeOfBodyLength === 2) cursor.pushUint16(bodyLength);
				else if (sizeOfBodyLength === 3) cursor.pushUint24(bodyLength);
				else cursor.pushUint32(bodyLength);
			}
			for (const { encode: encode$3 } of list) encode$3(cursor);
		}
	};
}
function getEncodableBytes(bytesOrHex) {
	const bytes = typeof bytesOrHex === "string" ? fromHex$2(bytesOrHex) : bytesOrHex;
	const sizeOfBytesLength = getSizeOfLength(bytes.length);
	return {
		length: (() => {
			if (bytes.length === 1 && bytes[0] < 128) return 1;
			if (bytes.length <= 55) return 1 + bytes.length;
			return 1 + sizeOfBytesLength + bytes.length;
		})(),
		encode(cursor) {
			if (bytes.length === 1 && bytes[0] < 128) cursor.pushBytes(bytes);
			else if (bytes.length <= 55) {
				cursor.pushByte(128 + bytes.length);
				cursor.pushBytes(bytes);
			} else {
				cursor.pushByte(183 + sizeOfBytesLength);
				if (sizeOfBytesLength === 1) cursor.pushUint8(bytes.length);
				else if (sizeOfBytesLength === 2) cursor.pushUint16(bytes.length);
				else if (sizeOfBytesLength === 3) cursor.pushUint24(bytes.length);
				else cursor.pushUint32(bytes.length);
				cursor.pushBytes(bytes);
			}
		}
	};
}
function getSizeOfLength(length) {
	if (length <= 255) return 1;
	if (length <= 65535) return 2;
	if (length <= 16777215) return 3;
	if (length <= 4294967295) return 4;
	throw new BaseError$2("Length is too large.");
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/core/Authorization.js
/**
* Computes the sign payload for an {@link ox#Authorization.Authorization} in [EIP-7702 format](https://eips.ethereum.org/EIPS/eip-7702): `keccak256('0x05' || rlp([chain_id, address, nonce]))`.
*
* @example
* The example below demonstrates computing the sign payload for an {@link ox#Authorization.Authorization}. This payload
* can then be passed to signing functions like {@link ox#Secp256k1.(sign:function)}.
*
* ```ts twoslash
* import { Authorization, Secp256k1 } from 'ox'
*
* const authorization = Authorization.from({
*   address: '0x1234567890abcdef1234567890abcdef12345678',
*   chainId: 1,
*   nonce: 69n,
* })
*
* const payload = Authorization.getSignPayload(authorization) // [!code focus]
*
* const signature = Secp256k1.sign({
*   payload,
*   privateKey: '0x...',
* })
* ```
*
* @param authorization - The {@link ox#Authorization.Authorization}.
* @returns The sign payload.
*/
function getSignPayload(authorization) {
	return hash(authorization, { presign: true });
}
/**
* Computes the hash for an {@link ox#Authorization.Authorization} in [EIP-7702 format](https://eips.ethereum.org/EIPS/eip-7702): `keccak256('0x05' || rlp([chain_id, address, nonce]))`.
*
* @example
* ```ts twoslash
* import { Authorization } from 'ox'
*
* const authorization = Authorization.from({
*   address: '0x1234567890abcdef1234567890abcdef12345678',
*   chainId: 1,
*   nonce: 69n,
* })
*
* const hash = Authorization.hash(authorization) // [!code focus]
* ```
*
* @param authorization - The {@link ox#Authorization.Authorization}.
* @returns The hash.
*/
function hash(authorization, options = {}) {
	const { presign } = options;
	return keccak256(concat("0x05", fromHex(toTuple(presign ? {
		address: authorization.address,
		chainId: authorization.chainId,
		nonce: authorization.nonce
	} : authorization))));
}
/**
* Converts an {@link ox#Authorization.Authorization} to an {@link ox#Authorization.Tuple}.
*
* @example
* ```ts twoslash
* import { Authorization } from 'ox'
*
* const authorization = Authorization.from({
*   address: '0x1234567890abcdef1234567890abcdef12345678',
*   chainId: 1,
*   nonce: 69n,
* })
*
* const tuple = Authorization.toTuple(authorization) // [!code focus]
* // @log: [
* // @log:   address: '0x1234567890abcdef1234567890abcdef12345678',
* // @log:   chainId: 1,
* // @log:   nonce: 69n,
* // @log: ]
* ```
*
* @param authorization - The {@link ox#Authorization.Authorization}.
* @returns An [EIP-7702](https://eips.ethereum.org/EIPS/eip-7702) Authorization tuple.
*/
function toTuple(authorization) {
	const { address: address$1, chainId, nonce } = authorization;
	const signature = extract(authorization);
	return [
		chainId ? fromNumber(chainId) : "0x",
		address$1,
		nonce ? fromNumber(nonce) : "0x",
		...signature ? toTuple$1(signature) : []
	];
}

//#endregion
//#region node_modules/porto/dist/viem/ContractActions.js
/**
* Returns the EIP-712 domain for a delegated account. Used for the execution
* signing payload.
*
* @param client - Client.
* @param parameters - Parameters.
* @returns EIP-712 domain.
*/
async function getEip712Domain$1(client, parameters) {
	const { account = client.account } = parameters;
	const account_ = account ? from$7(account) : void 0;
	if (!account_) throw new Error("account is required.");
	const { domain: { name, version } } = await getEip712Domain(client, { address: account_.address });
	if (!client.chain) throw new Error("client.chain is required");
	return {
		chainId: client.chain.id,
		name,
		verifyingContract: account_.address,
		version
	};
}

//#endregion
//#region node_modules/porto/dist/viem/RelayActions.js
var RelayActions_exports = /* @__PURE__ */ __export({
	addFaucetFunds: () => addFaucetFunds,
	createAccount: () => createAccount,
	decorator: () => decorator,
	getAssets: () => getAssets,
	getAuthorization: () => getAuthorization,
	getCallsStatus: () => getCallsStatus,
	getCapabilities: () => getCapabilities,
	getKeys: () => getKeys,
	getOnrampContactInfo: () => getOnrampContactInfo,
	health: () => health,
	onrampStatus: () => onrampStatus,
	prepareCalls: () => prepareCalls,
	prepareUpgradeAccount: () => prepareUpgradeAccount,
	resendVerifyPhone: () => resendVerifyPhone,
	sendCalls: () => sendCalls,
	sendPreparedCalls: () => sendPreparedCalls,
	setEmail: () => setEmail,
	setPhone: () => setPhone,
	signCalls: () => signCalls,
	upgradeAccount: () => upgradeAccount,
	verifyEmail: () => verifyEmail,
	verifyPhone: () => verifyPhone,
	verifySignature: () => verifySignature
});
/**
* Creates a new Porto Account using an ephemeral EOA.
*
* @example
* TODO
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Result.
*/
async function createAccount(client, parameters) {
	const account = fromPrivateKey(randomPrivateKey(), { keys: parameters.authorizeKeys });
	return await upgradeAccount(client, {
		...parameters,
		account
	});
}
/**
* Gets the keys for an account.
*
* @example
* TODO
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Account keys.
*/
async function getKeys(client, parameters) {
	const { account = client.account, chainIds } = parameters;
	const account_ = account ? from$7(account) : void 0;
	if (!account_) throw new Error("account is required.");
	const keys = await getKeys$1(client, {
		address: account_.address,
		chainIds
	});
	return Object.entries(keys).flatMap(([chainId, keys$1]) => keys$1.map((key) => fromRelay(key, { chainId: Number(chainId) })));
}
async function getOnrampContactInfo(client, parameters) {
	const { address: address$1, secret } = parameters;
	return await getOnrampContactInfo$1(client, {
		address: address$1,
		secret
	});
}
async function onrampStatus(client, parameters) {
	const { address: address$1 } = parameters;
	return await onrampStatus$1(client, { address: address$1 });
}
/**
* Prepares the digest to sign over and fills the request to send a call bundle.
*
* @example
* TODO
*
* @param client - Client.
* @param parameters - Prepare call bundle parameters.
* @returns Prepared properties.
*/
async function prepareCalls(client, parameters) {
	const { account = client.account, calls, chain = client.chain, feePayer, merchantUrl: merchantUrl$1, nonce, preCalls: preCalls$1, requiredFunds: requiredFunds$2, revokeKeys: revokeKeys$1 } = parameters;
	const account_ = account ? from$7(account) : void 0;
	const key = parameters.key ?? (account_ ? getKey(account_, { role: "admin" }) : void 0);
	const hasSessionKey = parameters.authorizeKeys?.some((x) => x.role === "session");
	const { contracts, fees: { tokens } } = await getCapabilities(client, { chainId: chain?.id });
	const orchestrator = hasSessionKey ? contracts.orchestrator.address : void 0;
	const authorizeKeys$1 = (parameters.authorizeKeys ?? []).map((key$1) => toRelay$1(key$1, {
		feeTokens: tokens,
		orchestrator
	}));
	const feeToken$1 = (() => {
		if (parameters.feeToken) return parameters.feeToken;
		return key?.permissions?.spend?.[0]?.token;
	})();
	const preCall = typeof preCalls$1 === "boolean" ? preCalls$1 : false;
	const signedPreCalls = typeof preCalls$1 === "object" ? preCalls$1.map(({ context: context$1, signature: signature$1 }) => ({
		...context$1.preCall,
		signature: signature$1
	})) : void 0;
	const args = {
		address: account_?.address,
		calls: calls ?? [],
		capabilities: {
			authorizeKeys: authorizeKeys$1,
			meta: {
				feePayer,
				feeToken: feeToken$1,
				nonce
			},
			preCall,
			preCalls: signedPreCalls,
			requiredFunds: requiredFunds$2,
			revokeKeys: revokeKeys$1?.map((key$1) => ({ hash: key$1.hash }))
		},
		chain,
		key: key ? toRelay$1(key, { feeTokens: tokens }) : void 0
	};
	const result = await (async () => {
		if (merchantUrl$1) {
			const client_ = createClient({
				chain: client.chain,
				transport: http(merchantUrl$1)
			});
			return await prepareCalls$1(client_, args).catch((e) => {
				console.error(e);
				return prepareCalls$1(client, args);
			});
		}
		return await prepareCalls$1(client, args);
	})();
	const { capabilities, context, digest, signature, typedData } = result;
	if (merchantUrl$1) {
		if (!await verifyPrepareCallsResponse(client, {
			response: result._raw,
			signature
		})) throw new Error(`cannot verify integrity of \`wallet_prepareCalls\` response from ${merchantUrl$1}`);
	}
	return {
		capabilities: {
			...capabilities,
			quote: context.quote
		},
		context,
		digest,
		key,
		typedData
	};
}
/**
* Prepares an account upgrade.
*
* @example
* TODO
*
* @param client - Client to use.
* @param parameters - Parameters.
* @returns Result.
*/
async function prepareUpgradeAccount(client, parameters) {
	const { address: address$1, authorizeKeys: keys, chain = client.chain } = parameters;
	if (!chain) throw new Error("chain is required.");
	const { contracts, fees: { tokens } } = await getCapabilities(client, { chainId: chain.id });
	const delegation = parameters.delegation ?? contracts.accountProxy.address;
	const orchestrator = keys.some((x) => x.role === "session") ? contracts.orchestrator.address : void 0;
	const authorizeKeys$1 = keys.map((key) => {
		const permissions$1 = key.role === "session" ? key.permissions : {};
		return toRelay$1({
			...key,
			permissions: permissions$1
		}, {
			feeTokens: tokens,
			orchestrator
		});
	});
	const { capabilities, chainId, context, digests, typedData } = await prepareUpgradeAccount$1(client, {
		address: address$1,
		authorizeKeys: authorizeKeys$1,
		chain,
		delegation
	});
	const account = from$7({
		address: address$1,
		keys
	});
	return {
		capabilities,
		chainId,
		context: {
			...context,
			account
		},
		digests,
		typedData
	};
}
async function resendVerifyPhone(client, parameters) {
	const { phone, walletAddress } = parameters;
	return await resendVerifyPhone$1(client, {
		phone,
		walletAddress
	});
}
/**
* Broadcasts a call bundle to the Relay.
*
* @example
* TODO
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Bundle identifier.
*/
async function sendCalls(client, parameters) {
	const { account = client.account, chain = client.chain, webAuthn } = parameters;
	if (!chain) throw new Error("`chain` is required.");
	const account_ = account ? from$7(account) : void 0;
	if (!account_) throw new Error("`account` is required.");
	const key = parameters.key ?? getKey(account_, parameters);
	if (!key && !account_.sign) throw new Error("`key` or `account` with `sign` is required");
	const preCalls$1 = await Promise.all((parameters.preCalls ?? []).map(async (pre) => {
		if (pre.signature) return pre;
		const { authorizeKeys: authorizeKeys$1, key: key$1, calls, revokeKeys: revokeKeys$1 } = pre;
		const { context: context$1, digest: digest$1 } = await prepareCalls(client, {
			account: account_,
			authorizeKeys: authorizeKeys$1,
			calls,
			chain,
			feeToken: parameters.feeToken,
			key: key$1,
			preCalls: true,
			revokeKeys: revokeKeys$1
		});
		return {
			context: context$1,
			signature: await sign$2(key$1, {
				address: null,
				payload: digest$1,
				webAuthn
			})
		};
	}));
	const { capabilities, context, digest } = await prepareCalls(client, {
		...parameters,
		account: account_,
		chain,
		key,
		preCalls: preCalls$1
	});
	const signature = await (async () => {
		if (key) return await sign$2(key, {
			address: null,
			payload: digest,
			webAuthn,
			wrap: false
		});
		return await account_.sign({ hash: digest });
	})();
	return await sendPreparedCalls(client, {
		capabilities: capabilities.feeSignature ? { feeSignature: capabilities.feeSignature } : void 0,
		context,
		key,
		signature
	});
}
async function signCalls(request, options) {
	const isPrecall = Boolean(request.context.preCall);
	const { account, key } = options;
	if (account) {
		const keyIndex = account.keys?.findIndex((k) => k.publicKey === request.key?.publicKey);
		if (keyIndex === -1) throw new Error("key not found");
		return await sign(account, {
			key: keyIndex,
			payload: request.digest,
			replaySafe: false,
			wrap: isPrecall
		});
	}
	if (key) return await sign$2(key, {
		address: null,
		payload: request.digest,
		wrap: isPrecall
	});
	throw new Error("no key or account provided");
}
async function sendPreparedCalls(client, parameters) {
	const { capabilities, context, key, signature } = parameters;
	return await sendPreparedCalls$1(client, {
		capabilities,
		context,
		key: key ? toRelay$1(key) : void 0,
		signature
	});
}
async function setEmail(client, parameters) {
	const { email, walletAddress } = parameters;
	return await setEmail$1(client, {
		email,
		walletAddress
	});
}
async function setPhone(client, parameters) {
	const { phone, walletAddress } = parameters;
	return await setPhone$1(client, {
		phone,
		walletAddress
	});
}
async function upgradeAccount(client, parameters) {
	if (parameters.account) {
		const { account: account$1 } = parameters;
		const authorizeKeys$1 = [...account$1.keys ?? [], ...parameters.authorizeKeys ?? []].filter((key, index, array$1) => array$1.findIndex((k) => k.id === key.id) === index);
		const { digests, ...request } = await prepareUpgradeAccount(client, {
			...parameters,
			address: account$1.address,
			authorizeKeys: authorizeKeys$1
		});
		const signatures$1 = {
			auth: await account$1.sign({ hash: digests.auth }),
			exec: await account$1.sign({ hash: digests.exec })
		};
		return await upgradeAccount(client, {
			...request,
			signatures: signatures$1
		});
	}
	const { context, signatures } = parameters;
	const account = from$7(context.account);
	await upgradeAccount$1(client, {
		context,
		signatures
	});
	return account;
}
async function verifyEmail(client, parameters) {
	const { chainId, email, signature, token, walletAddress } = parameters;
	return await verifyEmail$1(client, {
		chainId,
		email,
		signature,
		token,
		walletAddress
	});
}
async function verifyPhone(client, parameters) {
	const { code, phone, walletAddress } = parameters;
	return await verifyPhone$1(client, {
		code,
		phone,
		walletAddress
	});
}
function decorator(client) {
	return {
		createAccount: (parameters) => createAccount(client, parameters),
		getCallsStatus: (parameters) => getCallsStatus(client, parameters),
		getCapabilities: () => getCapabilities(client),
		getKeys: (parameters) => getKeys(client, parameters),
		health: () => health(client),
		prepareCalls: (parameters) => prepareCalls(client, parameters),
		prepareUpgradeAccount: (parameters) => prepareUpgradeAccount(client, parameters),
		sendCalls: (parameters) => sendCalls(client, parameters),
		sendPreparedCalls: (parameters) => sendPreparedCalls(client, parameters),
		upgradeAccount: (parameters) => upgradeAccount(client, parameters),
		verifySignature: (parameters) => verifySignature(client, parameters)
	};
}

//#endregion
//#region node_modules/porto/node_modules/ox/_esm/erc8010/SignatureErc8010.js
/**
* Magic bytes used to identify ERC-8010 wrapped signatures.
*/
const magicBytes = "0x8010801080108010801080108010801080108010801080108010801080108010";
/** Suffix ABI parameters for the ERC-8010 wrapped signature. */
const suffixParameters = from$17("(uint256 chainId, address delegation, uint256 nonce, uint8 yParity, uint256 r, uint256 s), address to, bytes data");
/**
* Asserts that the wrapped signature is valid.
*
* @example
* ```ts twoslash
* import { SignatureErc8010 } from 'ox/erc8010'
*
* SignatureErc8010.assert('0xdeadbeef')
* // @error: InvalidWrappedSignatureError: Value `0xdeadbeef` is an invalid ERC-8010 wrapped signature.
* ```
*
* @param value - The value to assert.
*/
function assert(value) {
	if (typeof value === "string") {
		if (slice(value, -32) !== magicBytes) throw new InvalidWrappedSignatureError(value);
	} else assert$1(value.authorization);
}
/**
* Wraps a signature into [ERC-8010 format](https://github.com/jxom/ERCs/blob/16f7e3891fff2e1e9c25dea0485497739db8a816/ERCS/erc-8010.md).
*
* @example
* ```ts twoslash
* // @noErrors
* import { Secp256k1, Signature } from 'ox'
* import { SignatureErc8010 } from 'ox/erc8010' // [!code focus]
*
* const signature = Secp256k1.sign({
*   payload: '0x...',
*   privateKey: '0x...',
* })
*
* const wrapped = SignatureErc8010.wrap({ // [!code focus]
*   authorization: { ... }, // [!code focus]
*   data: '0xdeadbeef', // [!code focus]
*   signature: Signature.toHex(signature), // [!code focus]
* }) // [!code focus]
* ```
*
* @param value - Values to wrap.
* @returns Wrapped signature.
*/
function wrap$1(value) {
	const { data, signature } = value;
	assert(value);
	const self = recoverAddress({
		payload: getSignPayload(value.authorization),
		signature: from$18(value.authorization)
	});
	const suffix = encode$2(suffixParameters, [
		{
			...value.authorization,
			delegation: value.authorization.address,
			chainId: BigInt(value.authorization.chainId)
		},
		value.to ?? self,
		data ?? "0x"
	]);
	const suffixLength = fromNumber(size(suffix), { size: 32 });
	return concat(signature, suffix, suffixLength, magicBytes);
}
/** Thrown when the ERC-8010 wrapped signature is invalid. */
var InvalidWrappedSignatureError = class extends BaseError$2 {
	constructor(wrapped) {
		super(`Value \`${wrapped}\` is an invalid ERC-8010 wrapped signature.`);
		Object.defineProperty(this, "name", {
			enumerable: true,
			configurable: true,
			writable: true,
			value: "SignatureErc8010.InvalidWrappedSignatureError"
		});
	}
};

//#endregion
//#region node_modules/porto/dist/core/internal/erc8010.js
/**
* Wraps a signature in ERC-8010 format to enable pre-delegated signature verification.
*
* @param client - The client to use.
* @param parameters - The parameters for the wrap function.
* @returns The wrapped signature.
*/
async function wrap(client, parameters) {
	const { address: address$1 } = parameters;
	const { authorization, data, to } = await getAuthorization(client, { address: address$1 });
	return wrap$1({
		authorization: {
			...authorization,
			nonce: BigInt(authorization.nonce),
			r: BigInt(authorization.r),
			s: BigInt(authorization.s)
		},
		data,
		signature: parameters.signature,
		to
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/requiredFunds.js
/**
* Transforms into Relay-formatted required funds.
*
* @param requiredFunds - The required funds object to convert.
* @param options - The options for the conversion.
* @returns The converted required funds object.
*/
function toRelay(requiredFunds$2, options) {
	const { tokens } = options;
	const interopTokens = tokens.filter((token) => token.interop);
	return requiredFunds$2.map((requiredFund) => {
		if (requiredFund.address) return requiredFund;
		const interopToken = interopTokens.find((token) => token.symbol === requiredFund.symbol);
		if (!interopToken) throw new Error(`interop token not found: ${requiredFund.symbol}`);
		return {
			address: interopToken.address,
			value: from$19(requiredFund.value, interopToken.decimals)
		};
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/tokens.js
/**
* Fetches all supported tokens for a given chain.
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Tokens.
*/
async function getTokens(client, parameters) {
	const { chain = client.chain } = parameters ?? {};
	return await getCapabilities(client, { chainId: chain?.id }).then((capabilities) => capabilities.fees.tokens);
}
/**
* Fetches a token for a given chain, provided an address or symbol.
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Token.
*/
async function getToken(client, parameters) {
	const { addressOrSymbol } = parameters;
	return (await getTokens(client, parameters)).find(getToken.predicate(addressOrSymbol));
}
(function(getToken$1) {
	function predicate(addressOrSymbol) {
		return (token) => {
			if (!addressOrSymbol) return false;
			if (validate$2(addressOrSymbol)) return isEqual(token.address, addressOrSymbol);
			if (addressOrSymbol === "native") return token.address === zeroAddress;
			return addressOrSymbol === token.symbol;
		};
	}
	getToken$1.predicate = predicate;
})(getToken || (getToken = {}));
/**
* Resolves the fee token to use. Resolves the provided address or symbol,
* or the defaults to the fee token stored in state.
*
* @param client - Client.
* @param parameters - Parameters.
* @returns Fee token.
*/
async function resolveFeeToken$1(client, parameters) {
	const { chain = client.chain, store } = parameters ?? {};
	const state = store?.getState() ?? {};
	const addressOrSymbol = parameters?.addressOrSymbol ?? state.feeToken;
	return (await getTokens(client, { chain }).then((tokens) => tokens.filter((token) => token.feeToken)))?.find((feeToken$1) => {
		if (!addressOrSymbol) return false;
		if (addressOrSymbol === "native" && feeToken$1.address === zeroAddress) return true;
		if (validate$2(addressOrSymbol) && isEqual(feeToken$1.address, addressOrSymbol)) return true;
		return addressOrSymbol === feeToken$1.symbol;
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/modes/relay.js
/**
* Mode for a WebAuthn-based environment that interacts with the Porto
* Relay. Account management, signing, and execution is coordinated
* between the library and the Relay.
*
* @param parameters - Parameters.
* @returns Mode.
*/
function relay(parameters = {}) {
	const config = parameters;
	const { mock, multichain = true, webAuthn } = config;
	let address_internal;
	let email_internal;
	const keystoreHost = (() => {
		if (config.keystoreHost === "self") return void 0;
		if (typeof window !== "undefined" && window.location?.hostname === "localhost") return void 0;
		return config.keystoreHost;
	})();
	return from$2({
		actions: {
			async addFunds() {
				throw new UnsupportedMethodError();
			},
			async createAccount(parameters$1) {
				const { admins, email, label, permissions: permissions$1, internal, signInWithEthereum: signInWithEthereum$1 } = parameters$1;
				const { client } = internal;
				const eoa = fromPrivateKey(randomPrivateKey());
				const feeTokens = await getTokens(client);
				const adminKey = !mock ? await createWebAuthnP256({
					createFn: webAuthn?.createFn,
					label: label || `${eoa.address.slice(0, 8)}\u2026${eoa.address.slice(-6)}`,
					rpId: keystoreHost,
					userId: from$20(eoa.address)
				}) : createHeadlessWebAuthnP256();
				const sessionKey = await toKey(permissions$1, {
					chainId: client.chain.id,
					feeTokens
				});
				const adminKeys = admins?.map((admin) => from$14(admin));
				const account = await upgradeAccount(client, {
					account: eoa,
					authorizeKeys: [
						adminKey,
						...adminKeys ?? [],
						...sessionKey ? [sessionKey] : []
					]
				});
				address_internal = eoa.address;
				if (email && label) await setEmail(client, {
					email: label,
					walletAddress: account.address
				});
				const signInWithEthereum_response = await (async () => {
					if (!signInWithEthereum$1) return void 0;
					const message = await buildMessage(client, signInWithEthereum$1, { address: account.address });
					const signature = await sign(eoa, { payload: getSignPayload$1(fromString(message)) });
					return {
						message,
						signature: await wrap(client, {
							address: account.address,
							signature
						})
					};
				})();
				return { account: {
					...account,
					signInWithEthereum: signInWithEthereum_response
				} };
			},
			async getAccountVersion(parameters$1) {
				const { address: address$1, internal } = parameters$1;
				const { client } = internal;
				const { contracts } = await getCapabilities(client);
				const { accountImplementation } = contracts;
				const latest = await getEip712Domain$1(client, { account: from$7(accountImplementation) }).then((x) => x.version);
				const current = await getEip712Domain$1(client, { account: address$1 }).then((x) => x.version).catch(() => latest);
				if (!current || !latest) throw new Error("version not found.");
				return {
					current,
					latest
				};
			},
			async getAssets(parameters$1) {
				const { account, chainFilter, assetFilter, assetTypeFilter, internal } = parameters$1;
				const { client } = internal;
				return await getAssets(client, {
					account,
					assetFilter,
					assetTypeFilter,
					chainFilter
				});
			},
			async getCallsStatus(parameters$1) {
				const { id, internal } = parameters$1;
				const { client } = internal;
				const result = await getCallsStatus(client, { id });
				return {
					atomic: true,
					chainId: fromNumber(client.chain.id),
					id,
					receipts: result.receipts?.map((receipt) => ({
						blockHash: receipt.blockHash,
						blockNumber: fromNumber(receipt.blockNumber),
						gasUsed: fromNumber(receipt.gasUsed),
						logs: receipt.logs,
						status: receipt.status,
						transactionHash: receipt.transactionHash
					})),
					status: result.status,
					version: "1.0"
				};
			},
			async getCapabilities(parameters$1) {
				const { chainIds, internal } = parameters$1;
				const { client } = internal;
				const base$1 = {
					atomic: { status: "supported" },
					atomicBatch: { supported: true },
					feeToken: {
						supported: true,
						tokens: []
					},
					merchant: { supported: true },
					permissions: { supported: true },
					requiredFunds: {
						supported: Boolean(multichain),
						tokens: []
					}
				};
				const capabilities = await getCapabilities(client, {
					chainIds: chainIds ? chainIds.map((id) => toNumber(id)) : "all",
					raw: true
				});
				return Object.entries(capabilities).reduce((acc, [chainId, capabilities$1]) => ({
					...acc,
					[chainId]: {
						...base$1,
						...capabilities$1,
						feeToken: {
							supported: true,
							tokens: capabilities$1.fees.tokens
						},
						requiredFunds: {
							supported: Boolean(multichain),
							tokens: multichain ? capabilities$1.fees.tokens.filter((token) => token.interop) : []
						}
					}
				}), {});
			},
			async getKeys(parameters$1) {
				const { account, chainIds, internal } = parameters$1;
				const { client } = internal;
				const keys = await getKeys(client, {
					account,
					chainIds
				});
				return uniqBy([...keys, ...account.keys ?? []], (key) => key.publicKey);
			},
			async grantAdmin(parameters$1) {
				const { account, internal } = parameters$1;
				const { client } = internal;
				const authorizeKey = from$14(parameters$1.key, { chainId: client.chain.id });
				const feeToken$1 = await resolveFeeToken$1(client, {
					addressOrSymbol: parameters$1.feeToken,
					store: internal.store
				});
				const { id } = await sendCalls(client, {
					account,
					authorizeKeys: [authorizeKey],
					feeToken: feeToken$1?.address,
					webAuthn
				});
				await waitForCallsStatus(client, {
					id,
					pollingInterval: 500
				});
				return { key: authorizeKey };
			},
			async grantPermissions(parameters$1) {
				const { account, internal, permissions: permissions$1 } = parameters$1;
				const { client } = internal;
				const feeTokens = await getTokens(client);
				const authorizeKey = await toKey(permissions$1, {
					chainId: client.chain.id,
					feeTokens
				});
				if (!authorizeKey) throw new Error("key to authorize not found.");
				const adminKey = account.keys?.find((key) => key.role === "admin" && key.privateKey);
				if (!adminKey) throw new Error("admin key not found.");
				const { context, digest } = await prepareCalls(client, {
					account,
					authorizeKeys: [authorizeKey],
					key: adminKey,
					preCalls: true
				});
				const signature = await sign$2(adminKey, {
					address: null,
					payload: digest
				});
				await sendPreparedCalls(client, {
					context,
					key: adminKey,
					signature
				});
				return { key: authorizeKey };
			},
			async loadAccounts(parameters$1) {
				const { internal, permissions: permissions$1, signInWithEthereum: signInWithEthereum$1 } = parameters$1;
				const { client } = internal;
				const feeTokens = await getTokens(client);
				const authorizeKey = await toKey(permissions$1, {
					chainId: client.chain.id,
					feeTokens
				});
				const { digest, digestType, message } = await (async () => {
					if (signInWithEthereum$1 && parameters$1.address) {
						const message$1 = await buildMessage(client, signInWithEthereum$1, { address: parameters$1.address });
						return {
							context: void 0,
							digest: getSignPayload$1(fromString(message$1)),
							digestType: "siwe",
							message: message$1
						};
					}
					return {
						context: void 0,
						digest: "0x",
						message: void 0
					};
				})();
				const { address: address$1, credentialId, webAuthnSignature } = await (async () => {
					if (mock) {
						if (!address_internal) throw new Error("address_internal not found.");
						return {
							address: address_internal,
							credentialId: void 0
						};
					}
					if (parameters$1.address && parameters$1.key) return {
						address: parameters$1.address,
						credentialId: parameters$1.key.credentialId
					};
					const webAuthnSignature$1 = await sign$3({
						challenge: digest,
						getFn: webAuthn?.getFn,
						rpId: keystoreHost
					});
					const response = webAuthnSignature$1.raw.response;
					return {
						address: toHex$1(new Uint8Array(response.userHandle)),
						credentialId: webAuthnSignature$1.raw.id,
						webAuthnSignature: webAuthnSignature$1
					};
				})();
				const keys = await getKeys(client, {
					account: address$1,
					chainIds: [client.chain.id]
				});
				const account = from$7({
					address: address$1,
					keys: [...keys, ...authorizeKey ? [authorizeKey] : []].map((key, i) => {
						if (i === 0) {
							if (key.type === "webauthn-p256") return fromWebAuthnP256({
								...key,
								credential: {
									id: credentialId,
									publicKey: fromHex$3(key.publicKey)
								},
								id: address$1,
								rpId: keystoreHost
							});
						}
						return key;
					})
				});
				const adminKey = getKey(account, { role: "admin" });
				const signature = await (async () => {
					if (digest === "0x") return void 0;
					if (webAuthnSignature) return wrapSignature(serializeWebAuthnSignature(webAuthnSignature), {
						keyType: "webauthn-p256",
						publicKey: adminKey.publicKey
					});
					return await sign$2(adminKey, {
						address: account.address,
						payload: digest
					});
				})();
				if (authorizeKey) {
					const { context, digest: digest$1 } = await prepareCalls(client, {
						account,
						authorizeKeys: [authorizeKey],
						preCalls: true
					});
					const signature$1 = await sign$2(adminKey, {
						address: null,
						payload: digest$1
					});
					await sendPreparedCalls(client, {
						context,
						key: adminKey,
						signature: signature$1
					});
				}
				const signInWithEthereum_response = await (async () => {
					if (!signInWithEthereum$1) return void 0;
					if (digestType === "siwe" && message && signature) return {
						message,
						signature: await wrap(client, {
							address: account.address,
							signature
						})
					};
					{
						const message$1 = await buildMessage(client, signInWithEthereum$1, { address: account.address });
						const signature$1 = await sign(account, {
							payload: getSignPayload$1(fromString(message$1)),
							role: "admin"
						});
						return {
							message: message$1,
							signature: await wrap(client, {
								address: account.address,
								signature: signature$1
							})
						};
					}
				})();
				return { accounts: [{
					...account,
					signInWithEthereum: signInWithEthereum_response
				}] };
			},
			async prepareCalls(parameters$1) {
				const { account, calls, internal, merchantUrl: merchantUrl$1 } = parameters$1;
				const { client } = internal;
				const key = parameters$1.key ?? await getAuthorizedExecuteKey({
					account,
					calls
				});
				if (!key) throw new Error("cannot find authorized key to sign with.");
				const [tokens, feeToken$1] = await Promise.all([getTokens(client), resolveFeeToken$1(client, {
					addressOrSymbol: parameters$1.feeToken,
					store: internal.store
				})]);
				const requiredFunds$2 = toRelay(parameters$1.requiredFunds ?? [], { tokens });
				const { capabilities, context, digest, typedData } = await prepareCalls(client, {
					account,
					calls,
					feeToken: feeToken$1?.address,
					key,
					merchantUrl: merchantUrl$1,
					requiredFunds: multichain ? requiredFunds$2 : void 0
				});
				const quotes = context.quote?.quotes ?? [];
				const outputQuote = quotes[quotes.length - 1];
				return {
					account,
					capabilities: {
						...capabilities,
						quote: context.quote
					},
					chainId: client.chain.id,
					context: {
						...context,
						account,
						calls,
						nonce: outputQuote?.intent.nonce
					},
					digest,
					key,
					typedData
				};
			},
			async prepareUpgradeAccount(parameters$1) {
				const { address: address$1, email, label, internal, permissions: permissions$1 } = parameters$1;
				const { client } = internal;
				const [tokens, feeToken$1] = await Promise.all([getTokens(client), resolveFeeToken$1(client, { store: internal.store })]);
				const adminKey = !mock ? await createWebAuthnP256({
					createFn: webAuthn?.createFn,
					label: label || `${address$1.slice(0, 8)}\u2026${address$1.slice(-6)}`,
					rpId: keystoreHost,
					userId: from$20(address$1)
				}) : createHeadlessWebAuthnP256();
				const sessionKey = await toKey(permissions$1, {
					chainId: client.chain.id,
					feeTokens: tokens
				});
				const { context, digests } = await prepareUpgradeAccount(client, {
					address: address$1,
					authorizeKeys: [adminKey, ...sessionKey ? [sessionKey] : []],
					feeToken: feeToken$1?.address
				});
				if (email) email_internal = label;
				return {
					context,
					digests
				};
			},
			async revokeAdmin(parameters$1) {
				const { account, id, internal } = parameters$1;
				const { client } = internal;
				const key = account.keys?.find((key$1) => key$1.id === id);
				if (!key) return;
				if (key.type === "webauthn-p256" && account.keys?.filter((key$1) => key$1.type === "webauthn-p256").length === 1) throw new Error("revoke the only WebAuthn key left.");
				try {
					const feeToken$1 = await resolveFeeToken$1(client, {
						addressOrSymbol: parameters$1.feeToken,
						store: internal.store
					});
					const { id: id$1 } = await sendCalls(client, {
						account,
						feeToken: feeToken$1?.address,
						revokeKeys: [key],
						webAuthn
					});
					await waitForCallsStatus(client, { id: id$1 });
				} catch (e) {
					const error = e;
					if (error.name === "Rpc.ExecutionError" && error.abiError?.name === "KeyDoesNotExist") return;
					throw e;
				}
			},
			async revokePermissions(parameters$1) {
				const { account, id, internal } = parameters$1;
				const { client } = internal;
				const key = account.keys?.find((key$1) => key$1.id === id);
				if (!key) return;
				if (key.role === "admin") throw new Error("cannot revoke admins.");
				try {
					const feeToken$1 = await resolveFeeToken$1(client, {
						addressOrSymbol: parameters$1.feeToken,
						store: internal.store
					});
					const { id: id$1 } = await sendCalls(client, {
						account,
						feeToken: feeToken$1?.address,
						revokeKeys: [key],
						webAuthn
					});
					await waitForCallsStatus(client, { id: id$1 });
				} catch (e) {
					const error = e;
					if (error.name === "Rpc.ExecutionError" && error.abiError?.name === "KeyDoesNotExist") return;
					throw e;
				}
			},
			async sendCalls(parameters$1) {
				const { account, asTxHash, calls, chainId, internal, merchantUrl: merchantUrl$1 } = parameters$1;
				const { client } = internal;
				const key = await getAuthorizedExecuteKey({
					account,
					calls,
					permissionsId: parameters$1.permissionsId
				});
				const [tokens, feeToken$1] = await Promise.all([getTokens(client), resolveFeeToken$1(client, {
					addressOrSymbol: parameters$1.feeToken,
					store: internal.store
				})]);
				const requiredFunds$2 = toRelay(parameters$1.requiredFunds ?? [], { tokens });
				const result = await sendCalls(client, {
					account,
					calls,
					feeToken: feeToken$1?.address,
					key,
					merchantUrl: merchantUrl$1,
					requiredFunds: multichain ? requiredFunds$2 : void 0,
					webAuthn,
					...chainId ? { chain: { id: chainId } } : {}
				});
				if (asTxHash) {
					const { id, receipts, status } = await waitForCallsStatus(client, {
						id: result.id,
						pollingInterval: 500
					});
					if (!receipts?.[0]) {
						if (status === "success") throw new UnknownBundleIdError({ message: "Call bundle with id: " + id + " not found." });
						throw new TransactionRejectedError({ message: "Transaction failed under call bundle id: " + id + "." });
					}
					return { id: receipts[0].transactionHash };
				}
				return result;
			},
			async sendPreparedCalls(parameters$1) {
				const { context, key, internal, signature } = parameters$1;
				const { client } = internal;
				const { id } = await sendPreparedCalls(client, {
					context,
					key,
					signature
				});
				return id;
			},
			async signPersonalMessage(parameters$1) {
				const { account, data, internal } = parameters$1;
				const { client } = internal;
				const key = account.keys?.find((key$1) => key$1.role === "admin" && key$1.privateKey);
				if (!key) throw new Error("cannot find admin key to sign with.");
				const signature = await sign(account, {
					key,
					payload: getSignPayload$1(data),
					webAuthn
				});
				return wrap(client, {
					address: account.address,
					signature
				});
			},
			async signTypedData(parameters$1) {
				const { account, internal } = parameters$1;
				const { client } = internal;
				const key = account.keys?.find((key$1) => key$1.role === "admin" && key$1.privateKey);
				if (!key) throw new Error("cannot find admin key to sign with.");
				const data = parse$1(parameters$1.data);
				const isOrchestrator = data.domain?.name === "Orchestrator";
				const signature = await sign(account, {
					key,
					payload: getSignPayload$2(data),
					replaySafe: !isOrchestrator,
					webAuthn
				});
				return isOrchestrator ? signature : wrap(client, {
					address: account.address,
					signature
				});
			},
			async upgradeAccount(parameters$1) {
				const { account, context, internal, signatures } = parameters$1;
				const { client } = internal;
				await upgradeAccount(client, {
					context,
					signatures
				});
				if (email_internal) await setEmail(client, {
					email: email_internal,
					walletAddress: account.address
				});
				return { account };
			},
			async verifyEmail(parameters$1) {
				const { account, chainId, email, token, internal, walletAddress } = parameters$1;
				const { client } = internal;
				const key = account.keys?.find((key$1) => key$1.role === "admin" && key$1.privateKey);
				if (!key) throw new Error("cannot find admin key to sign with.");
				const signature = await sign(account, {
					key,
					payload: keccak256(fromString(`${email}${token}`)),
					webAuthn
				});
				return await verifyEmail(client, {
					chainId,
					email,
					signature,
					token,
					walletAddress
				});
			}
		},
		config: parameters,
		name: "rpc"
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/modes/dialog.js
function dialog(parameters = {}) {
	const { fallback: fallback$1 = relay(), host = hostUrls.prod, renderer = iframe(), theme, themeController } = parameters;
	const listeners = /* @__PURE__ */ new Set();
	const requestStore = createStore$2();
	function getProvider(store) {
		return from$10({ async request(r) {
			const request = requestStore.prepare(r);
			store.setState((x) => {
				const account = x.accounts[0];
				const adminKey = account?.keys?.find((key) => key.role === "admin" && key.type === "webauthn-p256");
				return {
					...x,
					requestQueue: [...x.requestQueue, {
						account: account ? {
							address: account.address,
							key: adminKey ? {
								credentialId: adminKey?.credentialId,
								publicKey: adminKey.publicKey
							} : void 0
						} : void 0,
						request,
						status: "pending"
					}]
				};
			});
			return new Promise((resolve, reject) => {
				const listener = (requestQueue) => {
					const queued = requestQueue.find((x) => x.request.id === request.id);
					if (!queued && requestQueue.length === 0) {
						listeners.delete(listener);
						reject(new UserRejectedRequestError());
						return;
					}
					if (!queued) return;
					if (queued.status !== "success" && queued.status !== "error") return;
					listeners.delete(listener);
					if (queued.status === "success") resolve(queued.result);
					else reject(parseError(queued.error));
					store.setState((x) => ({
						...x,
						requestQueue: x.requestQueue.filter((x$1) => x$1.request.id !== request.id)
					}));
				};
				listeners.add(listener);
			});
		} }, { schema: from$4() });
	}
	return from$2({
		actions: {
			async addFunds(parameters$1) {
				const { internal } = parameters$1;
				const { request, store } = internal;
				if (request.method !== "wallet_addFunds") throw new Error("Cannot add funds for method: " + request.method);
				return await getProvider(store).request(request);
			},
			async createAccount(parameters$1) {
				const { internal } = parameters$1;
				const { client, config, request, store } = internal;
				const { storage } = config;
				const provider = getProvider(store);
				return { account: await (async () => {
					if (request.method === "wallet_connect") {
						const [{ capabilities, chainIds }] = request._decoded.params ?? [{}];
						const authUrl = getAuthUrl(capabilities?.signInWithEthereum?.authUrl ?? config.authUrl, { storage });
						const signInWithEthereum$1 = request.params?.[0]?.capabilities?.signInWithEthereum;
						const key = await toKey(capabilities?.grantPermissions, { chainId: client.chain.id });
						const permissionsRequest = key ? encode$1(Schema, fromKey(key)) : void 0;
						const { accounts } = await provider.request({
							...request,
							params: [{
								capabilities: {
									...request.params?.[0]?.capabilities,
									grantPermissions: permissionsRequest,
									signInWithEthereum: authUrl || signInWithEthereum$1 ? {
										...signInWithEthereum$1,
										authUrl
									} : void 0
								},
								chainIds: chainIds?.map((chainId) => fromNumber(chainId))
							}]
						});
						const [account] = accounts;
						if (!account) throw new Error("no account found.");
						const adminKeys = account.capabilities?.admins?.map((admin) => from$14(admin, { chainId: client.chain.id })).filter(Boolean);
						const sessionKeys = account.capabilities?.permissions?.map((permission) => {
							try {
								const key_permission = toKey$1(decode(Schema$1, permission));
								if (key_permission.id === key?.id) return {
									...key_permission,
									...key,
									permissions: key_permission.permissions
								};
								return key_permission;
							} catch {
								return;
							}
						}).filter(Boolean);
						const signInWithEthereum_response = await (async () => {
							if (!account.capabilities?.signInWithEthereum) return;
							const { message, signature } = account.capabilities.signInWithEthereum;
							if (!authUrl) return {
								message,
								signature
							};
							const { token } = await authenticate({
								address: account.address,
								authUrl,
								message,
								publicKey: account.capabilities?.admins?.[0]?.publicKey,
								signature
							});
							return {
								message,
								signature,
								token
							};
						})();
						return {
							...from$7({
								address: account.address,
								keys: [...adminKeys ?? [], ...sessionKeys ?? []]
							}),
							signInWithEthereum: signInWithEthereum_response
						};
					}
					throw new Error(`Account creation not supported on method: ${request.method}`);
				})() };
			},
			async disconnect(parameters$1) {
				const { internal } = parameters$1;
				const { config } = internal;
				const { storage } = config;
				const authUrl_storage = await storage.getItem("porto.authUrl") || void 0;
				const authUrl = getAuthUrl(config.authUrl ?? authUrl_storage, { storage });
				if (authUrl) await fetch(authUrl.logout, {
					credentials: "include",
					method: "POST"
				}).catch(() => {});
			},
			async getAccountVersion(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_getAccountVersion") throw new Error("Cannot get version for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.getAccountVersion(parameters$1);
				return await getProvider(store).request(request);
			},
			async getAssets(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_getAssets") throw new Error("Cannot get assets for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.getAssets(parameters$1);
				const result = await getProvider(store).request(request);
				return decode(wallet_getAssets.Response, result);
			},
			async getCallsStatus(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_getCallsStatus") throw new Error("Cannot get status for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.getCallsStatus(parameters$1);
				return await getProvider(store).request(request);
			},
			async getCapabilities(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_getCapabilities") throw new Error("Cannot get capabilities for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.getCapabilities(parameters$1);
				return await getProvider(store).request(request);
			},
			async getKeys(parameters$1) {
				const { account, chainIds, internal } = parameters$1;
				const { store } = internal;
				const keys = await (async () => {
					if (!renderer.supportsHeadless) return fallback$1.actions.getKeys(parameters$1);
					const result = await getProvider(store).request({
						method: "wallet_getKeys",
						params: [encode$1(wallet_getKeys.Parameters, {
							address: account.address,
							chainIds
						})]
					});
					return decode(wallet_getKeys.Response, result);
				})();
				return uniqBy([...keys, ...account.keys ?? []], (key) => key.publicKey);
			},
			async grantAdmin(parameters$1) {
				const { internal } = parameters$1;
				const { request, store } = internal;
				if (request.method !== "wallet_grantAdmin") throw new Error("Cannot authorize admin for method: " + request.method);
				const [params] = request._decoded.params;
				const key = from$14(params.key);
				if (!key) throw new Error("no key found.");
				const feeToken$1 = await resolveFeeToken(internal, parameters$1);
				await getProvider(store).request({
					method: "wallet_grantAdmin",
					params: [{
						...request.params?.[0],
						capabilities: {
							...request.params?.[0]?.capabilities,
							feeToken: feeToken$1
						}
					}]
				});
				return { key };
			},
			async grantPermissions(parameters$1) {
				const { internal } = parameters$1;
				const { client, request, store } = internal;
				if (request.method !== "wallet_grantPermissions") throw new Error("Cannot grant permissions for method: " + request.method);
				const [{ address: address$1, ...permissions$1 }] = request._decoded.params;
				const key = await toKey(permissions$1, { chainId: client.chain.id });
				if (!key) throw new Error("no key found.");
				const permissionsRequest = encode$1(Schema, fromKey(key));
				await getProvider(store).request({
					method: "wallet_grantPermissions",
					params: [permissionsRequest]
				});
				return { key };
			},
			async loadAccounts(parameters$1) {
				const { internal } = parameters$1;
				const { client, config, store } = internal;
				const { storage } = config;
				const provider = getProvider(store);
				const request = internal.request;
				if (request.method !== "wallet_connect" && request.method !== "eth_requestAccounts") throw new Error("Cannot load accounts for method: " + request.method);
				return { accounts: await (async () => {
					const [params] = request._decoded.params ?? [];
					const { capabilities } = params ?? {};
					const authUrl = getAuthUrl(capabilities?.signInWithEthereum?.authUrl ?? config.authUrl, { storage });
					const signInWithEthereum$1 = request.params?.[0]?.capabilities?.signInWithEthereum;
					const key = await toKey(capabilities?.grantPermissions, { chainId: client.chain.id });
					const permissionsRequest = key ? encode$1(Schema, fromKey(key)) : void 0;
					const { accounts } = await provider.request({
						method: "wallet_connect",
						params: [{
							...request.params?.[0],
							capabilities: {
								...request.params?.[0]?.capabilities,
								grantPermissions: permissionsRequest,
								signInWithEthereum: authUrl || signInWithEthereum$1 ? {
									...signInWithEthereum$1,
									authUrl
								} : void 0
							}
						}]
					});
					return Promise.all(accounts.map(async (account) => {
						const adminKeys = account.capabilities?.admins?.map((key$1) => from$14(key$1)).filter(Boolean);
						const sessionKeys = account.capabilities?.permissions?.map((permission) => {
							try {
								const key_permission = toKey$1(decode(Schema$1, permission));
								if (key_permission.id === key?.id) return {
									...key_permission,
									...key,
									permissions: key_permission.permissions
								};
								return key_permission;
							} catch {
								return;
							}
						}).filter(Boolean);
						const signInWithEthereum_response = await (async () => {
							if (!account.capabilities?.signInWithEthereum) return;
							const { message, signature } = account.capabilities.signInWithEthereum;
							if (!authUrl) return {
								message,
								signature
							};
							const { token } = await authenticate({
								address: account.address,
								authUrl,
								message,
								publicKey: account.capabilities?.admins?.[0]?.publicKey,
								signature
							});
							return {
								message,
								signature,
								token
							};
						})();
						return {
							...from$7({
								address: account.address,
								keys: [...adminKeys ?? [], ...sessionKeys ?? []]
							}),
							signInWithEthereum: signInWithEthereum_response
						};
					}));
				})() };
			},
			async prepareCalls(parameters$1) {
				const { account, internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_prepareCalls") throw new Error("Cannot prepare calls for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.prepareCalls(parameters$1);
				const feeToken$1 = await resolveFeeToken(internal, parameters$1);
				const provider = getProvider(store);
				const result = decode(wallet_prepareCalls.Response, await provider.request({
					...request,
					params: [{
						...request.params?.[0],
						capabilities: {
							...request.params?.[0]?.capabilities,
							feeToken: feeToken$1
						}
					}]
				}));
				return {
					account,
					chainId: Number(result.chainId),
					context: result.context,
					digest: result.digest,
					key: result.key,
					typedData: result.typedData
				};
			},
			async prepareUpgradeAccount(parameters$1) {
				const { internal } = parameters$1;
				const { client, store, request } = internal;
				if (request.method !== "wallet_prepareUpgradeAccount") throw new Error("Cannot prepare upgrade for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.prepareUpgradeAccount(parameters$1);
				const [{ capabilities }] = request._decoded.params ?? [{}];
				const key = await toKey(capabilities?.grantPermissions, { chainId: client.chain.id });
				const permissionsRequest = key ? encode$1(Schema, fromKey(key)) : void 0;
				const { context, digests } = await getProvider(store).request({
					...request,
					params: [{
						...request.params?.[0],
						capabilities: {
							...request.params?.[0]?.capabilities,
							grantPermissions: permissionsRequest
						}
					}]
				});
				const keys = context.account.keys?.map((k) => {
					if (k.id === key?.id) return {
						...k,
						...key
					};
					return k;
				});
				return {
					context: {
						...context,
						account: {
							...context.account,
							keys
						}
					},
					digests
				};
			},
			async revokeAdmin(parameters$1) {
				const { account, id, internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_revokeAdmin") throw new Error("Cannot revoke admin for method: " + request.method);
				const key = account.keys?.find((key$1) => key$1.id === id);
				if (!key) return;
				if (key.type === "webauthn-p256" && account.keys?.filter((key$1) => key$1.type === "webauthn-p256").length === 1) throw new Error("revoke the only WebAuthn key left.");
				const feeToken$1 = await resolveFeeToken(internal, parameters$1);
				return await getProvider(store).request({
					...request,
					params: [{
						...request.params?.[0],
						capabilities: {
							...request.params?.[0]?.capabilities,
							feeToken: feeToken$1
						}
					}]
				});
			},
			async revokePermissions(parameters$1) {
				const { account, id, internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_revokePermissions") throw new Error("Cannot revoke permissions for method: " + request.method);
				const key = account.keys?.find((key$1) => key$1.id === id);
				if (!key) return;
				if (key.role === "admin") throw new Error("cannot revoke permissions.");
				return await getProvider(store).request(request);
			},
			async sendCalls(parameters$1) {
				const { account, asTxHash, calls, chainId, internal, merchantUrl: merchantUrl$1, requiredFunds: requiredFunds$2 } = parameters$1;
				const { client, store, request } = internal;
				const provider = getProvider(store);
				const feeToken$1 = await resolveFeeToken(internal, parameters$1);
				const key = await getAuthorizedExecuteKey({
					account,
					calls,
					permissionsId: parameters$1.permissionsId
				});
				if (key && key.role === "session") {
					if (!renderer.supportsHeadless) return fallback$1.actions.sendCalls(parameters$1);
					try {
						const req = await provider.request(encode$1(wallet_prepareCalls.Request, {
							method: "wallet_prepareCalls",
							params: [{
								calls,
								capabilities: {
									...request._decoded.method === "wallet_sendCalls" ? request._decoded.params?.[0]?.capabilities : void 0,
									feeToken: feeToken$1,
									merchantUrl: merchantUrl$1,
									requiredFunds: requiredFunds$2
								},
								chainId,
								from: account.address,
								key
							}]
						}));
						const quotes = req.capabilities?.quote?.quotes ?? [];
						if (quotes.some((quote, index) => {
							if (index === quotes.length - 1 && quotes.length > 1) return false;
							return toBigInt(quote.feeTokenDeficit) > 0n;
						})) throw new Error("insufficient funds");
						const signature = await sign$2(key, {
							address: null,
							payload: req.digest,
							wrap: false
						});
						const response = (await provider.request({
							method: "wallet_sendPreparedCalls",
							params: [{
								...req,
								signature
							}]
						}))[0];
						if (!response) throw new Error("id not found");
						if (asTxHash) {
							const { id, receipts, status } = await waitForCallsStatus(client, {
								id: response.id,
								pollingInterval: 500
							});
							if (!receipts?.[0]) {
								if (status === "success") throw new UnknownBundleIdError({ message: "Call bundle with id: " + id + " not found." });
								throw new TransactionRejectedError({ message: "Transaction failed under call bundle id: " + id + "." });
							}
							return { id: receipts[0].transactionHash };
						}
						return response;
					} catch {}
				}
				if (request.method === "eth_sendTransaction") return { id: await provider.request({
					...request,
					params: [{
						...request.params?.[0],
						capabilities: {
							feeToken: feeToken$1,
							merchantUrl: merchantUrl$1
						},
						...chainId ? { chainId: fromNumber(chainId) } : {}
					}]
				}) };
				if (request.method === "wallet_sendCalls") return await provider.request({
					method: "wallet_sendCalls",
					params: [{
						...request.params?.[0],
						capabilities: {
							...request.params?.[0]?.capabilities,
							feeToken: feeToken$1,
							merchantUrl: merchantUrl$1
						},
						...chainId ? { chainId: fromNumber(chainId) } : {}
					}]
				});
				throw new Error("Cannot execute for method: " + request.method);
			},
			async sendPreparedCalls(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_sendPreparedCalls") throw new Error("Cannot send prepared calls for method: " + request.method);
				if (!renderer.supportsHeadless) return fallback$1.actions.sendPreparedCalls(parameters$1);
				const id = (await getProvider(store).request(request))[0]?.id;
				if (!id) throw new Error("id not found");
				return id;
			},
			async signPersonalMessage(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "personal_sign") throw new Error("Cannot sign personal message for method: " + request.method);
				return await getProvider(store).request(request);
			},
			async signTypedData(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "eth_signTypedData_v4") throw new Error("Cannot sign typed data for method: " + request.method);
				return await getProvider(store).request(request);
			},
			async switchChain(parameters$1) {
				const { internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_switchEthereumChain") throw new Error("Cannot switch chain for method: " + request.method);
				if (!renderer.supportsHeadless) return;
				return await getProvider(store).request(request);
			},
			async upgradeAccount(parameters$1) {
				const { account, internal } = parameters$1;
				const { store, request } = internal;
				if (request.method !== "wallet_upgradeAccount") throw new Error("Cannot upgrade account for method: " + request.method);
				await getProvider(store).request(request);
				return { account };
			},
			async verifyEmail(parameters$1) {
				const { internal } = parameters$1;
				const { request, store } = internal;
				if (request.method !== "account_verifyEmail") throw new Error("Cannot verify email for method: " + request.method);
				return await getProvider(store).request(request);
			}
		},
		config: parameters,
		name: "dialog",
		setup(parameters$1) {
			const { internal } = parameters$1;
			const { store } = internal;
			const dialog$1 = renderer.setup({
				host,
				internal,
				theme,
				themeController
			});
			const unsubscribe = store.subscribe((x) => x.requestQueue, (requestQueue) => {
				for (const listener of listeners) listener(requestQueue);
				const requests = requestQueue.map((x) => x.status === "pending" ? x : void 0).filter(Boolean);
				dialog$1.syncRequests(requests).catch(() => {});
				if (requests.length === 0) dialog$1.close();
			});
			return () => {
				unsubscribe();
				dialog$1.destroy();
			};
		}
	});
}
async function resolveFeeToken(internal, parameters) {
	const { config: { feeToken: feeToken$1 } } = internal;
	const { feeToken: overrideFeeToken } = parameters ?? {};
	return overrideFeeToken ?? feeToken$1;
}
function getAuthUrl(apiUrl, { storage }) {
	if (!apiUrl) return void 0;
	const authUrl = resolveAuthUrl(apiUrl, typeof window !== "undefined" ? window.location.origin : void 0);
	if (authUrl) storage.setItem("porto.authUrl", authUrl);
	return authUrl;
}

//#endregion
//#region node_modules/porto/dist/core/internal/modes/reactNative.js
function reactNative(parameters = {}) {
	if (!isReactNative()) return parameters.fallback ?? from$2({
		actions: relay().actions,
		name: "relay"
	});
	const { redirectUri, requestOptions, ...baseParameters } = parameters;
	return from$2({
		...dialog({
			...baseParameters,
			renderer: authSession({
				redirectUri,
				requestOptions
			})
		}),
		name: "reactNative"
	});
}

//#endregion
//#region node_modules/porto/dist/core/internal/types.js
var types_exports = {};

//#endregion
//#region node_modules/porto/dist/core/Mode.js
var Mode_exports = /* @__PURE__ */ __export({
	_internal_types: () => types_exports,
	dialog: () => dialog,
	from: () => from$2,
	reactNative: () => reactNative,
	relay: () => relay
});

//#endregion
//#region node_modules/porto/node_modules/zustand/esm/middleware.mjs
var shouldDispatchFromDevtools = (api) => !!api.dispatchFromDevtools && typeof api.dispatch === "function";
var trackedConnections = /* @__PURE__ */ new Map();
var getTrackedConnectionState = (name) => {
	const api = trackedConnections.get(name);
	if (!api) return {};
	return Object.fromEntries(Object.entries(api.stores).map(([key, api2]) => [key, api2.getState()]));
};
var extractConnectionInformation = (store, extensionConnector, options) => {
	if (store === void 0) return {
		type: "untracked",
		connection: extensionConnector.connect(options)
	};
	const existingConnection = trackedConnections.get(options.name);
	if (existingConnection) return {
		type: "tracked",
		store,
		...existingConnection
	};
	const newConnection = {
		connection: extensionConnector.connect(options),
		stores: {}
	};
	trackedConnections.set(options.name, newConnection);
	return {
		type: "tracked",
		store,
		...newConnection
	};
};
var removeStoreFromTrackedConnections = (name, store) => {
	if (store === void 0) return;
	const connectionInfo = trackedConnections.get(name);
	if (!connectionInfo) return;
	delete connectionInfo.stores[store];
	if (Object.keys(connectionInfo.stores).length === 0) trackedConnections.delete(name);
};
var findCallerName = (stack) => {
	var _a, _b;
	if (!stack) return void 0;
	const traceLines = stack.split("\n");
	const apiSetStateLineIndex = traceLines.findIndex((traceLine) => traceLine.includes("api.setState"));
	if (apiSetStateLineIndex < 0) return void 0;
	const callerLine = ((_a = traceLines[apiSetStateLineIndex + 1]) == null ? void 0 : _a.trim()) || "";
	return (_b = /.+ (.+) .+/.exec(callerLine)) == null ? void 0 : _b[1];
};
var devtoolsImpl = (fn, devtoolsOptions = {}) => (set$1, get$1, api) => {
	const { enabled, anonymousActionType, store, ...options } = devtoolsOptions;
	let extensionConnector;
	try {
		extensionConnector = (enabled != null ? enabled : (import.meta.env ? import.meta.env.MODE : void 0) !== "production") && window.__REDUX_DEVTOOLS_EXTENSION__;
	} catch (e) {}
	if (!extensionConnector) return fn(set$1, get$1, api);
	const { connection, ...connectionInformation } = extractConnectionInformation(store, extensionConnector, options);
	let isRecording = true;
	api.setState = ((state, replace, nameOrAction) => {
		const r = set$1(state, replace);
		if (!isRecording) return r;
		const action = nameOrAction === void 0 ? { type: anonymousActionType || findCallerName((/* @__PURE__ */ new Error()).stack) || "anonymous" } : typeof nameOrAction === "string" ? { type: nameOrAction } : nameOrAction;
		if (store === void 0) {
			connection?.send(action, get$1());
			return r;
		}
		connection?.send({
			...action,
			type: `${store}/${action.type}`
		}, {
			...getTrackedConnectionState(options.name),
			[store]: api.getState()
		});
		return r;
	});
	api.devtools = { cleanup: () => {
		if (connection && typeof connection.unsubscribe === "function") connection.unsubscribe();
		removeStoreFromTrackedConnections(options.name, store);
	} };
	const setStateFromDevtools = (...a) => {
		const originalIsRecording = isRecording;
		isRecording = false;
		set$1(...a);
		isRecording = originalIsRecording;
	};
	const initialState = fn(api.setState, get$1, api);
	if (connectionInformation.type === "untracked") connection?.init(initialState);
	else {
		connectionInformation.stores[connectionInformation.store] = api;
		connection?.init(Object.fromEntries(Object.entries(connectionInformation.stores).map(([key, store2]) => [key, key === connectionInformation.store ? initialState : store2.getState()])));
	}
	if (shouldDispatchFromDevtools(api)) {
		let didWarnAboutReservedActionType = false;
		const originalDispatch = api.dispatch;
		api.dispatch = (...args) => {
			if ((import.meta.env ? import.meta.env.MODE : void 0) !== "production" && args[0].type === "__setState" && !didWarnAboutReservedActionType) {
				console.warn("[zustand devtools middleware] \"__setState\" action type is reserved to set state from the devtools. Avoid using it.");
				didWarnAboutReservedActionType = true;
			}
			originalDispatch(...args);
		};
	}
	connection.subscribe((message) => {
		var _a;
		switch (message.type) {
			case "ACTION":
				if (typeof message.payload !== "string") {
					console.error("[zustand devtools middleware] Unsupported action format");
					return;
				}
				return parseJsonThen(message.payload, (action) => {
					if (action.type === "__setState") {
						if (store === void 0) {
							setStateFromDevtools(action.state);
							return;
						}
						if (Object.keys(action.state).length !== 1) console.error(`
                    [zustand devtools middleware] Unsupported __setState action format.
                    When using 'store' option in devtools(), the 'state' should have only one key, which is a value of 'store' that was passed in devtools(),
                    and value of this only key should be a state object. Example: { "type": "__setState", "state": { "abc123Store": { "foo": "bar" } } }
                    `);
						const stateFromDevtools = action.state[store];
						if (stateFromDevtools === void 0 || stateFromDevtools === null) return;
						if (JSON.stringify(api.getState()) !== JSON.stringify(stateFromDevtools)) setStateFromDevtools(stateFromDevtools);
						return;
					}
					if (shouldDispatchFromDevtools(api)) api.dispatch(action);
				});
			case "DISPATCH":
				switch (message.payload.type) {
					case "RESET":
						setStateFromDevtools(initialState);
						if (store === void 0) return connection == null ? void 0 : connection.init(api.getState());
						return connection == null ? void 0 : connection.init(getTrackedConnectionState(options.name));
					case "COMMIT":
						if (store === void 0) {
							connection?.init(api.getState());
							return;
						}
						return connection == null ? void 0 : connection.init(getTrackedConnectionState(options.name));
					case "ROLLBACK": return parseJsonThen(message.state, (state) => {
						if (store === void 0) {
							setStateFromDevtools(state);
							connection?.init(api.getState());
							return;
						}
						setStateFromDevtools(state[store]);
						connection?.init(getTrackedConnectionState(options.name));
					});
					case "JUMP_TO_STATE":
					case "JUMP_TO_ACTION": return parseJsonThen(message.state, (state) => {
						if (store === void 0) {
							setStateFromDevtools(state);
							return;
						}
						if (JSON.stringify(api.getState()) !== JSON.stringify(state[store])) setStateFromDevtools(state[store]);
					});
					case "IMPORT_STATE": {
						const { nextLiftedState } = message.payload;
						const lastComputedState = (_a = nextLiftedState.computedStates.slice(-1)[0]) == null ? void 0 : _a.state;
						if (!lastComputedState) return;
						if (store === void 0) setStateFromDevtools(lastComputedState);
						else setStateFromDevtools(lastComputedState[store]);
						connection?.send(null, nextLiftedState);
						return;
					}
					case "PAUSE_RECORDING": return isRecording = !isRecording;
				}
				return;
		}
	});
	return initialState;
};
var devtools = devtoolsImpl;
var parseJsonThen = (stringified, fn) => {
	let parsed;
	try {
		parsed = JSON.parse(stringified);
	} catch (e) {
		console.error("[zustand devtools middleware] Could not parse the received json", e);
	}
	if (parsed !== void 0) fn(parsed);
};
var subscribeWithSelectorImpl = (fn) => (set$1, get$1, api) => {
	const origSubscribe = api.subscribe;
	api.subscribe = ((selector, optListener, options) => {
		let listener = selector;
		if (optListener) {
			const equalityFn = (options == null ? void 0 : options.equalityFn) || Object.is;
			let currentSlice = selector(api.getState());
			listener = (state) => {
				const nextSlice = selector(state);
				if (!equalityFn(currentSlice, nextSlice)) {
					const previousSlice = currentSlice;
					optListener(currentSlice = nextSlice, previousSlice);
				}
			};
			if (options == null ? void 0 : options.fireImmediately) optListener(currentSlice, currentSlice);
		}
		return origSubscribe(listener);
	});
	return fn(set$1, get$1, api);
};
var subscribeWithSelector = subscribeWithSelectorImpl;
function createJSONStorage(getStorage, options) {
	let storage;
	try {
		storage = getStorage();
	} catch (e) {
		return;
	}
	return {
		getItem: (name) => {
			var _a;
			const parse$2 = (str2) => {
				if (str2 === null) return null;
				return JSON.parse(str2, options == null ? void 0 : options.reviver);
			};
			const str = (_a = storage.getItem(name)) != null ? _a : null;
			if (str instanceof Promise) return str.then(parse$2);
			return parse$2(str);
		},
		setItem: (name, newValue) => storage.setItem(name, JSON.stringify(newValue, options == null ? void 0 : options.replacer)),
		removeItem: (name) => storage.removeItem(name)
	};
}
var toThenable = (fn) => (input) => {
	try {
		const result = fn(input);
		if (result instanceof Promise) return result;
		return {
			then(onFulfilled) {
				return toThenable(onFulfilled)(result);
			},
			catch(_onRejected) {
				return this;
			}
		};
	} catch (e) {
		return {
			then(_onFulfilled) {
				return this;
			},
			catch(onRejected) {
				return toThenable(onRejected)(e);
			}
		};
	}
};
var persistImpl = (config, baseOptions) => (set$1, get$1, api) => {
	let options = {
		storage: createJSONStorage(() => window.localStorage),
		partialize: (state) => state,
		version: 0,
		merge: (persistedState, currentState) => ({
			...currentState,
			...persistedState
		}),
		...baseOptions
	};
	let hasHydrated = false;
	let hydrationVersion = 0;
	const hydrationListeners = /* @__PURE__ */ new Set();
	const finishHydrationListeners = /* @__PURE__ */ new Set();
	let storage = options.storage;
	if (!storage) return config((...args) => {
		console.warn(`[zustand persist middleware] Unable to update item '${options.name}', the given storage is currently unavailable.`);
		set$1(...args);
	}, get$1, api);
	const setItem = () => {
		const state = options.partialize({ ...get$1() });
		return storage.setItem(options.name, {
			state,
			version: options.version
		});
	};
	const savedSetState = api.setState;
	api.setState = (state, replace) => {
		savedSetState(state, replace);
		return setItem();
	};
	const configResult = config((...args) => {
		set$1(...args);
		return setItem();
	}, get$1, api);
	api.getInitialState = () => configResult;
	let stateFromStorage;
	const hydrate = () => {
		var _a, _b;
		if (!storage) return;
		const currentVersion = ++hydrationVersion;
		hasHydrated = false;
		hydrationListeners.forEach((cb) => {
			var _a2;
			return cb((_a2 = get$1()) != null ? _a2 : configResult);
		});
		const postRehydrationCallback = ((_b = options.onRehydrateStorage) == null ? void 0 : _b.call(options, (_a = get$1()) != null ? _a : configResult)) || void 0;
		return toThenable(storage.getItem.bind(storage))(options.name).then((deserializedStorageValue) => {
			if (deserializedStorageValue) if (typeof deserializedStorageValue.version === "number" && deserializedStorageValue.version !== options.version) {
				if (options.migrate) {
					const migration = options.migrate(deserializedStorageValue.state, deserializedStorageValue.version);
					if (migration instanceof Promise) return migration.then((result) => [true, result]);
					return [true, migration];
				}
				console.error(`State loaded from storage couldn't be migrated since no migrate function was provided`);
			} else return [false, deserializedStorageValue.state];
			return [false, void 0];
		}).then((migrationResult) => {
			var _a2;
			if (currentVersion !== hydrationVersion) return;
			const [migrated, migratedState] = migrationResult;
			stateFromStorage = options.merge(migratedState, (_a2 = get$1()) != null ? _a2 : configResult);
			set$1(stateFromStorage, true);
			if (migrated) return setItem();
		}).then(() => {
			if (currentVersion !== hydrationVersion) return;
			postRehydrationCallback?.(get$1(), void 0);
			stateFromStorage = get$1();
			hasHydrated = true;
			finishHydrationListeners.forEach((cb) => cb(stateFromStorage));
		}).catch((e) => {
			if (currentVersion !== hydrationVersion) return;
			postRehydrationCallback?.(void 0, e);
		});
	};
	api.persist = {
		setOptions: (newOptions) => {
			options = {
				...options,
				...newOptions
			};
			if (newOptions.storage) storage = newOptions.storage;
		},
		clearStorage: () => {
			storage?.removeItem(options.name);
		},
		getOptions: () => options,
		rehydrate: () => hydrate(),
		hasHydrated: () => hasHydrated,
		onHydrate: (cb) => {
			hydrationListeners.add(cb);
			return () => {
				hydrationListeners.delete(cb);
			};
		},
		onFinishHydration: (cb) => {
			finishHydrationListeners.add(cb);
			return () => {
				finishHydrationListeners.delete(cb);
			};
		}
	};
	if (!options.skipHydration) hydrate();
	return stateFromStorage || configResult;
};
var persist = persistImpl;

//#endregion
//#region node_modules/porto/node_modules/zustand/esm/vanilla.mjs
var createStoreImpl = (createState) => {
	let state;
	const listeners = /* @__PURE__ */ new Set();
	const setState = (partial$1, replace) => {
		const nextState = typeof partial$1 === "function" ? partial$1(state) : partial$1;
		if (!Object.is(nextState, state)) {
			const previousState = state;
			state = (replace != null ? replace : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
			listeners.forEach((listener) => listener(state, previousState));
		}
	};
	const getState = () => state;
	const getInitialState = () => initialState;
	const subscribe = (listener) => {
		listeners.add(listener);
		return () => listeners.delete(listener);
	};
	const api = {
		setState,
		getState,
		getInitialState,
		subscribe
	};
	const initialState = state = createState(setState, getState, api);
	return api;
};
var createStore = ((createState) => createState ? createStoreImpl(createState) : createStoreImpl);

//#endregion
//#region node_modules/porto/dist/core/Storage.js
var Storage_exports = /* @__PURE__ */ __export({
	combine: () => combine,
	cookie: () => cookie,
	from: () => from$1,
	idb: () => idb,
	localStorage: () => localStorage,
	memory: () => memory
});
function from$1(storage) {
	return storage;
}
function combine(...storages) {
	return {
		async getItem(name) {
			const result = (await Promise.allSettled(storages.map((x) => x.getItem(name)))).find((x) => x.status === "fulfilled" && x.value !== null);
			if (result?.status !== "fulfilled") return null;
			if (result.value === null) return null;
			return result.value;
		},
		async removeItem(name) {
			await Promise.allSettled(storages.map((x) => x.removeItem(name)));
		},
		async setItem(name, value) {
			await Promise.allSettled(storages.map((x) => x.setItem(name, value)));
		},
		sizeLimit: Math.min(...storages.map((x) => x.sizeLimit)),
		storages
	};
}
function idb() {
	const store = typeof indexedDB !== "undefined" ? createStore$1("porto", "store") : void 0;
	return from$1({
		async getItem(name) {
			const value = await get(name, store);
			if (value === null) return null;
			return value;
		},
		async removeItem(name) {
			await del(name, store);
		},
		async setItem(name, value) {
			await set(name, normalizeValue(value), store);
		},
		sizeLimit: 1024 * 1024 * 50
	});
}
function localStorage() {
	return from$1({
		async getItem(name) {
			const item = window.localStorage.getItem(name);
			if (item === null) return null;
			try {
				return parse$1(item);
			} catch {
				return null;
			}
		},
		async removeItem(name) {
			window.localStorage.removeItem(name);
		},
		async setItem(name, value) {
			window.localStorage.setItem(name, stringify(value));
		},
		sizeLimit: 1024 * 1024 * 5
	});
}
function cookie() {
	return from$1({
		async getItem(name) {
			const value = document.cookie.split("; ").find((x) => x.startsWith(`${name}=`));
			if (!value) return null;
			try {
				return parse$1(value.substring(name.length + 1));
			} catch {
				return null;
			}
		},
		async removeItem(name) {
			document.cookie = `${name}=;max-age=-1;path=/`;
		},
		async setItem(name, value) {
			document.cookie = `${name}=${stringify(value)};path=/;samesite=None;secure;max-age=31536000`;
		},
		sizeLimit: 1024 * 4
	});
}
function memory() {
	const store = /* @__PURE__ */ new Map();
	return from$1({
		getItem(name) {
			return store.get(name) ?? null;
		},
		removeItem(name) {
			store.delete(name);
		},
		setItem(name, value) {
			store.set(name, value);
		},
		sizeLimit: Number.POSITIVE_INFINITY
	});
}

//#endregion
//#region node_modules/porto/dist/core/Porto.js
var Porto_exports = /* @__PURE__ */ __export({
	create: () => create,
	defaultConfig: () => defaultConfig
});
var browser = typeof window !== "undefined" && typeof document !== "undefined";
const defaultConfig = {
	announceProvider: true,
	chains: all,
	mode: browser ? dialog({ host: hostUrls.prod }) : relay(),
	relay: http(relayUrls.prod.http),
	storage: browser && typeof indexedDB !== "undefined" ? idb() : memory(),
	storageKey: "porto.store"
};
function create(parameters = {}) {
	const chains = parameters.chains ?? defaultConfig.chains;
	const transports = Object.fromEntries(chains.map((chain) => [chain.id, parameters.transports?.[chain.id] ?? http()]));
	const config = {
		announceProvider: parameters.announceProvider ?? defaultConfig.announceProvider,
		authUrl: parameters.authUrl,
		chains,
		feeToken: parameters.feeToken,
		merchantUrl: parameters.merchantUrl,
		mode: parameters.mode ?? defaultConfig.mode,
		relay: parameters.relay ?? defaultConfig.relay,
		storage: parameters.storage ?? defaultConfig.storage,
		storageKey: parameters.storageKey ?? defaultConfig.storageKey,
		transports
	};
	const store = createStore(devtools(subscribeWithSelector(persist((_) => ({
		accounts: [],
		chainIds: config.chains.map((chain) => chain.id),
		feeToken: config.feeToken,
		requestQueue: []
	}), {
		merge(p, currentState) {
			const persistedState = p;
			const currentChainId = config.chains.find((chain) => chain.id === persistedState.chainIds[0])?.id ?? config.chains[0].id;
			const chainIds = [currentChainId, ...config.chains.map((chain) => chain.id).filter((id) => id !== currentChainId)];
			return {
				...currentState,
				...persistedState,
				chainIds
			};
		},
		name: config.storageKey,
		partialize: (state) => ({
			accounts: state.accounts.map((account) => normalizeValue(account)),
			chainIds: state.chainIds
		}),
		storage: config.storage,
		version: 5
	}))));
	let mode = config.mode;
	const internal = {
		config,
		getMode() {
			return mode;
		},
		id: uuidv4(),
		setMode(i) {
			destroy?.();
			mode = i;
			destroy = i.setup({ internal });
			return destroy;
		},
		store
	};
	const provider = from(internal);
	let destroy = mode !== null ? mode.setup({ internal }) : () => {};
	return {
		_internal: internal,
		config,
		destroy() {
			destroy();
			provider._internal.destroy();
		},
		provider
	};
}

//#endregion
export { Account_exports as Account, Chains_exports as Chains, Dialog_exports as Dialog, Key_exports as Key, Messenger_exports as Messenger, Mode_exports as Mode, Porto_exports as Porto, RelayActions_exports as RelayActions, RpcSchema_exports as RpcSchema, Storage_exports as Storage, Transport_exports as Transport, from };
//# sourceMappingURL=dist-DPZxa9Yw.js.map