import { c as __toESM } from "./chunk-CBgdiQcn.js";
import { t as require_react } from "./react-CqXAK9JW.js";
import { t as require_react_dom } from "./react-dom-Cpfbu0H1.js";
import { t as require_jsx_runtime } from "./jsx-runtime-Dq9gTbWS.js";
import { X as ut, fr as n, vr as E, zt as c } from "./useActiveWallet-9-7-ELBX-C7cHxRLi.js";
import { $n as l$2, An as H$2, Bn as import_react$1, Cn as y, Dn as T$1, En as x$1, Fn as i$3, Gn as u$4, Hn as K$2, In as l$5, Jn as o, Kn as a$5, Ln as m$2, Mn as E$3, Nn as L$2, On as k$3, Pn as a$7, Qn as d$1, Rn as n$7, Sn as p$2, Tn as k$4, Un as V$1, Vn as A, Wn as Y$1, Xn as p$1, Yn as n$4, Zn as o$1, _n as H$1, an as te, bn as R, cn as i, dn as x, fn as f, gn as G$1, hn as i$1, jn as y$1, kn as o$2, ln as u$3, mn as k$2, on as c$7, pn as u$1, qn as n$5, sn as c$5, un as N, vn as I, wn as S$1, xn as T, yn as K$1, zn as o$7 } from "./index-C6XL4TGs-DE3DVUiI.js";
import { F as createPublicClient, h as http } from "./_esm-CRgaECpT.js";
import { l as u, s as k } from "./ModalHeader-Bgc2_pdq-DksvhAB7.js";
import { t as ChevronDownIcon_default } from "./ChevronDownIcon-BKpTDOuk.js";
import { t as WalletIcon_default } from "./WalletIcon-g9Mit8aa.js";
import { t as e } from "./ErrorMessage-D8VaAP5m-0lrF8asS.js";
import { n as c$2, u as s$1 } from "./Layouts-BlFm53ED-qHQpqSIj.js";
import { t as t$1 } from "./FundWalletMethodHeader-CSJ-J1Fp-Dt_bkj37.js";
import { t as e$3 } from "./getChainName-DjpPdUSc-BgaeR3s9.js";
import { t as n$1 } from "./getErc20Balance-BZ1slwIc-C_FUciHX.js";
import { i as s$2, n as n$2, t as e$1 } from "./Value-tcJV9e0L-Dx79fs-Z.js";
import { t as r } from "./Subtitle-CV-2yKE4-CpcaIzPH.js";
import { t as e$2 } from "./Title-BnzYV3Is-CCeMC6tA.js";
import { t as n$3 } from "./Chip-D2-wZOHJ-BxQG6CMl.js";
import { n as w } from "./TransferOrBridgeLoadingScreen-1CtlWake-9TitffaZ.js";
import { n as e$4, t as d } from "./shared-FM0rljBt-Dl-gUvod.js";
import { t as t$2 } from "./formatErc20TokenAmount-BuPk9xcy-CcOoH-0R.js";
import { t as c$3 } from "./ethers-DxnUMtUS-DhYYlaY1.js";
import { l as p, n as c$4, o as l, t as a, u as s$3 } from "./styles-Dz2KU01o-DC0pbFiG.js";

//#region node_modules/@react-aria/utils/dist/useLayoutEffect.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var $f0a04ccd8dbdd83b$export$e5c5a5f917a5871c = typeof document !== "undefined" ? import_react.useLayoutEffect : () => {};

//#endregion
//#region node_modules/@react-aria/utils/dist/domHelpers.mjs
var $431fbd86ca7dc216$export$b204af158042fbac = (el) => {
	var _el_ownerDocument;
	return (_el_ownerDocument = el === null || el === void 0 ? void 0 : el.ownerDocument) !== null && _el_ownerDocument !== void 0 ? _el_ownerDocument : document;
};
var $431fbd86ca7dc216$export$f21a1ffae260145a = (el) => {
	if (el && "window" in el && el.window === el) return el;
	return $431fbd86ca7dc216$export$b204af158042fbac(el).defaultView || window;
};
/**
* Type guard that checks if a value is a Node. Verifies the presence and type of the nodeType property.
*/ function $431fbd86ca7dc216$var$isNode(value) {
	return value !== null && typeof value === "object" && "nodeType" in value && typeof value.nodeType === "number";
}
function $431fbd86ca7dc216$export$af51f0f06c0f328a(node) {
	return $431fbd86ca7dc216$var$isNode(node) && node.nodeType === Node.DOCUMENT_FRAGMENT_NODE && "host" in node;
}

//#endregion
//#region node_modules/@react-stately/flags/dist/import.mjs
var $f4e2df6bd15f8569$var$_shadowDOM = false;
function $f4e2df6bd15f8569$export$98658e8c59125e6a() {
	return $f4e2df6bd15f8569$var$_shadowDOM;
}

//#endregion
//#region node_modules/@react-aria/utils/dist/DOMFunctions.mjs
function $d4ee10de306f2510$export$4282f70798064fe0(node, otherNode) {
	if (!$f4e2df6bd15f8569$export$98658e8c59125e6a()) return otherNode && node ? node.contains(otherNode) : false;
	if (!node || !otherNode) return false;
	let currentNode = otherNode;
	while (currentNode !== null) {
		if (currentNode === node) return true;
		if (currentNode.tagName === "SLOT" && currentNode.assignedSlot) currentNode = currentNode.assignedSlot.parentNode;
		else if ($431fbd86ca7dc216$export$af51f0f06c0f328a(currentNode)) currentNode = currentNode.host;
		else currentNode = currentNode.parentNode;
	}
	return false;
}
var $d4ee10de306f2510$export$cd4e5573fbe2b576 = (doc = document) => {
	var _activeElement_shadowRoot;
	if (!$f4e2df6bd15f8569$export$98658e8c59125e6a()) return doc.activeElement;
	let activeElement$1 = doc.activeElement;
	while (activeElement$1 && "shadowRoot" in activeElement$1 && ((_activeElement_shadowRoot = activeElement$1.shadowRoot) === null || _activeElement_shadowRoot === void 0 ? void 0 : _activeElement_shadowRoot.activeElement)) activeElement$1 = activeElement$1.shadowRoot.activeElement;
	return activeElement$1;
};
function $d4ee10de306f2510$export$e58f029f0fbfdb29(event) {
	if ($f4e2df6bd15f8569$export$98658e8c59125e6a() && event.target.shadowRoot) {
		if (event.composedPath) return event.composedPath()[0];
	}
	return event.target;
}

//#endregion
//#region node_modules/@react-aria/utils/dist/focusWithoutScrolling.mjs
function $7215afc6de606d6b$export$de79e2c695e052f3(element) {
	if ($7215afc6de606d6b$var$supportsPreventScroll()) element.focus({ preventScroll: true });
	else {
		let scrollableElements = $7215afc6de606d6b$var$getScrollableElements(element);
		element.focus();
		$7215afc6de606d6b$var$restoreScrollPosition(scrollableElements);
	}
}
var $7215afc6de606d6b$var$supportsPreventScrollCached = null;
function $7215afc6de606d6b$var$supportsPreventScroll() {
	if ($7215afc6de606d6b$var$supportsPreventScrollCached == null) {
		$7215afc6de606d6b$var$supportsPreventScrollCached = false;
		try {
			document.createElement("div").focus({ get preventScroll() {
				$7215afc6de606d6b$var$supportsPreventScrollCached = true;
				return true;
			} });
		} catch {}
	}
	return $7215afc6de606d6b$var$supportsPreventScrollCached;
}
function $7215afc6de606d6b$var$getScrollableElements(element) {
	let parent = element.parentNode;
	let scrollableElements = [];
	let rootScrollingElement = document.scrollingElement || document.documentElement;
	while (parent instanceof HTMLElement && parent !== rootScrollingElement) {
		if (parent.offsetHeight < parent.scrollHeight || parent.offsetWidth < parent.scrollWidth) scrollableElements.push({
			element: parent,
			scrollTop: parent.scrollTop,
			scrollLeft: parent.scrollLeft
		});
		parent = parent.parentNode;
	}
	if (rootScrollingElement instanceof HTMLElement) scrollableElements.push({
		element: rootScrollingElement,
		scrollTop: rootScrollingElement.scrollTop,
		scrollLeft: rootScrollingElement.scrollLeft
	});
	return scrollableElements;
}
function $7215afc6de606d6b$var$restoreScrollPosition(scrollableElements) {
	for (let { element, scrollTop, scrollLeft } of scrollableElements) {
		element.scrollTop = scrollTop;
		element.scrollLeft = scrollLeft;
	}
}

//#endregion
//#region node_modules/@react-aria/utils/dist/platform.mjs
function $c87311424ea30a05$var$testUserAgent(re) {
	var _window_navigator_userAgentData;
	if (typeof window === "undefined" || window.navigator == null) return false;
	let brands = (_window_navigator_userAgentData = window.navigator["userAgentData"]) === null || _window_navigator_userAgentData === void 0 ? void 0 : _window_navigator_userAgentData.brands;
	return Array.isArray(brands) && brands.some((brand) => re.test(brand.brand)) || re.test(window.navigator.userAgent);
}
function $c87311424ea30a05$var$testPlatform(re) {
	var _window_navigator_userAgentData;
	return typeof window !== "undefined" && window.navigator != null ? re.test(((_window_navigator_userAgentData = window.navigator["userAgentData"]) === null || _window_navigator_userAgentData === void 0 ? void 0 : _window_navigator_userAgentData.platform) || window.navigator.platform) : false;
}
function $c87311424ea30a05$var$cached(fn) {
	let res = null;
	return () => {
		if (res == null) res = fn();
		return res;
	};
}
var $c87311424ea30a05$export$9ac100e40613ea10 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testPlatform(/^Mac/i);
});
var $c87311424ea30a05$export$186c6964ca17d99 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testPlatform(/^iPhone/i);
});
var $c87311424ea30a05$export$7bef049ce92e4224 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testPlatform(/^iPad/i) || $c87311424ea30a05$export$9ac100e40613ea10() && navigator.maxTouchPoints > 1;
});
var $c87311424ea30a05$export$fedb369cb70207f1 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$export$186c6964ca17d99() || $c87311424ea30a05$export$7bef049ce92e4224();
});
var $c87311424ea30a05$export$e1865c3bedcd822b = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$export$9ac100e40613ea10() || $c87311424ea30a05$export$fedb369cb70207f1();
});
var $c87311424ea30a05$export$78551043582a6a98 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testUserAgent(/AppleWebKit/i) && !$c87311424ea30a05$export$6446a186d09e379e();
});
var $c87311424ea30a05$export$6446a186d09e379e = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testUserAgent(/Chrome/i);
});
var $c87311424ea30a05$export$a11b0059900ceec8 = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testUserAgent(/Android/i);
});
var $c87311424ea30a05$export$b7d78993b74f766d = $c87311424ea30a05$var$cached(function() {
	return $c87311424ea30a05$var$testUserAgent(/Firefox/i);
});

//#endregion
//#region node_modules/@react-aria/utils/dist/openLink.mjs
function $ea8dcbcb9ea1b556$export$95185d699e05d4d7(target, modifiers, setOpening = true) {
	var _window_event_type, _window_event;
	let { metaKey, ctrlKey, altKey, shiftKey } = modifiers;
	if ($c87311424ea30a05$export$b7d78993b74f766d() && ((_window_event = window.event) === null || _window_event === void 0 ? void 0 : (_window_event_type = _window_event.type) === null || _window_event_type === void 0 ? void 0 : _window_event_type.startsWith("key")) && target.target === "_blank") if ($c87311424ea30a05$export$9ac100e40613ea10()) metaKey = true;
	else ctrlKey = true;
	let event = $c87311424ea30a05$export$78551043582a6a98() && $c87311424ea30a05$export$9ac100e40613ea10() && !$c87311424ea30a05$export$7bef049ce92e4224() && true ? new KeyboardEvent("keydown", {
		keyIdentifier: "Enter",
		metaKey,
		ctrlKey,
		altKey,
		shiftKey
	}) : new MouseEvent("click", {
		metaKey,
		ctrlKey,
		altKey,
		shiftKey,
		detail: 1,
		bubbles: true,
		cancelable: true
	});
	$ea8dcbcb9ea1b556$export$95185d699e05d4d7.isOpening = setOpening;
	$7215afc6de606d6b$export$de79e2c695e052f3(target);
	target.dispatchEvent(event);
	$ea8dcbcb9ea1b556$export$95185d699e05d4d7.isOpening = false;
}
$ea8dcbcb9ea1b556$export$95185d699e05d4d7.isOpening = false;

//#endregion
//#region node_modules/@react-aria/utils/dist/useGlobalListeners.mjs
function $03deb23ff14920c4$export$4eaf04e54aa8eed6() {
	let globalListeners = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	let addGlobalListener = (0, import_react.useCallback)((eventTarget, type, listener, options) => {
		let fn = (options === null || options === void 0 ? void 0 : options.once) ? (...args) => {
			globalListeners.current.delete(listener);
			listener(...args);
		} : listener;
		globalListeners.current.set(listener, {
			type,
			eventTarget,
			fn,
			options
		});
		eventTarget.addEventListener(type, fn, options);
	}, []);
	let removeGlobalListener = (0, import_react.useCallback)((eventTarget, type, listener, options) => {
		var _globalListeners_current_get;
		let fn = ((_globalListeners_current_get = globalListeners.current.get(listener)) === null || _globalListeners_current_get === void 0 ? void 0 : _globalListeners_current_get.fn) || listener;
		eventTarget.removeEventListener(type, fn, options);
		globalListeners.current.delete(listener);
	}, []);
	let removeAllGlobalListeners = (0, import_react.useCallback)(() => {
		globalListeners.current.forEach((value, key) => {
			removeGlobalListener(value.eventTarget, value.type, key, value.options);
		});
	}, [removeGlobalListener]);
	(0, import_react.useEffect)(() => {
		return removeAllGlobalListeners;
	}, [removeAllGlobalListeners]);
	return {
		addGlobalListener,
		removeGlobalListener,
		removeAllGlobalListeners
	};
}

//#endregion
//#region node_modules/@react-aria/utils/dist/isVirtualEvent.mjs
function $6a7db85432448f7f$export$60278871457622de(event) {
	if (event.pointerType === "" && event.isTrusted) return true;
	if ($c87311424ea30a05$export$a11b0059900ceec8() && event.pointerType) return event.type === "click" && event.buttons === 1;
	return event.detail === 0 && !event.pointerType;
}

//#endregion
//#region node_modules/@react-aria/interactions/dist/utils.mjs
function $8a9cb279dc87e130$export$525bc4921d56d4a(nativeEvent) {
	let event = nativeEvent;
	event.nativeEvent = nativeEvent;
	event.isDefaultPrevented = () => event.defaultPrevented;
	event.isPropagationStopped = () => event.cancelBubble;
	event.persist = () => {};
	return event;
}
function $8a9cb279dc87e130$export$c2b7abe5d61ec696(event, target) {
	Object.defineProperty(event, "target", { value: target });
	Object.defineProperty(event, "currentTarget", { value: target });
}
function $8a9cb279dc87e130$export$715c682d09d639cc(onBlur) {
	let stateRef = (0, import_react.useRef)({
		isFocused: false,
		observer: null
	});
	$f0a04ccd8dbdd83b$export$e5c5a5f917a5871c(() => {
		const state = stateRef.current;
		return () => {
			if (state.observer) {
				state.observer.disconnect();
				state.observer = null;
			}
		};
	}, []);
	return (0, import_react.useCallback)((e$7) => {
		if (e$7.target instanceof HTMLButtonElement || e$7.target instanceof HTMLInputElement || e$7.target instanceof HTMLTextAreaElement || e$7.target instanceof HTMLSelectElement) {
			stateRef.current.isFocused = true;
			let target = e$7.target;
			let onBlurHandler = (e$8) => {
				stateRef.current.isFocused = false;
				if (target.disabled) {
					let event = $8a9cb279dc87e130$export$525bc4921d56d4a(e$8);
					onBlur === null || onBlur === void 0 || onBlur(event);
				}
				if (stateRef.current.observer) {
					stateRef.current.observer.disconnect();
					stateRef.current.observer = null;
				}
			};
			target.addEventListener("focusout", onBlurHandler, { once: true });
			stateRef.current.observer = new MutationObserver(() => {
				if (stateRef.current.isFocused && target.disabled) {
					var _stateRef_current_observer;
					(_stateRef_current_observer = stateRef.current.observer) === null || _stateRef_current_observer === void 0 || _stateRef_current_observer.disconnect();
					let relatedTargetEl = target === document.activeElement ? null : document.activeElement;
					target.dispatchEvent(new FocusEvent("blur", { relatedTarget: relatedTargetEl }));
					target.dispatchEvent(new FocusEvent("focusout", {
						bubbles: true,
						relatedTarget: relatedTargetEl
					}));
				}
			});
			stateRef.current.observer.observe(target, {
				attributes: true,
				attributeFilter: ["disabled"]
			});
		}
	}, [onBlur]);
}
var $8a9cb279dc87e130$export$fda7da73ab5d4c48 = false;

//#endregion
//#region node_modules/@react-aria/interactions/dist/useFocusVisible.mjs
var $507fabe10e71c6fb$var$currentModality = null;
var $507fabe10e71c6fb$var$currentPointerType = "keyboard";
var $507fabe10e71c6fb$export$901e90a13c50a14e = /* @__PURE__ */ new Set();
var $507fabe10e71c6fb$export$d90243b58daecda7 = /* @__PURE__ */ new Map();
var $507fabe10e71c6fb$var$hasEventBeforeFocus = false;
var $507fabe10e71c6fb$var$hasBlurredWindowRecently = false;
var $507fabe10e71c6fb$var$FOCUS_VISIBLE_INPUT_KEYS = {
	Tab: true,
	Escape: true
};
function $507fabe10e71c6fb$var$triggerChangeHandlers(modality, e$7) {
	for (let handler of $507fabe10e71c6fb$export$901e90a13c50a14e) handler(modality, e$7);
}
/**
* Helper function to determine if a KeyboardEvent is unmodified and could make keyboard focus styles visible.
*/ function $507fabe10e71c6fb$var$isValidKey(e$7) {
	return !(e$7.metaKey || !$c87311424ea30a05$export$9ac100e40613ea10() && e$7.altKey || e$7.ctrlKey || e$7.key === "Control" || e$7.key === "Shift" || e$7.key === "Meta");
}
function $507fabe10e71c6fb$var$handleKeyboardEvent(e$7) {
	$507fabe10e71c6fb$var$hasEventBeforeFocus = true;
	if (!$ea8dcbcb9ea1b556$export$95185d699e05d4d7.isOpening && $507fabe10e71c6fb$var$isValidKey(e$7)) {
		$507fabe10e71c6fb$var$currentModality = "keyboard";
		$507fabe10e71c6fb$var$currentPointerType = "keyboard";
		$507fabe10e71c6fb$var$triggerChangeHandlers("keyboard", e$7);
	}
}
function $507fabe10e71c6fb$var$handlePointerEvent(e$7) {
	$507fabe10e71c6fb$var$currentModality = "pointer";
	$507fabe10e71c6fb$var$currentPointerType = "pointerType" in e$7 ? e$7.pointerType : "mouse";
	if (e$7.type === "mousedown" || e$7.type === "pointerdown") {
		$507fabe10e71c6fb$var$hasEventBeforeFocus = true;
		$507fabe10e71c6fb$var$triggerChangeHandlers("pointer", e$7);
	}
}
function $507fabe10e71c6fb$var$handleClickEvent(e$7) {
	if (!$ea8dcbcb9ea1b556$export$95185d699e05d4d7.isOpening && $6a7db85432448f7f$export$60278871457622de(e$7)) {
		$507fabe10e71c6fb$var$hasEventBeforeFocus = true;
		$507fabe10e71c6fb$var$currentModality = "virtual";
		$507fabe10e71c6fb$var$currentPointerType = "virtual";
	}
}
function $507fabe10e71c6fb$var$handleFocusEvent(e$7) {
	if (e$7.target === window || e$7.target === document || $8a9cb279dc87e130$export$fda7da73ab5d4c48 || !e$7.isTrusted) return;
	if (!$507fabe10e71c6fb$var$hasEventBeforeFocus && !$507fabe10e71c6fb$var$hasBlurredWindowRecently) {
		$507fabe10e71c6fb$var$currentModality = "virtual";
		$507fabe10e71c6fb$var$currentPointerType = "virtual";
		$507fabe10e71c6fb$var$triggerChangeHandlers("virtual", e$7);
	}
	$507fabe10e71c6fb$var$hasEventBeforeFocus = false;
	$507fabe10e71c6fb$var$hasBlurredWindowRecently = false;
}
function $507fabe10e71c6fb$var$handleWindowBlur() {
	if ($8a9cb279dc87e130$export$fda7da73ab5d4c48) return;
	$507fabe10e71c6fb$var$hasEventBeforeFocus = false;
	$507fabe10e71c6fb$var$hasBlurredWindowRecently = true;
}
/**
* Setup global event listeners to control when keyboard focus style should be visible.
*/ function $507fabe10e71c6fb$var$setupGlobalFocusEvents(element) {
	if (typeof window === "undefined" || typeof document === "undefined" || $507fabe10e71c6fb$export$d90243b58daecda7.get($431fbd86ca7dc216$export$f21a1ffae260145a(element))) return;
	const windowObject = $431fbd86ca7dc216$export$f21a1ffae260145a(element);
	const documentObject = $431fbd86ca7dc216$export$b204af158042fbac(element);
	let focus = windowObject.HTMLElement.prototype.focus;
	windowObject.HTMLElement.prototype.focus = function() {
		$507fabe10e71c6fb$var$hasEventBeforeFocus = true;
		focus.apply(this, arguments);
	};
	documentObject.addEventListener("keydown", $507fabe10e71c6fb$var$handleKeyboardEvent, true);
	documentObject.addEventListener("keyup", $507fabe10e71c6fb$var$handleKeyboardEvent, true);
	documentObject.addEventListener("click", $507fabe10e71c6fb$var$handleClickEvent, true);
	windowObject.addEventListener("focus", $507fabe10e71c6fb$var$handleFocusEvent, true);
	windowObject.addEventListener("blur", $507fabe10e71c6fb$var$handleWindowBlur, false);
	if (typeof PointerEvent !== "undefined") {
		documentObject.addEventListener("pointerdown", $507fabe10e71c6fb$var$handlePointerEvent, true);
		documentObject.addEventListener("pointermove", $507fabe10e71c6fb$var$handlePointerEvent, true);
		documentObject.addEventListener("pointerup", $507fabe10e71c6fb$var$handlePointerEvent, true);
	}
	windowObject.addEventListener("beforeunload", () => {
		$507fabe10e71c6fb$var$tearDownWindowFocusTracking(element);
	}, { once: true });
	$507fabe10e71c6fb$export$d90243b58daecda7.set(windowObject, { focus });
}
var $507fabe10e71c6fb$var$tearDownWindowFocusTracking = (element, loadListener) => {
	const windowObject = $431fbd86ca7dc216$export$f21a1ffae260145a(element);
	const documentObject = $431fbd86ca7dc216$export$b204af158042fbac(element);
	if (loadListener) documentObject.removeEventListener("DOMContentLoaded", loadListener);
	if (!$507fabe10e71c6fb$export$d90243b58daecda7.has(windowObject)) return;
	windowObject.HTMLElement.prototype.focus = $507fabe10e71c6fb$export$d90243b58daecda7.get(windowObject).focus;
	documentObject.removeEventListener("keydown", $507fabe10e71c6fb$var$handleKeyboardEvent, true);
	documentObject.removeEventListener("keyup", $507fabe10e71c6fb$var$handleKeyboardEvent, true);
	documentObject.removeEventListener("click", $507fabe10e71c6fb$var$handleClickEvent, true);
	windowObject.removeEventListener("focus", $507fabe10e71c6fb$var$handleFocusEvent, true);
	windowObject.removeEventListener("blur", $507fabe10e71c6fb$var$handleWindowBlur, false);
	if (typeof PointerEvent !== "undefined") {
		documentObject.removeEventListener("pointerdown", $507fabe10e71c6fb$var$handlePointerEvent, true);
		documentObject.removeEventListener("pointermove", $507fabe10e71c6fb$var$handlePointerEvent, true);
		documentObject.removeEventListener("pointerup", $507fabe10e71c6fb$var$handlePointerEvent, true);
	}
	$507fabe10e71c6fb$export$d90243b58daecda7.delete(windowObject);
};
function $507fabe10e71c6fb$export$2f1888112f558a7d(element) {
	const documentObject = $431fbd86ca7dc216$export$b204af158042fbac(element);
	let loadListener;
	if (documentObject.readyState !== "loading") $507fabe10e71c6fb$var$setupGlobalFocusEvents(element);
	else {
		loadListener = () => {
			$507fabe10e71c6fb$var$setupGlobalFocusEvents(element);
		};
		documentObject.addEventListener("DOMContentLoaded", loadListener);
	}
	return () => $507fabe10e71c6fb$var$tearDownWindowFocusTracking(element, loadListener);
}
if (typeof document !== "undefined") $507fabe10e71c6fb$export$2f1888112f558a7d();
function $507fabe10e71c6fb$export$b9b3dfddab17db27() {
	return $507fabe10e71c6fb$var$currentModality !== "pointer";
}
var $507fabe10e71c6fb$var$nonTextInputTypes = new Set([
	"checkbox",
	"radio",
	"range",
	"color",
	"file",
	"image",
	"button",
	"submit",
	"reset"
]);
/**
* If this is attached to text input component, return if the event is a focus event (Tab/Escape keys pressed) so that
* focus visible style can be properly set.
*/ function $507fabe10e71c6fb$var$isKeyboardFocusEvent(isTextInput, modality, e$7) {
	let document1 = $431fbd86ca7dc216$export$b204af158042fbac(e$7 === null || e$7 === void 0 ? void 0 : e$7.target);
	const IHTMLInputElement = typeof window !== "undefined" ? $431fbd86ca7dc216$export$f21a1ffae260145a(e$7 === null || e$7 === void 0 ? void 0 : e$7.target).HTMLInputElement : HTMLInputElement;
	const IHTMLTextAreaElement = typeof window !== "undefined" ? $431fbd86ca7dc216$export$f21a1ffae260145a(e$7 === null || e$7 === void 0 ? void 0 : e$7.target).HTMLTextAreaElement : HTMLTextAreaElement;
	const IHTMLElement = typeof window !== "undefined" ? $431fbd86ca7dc216$export$f21a1ffae260145a(e$7 === null || e$7 === void 0 ? void 0 : e$7.target).HTMLElement : HTMLElement;
	const IKeyboardEvent = typeof window !== "undefined" ? $431fbd86ca7dc216$export$f21a1ffae260145a(e$7 === null || e$7 === void 0 ? void 0 : e$7.target).KeyboardEvent : KeyboardEvent;
	isTextInput = isTextInput || document1.activeElement instanceof IHTMLInputElement && !$507fabe10e71c6fb$var$nonTextInputTypes.has(document1.activeElement.type) || document1.activeElement instanceof IHTMLTextAreaElement || document1.activeElement instanceof IHTMLElement && document1.activeElement.isContentEditable;
	return !(isTextInput && modality === "keyboard" && e$7 instanceof IKeyboardEvent && !$507fabe10e71c6fb$var$FOCUS_VISIBLE_INPUT_KEYS[e$7.key]);
}
function $507fabe10e71c6fb$export$ec71b4b83ac08ec3(fn, deps, opts) {
	$507fabe10e71c6fb$var$setupGlobalFocusEvents();
	(0, import_react.useEffect)(() => {
		if ((opts === null || opts === void 0 ? void 0 : opts.enabled) === false) return;
		let handler = (modality, e$7) => {
			if (!$507fabe10e71c6fb$var$isKeyboardFocusEvent(!!(opts === null || opts === void 0 ? void 0 : opts.isTextInput), modality, e$7)) return;
			fn($507fabe10e71c6fb$export$b9b3dfddab17db27());
		};
		$507fabe10e71c6fb$export$901e90a13c50a14e.add(handler);
		return () => {
			$507fabe10e71c6fb$export$901e90a13c50a14e.delete(handler);
		};
	}, deps);
}

//#endregion
//#region node_modules/@react-aria/interactions/dist/useFocus.mjs
function $a1ea59d68270f0dd$export$f8168d8dd8fd66e6(props) {
	let { isDisabled, onFocus: onFocusProp, onBlur: onBlurProp, onFocusChange } = props;
	const onBlur = (0, import_react.useCallback)((e$7) => {
		if (e$7.target === e$7.currentTarget) {
			if (onBlurProp) onBlurProp(e$7);
			if (onFocusChange) onFocusChange(false);
			return true;
		}
	}, [onBlurProp, onFocusChange]);
	const onSyntheticFocus = $8a9cb279dc87e130$export$715c682d09d639cc(onBlur);
	const onFocus = (0, import_react.useCallback)((e$7) => {
		const ownerDocument = $431fbd86ca7dc216$export$b204af158042fbac(e$7.target);
		const activeElement$1 = ownerDocument ? $d4ee10de306f2510$export$cd4e5573fbe2b576(ownerDocument) : $d4ee10de306f2510$export$cd4e5573fbe2b576();
		if (e$7.target === e$7.currentTarget && activeElement$1 === $d4ee10de306f2510$export$e58f029f0fbfdb29(e$7.nativeEvent)) {
			if (onFocusProp) onFocusProp(e$7);
			if (onFocusChange) onFocusChange(true);
			onSyntheticFocus(e$7);
		}
	}, [
		onFocusChange,
		onFocusProp,
		onSyntheticFocus
	]);
	return { focusProps: {
		onFocus: !isDisabled && (onFocusProp || onFocusChange || onBlurProp) ? onFocus : void 0,
		onBlur: !isDisabled && (onBlurProp || onFocusChange) ? onBlur : void 0
	} };
}

//#endregion
//#region node_modules/@react-aria/interactions/dist/useFocusWithin.mjs
function $9ab94262bd0047c7$export$420e68273165f4ec(props) {
	let { isDisabled, onBlurWithin, onFocusWithin, onFocusWithinChange } = props;
	let state = (0, import_react.useRef)({ isFocusWithin: false });
	let { addGlobalListener, removeAllGlobalListeners } = $03deb23ff14920c4$export$4eaf04e54aa8eed6();
	let onBlur = (0, import_react.useCallback)((e$7) => {
		if (!$d4ee10de306f2510$export$4282f70798064fe0(e$7.currentTarget, e$7.target)) return;
		if (state.current.isFocusWithin && !$d4ee10de306f2510$export$4282f70798064fe0(e$7.currentTarget, e$7.relatedTarget)) {
			state.current.isFocusWithin = false;
			removeAllGlobalListeners();
			if (onBlurWithin) onBlurWithin(e$7);
			if (onFocusWithinChange) onFocusWithinChange(false);
		}
	}, [
		onBlurWithin,
		onFocusWithinChange,
		state,
		removeAllGlobalListeners
	]);
	let onSyntheticFocus = $8a9cb279dc87e130$export$715c682d09d639cc(onBlur);
	let onFocus = (0, import_react.useCallback)((e$7) => {
		if (!$d4ee10de306f2510$export$4282f70798064fe0(e$7.currentTarget, e$7.target)) return;
		const ownerDocument = $431fbd86ca7dc216$export$b204af158042fbac(e$7.target);
		const activeElement$1 = $d4ee10de306f2510$export$cd4e5573fbe2b576(ownerDocument);
		if (!state.current.isFocusWithin && activeElement$1 === $d4ee10de306f2510$export$e58f029f0fbfdb29(e$7.nativeEvent)) {
			if (onFocusWithin) onFocusWithin(e$7);
			if (onFocusWithinChange) onFocusWithinChange(true);
			state.current.isFocusWithin = true;
			onSyntheticFocus(e$7);
			let currentTarget = e$7.currentTarget;
			addGlobalListener(ownerDocument, "focus", (e$8) => {
				if (state.current.isFocusWithin && !$d4ee10de306f2510$export$4282f70798064fe0(currentTarget, e$8.target)) {
					let nativeEvent = new ownerDocument.defaultView.FocusEvent("blur", { relatedTarget: e$8.target });
					$8a9cb279dc87e130$export$c2b7abe5d61ec696(nativeEvent, currentTarget);
					onBlur($8a9cb279dc87e130$export$525bc4921d56d4a(nativeEvent));
				}
			}, { capture: true });
		}
	}, [
		onFocusWithin,
		onFocusWithinChange,
		onSyntheticFocus,
		addGlobalListener,
		onBlur
	]);
	if (isDisabled) return { focusWithinProps: {
		onFocus: void 0,
		onBlur: void 0
	} };
	return { focusWithinProps: {
		onFocus,
		onBlur
	} };
}

//#endregion
//#region node_modules/@react-aria/interactions/dist/useHover.mjs
var $6179b936705e76d3$var$globalIgnoreEmulatedMouseEvents = false;
var $6179b936705e76d3$var$hoverCount = 0;
function $6179b936705e76d3$var$setGlobalIgnoreEmulatedMouseEvents() {
	$6179b936705e76d3$var$globalIgnoreEmulatedMouseEvents = true;
	setTimeout(() => {
		$6179b936705e76d3$var$globalIgnoreEmulatedMouseEvents = false;
	}, 50);
}
function $6179b936705e76d3$var$handleGlobalPointerEvent(e$7) {
	if (e$7.pointerType === "touch") $6179b936705e76d3$var$setGlobalIgnoreEmulatedMouseEvents();
}
function $6179b936705e76d3$var$setupGlobalTouchEvents() {
	if (typeof document === "undefined") return;
	if ($6179b936705e76d3$var$hoverCount === 0) {
		if (typeof PointerEvent !== "undefined") document.addEventListener("pointerup", $6179b936705e76d3$var$handleGlobalPointerEvent);
	}
	$6179b936705e76d3$var$hoverCount++;
	return () => {
		$6179b936705e76d3$var$hoverCount--;
		if ($6179b936705e76d3$var$hoverCount > 0) return;
		if (typeof PointerEvent !== "undefined") document.removeEventListener("pointerup", $6179b936705e76d3$var$handleGlobalPointerEvent);
	};
}
function $6179b936705e76d3$export$ae780daf29e6d456(props) {
	let { onHoverStart, onHoverChange, onHoverEnd, isDisabled } = props;
	let [isHovered, setHovered] = (0, import_react.useState)(false);
	let state = (0, import_react.useRef)({
		isHovered: false,
		ignoreEmulatedMouseEvents: false,
		pointerType: "",
		target: null
	}).current;
	(0, import_react.useEffect)($6179b936705e76d3$var$setupGlobalTouchEvents, []);
	let { addGlobalListener, removeAllGlobalListeners } = $03deb23ff14920c4$export$4eaf04e54aa8eed6();
	let { hoverProps, triggerHoverEnd } = (0, import_react.useMemo)(() => {
		let triggerHoverStart = (event, pointerType) => {
			state.pointerType = pointerType;
			if (isDisabled || pointerType === "touch" || state.isHovered || !$d4ee10de306f2510$export$4282f70798064fe0(event.currentTarget, event.target)) return;
			state.isHovered = true;
			let target = event.currentTarget;
			state.target = target;
			addGlobalListener($431fbd86ca7dc216$export$b204af158042fbac(event.target), "pointerover", (e$7) => {
				if (state.isHovered && state.target && !$d4ee10de306f2510$export$4282f70798064fe0(state.target, e$7.target)) triggerHoverEnd$1(e$7, e$7.pointerType);
			}, { capture: true });
			if (onHoverStart) onHoverStart({
				type: "hoverstart",
				target,
				pointerType
			});
			if (onHoverChange) onHoverChange(true);
			setHovered(true);
		};
		let triggerHoverEnd$1 = (event, pointerType) => {
			let target = state.target;
			state.pointerType = "";
			state.target = null;
			if (pointerType === "touch" || !state.isHovered || !target) return;
			state.isHovered = false;
			removeAllGlobalListeners();
			if (onHoverEnd) onHoverEnd({
				type: "hoverend",
				target,
				pointerType
			});
			if (onHoverChange) onHoverChange(false);
			setHovered(false);
		};
		let hoverProps$1 = {};
		if (typeof PointerEvent !== "undefined") {
			hoverProps$1.onPointerEnter = (e$7) => {
				if ($6179b936705e76d3$var$globalIgnoreEmulatedMouseEvents && e$7.pointerType === "mouse") return;
				triggerHoverStart(e$7, e$7.pointerType);
			};
			hoverProps$1.onPointerLeave = (e$7) => {
				if (!isDisabled && $d4ee10de306f2510$export$4282f70798064fe0(e$7.currentTarget, e$7.target)) triggerHoverEnd$1(e$7, e$7.pointerType);
			};
		}
		return {
			hoverProps: hoverProps$1,
			triggerHoverEnd: triggerHoverEnd$1
		};
	}, [
		onHoverStart,
		onHoverChange,
		onHoverEnd,
		isDisabled,
		state,
		addGlobalListener,
		removeAllGlobalListeners
	]);
	(0, import_react.useEffect)(() => {
		if (isDisabled) triggerHoverEnd({ currentTarget: state.target }, state.pointerType);
	}, [isDisabled]);
	return {
		hoverProps,
		isHovered
	};
}

//#endregion
//#region node_modules/@react-aria/focus/dist/useFocusRing.mjs
function $f7dceffc5ad7768b$export$4e328f61c538687f(props = {}) {
	let { autoFocus = false, isTextInput, within } = props;
	let state = (0, import_react.useRef)({
		isFocused: false,
		isFocusVisible: autoFocus || $507fabe10e71c6fb$export$b9b3dfddab17db27()
	});
	let [isFocused, setFocused] = (0, import_react.useState)(false);
	let [isFocusVisibleState, setFocusVisible] = (0, import_react.useState)(() => state.current.isFocused && state.current.isFocusVisible);
	let updateState = (0, import_react.useCallback)(() => setFocusVisible(state.current.isFocused && state.current.isFocusVisible), []);
	let onFocusChange = (0, import_react.useCallback)((isFocused$1) => {
		state.current.isFocused = isFocused$1;
		state.current.isFocusVisible = $507fabe10e71c6fb$export$b9b3dfddab17db27();
		setFocused(isFocused$1);
		updateState();
	}, [updateState]);
	$507fabe10e71c6fb$export$ec71b4b83ac08ec3((isFocusVisible) => {
		state.current.isFocusVisible = isFocusVisible;
		updateState();
	}, [isTextInput, isFocused], {
		enabled: isFocused,
		isTextInput
	});
	let { focusProps } = $a1ea59d68270f0dd$export$f8168d8dd8fd66e6({
		isDisabled: within,
		onFocusChange
	});
	let { focusWithinProps } = $9ab94262bd0047c7$export$420e68273165f4ec({
		isDisabled: !within,
		onFocusWithinChange: onFocusChange
	});
	return {
		isFocused,
		isFocusVisible: isFocusVisibleState,
		focusProps: within ? focusWithinProps : focusProps
	};
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-active-press.js
function E$2(e$7) {
	let t$4 = e$7.width / 2, n$8 = e$7.height / 2;
	return {
		top: e$7.clientY - n$8,
		right: e$7.clientX + t$4,
		bottom: e$7.clientY + n$8,
		left: e$7.clientX - t$4
	};
}
function P$1(e$7, t$4) {
	return !(!e$7 || !t$4 || e$7.right < t$4.left || e$7.left > t$4.right || e$7.bottom < t$4.top || e$7.top > t$4.bottom);
}
function w$1({ disabled: e$7 = !1 } = {}) {
	let t$4 = (0, import_react.useRef)(null), [n$8, l$6] = (0, import_react.useState)(!1), r$3 = p$1(), o$8 = o(() => {
		t$4.current = null, l$6(!1), r$3.dispose();
	}), f$3 = o((s$8) => {
		if (r$3.dispose(), t$4.current === null) {
			t$4.current = s$8.currentTarget, l$6(!0);
			{
				let i$4 = l$2(s$8.currentTarget);
				r$3.addEventListener(i$4, "pointerup", o$8, !1), r$3.addEventListener(i$4, "pointermove", (c$9) => {
					if (t$4.current) l$6(P$1(E$2(c$9), t$4.current.getBoundingClientRect()));
				}, !1), r$3.addEventListener(i$4, "pointercancel", o$8, !1);
			}
		}
	});
	return {
		pressed: n$8,
		pressProps: e$7 ? {} : {
			onPointerDown: f$3,
			onPointerUp: o$8,
			onClick: o$8
		}
	};
}

//#endregion
//#region node_modules/@headlessui/react/dist/internal/id.js
var e$6 = (0, import_react.createContext)(void 0);
function u$7() {
	return (0, import_react.useContext)(e$6);
}

//#endregion
//#region node_modules/@headlessui/react/dist/utils/bugs.js
function s$7(l$6) {
	let e$7 = l$6.parentElement, t$4 = null;
	for (; e$7 && !a$7(e$7);) E$3(e$7) && (t$4 = e$7), e$7 = e$7.parentElement;
	let i$4 = (e$7 == null ? void 0 : e$7.getAttribute("disabled")) === "";
	return i$4 && r$2(t$4) ? !1 : i$4;
}
function r$2(l$6) {
	if (!l$6) return !1;
	let e$7 = l$6.previousElementSibling;
	for (; e$7 !== null;) {
		if (E$3(e$7)) return !1;
		e$7 = e$7.previousElementSibling;
	}
	return !0;
}

//#endregion
//#region node_modules/@headlessui/react/dist/components/label/label.js
var L$1 = (0, import_react.createContext)(null);
L$1.displayName = "LabelContext";
function C() {
	let n$8 = (0, import_react.useContext)(L$1);
	if (n$8 === null) {
		let l$6 = /* @__PURE__ */ new Error("You used a <Label /> component, but it is not inside a relevant parent.");
		throw Error.captureStackTrace && Error.captureStackTrace(l$6, C), l$6;
	}
	return n$8;
}
function N$1(n$8) {
	var a$8, e$7, o$8;
	let l$6 = (e$7 = (a$8 = (0, import_react.useContext)(L$1)) == null ? void 0 : a$8.value) != null ? e$7 : void 0;
	return ((o$8 = n$8 == null ? void 0 : n$8.length) != null ? o$8 : 0) > 0 ? [l$6, ...n$8].filter(Boolean).join(" ") : l$6;
}
function V$2({ inherit: n$8 = !1 } = {}) {
	let l$6 = N$1(), [a$8, e$7] = (0, import_react.useState)([]), o$8 = n$8 ? [l$6, ...a$8].filter(Boolean) : a$8;
	return [o$8.length > 0 ? o$8.join(" ") : void 0, (0, import_react.useMemo)(() => function(t$4) {
		let p$5 = o((i$4) => (e$7((u$8) => [...u$8, i$4]), () => e$7((u$8) => {
			let d$3 = u$8.slice(), f$3 = d$3.indexOf(i$4);
			return f$3 !== -1 && d$3.splice(f$3, 1), d$3;
		}))), b = (0, import_react.useMemo)(() => ({
			register: p$5,
			slot: t$4.slot,
			name: t$4.name,
			props: t$4.props,
			value: t$4.value
		}), [
			p$5,
			t$4.slot,
			t$4.name,
			t$4.props,
			t$4.value
		]);
		return import_react.createElement(L$1.Provider, { value: b }, t$4.children);
	}, [e$7])];
}
var G$2 = "label";
function U(n$8, l$6) {
	var y$4;
	let a$8 = (0, import_react$1.useId)(), e$7 = C(), o$8 = u$7(), T$4 = a$5(), { id: t$4 = `headlessui-label-${a$8}`, htmlFor: p$5 = o$8 != null ? o$8 : (y$4 = e$7.props) == null ? void 0 : y$4.htmlFor, passive: b = !1, ...i$4 } = n$8, u$8 = y$1(l$6);
	n$4(() => e$7.register(t$4), [t$4, e$7.register]);
	let d$3 = o((s$8) => {
		let g$1 = s$8.currentTarget;
		if (!(s$8.target !== s$8.currentTarget && L$2(s$8.target)) && (m$2(g$1) && s$8.preventDefault(), e$7.props && "onClick" in e$7.props && typeof e$7.props.onClick == "function" && e$7.props.onClick(s$8), m$2(g$1))) {
			let r$3 = document.getElementById(g$1.htmlFor);
			if (r$3) {
				let E$4 = r$3.getAttribute("disabled");
				if (E$4 === "true" || E$4 === "") return;
				let x$3 = r$3.getAttribute("aria-disabled");
				if (x$3 === "true" || x$3 === "") return;
				(l$5(r$3) && (r$3.type === "file" || r$3.type === "radio" || r$3.type === "checkbox") || r$3.role === "radio" || r$3.role === "checkbox" || r$3.role === "switch") && r$3.click(), r$3.focus({ preventScroll: !0 });
			}
		}
	}), f$3 = n$5({
		...e$7.slot,
		disabled: T$4 || !1
	}), c$9 = {
		ref: u$8,
		...e$7.props,
		id: t$4,
		htmlFor: p$5,
		onClick: d$3
	};
	return b && ("onClick" in c$9 && (delete c$9.htmlFor, delete c$9.onClick), "onClick" in i$4 && delete i$4.onClick), K$2()({
		ourProps: c$9,
		theirProps: i$4,
		slot: f$3,
		defaultTag: p$5 ? G$2 : "div",
		name: e$7.name || "Label"
	});
}
var j = Y$1(U), Z$1 = Object.assign(j, {});

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-element-size.js
function h$2(i$4) {
	if (i$4 === null) return {
		width: 0,
		height: 0
	};
	let { width: t$4, height: e$7 } = i$4.getBoundingClientRect();
	return {
		width: t$4,
		height: e$7
	};
}
function w$2(i$4, t$4, e$7 = !1) {
	let [r$3, f$3] = (0, import_react.useState)(() => h$2(t$4));
	return n$4(() => {
		if (!t$4 || !i$4) return;
		let n$8 = o$1();
		return n$8.requestAnimationFrame(function s$8() {
			n$8.requestAnimationFrame(s$8), f$3((u$8) => {
				let o$8 = h$2(t$4);
				return o$8.width === u$8.width && o$8.height === u$8.height ? u$8 : o$8;
			});
		}), () => {
			n$8.dispose();
		};
	}, [t$4, i$4]), e$7 ? {
		width: `${r$3.width}px`,
		height: `${r$3.height}px`
	} : r$3;
}

//#endregion
//#region node_modules/@headlessui/react/dist/components/mouse.js
var g = ((f$3) => (f$3[f$3.Left = 0] = "Left", f$3[f$3.Right = 2] = "Right", f$3))(g || {});

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-handle-toggle.js
function s$4(t$4) {
	let r$3 = (0, import_react.useRef)(null);
	return {
		onPointerDown: o((e$7) => {
			r$3.current = e$7.pointerType, !s$7(e$7.currentTarget) && e$7.pointerType === "mouse" && e$7.button === g.Left && (e$7.preventDefault(), t$4(e$7));
		}),
		onClick: o((e$7) => {
			r$3.current !== "mouse" && (s$7(e$7.currentTarget) || t$4(e$7));
		})
	};
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-quick-release.js
var H$3 = ((e$7) => (e$7[e$7.Ignore = 0] = "Ignore", e$7[e$7.Select = 1] = "Select", e$7[e$7.Close = 2] = "Close", e$7))(H$3 || {});
var S = {
	Ignore: { kind: 0 },
	Select: (r$3) => ({
		kind: 1,
		target: r$3
	}),
	Close: { kind: 2 }
}, M$1 = 200, f$2 = 5;
function L(r$3, { trigger: n$8, action: T$4, close: e$7, select: p$5 }) {
	let l$6 = (0, import_react.useRef)(null), i$4 = (0, import_react.useRef)(null), u$8 = (0, import_react.useRef)(null);
	i$1(r$3 && n$8 !== null, "pointerdown", (t$4) => {
		o$7(t$4 == null ? void 0 : t$4.target) && n$8 != null && n$8.contains(t$4.target) && (i$4.current = t$4.x, u$8.current = t$4.y, l$6.current = t$4.timeStamp);
	}), i$1(r$3 && n$8 !== null, "pointerup", (t$4) => {
		var s$8, m$3;
		let c$9 = l$6.current;
		if (c$9 === null || (l$6.current = null, !i$3(t$4.target)) || Math.abs(t$4.x - ((s$8 = i$4.current) != null ? s$8 : t$4.x)) < f$2 && Math.abs(t$4.y - ((m$3 = u$8.current) != null ? m$3 : t$4.y)) < f$2) return;
		let a$8 = T$4(t$4);
		switch (a$8.kind) {
			case 0: return;
			case 1:
				t$4.timeStamp - c$9 > M$1 && (p$5(a$8.target), e$7());
				break;
			case 2:
				e$7();
				break;
		}
	}, { capture: !0 });
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-resolve-button-type.js
function e$5(t$4, u$8) {
	return (0, import_react.useMemo)(() => {
		var n$8;
		if (t$4.type) return t$4.type;
		let r$3 = (n$8 = t$4.as) != null ? n$8 : "button";
		if (typeof r$3 == "string" && r$3.toLowerCase() === "button" || (u$8 == null ? void 0 : u$8.tagName) === "BUTTON" && !u$8.hasAttribute("type")) return "button";
	}, [
		t$4.type,
		t$4.as,
		u$8
	]);
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-tracked-pointer.js
function t$3(e$7) {
	return [e$7.screenX, e$7.screenY];
}
function u$2() {
	let e$7 = (0, import_react.useRef)([-1, -1]);
	return {
		wasMoved(r$3) {
			let n$8 = t$3(r$3);
			return e$7.current[0] === n$8[0] && e$7.current[1] === n$8[1] ? !1 : (e$7.current = n$8, !0);
		},
		update(r$3) {
			e$7.current = t$3(r$3);
		}
	};
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-tree-walker.js
function F(c$9, { container: e$7, accept: t$4, walk: r$3 }) {
	let o$8 = (0, import_react.useRef)(t$4), l$6 = (0, import_react.useRef)(r$3);
	(0, import_react.useEffect)(() => {
		o$8.current = t$4, l$6.current = r$3;
	}, [t$4, r$3]), n$4(() => {
		if (!e$7 || !c$9) return;
		let n$8 = l$2(e$7);
		if (!n$8) return;
		let f$3 = o$8.current, p$5 = l$6.current, i$4 = Object.assign((m$3) => f$3(m$3), { acceptNode: f$3 }), u$8 = n$8.createTreeWalker(e$7, NodeFilter.SHOW_ELEMENT, i$4, !1);
		for (; u$8.nextNode();) p$5(u$8.currentNode);
	}, [
		e$7,
		c$9,
		o$8,
		l$6
	]);
}

//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
	return typeof window !== "undefined";
}
function getNodeName(node) {
	if (isNode(node)) return (node.nodeName || "").toLowerCase();
	return "#document";
}
function getWindow(node) {
	var _node$ownerDocument;
	return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node) {
	var _ref;
	return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
	if (!hasWindow()) return false;
	return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement(value) {
	if (!hasWindow()) return false;
	return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
	if (!hasWindow()) return false;
	return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
	if (!hasWindow() || typeof ShadowRoot === "undefined") return false;
	return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
var invalidOverflowDisplayValues = /* @__PURE__ */ new Set(["inline", "contents"]);
function isOverflowElement(element) {
	const { overflow, overflowX, overflowY, display } = getComputedStyle$1(element);
	return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && !invalidOverflowDisplayValues.has(display);
}
var tableElements = /* @__PURE__ */ new Set([
	"table",
	"td",
	"th"
]);
function isTableElement(element) {
	return tableElements.has(getNodeName(element));
}
var topLayerSelectors = [":popover-open", ":modal"];
function isTopLayer(element) {
	return topLayerSelectors.some((selector) => {
		try {
			return element.matches(selector);
		} catch (_e) {
			return false;
		}
	});
}
var transformProperties = [
	"transform",
	"translate",
	"scale",
	"rotate",
	"perspective"
];
var willChangeValues = [
	"transform",
	"translate",
	"scale",
	"rotate",
	"perspective",
	"filter"
];
var containValues = [
	"paint",
	"layout",
	"strict",
	"content"
];
function isContainingBlock(elementOrCss) {
	const webkit = isWebKit();
	const css = isElement(elementOrCss) ? getComputedStyle$1(elementOrCss) : elementOrCss;
	return transformProperties.some((value) => css[value] ? css[value] !== "none" : false) || (css.containerType ? css.containerType !== "normal" : false) || !webkit && (css.backdropFilter ? css.backdropFilter !== "none" : false) || !webkit && (css.filter ? css.filter !== "none" : false) || willChangeValues.some((value) => (css.willChange || "").includes(value)) || containValues.some((value) => (css.contain || "").includes(value));
}
function getContainingBlock(element) {
	let currentNode = getParentNode(element);
	while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
		if (isContainingBlock(currentNode)) return currentNode;
		else if (isTopLayer(currentNode)) return null;
		currentNode = getParentNode(currentNode);
	}
	return null;
}
function isWebKit() {
	if (typeof CSS === "undefined" || !CSS.supports) return false;
	return CSS.supports("-webkit-backdrop-filter", "none");
}
var lastTraversableNodeNames = /* @__PURE__ */ new Set([
	"html",
	"body",
	"#document"
]);
function isLastTraversableNode(node) {
	return lastTraversableNodeNames.has(getNodeName(node));
}
function getComputedStyle$1(element) {
	return getWindow(element).getComputedStyle(element);
}
function getNodeScroll(element) {
	if (isElement(element)) return {
		scrollLeft: element.scrollLeft,
		scrollTop: element.scrollTop
	};
	return {
		scrollLeft: element.scrollX,
		scrollTop: element.scrollY
	};
}
function getParentNode(node) {
	if (getNodeName(node) === "html") return node;
	const result = node.assignedSlot || node.parentNode || isShadowRoot(node) && node.host || getDocumentElement(node);
	return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
	const parentNode = getParentNode(node);
	if (isLastTraversableNode(parentNode)) return node.ownerDocument ? node.ownerDocument.body : node.body;
	if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) return parentNode;
	return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
	var _node$ownerDocument2;
	if (list === void 0) list = [];
	if (traverseIframes === void 0) traverseIframes = true;
	const scrollableAncestor = getNearestOverflowAncestor(node);
	const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
	const win = getWindow(scrollableAncestor);
	if (isBody) {
		const frameElement = getFrameElement(win);
		return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
	}
	return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement(win) {
	return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}

//#endregion
//#region node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
function getUserAgent() {
	const uaData = navigator.userAgentData;
	if (uaData && Array.isArray(uaData.brands)) return uaData.brands.map((_ref) => {
		let { brand, version } = _ref;
		return brand + "/" + version;
	}).join(" ");
	return navigator.userAgent;
}

//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v$2) => ({
	x: v$2,
	y: v$2
});
var oppositeSideMap = {
	left: "right",
	right: "left",
	bottom: "top",
	top: "bottom"
};
var oppositeAlignmentMap = {
	start: "end",
	end: "start"
};
function clamp(start, value, end) {
	return max(start, min(value, end));
}
function evaluate(value, param) {
	return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
	return placement.split("-")[0];
}
function getAlignment(placement) {
	return placement.split("-")[1];
}
function getOppositeAxis(axis) {
	return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
	return axis === "y" ? "height" : "width";
}
var yAxisSides = /* @__PURE__ */ new Set(["top", "bottom"]);
function getSideAxis(placement) {
	return yAxisSides.has(getSide(placement)) ? "y" : "x";
}
function getAlignmentAxis(placement) {
	return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
	if (rtl === void 0) rtl = false;
	const alignment = getAlignment(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const length = getAxisLength(alignmentAxis);
	let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
	if (rects.reference[length] > rects.floating[length]) mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
	return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
	const oppositePlacement = getOppositePlacement(placement);
	return [
		getOppositeAlignmentPlacement(placement),
		oppositePlacement,
		getOppositeAlignmentPlacement(oppositePlacement)
	];
}
function getOppositeAlignmentPlacement(placement) {
	return placement.replace(/start|end/g, (alignment) => oppositeAlignmentMap[alignment]);
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
	switch (side) {
		case "top":
		case "bottom":
			if (rtl) return isStart ? rlPlacement : lrPlacement;
			return isStart ? lrPlacement : rlPlacement;
		case "left":
		case "right": return isStart ? tbPlacement : btPlacement;
		default: return [];
	}
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
	const alignment = getAlignment(placement);
	let list = getSideList(getSide(placement), direction === "start", rtl);
	if (alignment) {
		list = list.map((side) => side + "-" + alignment);
		if (flipAlignment) list = list.concat(list.map(getOppositeAlignmentPlacement));
	}
	return list;
}
function getOppositePlacement(placement) {
	return placement.replace(/left|right|bottom|top/g, (side) => oppositeSideMap[side]);
}
function expandPaddingObject(padding) {
	return {
		top: 0,
		right: 0,
		bottom: 0,
		left: 0,
		...padding
	};
}
function getPaddingObject(padding) {
	return typeof padding !== "number" ? expandPaddingObject(padding) : {
		top: padding,
		right: padding,
		bottom: padding,
		left: padding
	};
}
function rectToClientRect(rect) {
	const { x: x$3, y: y$4, width, height } = rect;
	return {
		width,
		height,
		top: y$4,
		left: x$3,
		right: x$3 + width,
		bottom: y$4 + height,
		x: x$3,
		y: y$4
	};
}

//#endregion
//#region node_modules/tabbable/dist/index.esm.js
var NoElement = typeof Element === "undefined";
var matches = NoElement ? function() {} : Element.prototype.matches || Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
var getRootNode = !NoElement && Element.prototype.getRootNode ? function(element) {
	var _element$getRootNode;
	return element === null || element === void 0 ? void 0 : (_element$getRootNode = element.getRootNode) === null || _element$getRootNode === void 0 ? void 0 : _element$getRootNode.call(element);
} : function(element) {
	return element === null || element === void 0 ? void 0 : element.ownerDocument;
};

//#endregion
//#region node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
	let { reference, floating } = _ref;
	const sideAxis = getSideAxis(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const alignLength = getAxisLength(alignmentAxis);
	const side = getSide(placement);
	const isVertical = sideAxis === "y";
	const commonX = reference.x + reference.width / 2 - floating.width / 2;
	const commonY = reference.y + reference.height / 2 - floating.height / 2;
	const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
	let coords;
	switch (side) {
		case "top":
			coords = {
				x: commonX,
				y: reference.y - floating.height
			};
			break;
		case "bottom":
			coords = {
				x: commonX,
				y: reference.y + reference.height
			};
			break;
		case "right":
			coords = {
				x: reference.x + reference.width,
				y: commonY
			};
			break;
		case "left":
			coords = {
				x: reference.x - floating.width,
				y: commonY
			};
			break;
		default: coords = {
			x: reference.x,
			y: reference.y
		};
	}
	switch (getAlignment(placement)) {
		case "start":
			coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
			break;
		case "end":
			coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
			break;
	}
	return coords;
}
/**
* Resolves with an object of overflow side offsets that determine how much the
* element is overflowing a given clipping boundary on each side.
* - positive = overflowing the boundary by that number of pixels
* - negative = how many pixels left before it will overflow
* - 0 = lies flush with the boundary
* @see https://floating-ui.com/docs/detectOverflow
*/
async function detectOverflow$1(state, options) {
	var _await$platform$isEle;
	if (options === void 0) options = {};
	const { x: x$3, y: y$4, platform: platform$1, rects, elements, strategy } = state;
	const { boundary = "clippingAncestors", rootBoundary = "viewport", elementContext = "floating", altBoundary = false, padding = 0 } = evaluate(options, state);
	const paddingObject = getPaddingObject(padding);
	const element = elements[altBoundary ? elementContext === "floating" ? "reference" : "floating" : elementContext];
	const clippingClientRect = rectToClientRect(await platform$1.getClippingRect({
		element: ((_await$platform$isEle = await (platform$1.isElement == null ? void 0 : platform$1.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform$1.getDocumentElement == null ? void 0 : platform$1.getDocumentElement(elements.floating)),
		boundary,
		rootBoundary,
		strategy
	}));
	const rect = elementContext === "floating" ? {
		x: x$3,
		y: y$4,
		width: rects.floating.width,
		height: rects.floating.height
	} : rects.reference;
	const offsetParent = await (platform$1.getOffsetParent == null ? void 0 : platform$1.getOffsetParent(elements.floating));
	const offsetScale = await (platform$1.isElement == null ? void 0 : platform$1.isElement(offsetParent)) ? await (platform$1.getScale == null ? void 0 : platform$1.getScale(offsetParent)) || {
		x: 1,
		y: 1
	} : {
		x: 1,
		y: 1
	};
	const elementClientRect = rectToClientRect(platform$1.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform$1.convertOffsetParentRelativeRectToViewportRelativeRect({
		elements,
		rect,
		offsetParent,
		strategy
	}) : rect);
	return {
		top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
		bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
		left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
		right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
	};
}
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*
* This export does not have any `platform` interface logic. You will need to
* write one for the platform you are using Floating UI with.
*/
var computePosition$1 = async (reference, floating, config) => {
	const { placement = "bottom", strategy = "absolute", middleware = [], platform: platform$1 } = config;
	const validMiddleware = middleware.filter(Boolean);
	const rtl = await (platform$1.isRTL == null ? void 0 : platform$1.isRTL(floating));
	let rects = await platform$1.getElementRects({
		reference,
		floating,
		strategy
	});
	let { x: x$3, y: y$4 } = computeCoordsFromPlacement(rects, placement, rtl);
	let statefulPlacement = placement;
	let middlewareData = {};
	let resetCount = 0;
	for (let i$4 = 0; i$4 < validMiddleware.length; i$4++) {
		var _platform$detectOverf;
		const { name, fn } = validMiddleware[i$4];
		const { x: nextX, y: nextY, data, reset } = await fn({
			x: x$3,
			y: y$4,
			initialPlacement: placement,
			placement: statefulPlacement,
			strategy,
			middlewareData,
			rects,
			platform: {
				...platform$1,
				detectOverflow: (_platform$detectOverf = platform$1.detectOverflow) != null ? _platform$detectOverf : detectOverflow$1
			},
			elements: {
				reference,
				floating
			}
		});
		x$3 = nextX != null ? nextX : x$3;
		y$4 = nextY != null ? nextY : y$4;
		middlewareData = {
			...middlewareData,
			[name]: {
				...middlewareData[name],
				...data
			}
		};
		if (reset && resetCount <= 50) {
			resetCount++;
			if (typeof reset === "object") {
				if (reset.placement) statefulPlacement = reset.placement;
				if (reset.rects) rects = reset.rects === true ? await platform$1.getElementRects({
					reference,
					floating,
					strategy
				}) : reset.rects;
				({x: x$3, y: y$4} = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
			}
			i$4 = -1;
		}
	}
	return {
		x: x$3,
		y: y$4,
		placement: statefulPlacement,
		strategy,
		middlewareData
	};
};
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "flip",
		options,
		async fn(state) {
			var _middlewareData$arrow, _middlewareData$flip;
			const { placement, middlewareData, rects, initialPlacement, platform: platform$1, elements } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true, fallbackPlacements: specifiedFallbackPlacements, fallbackStrategy = "bestFit", fallbackAxisSideDirection = "none", flipAlignment = true, ...detectOverflowOptions } = evaluate(options, state);
			if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			const side = getSide(placement);
			const initialSideAxis = getSideAxis(initialPlacement);
			const isBasePlacement = getSide(initialPlacement) === initialPlacement;
			const rtl = await (platform$1.isRTL == null ? void 0 : platform$1.isRTL(elements.floating));
			const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
			const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
			if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
			const placements$1 = [initialPlacement, ...fallbackPlacements];
			const overflow = await platform$1.detectOverflow(state, detectOverflowOptions);
			const overflows = [];
			let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
			if (checkMainAxis) overflows.push(overflow[side]);
			if (checkCrossAxis) {
				const sides$1 = getAlignmentSides(placement, rects, rtl);
				overflows.push(overflow[sides$1[0]], overflow[sides$1[1]]);
			}
			overflowsData = [...overflowsData, {
				placement,
				overflows
			}];
			if (!overflows.every((side$1) => side$1 <= 0)) {
				var _middlewareData$flip2, _overflowsData$filter;
				const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
				const nextPlacement = placements$1[nextIndex];
				if (nextPlacement) {
					if (!(checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false) || overflowsData.every((d$3) => getSideAxis(d$3.placement) === initialSideAxis ? d$3.overflows[0] > 0 : true)) return {
						data: {
							index: nextIndex,
							overflows: overflowsData
						},
						reset: { placement: nextPlacement }
					};
				}
				let resetPlacement = (_overflowsData$filter = overflowsData.filter((d$3) => d$3.overflows[0] <= 0).sort((a$8, b) => a$8.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
				if (!resetPlacement) switch (fallbackStrategy) {
					case "bestFit": {
						var _overflowsData$filter2;
						const placement$1 = (_overflowsData$filter2 = overflowsData.filter((d$3) => {
							if (hasFallbackAxisSideDirection) {
								const currentSideAxis = getSideAxis(d$3.placement);
								return currentSideAxis === initialSideAxis || currentSideAxis === "y";
							}
							return true;
						}).map((d$3) => [d$3.placement, d$3.overflows.filter((overflow$1) => overflow$1 > 0).reduce((acc, overflow$1) => acc + overflow$1, 0)]).sort((a$8, b) => a$8[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
						if (placement$1) resetPlacement = placement$1;
						break;
					}
					case "initialPlacement":
						resetPlacement = initialPlacement;
						break;
				}
				if (placement !== resetPlacement) return { reset: { placement: resetPlacement } };
			}
			return {};
		}
	};
};
var originSides = /* @__PURE__ */ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
	const { placement, platform: platform$1, elements } = state;
	const rtl = await (platform$1.isRTL == null ? void 0 : platform$1.isRTL(elements.floating));
	const side = getSide(placement);
	const alignment = getAlignment(placement);
	const isVertical = getSideAxis(placement) === "y";
	const mainAxisMulti = originSides.has(side) ? -1 : 1;
	const crossAxisMulti = rtl && isVertical ? -1 : 1;
	const rawValue = evaluate(options, state);
	let { mainAxis, crossAxis, alignmentAxis } = typeof rawValue === "number" ? {
		mainAxis: rawValue,
		crossAxis: 0,
		alignmentAxis: null
	} : {
		mainAxis: rawValue.mainAxis || 0,
		crossAxis: rawValue.crossAxis || 0,
		alignmentAxis: rawValue.alignmentAxis
	};
	if (alignment && typeof alignmentAxis === "number") crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
	return isVertical ? {
		x: crossAxis * crossAxisMulti,
		y: mainAxis * mainAxisMulti
	} : {
		x: mainAxis * mainAxisMulti,
		y: crossAxis * crossAxisMulti
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$2 = function(options) {
	if (options === void 0) options = 0;
	return {
		name: "offset",
		options,
		async fn(state) {
			var _middlewareData$offse, _middlewareData$arrow;
			const { x: x$3, y: y$4, placement, middlewareData } = state;
			const diffCoords = await convertValueToCoords(state, options);
			if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			return {
				x: x$3 + diffCoords.x,
				y: y$4 + diffCoords.y,
				data: {
					...diffCoords,
					placement
				}
			};
		}
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "shift",
		options,
		async fn(state) {
			const { x: x$3, y: y$4, placement, platform: platform$1 } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = false, limiter = { fn: (_ref) => {
				let { x: x$4, y: y$5 } = _ref;
				return {
					x: x$4,
					y: y$5
				};
			} }, ...detectOverflowOptions } = evaluate(options, state);
			const coords = {
				x: x$3,
				y: y$4
			};
			const overflow = await platform$1.detectOverflow(state, detectOverflowOptions);
			const crossAxis = getSideAxis(getSide(placement));
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			if (checkMainAxis) {
				const minSide = mainAxis === "y" ? "top" : "left";
				const maxSide = mainAxis === "y" ? "bottom" : "right";
				const min$1 = mainAxisCoord + overflow[minSide];
				const max$1 = mainAxisCoord - overflow[maxSide];
				mainAxisCoord = clamp(min$1, mainAxisCoord, max$1);
			}
			if (checkCrossAxis) {
				const minSide = crossAxis === "y" ? "top" : "left";
				const maxSide = crossAxis === "y" ? "bottom" : "right";
				const min$1 = crossAxisCoord + overflow[minSide];
				const max$1 = crossAxisCoord - overflow[maxSide];
				crossAxisCoord = clamp(min$1, crossAxisCoord, max$1);
			}
			const limitedCoords = limiter.fn({
				...state,
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			});
			return {
				...limitedCoords,
				data: {
					x: limitedCoords.x - x$3,
					y: limitedCoords.y - y$4,
					enabled: {
						[mainAxis]: checkMainAxis,
						[crossAxis]: checkCrossAxis
					}
				}
			};
		}
	};
};
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "size",
		options,
		async fn(state) {
			var _state$middlewareData, _state$middlewareData2;
			const { placement, rects, platform: platform$1, elements } = state;
			const { apply = () => {}, ...detectOverflowOptions } = evaluate(options, state);
			const overflow = await platform$1.detectOverflow(state, detectOverflowOptions);
			const side = getSide(placement);
			const alignment = getAlignment(placement);
			const isYAxis = getSideAxis(placement) === "y";
			const { width, height } = rects.floating;
			let heightSide;
			let widthSide;
			if (side === "top" || side === "bottom") {
				heightSide = side;
				widthSide = alignment === (await (platform$1.isRTL == null ? void 0 : platform$1.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
			} else {
				widthSide = side;
				heightSide = alignment === "end" ? "top" : "bottom";
			}
			const maximumClippingHeight = height - overflow.top - overflow.bottom;
			const maximumClippingWidth = width - overflow.left - overflow.right;
			const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
			const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
			const noShift = !state.middlewareData.shift;
			let availableHeight = overflowAvailableHeight;
			let availableWidth = overflowAvailableWidth;
			if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) availableWidth = maximumClippingWidth;
			if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) availableHeight = maximumClippingHeight;
			if (noShift && !alignment) {
				const xMin = max(overflow.left, 0);
				const xMax = max(overflow.right, 0);
				const yMin = max(overflow.top, 0);
				const yMax = max(overflow.bottom, 0);
				if (isYAxis) availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
				else availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
			}
			await apply({
				...state,
				availableWidth,
				availableHeight
			});
			const nextDimensions = await platform$1.getDimensions(elements.floating);
			if (width !== nextDimensions.width || height !== nextDimensions.height) return { reset: { rects: true } };
			return {};
		}
	};
};

//#endregion
//#region node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
	const css = getComputedStyle$1(element);
	let width = parseFloat(css.width) || 0;
	let height = parseFloat(css.height) || 0;
	const hasOffset = isHTMLElement(element);
	const offsetWidth = hasOffset ? element.offsetWidth : width;
	const offsetHeight = hasOffset ? element.offsetHeight : height;
	const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
	if (shouldFallback) {
		width = offsetWidth;
		height = offsetHeight;
	}
	return {
		width,
		height,
		$: shouldFallback
	};
}
function unwrapElement(element) {
	return !isElement(element) ? element.contextElement : element;
}
function getScale(element) {
	const domElement = unwrapElement(element);
	if (!isHTMLElement(domElement)) return createCoords(1);
	const rect = domElement.getBoundingClientRect();
	const { width, height, $: $$2 } = getCssDimensions(domElement);
	let x$3 = ($$2 ? round(rect.width) : rect.width) / width;
	let y$4 = ($$2 ? round(rect.height) : rect.height) / height;
	if (!x$3 || !Number.isFinite(x$3)) x$3 = 1;
	if (!y$4 || !Number.isFinite(y$4)) y$4 = 1;
	return {
		x: x$3,
		y: y$4
	};
}
var noOffsets = /* @__PURE__ */ createCoords(0);
function getVisualOffsets(element) {
	const win = getWindow(element);
	if (!isWebKit() || !win.visualViewport) return noOffsets;
	return {
		x: win.visualViewport.offsetLeft,
		y: win.visualViewport.offsetTop
	};
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
	if (isFixed === void 0) isFixed = false;
	if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) return false;
	return isFixed;
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
	if (includeScale === void 0) includeScale = false;
	if (isFixedStrategy === void 0) isFixedStrategy = false;
	const clientRect = element.getBoundingClientRect();
	const domElement = unwrapElement(element);
	let scale = createCoords(1);
	if (includeScale) if (offsetParent) {
		if (isElement(offsetParent)) scale = getScale(offsetParent);
	} else scale = getScale(element);
	const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
	let x$3 = (clientRect.left + visualOffsets.x) / scale.x;
	let y$4 = (clientRect.top + visualOffsets.y) / scale.y;
	let width = clientRect.width / scale.x;
	let height = clientRect.height / scale.y;
	if (domElement) {
		const win = getWindow(domElement);
		const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
		let currentWin = win;
		let currentIFrame = getFrameElement(currentWin);
		while (currentIFrame && offsetParent && offsetWin !== currentWin) {
			const iframeScale = getScale(currentIFrame);
			const iframeRect = currentIFrame.getBoundingClientRect();
			const css = getComputedStyle$1(currentIFrame);
			const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
			const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
			x$3 *= iframeScale.x;
			y$4 *= iframeScale.y;
			width *= iframeScale.x;
			height *= iframeScale.y;
			x$3 += left;
			y$4 += top;
			currentWin = getWindow(currentIFrame);
			currentIFrame = getFrameElement(currentWin);
		}
	}
	return rectToClientRect({
		width,
		height,
		x: x$3,
		y: y$4
	});
}
function getWindowScrollBarX(element, rect) {
	const leftScroll = getNodeScroll(element).scrollLeft;
	if (!rect) return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
	return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
	const htmlRect = documentElement.getBoundingClientRect();
	return {
		x: htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect),
		y: htmlRect.top + scroll.scrollTop
	};
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
	let { elements, rect, offsetParent, strategy } = _ref;
	const isFixed = strategy === "fixed";
	const documentElement = getDocumentElement(offsetParent);
	const topLayer = elements ? isTopLayer(elements.floating) : false;
	if (offsetParent === documentElement || topLayer && isFixed) return rect;
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	let scale = createCoords(1);
	const offsets = createCoords(0);
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isHTMLElement(offsetParent)) {
			const offsetRect = getBoundingClientRect(offsetParent);
			scale = getScale(offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		width: rect.width * scale.x,
		height: rect.height * scale.y,
		x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
		y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
	};
}
function getClientRects(element) {
	return Array.from(element.getClientRects());
}
function getDocumentRect(element) {
	const html = getDocumentElement(element);
	const scroll = getNodeScroll(element);
	const body = element.ownerDocument.body;
	const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
	const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
	let x$3 = -scroll.scrollLeft + getWindowScrollBarX(element);
	const y$4 = -scroll.scrollTop;
	if (getComputedStyle$1(body).direction === "rtl") x$3 += max(html.clientWidth, body.clientWidth) - width;
	return {
		width,
		height,
		x: x$3,
		y: y$4
	};
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element, strategy) {
	const win = getWindow(element);
	const html = getDocumentElement(element);
	const visualViewport = win.visualViewport;
	let width = html.clientWidth;
	let height = html.clientHeight;
	let x$3 = 0;
	let y$4 = 0;
	if (visualViewport) {
		width = visualViewport.width;
		height = visualViewport.height;
		const visualViewportBased = isWebKit();
		if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
			x$3 = visualViewport.offsetLeft;
			y$4 = visualViewport.offsetTop;
		}
	}
	const windowScrollbarX = getWindowScrollBarX(html);
	if (windowScrollbarX <= 0) {
		const doc = html.ownerDocument;
		const body = doc.body;
		const bodyStyles = getComputedStyle(body);
		const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
		const clippingStableScrollbarWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
		if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) width -= clippingStableScrollbarWidth;
	} else if (windowScrollbarX <= SCROLLBAR_MAX) width += windowScrollbarX;
	return {
		width,
		height,
		x: x$3,
		y: y$4
	};
}
var absoluteOrFixed = /* @__PURE__ */ new Set(["absolute", "fixed"]);
function getInnerBoundingClientRect(element, strategy) {
	const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
	const top = clientRect.top + element.clientTop;
	const left = clientRect.left + element.clientLeft;
	const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
	return {
		width: element.clientWidth * scale.x,
		height: element.clientHeight * scale.y,
		x: left * scale.x,
		y: top * scale.y
	};
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
	let rect;
	if (clippingAncestor === "viewport") rect = getViewportRect(element, strategy);
	else if (clippingAncestor === "document") rect = getDocumentRect(getDocumentElement(element));
	else if (isElement(clippingAncestor)) rect = getInnerBoundingClientRect(clippingAncestor, strategy);
	else {
		const visualOffsets = getVisualOffsets(element);
		rect = {
			x: clippingAncestor.x - visualOffsets.x,
			y: clippingAncestor.y - visualOffsets.y,
			width: clippingAncestor.width,
			height: clippingAncestor.height
		};
	}
	return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element, stopNode) {
	const parentNode = getParentNode(element);
	if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) return false;
	return getComputedStyle$1(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element, cache) {
	const cachedResult = cache.get(element);
	if (cachedResult) return cachedResult;
	let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
	let currentContainingBlockComputedStyle = null;
	const elementIsFixed = getComputedStyle$1(element).position === "fixed";
	let currentNode = elementIsFixed ? getParentNode(element) : element;
	while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
		const computedStyle = getComputedStyle$1(currentNode);
		const currentNodeIsContaining = isContainingBlock(currentNode);
		if (!currentNodeIsContaining && computedStyle.position === "fixed") currentContainingBlockComputedStyle = null;
		if (elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && absoluteOrFixed.has(currentContainingBlockComputedStyle.position) || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode)) result = result.filter((ancestor) => ancestor !== currentNode);
		else currentContainingBlockComputedStyle = computedStyle;
		currentNode = getParentNode(currentNode);
	}
	cache.set(element, result);
	return result;
}
function getClippingRect(_ref) {
	let { element, boundary, rootBoundary, strategy } = _ref;
	const clippingAncestors = [...boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary), rootBoundary];
	const firstClippingAncestor = clippingAncestors[0];
	const clippingRect = clippingAncestors.reduce((accRect, clippingAncestor) => {
		const rect = getClientRectFromClippingAncestor(element, clippingAncestor, strategy);
		accRect.top = max(rect.top, accRect.top);
		accRect.right = min(rect.right, accRect.right);
		accRect.bottom = min(rect.bottom, accRect.bottom);
		accRect.left = max(rect.left, accRect.left);
		return accRect;
	}, getClientRectFromClippingAncestor(element, firstClippingAncestor, strategy));
	return {
		width: clippingRect.right - clippingRect.left,
		height: clippingRect.bottom - clippingRect.top,
		x: clippingRect.left,
		y: clippingRect.top
	};
}
function getDimensions(element) {
	const { width, height } = getCssDimensions(element);
	return {
		width,
		height
	};
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
	const isOffsetParentAnElement = isHTMLElement(offsetParent);
	const documentElement = getDocumentElement(offsetParent);
	const isFixed = strategy === "fixed";
	const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	const offsets = createCoords(0);
	function setLeftRTLScrollbarOffset() {
		offsets.x = getWindowScrollBarX(documentElement);
	}
	if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		} else if (documentElement) setLeftRTLScrollbarOffset();
	}
	if (isFixed && !isOffsetParentAnElement && documentElement) setLeftRTLScrollbarOffset();
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		x: rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x,
		y: rect.top + scroll.scrollTop - offsets.y - htmlOffset.y,
		width: rect.width,
		height: rect.height
	};
}
function isStaticPositioned(element) {
	return getComputedStyle$1(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
	if (!isHTMLElement(element) || getComputedStyle$1(element).position === "fixed") return null;
	if (polyfill) return polyfill(element);
	let rawOffsetParent = element.offsetParent;
	if (getDocumentElement(element) === rawOffsetParent) rawOffsetParent = rawOffsetParent.ownerDocument.body;
	return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
	const win = getWindow(element);
	if (isTopLayer(element)) return win;
	if (!isHTMLElement(element)) {
		let svgOffsetParent = getParentNode(element);
		while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
			if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) return svgOffsetParent;
			svgOffsetParent = getParentNode(svgOffsetParent);
		}
		return win;
	}
	let offsetParent = getTrueOffsetParent(element, polyfill);
	while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) offsetParent = getTrueOffsetParent(offsetParent, polyfill);
	if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) return win;
	return offsetParent || getContainingBlock(element) || win;
}
var getElementRects = async function(data) {
	const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
	const getDimensionsFn = this.getDimensions;
	const floatingDimensions = await getDimensionsFn(data.floating);
	return {
		reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
		floating: {
			x: 0,
			y: 0,
			width: floatingDimensions.width,
			height: floatingDimensions.height
		}
	};
};
function isRTL(element) {
	return getComputedStyle$1(element).direction === "rtl";
}
var platform = {
	convertOffsetParentRelativeRectToViewportRelativeRect,
	getDocumentElement,
	getClippingRect,
	getOffsetParent,
	getElementRects,
	getClientRects,
	getDimensions,
	getScale,
	isElement,
	isRTL
};
function rectsAreEqual(a$8, b) {
	return a$8.x === b.x && a$8.y === b.y && a$8.width === b.width && a$8.height === b.height;
}
function observeMove(element, onMove) {
	let io = null;
	let timeoutId;
	const root = getDocumentElement(element);
	function cleanup() {
		var _io;
		clearTimeout(timeoutId);
		(_io = io) == null || _io.disconnect();
		io = null;
	}
	function refresh(skip, threshold) {
		if (skip === void 0) skip = false;
		if (threshold === void 0) threshold = 1;
		cleanup();
		const elementRectForRootMargin = element.getBoundingClientRect();
		const { left, top, width, height } = elementRectForRootMargin;
		if (!skip) onMove();
		if (!width || !height) return;
		const insetTop = floor(top);
		const insetRight = floor(root.clientWidth - (left + width));
		const insetBottom = floor(root.clientHeight - (top + height));
		const insetLeft = floor(left);
		const options = {
			rootMargin: -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px",
			threshold: max(0, min(1, threshold)) || 1
		};
		let isFirstUpdate = true;
		function handleObserve(entries) {
			const ratio = entries[0].intersectionRatio;
			if (ratio !== threshold) {
				if (!isFirstUpdate) return refresh();
				if (!ratio) timeoutId = setTimeout(() => {
					refresh(false, 1e-7);
				}, 1e3);
				else refresh(false, ratio);
			}
			if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) refresh();
			isFirstUpdate = false;
		}
		try {
			io = new IntersectionObserver(handleObserve, {
				...options,
				root: root.ownerDocument
			});
		} catch (_e) {
			io = new IntersectionObserver(handleObserve, options);
		}
		io.observe(element);
	}
	refresh(true);
	return cleanup;
}
/**
* Automatically updates the position of the floating element when necessary.
* Should only be called when the floating element is mounted on the DOM or
* visible on the screen.
* @returns cleanup function that should be invoked when the floating element is
* removed from the DOM or hidden from the screen.
* @see https://floating-ui.com/docs/autoUpdate
*/
function autoUpdate(reference, floating, update, options) {
	if (options === void 0) options = {};
	const { ancestorScroll = true, ancestorResize = true, elementResize = typeof ResizeObserver === "function", layoutShift = typeof IntersectionObserver === "function", animationFrame = false } = options;
	const referenceEl = unwrapElement(reference);
	const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...getOverflowAncestors(floating)] : [];
	ancestors.forEach((ancestor) => {
		ancestorScroll && ancestor.addEventListener("scroll", update, { passive: true });
		ancestorResize && ancestor.addEventListener("resize", update);
	});
	const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
	let reobserveFrame = -1;
	let resizeObserver = null;
	if (elementResize) {
		resizeObserver = new ResizeObserver((_ref) => {
			let [firstEntry] = _ref;
			if (firstEntry && firstEntry.target === referenceEl && resizeObserver) {
				resizeObserver.unobserve(floating);
				cancelAnimationFrame(reobserveFrame);
				reobserveFrame = requestAnimationFrame(() => {
					var _resizeObserver;
					(_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
				});
			}
			update();
		});
		if (referenceEl && !animationFrame) resizeObserver.observe(referenceEl);
		resizeObserver.observe(floating);
	}
	let frameId;
	let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
	if (animationFrame) frameLoop();
	function frameLoop() {
		const nextRefRect = getBoundingClientRect(reference);
		if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) update();
		prevRefRect = nextRefRect;
		frameId = requestAnimationFrame(frameLoop);
	}
	update();
	return () => {
		var _resizeObserver2;
		ancestors.forEach((ancestor) => {
			ancestorScroll && ancestor.removeEventListener("scroll", update);
			ancestorResize && ancestor.removeEventListener("resize", update);
		});
		cleanupIo?.();
		(_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
		resizeObserver = null;
		if (animationFrame) cancelAnimationFrame(frameId);
	};
}
/**
* Resolves with an object of overflow side offsets that determine how much the
* element is overflowing a given clipping boundary on each side.
* - positive = overflowing the boundary by that number of pixels
* - negative = how many pixels left before it will overflow
* - 0 = lies flush with the boundary
* @see https://floating-ui.com/docs/detectOverflow
*/
var detectOverflow = detectOverflow$1;
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$1 = offset$2;
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$1 = shift$2;
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$1 = flip$2;
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$1 = size$2;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*/
var computePosition = (reference, floating, options) => {
	const cache = /* @__PURE__ */ new Map();
	const mergedOptions = {
		platform,
		...options
	};
	const platformWithCache = {
		...mergedOptions.platform,
		_c: cache
	};
	return computePosition$1(reference, floating, {
		...mergedOptions,
		platform: platformWithCache
	});
};

//#endregion
//#region node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
var import_react_dom$2 = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var index$1 = typeof document !== "undefined" ? import_react.useLayoutEffect : function noop() {};
function deepEqual(a$8, b) {
	if (a$8 === b) return true;
	if (typeof a$8 !== typeof b) return false;
	if (typeof a$8 === "function" && a$8.toString() === b.toString()) return true;
	let length;
	let i$4;
	let keys;
	if (a$8 && b && typeof a$8 === "object") {
		if (Array.isArray(a$8)) {
			length = a$8.length;
			if (length !== b.length) return false;
			for (i$4 = length; i$4-- !== 0;) if (!deepEqual(a$8[i$4], b[i$4])) return false;
			return true;
		}
		keys = Object.keys(a$8);
		length = keys.length;
		if (length !== Object.keys(b).length) return false;
		for (i$4 = length; i$4-- !== 0;) if (!{}.hasOwnProperty.call(b, keys[i$4])) return false;
		for (i$4 = length; i$4-- !== 0;) {
			const key = keys[i$4];
			if (key === "_owner" && a$8.$$typeof) continue;
			if (!deepEqual(a$8[key], b[key])) return false;
		}
		return true;
	}
	return a$8 !== a$8 && b !== b;
}
function getDPR(element) {
	if (typeof window === "undefined") return 1;
	return (element.ownerDocument.defaultView || window).devicePixelRatio || 1;
}
function roundByDPR(element, value) {
	const dpr = getDPR(element);
	return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
	const ref = import_react.useRef(value);
	index$1(() => {
		ref.current = value;
	});
	return ref;
}
/**
* Provides data to position a floating element.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating$1(options) {
	if (options === void 0) options = {};
	const { placement = "bottom", strategy = "absolute", middleware = [], platform: platform$1, elements: { reference: externalReference, floating: externalFloating } = {}, transform = true, whileElementsMounted, open } = options;
	const [data, setData] = import_react.useState({
		x: 0,
		y: 0,
		strategy,
		placement,
		middlewareData: {},
		isPositioned: false
	});
	const [latestMiddleware, setLatestMiddleware] = import_react.useState(middleware);
	if (!deepEqual(latestMiddleware, middleware)) setLatestMiddleware(middleware);
	const [_reference, _setReference] = import_react.useState(null);
	const [_floating, _setFloating] = import_react.useState(null);
	const setReference = import_react.useCallback((node) => {
		if (node !== referenceRef.current) {
			referenceRef.current = node;
			_setReference(node);
		}
	}, []);
	const setFloating = import_react.useCallback((node) => {
		if (node !== floatingRef.current) {
			floatingRef.current = node;
			_setFloating(node);
		}
	}, []);
	const referenceEl = externalReference || _reference;
	const floatingEl = externalFloating || _floating;
	const referenceRef = import_react.useRef(null);
	const floatingRef = import_react.useRef(null);
	const dataRef = import_react.useRef(data);
	const hasWhileElementsMounted = whileElementsMounted != null;
	const whileElementsMountedRef = useLatestRef(whileElementsMounted);
	const platformRef = useLatestRef(platform$1);
	const openRef = useLatestRef(open);
	const update = import_react.useCallback(() => {
		if (!referenceRef.current || !floatingRef.current) return;
		const config = {
			placement,
			strategy,
			middleware: latestMiddleware
		};
		if (platformRef.current) config.platform = platformRef.current;
		computePosition(referenceRef.current, floatingRef.current, config).then((data$1) => {
			const fullData = {
				...data$1,
				isPositioned: openRef.current !== false
			};
			if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
				dataRef.current = fullData;
				import_react_dom$2.flushSync(() => {
					setData(fullData);
				});
			}
		});
	}, [
		latestMiddleware,
		placement,
		strategy,
		platformRef,
		openRef
	]);
	index$1(() => {
		if (open === false && dataRef.current.isPositioned) {
			dataRef.current.isPositioned = false;
			setData((data$1) => ({
				...data$1,
				isPositioned: false
			}));
		}
	}, [open]);
	const isMountedRef = import_react.useRef(false);
	index$1(() => {
		isMountedRef.current = true;
		return () => {
			isMountedRef.current = false;
		};
	}, []);
	index$1(() => {
		if (referenceEl) referenceRef.current = referenceEl;
		if (floatingEl) floatingRef.current = floatingEl;
		if (referenceEl && floatingEl) {
			if (whileElementsMountedRef.current) return whileElementsMountedRef.current(referenceEl, floatingEl, update);
			update();
		}
	}, [
		referenceEl,
		floatingEl,
		update,
		whileElementsMountedRef,
		hasWhileElementsMounted
	]);
	const refs = import_react.useMemo(() => ({
		reference: referenceRef,
		floating: floatingRef,
		setReference,
		setFloating
	}), [setReference, setFloating]);
	const elements = import_react.useMemo(() => ({
		reference: referenceEl,
		floating: floatingEl
	}), [referenceEl, floatingEl]);
	const floatingStyles = import_react.useMemo(() => {
		const initialStyles = {
			position: strategy,
			left: 0,
			top: 0
		};
		if (!elements.floating) return initialStyles;
		const x$3 = roundByDPR(elements.floating, data.x);
		const y$4 = roundByDPR(elements.floating, data.y);
		if (transform) return {
			...initialStyles,
			transform: "translate(" + x$3 + "px, " + y$4 + "px)",
			...getDPR(elements.floating) >= 1.5 && { willChange: "transform" }
		};
		return {
			position: strategy,
			left: x$3,
			top: y$4
		};
	}, [
		strategy,
		transform,
		elements.floating,
		data.x,
		data.y
	]);
	return import_react.useMemo(() => ({
		...data,
		update,
		refs,
		elements,
		floatingStyles
	}), [
		data,
		update,
		refs,
		elements,
		floatingStyles
	]);
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset = (options, deps) => ({
	...offset$1(options),
	options: [options, deps]
});
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift = (options, deps) => ({
	...shift$1(options),
	options: [options, deps]
});
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip = (options, deps) => ({
	...flip$1(options),
	options: [options, deps]
});
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size = (options, deps) => ({
	...size$1(options),
	options: [options, deps]
});

//#endregion
//#region node_modules/@floating-ui/react/dist/floating-ui.react.mjs
var import_react_dom$1 = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var SafeReact = { ...import_react };
var useSafeInsertionEffect = SafeReact.useInsertionEffect || ((fn) => fn());
function useEffectEvent(callback) {
	const ref = import_react.useRef(() => {
		throw new Error("Cannot call an event handler while rendering.");
	});
	useSafeInsertionEffect(() => {
		ref.current = callback;
	});
	return import_react.useCallback(function() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		return ref.current == null ? void 0 : ref.current(...args);
	}, []);
}
var ARROW_UP = "ArrowUp";
var ARROW_DOWN = "ArrowDown";
var ARROW_LEFT = "ArrowLeft";
var ARROW_RIGHT = "ArrowRight";
var index = typeof document !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
var horizontalKeys = [ARROW_LEFT, ARROW_RIGHT];
var verticalKeys = [ARROW_UP, ARROW_DOWN];
var allKeys = [...horizontalKeys, ...verticalKeys];
var serverHandoffComplete = false;
var count = 0;
var genId = () => "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++;
function useFloatingId() {
	const [id, setId] = import_react.useState(() => serverHandoffComplete ? genId() : void 0);
	index(() => {
		if (id == null) setId(genId());
	}, []);
	import_react.useEffect(() => {
		serverHandoffComplete = true;
	}, []);
	return id;
}
/**
* Uses React 18's built-in `useId()` when available, or falls back to a
* slightly less performant (requiring a double render) implementation for
* earlier React versions.
* @see https://floating-ui.com/docs/react-utils#useid
*/
var useId = SafeReact.useId || useFloatingId;
var devMessageSet;
devMessageSet = /* @__PURE__ */ new Set();
function warn() {
	var _devMessageSet;
	for (var _len = arguments.length, messages = new Array(_len), _key = 0; _key < _len; _key++) messages[_key] = arguments[_key];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet = devMessageSet) != null && _devMessageSet.has(message))) {
		var _devMessageSet2;
		(_devMessageSet2 = devMessageSet) == null || _devMessageSet2.add(message);
		console.warn(message);
	}
}
function error() {
	var _devMessageSet3;
	for (var _len2 = arguments.length, messages = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) messages[_key2] = arguments[_key2];
	const message = "Floating UI: " + messages.join(" ");
	if (!((_devMessageSet3 = devMessageSet) != null && _devMessageSet3.has(message))) {
		var _devMessageSet4;
		(_devMessageSet4 = devMessageSet) == null || _devMessageSet4.add(message);
		console.error(message);
	}
}
function createPubSub() {
	const map = /* @__PURE__ */ new Map();
	return {
		emit(event, data) {
			var _map$get;
			(_map$get = map.get(event)) == null || _map$get.forEach((handler) => handler(data));
		},
		on(event, listener) {
			map.set(event, [...map.get(event) || [], listener]);
		},
		off(event, listener) {
			var _map$get2;
			map.set(event, ((_map$get2 = map.get(event)) == null ? void 0 : _map$get2.filter((l$6) => l$6 !== listener)) || []);
		}
	};
}
var FloatingNodeContext = /* @__PURE__ */ import_react.createContext(null);
var FloatingTreeContext = /* @__PURE__ */ import_react.createContext(null);
/**
* Returns the parent node id for nested floating elements, if available.
* Returns `null` for top-level floating elements.
*/
var useFloatingParentNodeId = () => {
	var _React$useContext;
	return ((_React$useContext = import_react.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
};
/**
* Returns the nearest floating tree context, if available.
*/
var useFloatingTree = () => import_react.useContext(FloatingTreeContext);
var FOCUSABLE_ATTRIBUTE = "data-floating-ui-focusable";
function useFloatingRootContext(options) {
	const { open = false, onOpenChange: onOpenChangeProp, elements: elementsProp } = options;
	const floatingId = useId();
	const dataRef = import_react.useRef({});
	const [events] = import_react.useState(() => createPubSub());
	const nested = useFloatingParentNodeId() != null;
	{
		const optionDomReference = elementsProp.reference;
		if (optionDomReference && !isElement(optionDomReference)) error("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
	}
	const [positionReference, setPositionReference] = import_react.useState(elementsProp.reference);
	const onOpenChange = useEffectEvent((open$1, event, reason) => {
		dataRef.current.openEvent = open$1 ? event : void 0;
		events.emit("openchange", {
			open: open$1,
			event,
			reason,
			nested
		});
		onOpenChangeProp?.(open$1, event, reason);
	});
	const refs = import_react.useMemo(() => ({ setPositionReference }), []);
	const elements = import_react.useMemo(() => ({
		reference: positionReference || elementsProp.reference || null,
		floating: elementsProp.floating || null,
		domReference: elementsProp.reference
	}), [
		positionReference,
		elementsProp.reference,
		elementsProp.floating
	]);
	return import_react.useMemo(() => ({
		dataRef,
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	}), [
		open,
		onOpenChange,
		elements,
		events,
		floatingId,
		refs
	]);
}
/**
* Provides data to position a floating element and context to add interactions.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating(options) {
	if (options === void 0) options = {};
	const { nodeId } = options;
	const internalRootContext = useFloatingRootContext({
		...options,
		elements: {
			reference: null,
			floating: null,
			...options.elements
		}
	});
	const rootContext = options.rootContext || internalRootContext;
	const computedElements = rootContext.elements;
	const [_domReference, setDomReference] = import_react.useState(null);
	const [positionReference, _setPositionReference] = import_react.useState(null);
	const domReference = (computedElements == null ? void 0 : computedElements.domReference) || _domReference;
	const domReferenceRef = import_react.useRef(null);
	const tree = useFloatingTree();
	index(() => {
		if (domReference) domReferenceRef.current = domReference;
	}, [domReference]);
	const position = useFloating$1({
		...options,
		elements: {
			...computedElements,
			...positionReference && { reference: positionReference }
		}
	});
	const setPositionReference = import_react.useCallback((node) => {
		const computedPositionReference = isElement(node) ? {
			getBoundingClientRect: () => node.getBoundingClientRect(),
			contextElement: node
		} : node;
		_setPositionReference(computedPositionReference);
		position.refs.setReference(computedPositionReference);
	}, [position.refs]);
	const setReference = import_react.useCallback((node) => {
		if (isElement(node) || node === null) {
			domReferenceRef.current = node;
			setDomReference(node);
		}
		if (isElement(position.refs.reference.current) || position.refs.reference.current === null || node !== null && !isElement(node)) position.refs.setReference(node);
	}, [position.refs]);
	const refs = import_react.useMemo(() => ({
		...position.refs,
		setReference,
		setPositionReference,
		domReference: domReferenceRef
	}), [
		position.refs,
		setReference,
		setPositionReference
	]);
	const elements = import_react.useMemo(() => ({
		...position.elements,
		domReference
	}), [position.elements, domReference]);
	const context = import_react.useMemo(() => ({
		...position,
		...rootContext,
		refs,
		elements,
		nodeId
	}), [
		position,
		refs,
		elements,
		nodeId,
		rootContext
	]);
	index(() => {
		rootContext.dataRef.current.floatingContext = context;
		const node = tree == null ? void 0 : tree.nodesRef.current.find((node$1) => node$1.id === nodeId);
		if (node) node.context = context;
	});
	return import_react.useMemo(() => ({
		...position,
		context,
		refs,
		elements
	}), [
		position,
		refs,
		elements,
		context
	]);
}
var ACTIVE_KEY = "active";
var SELECTED_KEY = "selected";
function mergeProps(userProps, propsList, elementKey) {
	const map = /* @__PURE__ */ new Map();
	const isItem = elementKey === "item";
	let domUserProps = userProps;
	if (isItem && userProps) {
		const { [ACTIVE_KEY]: _$2, [SELECTED_KEY]: __, ...validProps } = userProps;
		domUserProps = validProps;
	}
	return {
		...elementKey === "floating" && {
			tabIndex: -1,
			[FOCUSABLE_ATTRIBUTE]: ""
		},
		...domUserProps,
		...propsList.map((value) => {
			const propsOrGetProps = value ? value[elementKey] : null;
			if (typeof propsOrGetProps === "function") return userProps ? propsOrGetProps(userProps) : null;
			return propsOrGetProps;
		}).concat(userProps).reduce((acc, props) => {
			if (!props) return acc;
			Object.entries(props).forEach((_ref) => {
				let [key, value] = _ref;
				if (isItem && [ACTIVE_KEY, SELECTED_KEY].includes(key)) return;
				if (key.indexOf("on") === 0) {
					if (!map.has(key)) map.set(key, []);
					if (typeof value === "function") {
						var _map$get;
						(_map$get = map.get(key)) == null || _map$get.push(value);
						acc[key] = function() {
							var _map$get2;
							for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
							return (_map$get2 = map.get(key)) == null ? void 0 : _map$get2.map((fn) => fn(...args)).find((val) => val !== void 0);
						};
					}
				} else acc[key] = value;
			});
			return acc;
		}, {})
	};
}
/**
* Merges an array of interaction hooks' props into prop getters, allowing
* event handler functions to be composed together without overwriting one
* another.
* @see https://floating-ui.com/docs/useInteractions
*/
function useInteractions(propsList) {
	if (propsList === void 0) propsList = [];
	const referenceDeps = propsList.map((key) => key == null ? void 0 : key.reference);
	const floatingDeps = propsList.map((key) => key == null ? void 0 : key.floating);
	const itemDeps = propsList.map((key) => key == null ? void 0 : key.item);
	const getReferenceProps = import_react.useCallback((userProps) => mergeProps(userProps, propsList, "reference"), referenceDeps);
	const getFloatingProps = import_react.useCallback((userProps) => mergeProps(userProps, propsList, "floating"), floatingDeps);
	const getItemProps = import_react.useCallback((userProps) => mergeProps(userProps, propsList, "item"), itemDeps);
	return import_react.useMemo(() => ({
		getReferenceProps,
		getFloatingProps,
		getItemProps
	}), [
		getReferenceProps,
		getFloatingProps,
		getItemProps
	]);
}
function getArgsWithCustomFloatingHeight(state, height) {
	return {
		...state,
		rects: {
			...state.rects,
			floating: {
				...state.rects.floating,
				height
			}
		}
	};
}
/**
* Positions the floating element such that an inner element inside of it is
* anchored to the reference element.
* @see https://floating-ui.com/docs/inner
*/
var inner = (props) => ({
	name: "inner",
	options: props,
	async fn(state) {
		const { listRef, overflowRef, onFallbackChange, offset: innerOffset = 0, index: index$2 = 0, minItemsVisible = 4, referenceOverflowThreshold = 0, scrollRef, ...detectOverflowOptions } = evaluate(props, state);
		const { rects, elements: { floating } } = state;
		const item = listRef.current[index$2];
		const scrollEl = (scrollRef == null ? void 0 : scrollRef.current) || floating;
		const clientTop = floating.clientTop || scrollEl.clientTop;
		const floatingIsBordered = floating.clientTop !== 0;
		const scrollElIsBordered = scrollEl.clientTop !== 0;
		const floatingIsScrollEl = floating === scrollEl;
		if (!state.placement.startsWith("bottom")) warn("`placement` side must be \"bottom\" when using the `inner`", "middleware.");
		if (!item) return {};
		const nextArgs = {
			...state,
			...await offset(-item.offsetTop - floating.clientTop - rects.reference.height / 2 - item.offsetHeight / 2 - innerOffset).fn(state)
		};
		const overflow = await detectOverflow(getArgsWithCustomFloatingHeight(nextArgs, scrollEl.scrollHeight + clientTop + floating.clientTop), detectOverflowOptions);
		const refOverflow = await detectOverflow(nextArgs, {
			...detectOverflowOptions,
			elementContext: "reference"
		});
		const diffY = max(0, overflow.top);
		const nextY = nextArgs.y + diffY;
		const maxHeight = (scrollEl.scrollHeight > scrollEl.clientHeight ? (v$2) => v$2 : round)(max(0, scrollEl.scrollHeight + (floatingIsBordered && floatingIsScrollEl || scrollElIsBordered ? clientTop * 2 : 0) - diffY - max(0, overflow.bottom)));
		scrollEl.style.maxHeight = maxHeight + "px";
		scrollEl.scrollTop = diffY;
		if (onFallbackChange) {
			const shouldFallback = scrollEl.offsetHeight < item.offsetHeight * min(minItemsVisible, listRef.current.length) - 1 || refOverflow.top >= -referenceOverflowThreshold || refOverflow.bottom >= -referenceOverflowThreshold;
			import_react_dom$1.flushSync(() => onFallbackChange(shouldFallback));
		}
		if (overflowRef) overflowRef.current = await detectOverflow(getArgsWithCustomFloatingHeight({
			...nextArgs,
			y: nextY
		}, scrollEl.offsetHeight + clientTop + floating.clientTop), detectOverflowOptions);
		return { y: nextY };
	}
});
/**
* Changes the `inner` middleware's `offset` upon a `wheel` event to
* expand the floating element's height, revealing more list items.
* @see https://floating-ui.com/docs/inner
*/
function useInnerOffset(context, props) {
	const { open, elements } = context;
	const { enabled = true, overflowRef, scrollRef, onChange: unstable_onChange } = props;
	const onChange = useEffectEvent(unstable_onChange);
	const controlledScrollingRef = import_react.useRef(false);
	const prevScrollTopRef = import_react.useRef(null);
	const initialOverflowRef = import_react.useRef(null);
	import_react.useEffect(() => {
		if (!enabled) return;
		function onWheel(e$7) {
			if (e$7.ctrlKey || !el || overflowRef.current == null) return;
			const dY = e$7.deltaY;
			const isAtTop = overflowRef.current.top >= -.5;
			const isAtBottom = overflowRef.current.bottom >= -.5;
			const remainingScroll = el.scrollHeight - el.clientHeight;
			const sign = dY < 0 ? -1 : 1;
			const method = dY < 0 ? "max" : "min";
			if (el.scrollHeight <= el.clientHeight) return;
			if (!isAtTop && dY > 0 || !isAtBottom && dY < 0) {
				e$7.preventDefault();
				import_react_dom$1.flushSync(() => {
					onChange((d$3) => d$3 + Math[method](dY, remainingScroll * sign));
				});
			} else if (/firefox/i.test(getUserAgent())) el.scrollTop += dY;
		}
		const el = (scrollRef == null ? void 0 : scrollRef.current) || elements.floating;
		if (open && el) {
			el.addEventListener("wheel", onWheel);
			requestAnimationFrame(() => {
				prevScrollTopRef.current = el.scrollTop;
				if (overflowRef.current != null) initialOverflowRef.current = { ...overflowRef.current };
			});
			return () => {
				prevScrollTopRef.current = null;
				initialOverflowRef.current = null;
				el.removeEventListener("wheel", onWheel);
			};
		}
	}, [
		enabled,
		open,
		elements.floating,
		overflowRef,
		scrollRef,
		onChange
	]);
	const floating = import_react.useMemo(() => ({
		onKeyDown() {
			controlledScrollingRef.current = true;
		},
		onWheel() {
			controlledScrollingRef.current = false;
		},
		onPointerMove() {
			controlledScrollingRef.current = false;
		},
		onScroll() {
			const el = (scrollRef == null ? void 0 : scrollRef.current) || elements.floating;
			if (!overflowRef.current || !el || !controlledScrollingRef.current) return;
			if (prevScrollTopRef.current !== null) {
				const scrollDiff = el.scrollTop - prevScrollTopRef.current;
				if (overflowRef.current.bottom < -.5 && scrollDiff < -1 || overflowRef.current.top < -.5 && scrollDiff > 1) import_react_dom$1.flushSync(() => onChange((d$3) => d$3 + scrollDiff));
			}
			requestAnimationFrame(() => {
				prevScrollTopRef.current = el.scrollTop;
			});
		}
	}), [
		elements.floating,
		onChange,
		overflowRef,
		scrollRef
	]);
	return import_react.useMemo(() => enabled ? { floating } : {}, [enabled, floating]);
}

//#endregion
//#region node_modules/@headlessui/react/dist/internal/floating.js
var y$3 = (0, import_react.createContext)({
	styles: void 0,
	setReference: () => {},
	setFloating: () => {},
	getReferenceProps: () => ({}),
	getFloatingProps: () => ({}),
	slot: {}
});
y$3.displayName = "FloatingContext";
var $$1 = (0, import_react.createContext)(null);
$$1.displayName = "PlacementContext";
function ye$1(e$7) {
	return (0, import_react.useMemo)(() => e$7 ? typeof e$7 == "string" ? { to: e$7 } : e$7 : null, [e$7]);
}
function Fe() {
	return (0, import_react.useContext)(y$3).setReference;
}
function be() {
	return (0, import_react.useContext)(y$3).getReferenceProps;
}
function Te() {
	let { getFloatingProps: e$7, slot: t$4 } = (0, import_react.useContext)(y$3);
	return (0, import_react.useCallback)((...n$8) => Object.assign({}, e$7(...n$8), { "data-anchor": t$4.anchor }), [e$7, t$4]);
}
function Re(e$7 = null) {
	e$7 === !1 && (e$7 = null), typeof e$7 == "string" && (e$7 = { to: e$7 });
	let t$4 = (0, import_react.useContext)($$1), n$8 = (0, import_react.useMemo)(() => e$7, [JSON.stringify(e$7, (l$6, o$8) => {
		var u$8;
		return (u$8 = o$8 == null ? void 0 : o$8.outerHTML) != null ? u$8 : o$8;
	})]);
	n$4(() => {
		t$4?.(n$8 != null ? n$8 : null);
	}, [t$4, n$8]);
	let r$3 = (0, import_react.useContext)(y$3);
	return (0, import_react.useMemo)(() => [r$3.setFloating, e$7 ? r$3.styles : {}], [
		r$3.setFloating,
		e$7,
		r$3.styles
	]);
}
var D$1 = 4;
function Ae({ children: e$7, enabled: t$4 = !0 }) {
	let [n$8, r$3] = (0, import_react.useState)(null), [l$6, o$8] = (0, import_react.useState)(0), u$8 = (0, import_react.useRef)(null), [f$3, s$8] = (0, import_react.useState)(null);
	ce(f$3);
	let i$4 = t$4 && n$8 !== null && f$3 !== null, { to: F$2 = "bottom", gap: E$4 = 0, offset: A$1 = 0, padding: c$9 = 0, inner: h$4 } = ge(n$8, f$3), [a$8, p$5 = "center"] = F$2.split(" ");
	n$4(() => {
		i$4 && o$8(0);
	}, [i$4]);
	let { refs: b, floatingStyles: S$3, context: g$1 } = useFloating({
		open: i$4,
		placement: a$8 === "selection" ? p$5 === "center" ? "bottom" : `bottom-${p$5}` : p$5 === "center" ? `${a$8}` : `${a$8}-${p$5}`,
		strategy: "absolute",
		transform: !1,
		middleware: [
			offset({
				mainAxis: a$8 === "selection" ? 0 : E$4,
				crossAxis: A$1
			}),
			shift({ padding: c$9 }),
			a$8 !== "selection" && flip({ padding: c$9 }),
			a$8 === "selection" && h$4 ? inner({
				...h$4,
				padding: c$9,
				overflowRef: u$8,
				offset: l$6,
				minItemsVisible: D$1,
				referenceOverflowThreshold: c$9,
				onFallbackChange(P$2) {
					var L$3, N$2;
					if (!P$2) return;
					let d$3 = g$1.elements.floating;
					if (!d$3) return;
					let M$2 = parseFloat(getComputedStyle(d$3).scrollPaddingBottom) || 0, I$1 = Math.min(D$1, d$3.childElementCount), W$1 = 0, B$1 = 0;
					for (let m$3 of (N$2 = (L$3 = g$1.elements.floating) == null ? void 0 : L$3.childNodes) != null ? N$2 : []) if (n$7(m$3)) {
						let x$3 = m$3.offsetTop, k$7 = x$3 + m$3.clientHeight + M$2, H$4 = d$3.scrollTop, U$1 = H$4 + d$3.clientHeight;
						if (x$3 >= H$4 && k$7 <= U$1) I$1--;
						else {
							B$1 = Math.max(0, Math.min(k$7, U$1) - Math.max(x$3, H$4)), W$1 = m$3.clientHeight;
							break;
						}
					}
					I$1 >= 1 && o$8((m$3) => {
						let x$3 = W$1 * I$1 - B$1 + M$2;
						return m$3 >= x$3 ? m$3 : x$3;
					});
				}
			}) : null,
			size({
				padding: c$9,
				apply({ availableWidth: P$2, availableHeight: d$3, elements: M$2 }) {
					Object.assign(M$2.floating.style, {
						overflow: "auto",
						maxWidth: `${P$2}px`,
						maxHeight: `min(var(--anchor-max-height, 100vh), ${d$3}px)`
					});
				}
			})
		].filter(Boolean),
		whileElementsMounted: autoUpdate
	}), [w$3 = a$8, V$3 = p$5] = g$1.placement.split("-");
	a$8 === "selection" && (w$3 = "selection");
	let G$3 = (0, import_react.useMemo)(() => ({ anchor: [w$3, V$3].filter(Boolean).join(" ") }), [w$3, V$3]), { getReferenceProps: Q$2, getFloatingProps: X$1 } = useInteractions([useInnerOffset(g$1, {
		overflowRef: u$8,
		onChange: o$8
	})]), Y$2 = o((P$2) => {
		s$8(P$2), b.setFloating(P$2);
	});
	return import_react.createElement($$1.Provider, { value: r$3 }, import_react.createElement(y$3.Provider, { value: {
		setFloating: Y$2,
		setReference: b.setReference,
		styles: S$3,
		getReferenceProps: Q$2,
		getFloatingProps: X$1,
		slot: G$3
	} }, e$7));
}
function ce(e$7) {
	n$4(() => {
		if (!e$7) return;
		let t$4 = new MutationObserver(() => {
			let n$8 = window.getComputedStyle(e$7).maxHeight, r$3 = parseFloat(n$8);
			if (isNaN(r$3)) return;
			let l$6 = parseInt(n$8);
			isNaN(l$6) || r$3 !== l$6 && (e$7.style.maxHeight = `${Math.ceil(r$3)}px`);
		});
		return t$4.observe(e$7, {
			attributes: !0,
			attributeFilter: ["style"]
		}), () => {
			t$4.disconnect();
		};
	}, [e$7]);
}
function ge(e$7, t$4) {
	var o$8, u$8, f$3;
	let n$8 = O$1((o$8 = e$7 == null ? void 0 : e$7.gap) != null ? o$8 : "var(--anchor-gap, 0)", t$4), r$3 = O$1((u$8 = e$7 == null ? void 0 : e$7.offset) != null ? u$8 : "var(--anchor-offset, 0)", t$4), l$6 = O$1((f$3 = e$7 == null ? void 0 : e$7.padding) != null ? f$3 : "var(--anchor-padding, 0)", t$4);
	return {
		...e$7,
		gap: n$8,
		offset: r$3,
		padding: l$6
	};
}
function O$1(e$7, t$4, n$8 = void 0) {
	let r$3 = p$1(), l$6 = o((s$8, i$4) => {
		if (s$8 == null) return [n$8, null];
		if (typeof s$8 == "number") return [s$8, null];
		if (typeof s$8 == "string") {
			if (!i$4) return [n$8, null];
			let F$2 = J$1(s$8, i$4);
			return [F$2, (E$4) => {
				let A$1 = q$1(s$8);
				{
					let c$9 = A$1.map((h$4) => window.getComputedStyle(i$4).getPropertyValue(h$4));
					r$3.requestAnimationFrame(function h$4() {
						r$3.nextFrame(h$4);
						let a$8 = !1;
						for (let [b, S$3] of A$1.entries()) {
							let g$1 = window.getComputedStyle(i$4).getPropertyValue(S$3);
							if (c$9[b] !== g$1) {
								c$9[b] = g$1, a$8 = !0;
								break;
							}
						}
						if (!a$8) return;
						let p$5 = J$1(s$8, i$4);
						F$2 !== p$5 && (E$4(p$5), F$2 = p$5);
					});
				}
				return r$3.dispose;
			}];
		}
		return [n$8, null];
	}), o$8 = (0, import_react.useMemo)(() => l$6(e$7, t$4)[0], [e$7, t$4]), [u$8 = o$8, f$3] = (0, import_react.useState)();
	return n$4(() => {
		let [s$8, i$4] = l$6(e$7, t$4);
		if (f$3(s$8), !!i$4) return i$4(f$3);
	}, [e$7, t$4]), u$8;
}
function q$1(e$7) {
	let t$4 = /var\((.*)\)/.exec(e$7);
	if (t$4) {
		let n$8 = t$4[1].indexOf(",");
		if (n$8 === -1) return [t$4[1]];
		let r$3 = t$4[1].slice(0, n$8).trim(), l$6 = t$4[1].slice(n$8 + 1).trim();
		return l$6 ? [r$3, ...q$1(l$6)] : [r$3];
	}
	return [];
}
function J$1(e$7, t$4) {
	let n$8 = document.createElement("div");
	t$4.appendChild(n$8), n$8.style.setProperty("margin-top", "0px", "important"), n$8.style.setProperty("margin-top", e$7, "important");
	let r$3 = parseFloat(window.getComputedStyle(n$8).marginTop) || 0;
	return t$4.removeChild(n$8), r$3;
}

//#endregion
//#region node_modules/@headlessui/react/dist/utils/calculate-active-index.js
function u$6(l$6) {
	throw new Error("Unexpected object: " + l$6);
}
var c$6 = ((i$4) => (i$4[i$4.First = 0] = "First", i$4[i$4.Previous = 1] = "Previous", i$4[i$4.Next = 2] = "Next", i$4[i$4.Last = 3] = "Last", i$4[i$4.Specific = 4] = "Specific", i$4[i$4.Nothing = 5] = "Nothing", i$4))(c$6 || {});
function f$1(l$6, n$8) {
	let t$4 = n$8.resolveItems();
	if (t$4.length <= 0) return null;
	let r$3 = n$8.resolveActiveIndex(), s$8 = r$3 != null ? r$3 : -1;
	switch (l$6.focus) {
		case 0:
			for (let e$7 = 0; e$7 < t$4.length; ++e$7) if (!n$8.resolveDisabled(t$4[e$7], e$7, t$4)) return e$7;
			return r$3;
		case 1:
			s$8 === -1 && (s$8 = t$4.length);
			for (let e$7 = s$8 - 1; e$7 >= 0; --e$7) if (!n$8.resolveDisabled(t$4[e$7], e$7, t$4)) return e$7;
			return r$3;
		case 2:
			for (let e$7 = s$8 + 1; e$7 < t$4.length; ++e$7) if (!n$8.resolveDisabled(t$4[e$7], e$7, t$4)) return e$7;
			return r$3;
		case 3:
			for (let e$7 = t$4.length - 1; e$7 >= 0; --e$7) if (!n$8.resolveDisabled(t$4[e$7], e$7, t$4)) return e$7;
			return r$3;
		case 4:
			for (let e$7 = 0; e$7 < t$4.length; ++e$7) if (n$8.resolveId(t$4[e$7], e$7, t$4) === l$6.id) return e$7;
			return r$3;
		case 5: return null;
		default: u$6(l$6);
	}
}

//#endregion
//#region node_modules/@headlessui/react/dist/utils/element-movement.js
var c$8 = {
	Idle: { kind: "Idle" },
	Tracked: (e$7) => ({
		kind: "Tracked",
		position: e$7
	}),
	Moved: { kind: "Moved" }
};
function a$2(e$7) {
	let t$4 = e$7.getBoundingClientRect();
	return `${t$4.x},${t$4.y}`;
}
function p$4(e$7, t$4, i$4) {
	let n$8 = o$1();
	if (t$4.kind === "Tracked") {
		let o$8 = function() {
			d$3 !== a$2(e$7) && (n$8.dispose(), i$4());
		};
		let { position: d$3 } = t$4, s$8 = new ResizeObserver(o$8);
		s$8.observe(e$7), n$8.add(() => s$8.disconnect()), n$8.addEventListener(window, "scroll", o$8, { passive: !0 }), n$8.addEventListener(window, "resize", o$8);
	}
	return () => n$8.dispose();
}

//#endregion
//#region node_modules/@headlessui/react/dist/utils/get-text-value.js
var a$3 = /([\u2700-\u27BF]|[\uE000-\uF8FF]|\uD83C[\uDC00-\uDFFF]|\uD83D[\uDC00-\uDFFF]|[\u2011-\u26FF]|\uD83E[\uDD10-\uDDFF])/g;
function o$4(e$7) {
	var l$6, n$8;
	let i$4 = (l$6 = e$7.innerText) != null ? l$6 : "", t$4 = e$7.cloneNode(!0);
	if (!n$7(t$4)) return i$4;
	let u$8 = !1;
	for (let f$3 of t$4.querySelectorAll("[hidden],[aria-hidden],[role=\"img\"]")) f$3.remove(), u$8 = !0;
	let r$3 = u$8 ? (n$8 = t$4.innerText) != null ? n$8 : "" : i$4;
	return a$3.test(r$3) && (r$3 = r$3.replace(a$3, "")), r$3;
}
function F$1(e$7) {
	let i$4 = e$7.getAttribute("aria-label");
	if (typeof i$4 == "string") return i$4.trim();
	let t$4 = e$7.getAttribute("aria-labelledby");
	if (t$4) {
		let u$8 = t$4.split(" ").map((r$3) => {
			let l$6 = document.getElementById(r$3);
			if (l$6) {
				let n$8 = l$6.getAttribute("aria-label");
				return typeof n$8 == "string" ? n$8.trim() : o$4(l$6).trim();
			}
			return null;
		}).filter(Boolean);
		if (u$8.length > 0) return u$8.join(", ");
	}
	return o$4(e$7).trim();
}

//#endregion
//#region node_modules/@headlessui/react/dist/hooks/use-text-value.js
function s$5(c$9) {
	let t$4 = (0, import_react.useRef)(""), r$3 = (0, import_react.useRef)("");
	return o(() => {
		let e$7 = c$9.current;
		if (!e$7) return "";
		let u$8 = e$7.innerText;
		if (t$4.current === u$8) return r$3.current;
		let n$8 = F$1(e$7).trim().toLowerCase();
		return t$4.current = u$8, r$3.current = n$8, n$8;
	});
}

//#endregion
//#region node_modules/@headlessui/react/dist/components/menu/menu-machine.js
var y$2 = Object.defineProperty;
var M = (e$7, i$4, t$4) => i$4 in e$7 ? y$2(e$7, i$4, {
	enumerable: !0,
	configurable: !0,
	writable: !0,
	value: t$4
}) : e$7[i$4] = t$4;
var S$2 = (e$7, i$4, t$4) => (M(e$7, typeof i$4 != "symbol" ? i$4 + "" : i$4, t$4), t$4);
var P = ((t$4) => (t$4[t$4.Open = 0] = "Open", t$4[t$4.Closed = 1] = "Closed", t$4))(P || {}), D = ((t$4) => (t$4[t$4.Pointer = 0] = "Pointer", t$4[t$4.Other = 1] = "Other", t$4))(D || {}), C$1 = ((o$8) => (o$8[o$8.OpenMenu = 0] = "OpenMenu", o$8[o$8.CloseMenu = 1] = "CloseMenu", o$8[o$8.GoToItem = 2] = "GoToItem", o$8[o$8.Search = 3] = "Search", o$8[o$8.ClearSearch = 4] = "ClearSearch", o$8[o$8.RegisterItems = 5] = "RegisterItems", o$8[o$8.UnregisterItems = 6] = "UnregisterItems", o$8[o$8.SetButtonElement = 7] = "SetButtonElement", o$8[o$8.SetItemsElement = 8] = "SetItemsElement", o$8[o$8.SortItems = 9] = "SortItems", o$8[o$8.MarkButtonAsMoved = 10] = "MarkButtonAsMoved", o$8))(C$1 || {});
function x$2(e$7, i$4 = (t$5) => t$5) {
	let t$4 = e$7.activeItemIndex !== null ? e$7.items[e$7.activeItemIndex] : null, n$8 = G$1(i$4(e$7.items.slice()), (s$8) => s$8.dataRef.current.domRef.current), r$3 = t$4 ? n$8.indexOf(t$4) : null;
	return r$3 === -1 && (r$3 = null), {
		items: n$8,
		activeItemIndex: r$3
	};
}
var k$5 = {
	[1](e$7) {
		if (e$7.menuState === 1) return e$7;
		let i$4 = e$7.buttonElement ? c$8.Tracked(a$2(e$7.buttonElement)) : e$7.buttonPositionState;
		return {
			...e$7,
			activeItemIndex: null,
			pendingFocus: { focus: c$6.Nothing },
			menuState: 1,
			buttonPositionState: i$4
		};
	},
	[0](e$7, i$4) {
		return e$7.menuState === 0 ? e$7 : {
			...e$7,
			__demoMode: !1,
			pendingFocus: i$4.focus,
			menuState: 0,
			buttonPositionState: c$8.Idle
		};
	},
	[2]: (e$7, i$4) => {
		var s$8, l$6, a$8, I$1, f$3;
		if (e$7.menuState === 1) return e$7;
		let t$4 = {
			...e$7,
			searchQuery: "",
			activationTrigger: (s$8 = i$4.trigger) != null ? s$8 : 1,
			__demoMode: !1
		};
		if (i$4.focus === c$6.Nothing) return {
			...t$4,
			activeItemIndex: null
		};
		if (i$4.focus === c$6.Specific) return {
			...t$4,
			activeItemIndex: e$7.items.findIndex((d$3) => d$3.id === i$4.id)
		};
		if (i$4.focus === c$6.Previous) {
			let d$3 = e$7.activeItemIndex;
			if (d$3 !== null) {
				let o$8 = e$7.items[d$3].dataRef.current.domRef, c$9 = f$1(i$4, {
					resolveItems: () => e$7.items,
					resolveActiveIndex: () => e$7.activeItemIndex,
					resolveId: (u$8) => u$8.id,
					resolveDisabled: (u$8) => u$8.dataRef.current.disabled
				});
				if (c$9 !== null) {
					let u$8 = e$7.items[c$9].dataRef.current.domRef;
					if (((l$6 = o$8.current) == null ? void 0 : l$6.previousElementSibling) === u$8.current || ((a$8 = u$8.current) == null ? void 0 : a$8.previousElementSibling) === null) return {
						...t$4,
						activeItemIndex: c$9
					};
				}
			}
		} else if (i$4.focus === c$6.Next) {
			let d$3 = e$7.activeItemIndex;
			if (d$3 !== null) {
				let o$8 = e$7.items[d$3].dataRef.current.domRef, c$9 = f$1(i$4, {
					resolveItems: () => e$7.items,
					resolveActiveIndex: () => e$7.activeItemIndex,
					resolveId: (u$8) => u$8.id,
					resolveDisabled: (u$8) => u$8.dataRef.current.disabled
				});
				if (c$9 !== null) {
					let u$8 = e$7.items[c$9].dataRef.current.domRef;
					if (((I$1 = o$8.current) == null ? void 0 : I$1.nextElementSibling) === u$8.current || ((f$3 = u$8.current) == null ? void 0 : f$3.nextElementSibling) === null) return {
						...t$4,
						activeItemIndex: c$9
					};
				}
			}
		}
		let n$8 = x$2(e$7), r$3 = f$1(i$4, {
			resolveItems: () => n$8.items,
			resolveActiveIndex: () => n$8.activeItemIndex,
			resolveId: (d$3) => d$3.id,
			resolveDisabled: (d$3) => d$3.dataRef.current.disabled
		});
		return {
			...t$4,
			...n$8,
			activeItemIndex: r$3
		};
	},
	[3]: (e$7, i$4) => {
		let n$8 = e$7.searchQuery !== "" ? 0 : 1, r$3 = e$7.searchQuery + i$4.value.toLowerCase(), l$6 = (e$7.activeItemIndex !== null ? e$7.items.slice(e$7.activeItemIndex + n$8).concat(e$7.items.slice(0, e$7.activeItemIndex + n$8)) : e$7.items).find((I$1) => {
			var f$3;
			return ((f$3 = I$1.dataRef.current.textValue) == null ? void 0 : f$3.startsWith(r$3)) && !I$1.dataRef.current.disabled;
		}), a$8 = l$6 ? e$7.items.indexOf(l$6) : -1;
		return a$8 === -1 || a$8 === e$7.activeItemIndex ? {
			...e$7,
			searchQuery: r$3
		} : {
			...e$7,
			searchQuery: r$3,
			activeItemIndex: a$8,
			activationTrigger: 1
		};
	},
	[4](e$7) {
		return e$7.searchQuery === "" ? e$7 : {
			...e$7,
			searchQuery: "",
			searchActiveItemIndex: null
		};
	},
	[5]: (e$7, i$4) => {
		let t$4 = e$7.items.concat(i$4.items.map((r$3) => r$3)), n$8 = e$7.activeItemIndex;
		return e$7.pendingFocus.focus !== c$6.Nothing && (n$8 = f$1(e$7.pendingFocus, {
			resolveItems: () => t$4,
			resolveActiveIndex: () => e$7.activeItemIndex,
			resolveId: (r$3) => r$3.id,
			resolveDisabled: (r$3) => r$3.dataRef.current.disabled
		})), {
			...e$7,
			items: t$4,
			activeItemIndex: n$8,
			pendingFocus: { focus: c$6.Nothing },
			pendingShouldSort: !0
		};
	},
	[6]: (e$7, i$4) => {
		let t$4 = e$7.items, n$8 = [], r$3 = new Set(i$4.items);
		for (let [s$8, l$6] of t$4.entries()) if (r$3.has(l$6.id) && (n$8.push(s$8), r$3.delete(l$6.id), r$3.size === 0)) break;
		if (n$8.length > 0) {
			t$4 = t$4.slice();
			for (let s$8 of n$8.reverse()) t$4.splice(s$8, 1);
		}
		return {
			...e$7,
			items: t$4,
			activationTrigger: 1
		};
	},
	[7]: (e$7, i$4) => e$7.buttonElement === i$4.element ? e$7 : {
		...e$7,
		buttonElement: i$4.element
	},
	[8]: (e$7, i$4) => e$7.itemsElement === i$4.element ? e$7 : {
		...e$7,
		itemsElement: i$4.element
	},
	[9]: (e$7) => e$7.pendingShouldSort ? {
		...e$7,
		...x$2(e$7),
		pendingShouldSort: !1
	} : e$7,
	[10](e$7) {
		return e$7.buttonPositionState.kind !== "Tracked" ? e$7 : {
			...e$7,
			buttonPositionState: c$8.Moved
		};
	}
};
var h$1 = class h$1 extends T$1 {
	constructor(t$4) {
		super(t$4);
		S$2(this, "actions", {
			registerItem: k$3(() => {
				let t$5 = [], n$8 = /* @__PURE__ */ new Set();
				return [(r$3, s$8) => {
					n$8.has(s$8) || (n$8.add(s$8), t$5.push({
						id: r$3,
						dataRef: s$8
					}));
				}, () => (n$8.clear(), this.send({
					type: 5,
					items: t$5.splice(0)
				}))];
			}),
			unregisterItem: k$3(() => {
				let t$5 = [];
				return [(n$8) => t$5.push(n$8), () => this.send({
					type: 6,
					items: t$5.splice(0)
				})];
			})
		});
		S$2(this, "selectors", {
			activeDescendantId(t$5) {
				var s$8;
				let n$8 = t$5.activeItemIndex, r$3 = t$5.items;
				return n$8 === null || (s$8 = r$3[n$8]) == null ? void 0 : s$8.id;
			},
			isActive(t$5, n$8) {
				var l$6;
				let r$3 = t$5.activeItemIndex, s$8 = t$5.items;
				return r$3 !== null ? ((l$6 = s$8[r$3]) == null ? void 0 : l$6.id) === n$8 : !1;
			},
			shouldScrollIntoView(t$5, n$8) {
				return t$5.__demoMode || t$5.menuState !== 0 || t$5.activationTrigger === 0 ? !1 : this.isActive(t$5, n$8);
			},
			didButtonMove(t$5) {
				return t$5.buttonPositionState.kind === "Moved";
			}
		});
		this.on(5, () => {
			this.disposables.requestAnimationFrame(() => {
				this.send({ type: 9 });
			});
		});
		{
			let n$8 = this.state.id, r$3 = x$1.get(null);
			this.disposables.add(r$3.on(k$4.Push, (s$8) => {
				!r$3.selectors.isTop(s$8, n$8) && this.state.menuState === 0 && this.send({ type: 1 });
			})), this.on(0, () => r$3.actions.push(n$8)), this.on(1, () => r$3.actions.pop(n$8));
		}
		this.disposables.group((n$8) => {
			this.on(1, (r$3) => {
				r$3.buttonElement && (n$8.dispose(), n$8.add(p$4(r$3.buttonElement, r$3.buttonPositionState, () => {
					this.send({ type: 10 });
				})));
			});
		});
	}
	static new({ id: t$4, __demoMode: n$8 = !1 }) {
		return new h$1({
			id: t$4,
			__demoMode: n$8,
			menuState: n$8 ? 0 : 1,
			buttonElement: null,
			itemsElement: null,
			items: [],
			searchQuery: "",
			activeItemIndex: null,
			activationTrigger: 1,
			pendingShouldSort: !1,
			pendingFocus: { focus: c$6.Nothing },
			buttonPositionState: c$8.Idle
		});
	}
	reduce(t$4, n$8) {
		return u$4(n$8.type, k$5, t$4, n$8);
	}
};

//#endregion
//#region node_modules/@headlessui/react/dist/components/menu/menu-machine-glue.js
var a$1 = (0, import_react.createContext)(null);
function p$3(t$4) {
	let n$8 = (0, import_react.useContext)(a$1);
	if (n$8 === null) {
		let e$7 = /* @__PURE__ */ new Error(`<${t$4} /> is missing a parent <Menu /> component.`);
		throw Error.captureStackTrace && Error.captureStackTrace(e$7, s$6), e$7;
	}
	return n$8;
}
function s$6({ id: t$4, __demoMode: n$8 = !1 }) {
	let e$7 = (0, import_react.useMemo)(() => h$1.new({
		id: t$4,
		__demoMode: n$8
	}), []);
	return c$7(() => e$7.dispose()), e$7;
}

//#endregion
//#region node_modules/@headlessui/react/dist/components/menu/menu.js
var import_react_dom = require_react_dom();
var Ze = import_react.Fragment;
function et(m$3, y$4) {
	let l$6 = (0, import_react$1.useId)(), { __demoMode: a$8 = !1, ...p$5 } = m$3, s$8 = s$6({
		id: l$6,
		__demoMode: a$8
	}), [n$8, M$2, f$3] = S$1(s$8, (d$3) => [
		d$3.menuState,
		d$3.itemsElement,
		d$3.buttonElement
	]), _$2 = y$1(y$4), o$8 = x$1.get(null);
	k$2(S$1(o$8, (0, import_react.useCallback)((d$3) => o$8.selectors.isTop(d$3, l$6), [o$8, l$6])), [f$3, M$2], (d$3, T$4) => {
		var P$2;
		s$8.send({ type: C$1.CloseMenu }), H$1(T$4, I.Loose) || (d$3.preventDefault(), (P$2 = s$8.state.buttonElement) == null || P$2.focus());
	});
	let I$1 = o(() => {
		s$8.send({ type: C$1.CloseMenu });
	}), b = n$5({
		open: n$8 === P.Open,
		close: I$1
	}), i$4 = { ref: _$2 }, g$1 = K$2();
	return import_react.createElement(Ae, null, import_react.createElement(a$1.Provider, { value: s$8 }, import_react.createElement(c$5, { value: u$4(n$8, {
		[P.Open]: i.Open,
		[P.Closed]: i.Closed
	}) }, g$1({
		ourProps: i$4,
		theirProps: p$5,
		slot: b,
		defaultTag: Ze,
		name: "Menu"
	}))));
}
var tt = "button";
function ot(m$3, y$4) {
	let l$6 = p$3("Menu.Button"), a$8 = (0, import_react$1.useId)(), { id: p$5 = `headlessui-menu-button-${a$8}`, disabled: s$8 = !1, autoFocus: n$8 = !1, ...M$2 } = m$3, f$3 = (0, import_react.useRef)(null), _$2 = be(), o$8 = y$1(y$4, f$3, Fe(), o((t$4) => l$6.send({
		type: C$1.SetButtonElement,
		element: t$4
	}))), F$2 = o((t$4) => {
		switch (t$4.key) {
			case o$2.Space:
			case o$2.Enter:
			case o$2.ArrowDown:
				t$4.preventDefault(), t$4.stopPropagation(), l$6.send({
					type: C$1.OpenMenu,
					focus: { focus: c$6.First }
				});
				break;
			case o$2.ArrowUp:
				t$4.preventDefault(), t$4.stopPropagation(), l$6.send({
					type: C$1.OpenMenu,
					focus: { focus: c$6.Last }
				});
				break;
		}
	}), I$1 = o((t$4) => {
		switch (t$4.key) {
			case o$2.Space:
				t$4.preventDefault();
				break;
		}
	}), [b, i$4, g$1] = S$1(l$6, (t$4) => [
		t$4.menuState,
		t$4.buttonElement,
		t$4.itemsElement
	]);
	L(b === P.Open, {
		trigger: i$4,
		action: (0, import_react.useCallback)((t$4) => {
			if (i$4 != null && i$4.contains(t$4.target)) return S.Ignore;
			let S$3 = t$4.target.closest("[role=\"menuitem\"]:not([data-disabled])");
			return n$7(S$3) ? S.Select(S$3) : g$1 != null && g$1.contains(t$4.target) ? S.Ignore : S.Close;
		}, [i$4, g$1]),
		close: (0, import_react.useCallback)(() => l$6.send({ type: C$1.CloseMenu }), []),
		select: (0, import_react.useCallback)((t$4) => t$4.click(), [])
	});
	let T$4 = s$4((t$4) => {
		var S$3;
		s$8 || (b === P.Open ? ((0, import_react_dom.flushSync)(() => l$6.send({ type: C$1.CloseMenu })), (S$3 = f$3.current) == null || S$3.focus({ preventScroll: !0 })) : (t$4.preventDefault(), l$6.send({
			type: C$1.OpenMenu,
			focus: { focus: c$6.Nothing },
			trigger: D.Pointer
		})));
	}), { isFocusVisible: P$2, focusProps: L$3 } = $f7dceffc5ad7768b$export$4e328f61c538687f({ autoFocus: n$8 }), { isHovered: O$2, hoverProps: v$2 } = $6179b936705e76d3$export$ae780daf29e6d456({ isDisabled: s$8 }), { pressed: D$3, pressProps: U$1 } = w$1({ disabled: s$8 }), H$4 = n$5({
		open: b === P.Open,
		active: D$3 || b === P.Open,
		disabled: s$8,
		hover: O$2,
		focus: P$2,
		autofocus: n$8
	}), G$3 = V$1(_$2(), {
		ref: o$8,
		id: p$5,
		type: e$5(m$3, f$3.current),
		"aria-haspopup": "menu",
		"aria-controls": g$1 == null ? void 0 : g$1.id,
		"aria-expanded": b === P.Open,
		disabled: s$8 || void 0,
		autoFocus: n$8,
		onKeyDown: F$2,
		onKeyUp: I$1
	}, T$4, L$3, v$2, U$1);
	return K$2()({
		ourProps: G$3,
		theirProps: M$2,
		slot: H$4,
		defaultTag: tt,
		name: "Menu.Button"
	});
}
var nt = "div", rt = A.RenderStrategy | A.Static;
function at(m$3, y$4) {
	let l$6 = (0, import_react$1.useId)(), { id: a$8 = `headlessui-menu-items-${l$6}`, anchor: p$5, portal: s$8 = !1, modal: n$8 = !0, transition: M$2 = !1, ...f$3 } = m$3, _$2 = ye$1(p$5), o$8 = p$3("Menu.Items"), [F$2, I$1] = Re(_$2), b = Te(), [i$4, g$1] = (0, import_react.useState)(null), d$3 = y$1(y$4, _$2 ? F$2 : null, o((e$7) => o$8.send({
		type: C$1.SetItemsElement,
		element: e$7
	})), g$1), [T$4, P$2] = S$1(o$8, (e$7) => [e$7.menuState, e$7.buttonElement]), L$3 = u$1(P$2), O$2 = u$1(i$4);
	_$2 && (s$8 = !0);
	let v$2 = u$3(), [D$3, U$1] = N(M$2, i$4, v$2 !== null ? (v$2 & i.Open) === i.Open : T$4 === P.Open);
	p$2(D$3, P$2, () => {
		o$8.send({ type: C$1.CloseMenu });
	});
	let H$4 = S$1(o$8, (e$7) => e$7.__demoMode);
	f(H$4 ? !1 : n$8 && T$4 === P.Open, O$2);
	y(H$4 ? !1 : n$8 && T$4 === P.Open, { allowed: (0, import_react.useCallback)(() => [P$2, i$4], [P$2, i$4]) });
	let S$3 = S$1(o$8, o$8.selectors.didButtonMove) ? !1 : D$3;
	(0, import_react.useEffect)(() => {
		let e$7 = i$4;
		e$7 && T$4 === P.Open && (d$1(e$7) || e$7.focus({ preventScroll: !0 }));
	}, [T$4, i$4]), F(T$4 === P.Open, {
		container: i$4,
		accept(e$7) {
			return e$7.getAttribute("role") === "menuitem" ? NodeFilter.FILTER_REJECT : e$7.hasAttribute("role") ? NodeFilter.FILTER_SKIP : NodeFilter.FILTER_ACCEPT;
		},
		walk(e$7) {
			e$7.setAttribute("role", "none");
		}
	});
	let z$1 = p$1(), le = o((e$7) => {
		var N$2, Y$2, Z$2;
		switch (z$1.dispose(), e$7.key) {
			case o$2.Space: if (o$8.state.searchQuery !== "") return e$7.preventDefault(), e$7.stopPropagation(), o$8.send({
				type: C$1.Search,
				value: e$7.key
			});
			case o$2.Enter:
				if (e$7.preventDefault(), e$7.stopPropagation(), o$8.state.activeItemIndex !== null) {
					let { dataRef: de } = o$8.state.items[o$8.state.activeItemIndex];
					(Y$2 = (N$2 = de.current) == null ? void 0 : N$2.domRef.current) == null || Y$2.click();
				}
				o$8.send({ type: C$1.CloseMenu }), K$1(o$8.state.buttonElement);
				break;
			case o$2.ArrowDown: return e$7.preventDefault(), e$7.stopPropagation(), o$8.send({
				type: C$1.GoToItem,
				focus: c$6.Next
			});
			case o$2.ArrowUp: return e$7.preventDefault(), e$7.stopPropagation(), o$8.send({
				type: C$1.GoToItem,
				focus: c$6.Previous
			});
			case o$2.Home:
			case o$2.PageUp: return e$7.preventDefault(), e$7.stopPropagation(), o$8.send({
				type: C$1.GoToItem,
				focus: c$6.First
			});
			case o$2.End:
			case o$2.PageDown: return e$7.preventDefault(), e$7.stopPropagation(), o$8.send({
				type: C$1.GoToItem,
				focus: c$6.Last
			});
			case o$2.Escape:
				e$7.preventDefault(), e$7.stopPropagation(), (0, import_react_dom.flushSync)(() => o$8.send({ type: C$1.CloseMenu })), (Z$2 = o$8.state.buttonElement) == null || Z$2.focus({ preventScroll: !0 });
				break;
			case o$2.Tab:
				e$7.preventDefault(), e$7.stopPropagation(), (0, import_react_dom.flushSync)(() => o$8.send({ type: C$1.CloseMenu })), R(o$8.state.buttonElement, e$7.shiftKey ? T.Previous : T.Next);
				break;
			default:
				e$7.key.length === 1 && (o$8.send({
					type: C$1.Search,
					value: e$7.key
				}), z$1.setTimeout(() => o$8.send({ type: C$1.ClearSearch }), 350));
				break;
		}
	}), pe = o((e$7) => {
		switch (e$7.key) {
			case o$2.Space:
				e$7.preventDefault();
				break;
		}
	}), ie = n$5({ open: T$4 === P.Open }), ue$1 = V$1(_$2 ? b() : {}, {
		"aria-activedescendant": S$1(o$8, o$8.selectors.activeDescendantId),
		"aria-labelledby": S$1(o$8, (e$7) => {
			var N$2;
			return (N$2 = e$7.buttonElement) == null ? void 0 : N$2.id;
		}),
		id: a$8,
		onKeyDown: le,
		onKeyUp: pe,
		role: "menu",
		tabIndex: T$4 === P.Open ? 0 : void 0,
		ref: d$3,
		style: {
			...f$3.style,
			...I$1,
			"--button-width": w$2(D$3, P$2, !0).width
		},
		...x(U$1)
	}), me = K$2();
	return import_react.createElement(te, {
		enabled: s$8 ? m$3.static || D$3 : !1,
		ownerDocument: L$3
	}, me({
		ourProps: ue$1,
		theirProps: f$3,
		slot: ie,
		defaultTag: nt,
		features: rt,
		visible: S$3,
		name: "Menu.Items"
	}));
}
var st = import_react.Fragment;
function lt(m$3, y$4) {
	let l$6 = (0, import_react$1.useId)(), { id: a$8 = `headlessui-menu-item-${l$6}`, disabled: p$5 = !1, ...s$8 } = m$3, n$8 = p$3("Menu.Item"), M$2 = S$1(n$8, (t$4) => n$8.selectors.isActive(t$4, a$8)), f$3 = (0, import_react.useRef)(null), _$2 = y$1(y$4, f$3), o$8 = S$1(n$8, (t$4) => n$8.selectors.shouldScrollIntoView(t$4, a$8));
	n$4(() => {
		if (o$8) return o$1().requestAnimationFrame(() => {
			var t$4, S$3;
			(S$3 = (t$4 = f$3.current) == null ? void 0 : t$4.scrollIntoView) == null || S$3.call(t$4, { block: "nearest" });
		});
	}, [o$8, f$3]);
	let F$2 = s$5(f$3), I$1 = (0, import_react.useRef)({
		disabled: p$5,
		domRef: f$3,
		get textValue() {
			return F$2();
		}
	});
	n$4(() => {
		I$1.current.disabled = p$5;
	}, [I$1, p$5]), n$4(() => (n$8.actions.registerItem(a$8, I$1), () => n$8.actions.unregisterItem(a$8)), [I$1, a$8]);
	let b = o(() => {
		n$8.send({ type: C$1.CloseMenu });
	}), i$4 = o((t$4) => {
		if (p$5) return t$4.preventDefault();
		n$8.send({ type: C$1.CloseMenu }), K$1(n$8.state.buttonElement);
	}), g$1 = o(() => {
		if (p$5) return n$8.send({
			type: C$1.GoToItem,
			focus: c$6.Nothing
		});
		n$8.send({
			type: C$1.GoToItem,
			focus: c$6.Specific,
			id: a$8
		});
	}), d$3 = u$2(), T$4 = o((t$4) => d$3.update(t$4)), P$2 = o((t$4) => {
		d$3.wasMoved(t$4) && (p$5 || M$2 || n$8.send({
			type: C$1.GoToItem,
			focus: c$6.Specific,
			id: a$8,
			trigger: D.Pointer
		}));
	}), L$3 = o((t$4) => {
		d$3.wasMoved(t$4) && (p$5 || M$2 && n$8.state.activationTrigger === D.Pointer && n$8.send({
			type: C$1.GoToItem,
			focus: c$6.Nothing
		}));
	}), [O$2, v$2] = V$2(), [D$3, U$1] = H$2(), H$4 = n$5({
		active: M$2,
		focus: M$2,
		disabled: p$5,
		close: b
	}), G$3 = {
		id: a$8,
		ref: _$2,
		role: "menuitem",
		tabIndex: p$5 === !0 ? void 0 : -1,
		"aria-disabled": p$5 === !0 ? !0 : void 0,
		"aria-labelledby": O$2,
		"aria-describedby": D$3,
		disabled: void 0,
		onClick: i$4,
		onFocus: g$1,
		onPointerEnter: T$4,
		onMouseEnter: T$4,
		onPointerMove: P$2,
		onMouseMove: P$2,
		onPointerLeave: L$3,
		onMouseLeave: L$3
	}, w$3 = K$2();
	return import_react.createElement(v$2, null, import_react.createElement(U$1, null, w$3({
		ourProps: G$3,
		theirProps: s$8,
		slot: H$4,
		defaultTag: st,
		name: "Menu.Item"
	})));
}
var pt = "div";
function it(m$3, y$4) {
	let [l$6, a$8] = V$2(), p$5 = m$3, s$8 = {
		ref: y$4,
		"aria-labelledby": l$6,
		role: "group"
	}, n$8 = K$2();
	return import_react.createElement(a$8, null, n$8({
		ourProps: s$8,
		theirProps: p$5,
		slot: {},
		defaultTag: pt,
		name: "Menu.Section"
	}));
}
var ut$1 = "header";
function mt(m$3, y$4) {
	let l$6 = (0, import_react$1.useId)(), { id: a$8 = `headlessui-menu-heading-${l$6}`, ...p$5 } = m$3, s$8 = C();
	n$4(() => s$8.register(a$8), [a$8, s$8.register]);
	let n$8 = {
		id: a$8,
		ref: y$4,
		role: "presentation",
		...s$8.props
	};
	return K$2()({
		ourProps: n$8,
		theirProps: p$5,
		slot: {},
		defaultTag: ut$1,
		name: "Menu.Heading"
	});
}
var dt = "div";
function Tt(m$3, y$4) {
	let l$6 = m$3, a$8 = {
		ref: y$4,
		role: "separator"
	};
	return K$2()({
		ourProps: a$8,
		theirProps: l$6,
		slot: {},
		defaultTag: dt,
		name: "Menu.Separator"
	});
}
var ct = Y$1(et), ft = Y$1(ot), yt = Y$1(at), gt = Y$1(lt), Pt = Y$1(it), Et = Y$1(mt), Mt = Y$1(Tt), lo = Object.assign(ct, {
	Button: ft,
	Items: yt,
	Item: gt,
	Section: Pt,
	Heading: Et,
	Separator: Mt
});

//#endregion
//#region node_modules/@privy-io/react-auth/dist/esm/BridgeNetworkSelectionView-BRZzs71K.mjs
var import_jsx_runtime = require_jsx_runtime();
var W = ({ chains: o$8, appId: t$4, address: s$8, rpcConfig: c$9 }) => Promise.all(o$8.map((async (o$9) => {
	let l$6 = await createPublicClient({
		chain: o$9,
		transport: http(c(o$9, c$9, t$4))
	}).getBalance({ address: s$8 }).catch((() => 0n)), d$3 = null, h$4 = E[o$9.id];
	if (h$4) {
		let { balance: e$7 } = await n$1({
			address: s$8,
			chain: o$9,
			rpcConfig: c$9,
			appId: t$4,
			erc20Address: h$4
		});
		d$3 = e$7;
	}
	return {
		balance: l$6,
		erc20Balance: d$3,
		erc20Address: h$4,
		chain: o$9
	};
}))), H = ({ balance: e$7, className: r$3, chain: a$8 }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(d, {
	className: r$3,
	$state: void 0,
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Q, {
		balance: e$7,
		chain: a$8
	})
}), Q = ({ balance: e$7, chain: r$3 }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(V, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(z, { chainId: "object" == typeof r$3 ? r$3.id : "solana" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(n$2, { children: "object" == typeof r$3 ? r$3.name : e$3(r$3) })] }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(n$3, {
	isLoading: !1,
	isPulsing: !1,
	color: "gray",
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(q, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WalletIcon_default, {}) }), e$7]
})] });
var V = ut.div`
  display: flex;
  align-items: center;
`, q = ut.div`
  height: 0.75rem;
  width: 0.75rem;
  margin-right: 0.2rem;
`, z = ut(w)`
  height: 1.25rem;
  width: 1.25rem;
  display: inline-block;
  margin-right: 0.5rem;
  border-radius: 4px;
`;
var O = ({ options: e$7, onSelect: r$3, selected: a$8, className: o$8 }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(lo, {
	as: Y,
	children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ft, {
		as: X,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Q, {
			balance: a$8.balance,
			chain: a$8.chain
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(K, { height: 16 })]
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(yt, {
		as: G,
		className: o$8,
		children: e$7.map(((e$8, a$9) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(gt, {
			as: J,
			onClick: () => r$3(a$9),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Q, {
				balance: e$8.balance,
				chain: e$8.chain
			})
		}, a$9)))
	})]
});
var Y = ut.div`
  width: 100%;
  position: relative;
`, G = ut.div`
  width: 100%;
  margin-top: 0.5rem;
  position: absolute;
  background-color: var(--privy-color-background);
  border-radius: var(--privy-border-radius-md);
  overflow-x: hidden;
  overflow-y: auto;
  box-shadow: 0px 1px 2px 0px rgba(16, 24, 40, 0.05);
  max-height: 11.75rem;

  && {
    border: solid 1px var(--privy-color-foreground-4);
  }

  z-index: 1;
`, J = ut.button`
  width: 100%;
  display: flex;
  justify-content: space-between;

  && {
    padding: 1rem;
  }

  :not(:last-child) {
    border-bottom: solid 1px var(--privy-color-foreground-4);
  }

  :hover {
    background: var(--privy-color-background-2);
  }
`, K = ut(ChevronDownIcon_default)`
  height: 1rem;
  margin-left: 0.5rem;
`, X = ut.button`
  ${e$4}

  /* Push the chip all the way to the right */
  span {
    margin-left: auto;
  }

  ${K} {
    transition: rotate 100ms ease-in;
  }

  &[aria-expanded='true'] {
    ${K} {
      rotate: -180deg;
    }
  }
`;
var Z = ({ displayName: e$7, errorMessage: r$3, configuredFundingChain: a$8, formattedBalance: i$4, fundingAmount: n$8, fundingCurrency: b, fundingAmountInUsd: w$3, options: C$2, selectedOption: k$7, isPreparing: N$2, isSubmitting: T$4, addressToFund: B$1, fundingWalletAddress: S$3, onSubmit: A$1, onSelect: I$1, onAmountChange: $$2 }) => {
	let E$4 = (0, import_react.useRef)(null);
	return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(t$1, {}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(c$2, {}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(e$2, { children: "Transfer from another network" }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(r, { children: [
			"You need more funds on the",
			" ",
			"object" == typeof a$8 ? a$8.name : e$3(a$8),
			" ",
			"network. Bridge from another blockchain network."
		] }),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(a, {
			style: { marginTop: "2rem" },
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(p, {
				onClick: () => E$4.current?.focus(),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(s$3, {
					ref: E$4,
					value: n$8,
					onChange: (e$8) => {
						let r$4 = e$8.target.value;
						if (/^[0-9.]*$/.test(r$4) && r$4.split(".").length - 1 <= 1) {
							let e$9 = /\.$/.test(r$4) ? "." : "", a$9 = Number(r$4.replace(/\.$/, "") || "0");
							if (Number.isNaN(a$9)) return void $$2("0");
							$$2(a$9.toString() + e$9);
						}
					}
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(c$4, { children: b })]
			}), w$3 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(l, { children: w$3 })]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(s$2, {
			style: { marginTop: "1.5rem" },
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(e$1, { children: "From" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(e$1, { children: n(S$3) })]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(O, {
			selected: {
				chain: k$7.chain,
				balance: k$7.isErc20Quote ? t$2({
					amount: k$7.erc20Balance ?? 0n,
					decimals: 6
				}) + " USDC" : c$3(k$7.balance, k$7.chain.nativeCurrency.symbol, 3, !0)
			},
			options: C$2.map((({ chain: e$8, balance: r$4, isErc20Quote: a$9, erc20Balance: o$8 }) => ({
				chain: e$8,
				balance: a$9 ? t$2({
					amount: o$8 ?? 0n,
					decimals: 6
				}) + " USDC" : c$3(r$4, e$8.nativeCurrency.symbol, 3, !0)
			}))),
			onSelect: I$1
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(s$2, {
			style: { marginTop: "1.5rem" },
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(e$1, { children: "To" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(e$1, { children: n(B$1) })]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(H, {
			chain: a$8,
			balance: i$4
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(e, {
			style: { marginTop: "1rem" },
			children: r$3
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(k, {
			style: { marginTop: "1rem" },
			loading: T$4 || N$2,
			disabled: N$2 || T$4,
			onClick: A$1,
			children: ["Confirm with ", e$7]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(s$1, {}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(u, {})
	] });
};

//#endregion
export { Z as n, W as t };
//# sourceMappingURL=BridgeNetworkSelectionView-BRZzs71K-BQqsvaq3.js.map