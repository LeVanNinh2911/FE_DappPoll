import { c as __toESM } from "./chunk-CBgdiQcn.js";
import { t as require_react } from "./react-CqXAK9JW.js";
import { t as require_react_dom } from "./react-dom-Cpfbu0H1.js";
import { t as require_jsx_runtime } from "./jsx-runtime-Dq9gTbWS.js";
import { Kn as g, Nt as ye, X as ut, Z as require_lib, _t as ae, b as Z, fn as E, g as Ue, h as U, o as G, pn as I, s as H, tr as re, tt as Ce, u as M, w as de, wn as o$1, y as X } from "./useActiveWallet-9-7-ELBX-C7cHxRLi.js";
import { o as h, u as y } from "./ModalHeader-Bgc2_pdq-DksvhAB7.js";
import { t as w } from "./Screen-BGB_WBl_-DTt10425.js";
import { n as MagnifyingGlassIcon_default } from "./esm-C7B7jE-N.js";
import { t as m } from "./CopyableText-BCytXyJL-DGYyDS3z.js";
import { t as n$1 } from "./Link-DJ5gq9Di-0Z3cY9vx.js";
import { t as C } from "./QrCode-BOa7PXxn-CJtKO4m7.js";
import { n as e$1 } from "./EmailInputForm-Dgoii4vf-BZYbhqbW.js";
import { t as n$2 } from "./useI18n-BhqMnncw-cmB8O7I0.js";
import { t as e$2 } from "./WalletCards-DH1rqayz-9z1z249r.js";

//#region node_modules/@tanstack/virtual-core/dist/esm/utils.js
function memo(getDeps, fn, opts) {
	let deps = opts.initialDeps ?? [];
	let result;
	let isInitial = true;
	function memoizedFunction() {
		var _a, _b, _c;
		let depTime;
		if (opts.key && ((_a = opts.debug) == null ? void 0 : _a.call(opts))) depTime = Date.now();
		const newDeps = getDeps();
		if (!(newDeps.length !== deps.length || newDeps.some((dep, index) => deps[index] !== dep))) return result;
		deps = newDeps;
		let resultTime;
		if (opts.key && ((_b = opts.debug) == null ? void 0 : _b.call(opts))) resultTime = Date.now();
		result = fn(...newDeps);
		if (opts.key && ((_c = opts.debug) == null ? void 0 : _c.call(opts))) {
			const depEndTime = Math.round((Date.now() - depTime) * 100) / 100;
			const resultEndTime = Math.round((Date.now() - resultTime) * 100) / 100;
			const resultFpsPercentage = resultEndTime / 16;
			const pad = (str, num) => {
				str = String(str);
				while (str.length < num) str = " " + str;
				return str;
			};
			console.info(`%c⏱ ${pad(resultEndTime, 5)} /${pad(depEndTime, 5)} ms`, `
            font-size: .6rem;
            font-weight: bold;
            color: hsl(${Math.max(0, Math.min(120 - 120 * resultFpsPercentage, 120))}deg 100% 31%);`, opts == null ? void 0 : opts.key);
		}
		if ((opts == null ? void 0 : opts.onChange) && !(isInitial && opts.skipInitialOnChange)) opts.onChange(result);
		isInitial = false;
		return result;
	}
	memoizedFunction.updateDeps = (newDeps) => {
		deps = newDeps;
	};
	return memoizedFunction;
}
function notUndefined(value, msg) {
	if (value === void 0) throw new Error(`Unexpected undefined${msg ? `: ${msg}` : ""}`);
	else return value;
}
var approxEqual = (a$1, b) => Math.abs(a$1 - b) < 1.01;
var debounce = (targetWindow, fn, ms) => {
	let timeoutId;
	return function(...args) {
		targetWindow.clearTimeout(timeoutId);
		timeoutId = targetWindow.setTimeout(() => fn.apply(this, args), ms);
	};
};

//#endregion
//#region node_modules/@tanstack/virtual-core/dist/esm/index.js
var getRect = (element) => {
	const { offsetWidth, offsetHeight } = element;
	return {
		width: offsetWidth,
		height: offsetHeight
	};
};
var defaultKeyExtractor = (index) => index;
var defaultRangeExtractor = (range) => {
	const start = Math.max(range.startIndex - range.overscan, 0);
	const end = Math.min(range.endIndex + range.overscan, range.count - 1);
	const arr = [];
	for (let i = start; i <= end; i++) arr.push(i);
	return arr;
};
var observeElementRect = (instance, cb) => {
	const element = instance.scrollElement;
	if (!element) return;
	const targetWindow = instance.targetWindow;
	if (!targetWindow) return;
	const handler = (rect) => {
		const { width, height } = rect;
		cb({
			width: Math.round(width),
			height: Math.round(height)
		});
	};
	handler(getRect(element));
	if (!targetWindow.ResizeObserver) return () => {};
	const observer = new targetWindow.ResizeObserver((entries) => {
		const run = () => {
			const entry = entries[0];
			if (entry == null ? void 0 : entry.borderBoxSize) {
				const box = entry.borderBoxSize[0];
				if (box) {
					handler({
						width: box.inlineSize,
						height: box.blockSize
					});
					return;
				}
			}
			handler(getRect(element));
		};
		instance.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
	});
	observer.observe(element, { box: "border-box" });
	return () => {
		observer.unobserve(element);
	};
};
var addEventListenerOptions = { passive: true };
var supportsScrollend = typeof window == "undefined" ? true : "onscrollend" in window;
var observeElementOffset = (instance, cb) => {
	const element = instance.scrollElement;
	if (!element) return;
	const targetWindow = instance.targetWindow;
	if (!targetWindow) return;
	let offset = 0;
	const fallback = instance.options.useScrollendEvent && supportsScrollend ? () => void 0 : debounce(targetWindow, () => {
		cb(offset, false);
	}, instance.options.isScrollingResetDelay);
	const createHandler = (isScrolling) => () => {
		const { horizontal, isRtl } = instance.options;
		offset = horizontal ? element["scrollLeft"] * (isRtl && -1 || 1) : element["scrollTop"];
		fallback();
		cb(offset, isScrolling);
	};
	const handler = createHandler(true);
	const endHandler = createHandler(false);
	element.addEventListener("scroll", handler, addEventListenerOptions);
	const registerScrollendEvent = instance.options.useScrollendEvent && supportsScrollend;
	if (registerScrollendEvent) element.addEventListener("scrollend", endHandler, addEventListenerOptions);
	return () => {
		element.removeEventListener("scroll", handler);
		if (registerScrollendEvent) element.removeEventListener("scrollend", endHandler);
	};
};
var measureElement = (element, entry, instance) => {
	if (entry == null ? void 0 : entry.borderBoxSize) {
		const box = entry.borderBoxSize[0];
		if (box) return Math.round(box[instance.options.horizontal ? "inlineSize" : "blockSize"]);
	}
	return element[instance.options.horizontal ? "offsetWidth" : "offsetHeight"];
};
var elementScroll = (offset, { adjustments = 0, behavior }, instance) => {
	var _a, _b;
	const toOffset = offset + adjustments;
	(_b = (_a = instance.scrollElement) == null ? void 0 : _a.scrollTo) == null || _b.call(_a, {
		[instance.options.horizontal ? "left" : "top"]: toOffset,
		behavior
	});
};
var Virtualizer = class {
	constructor(opts) {
		this.unsubs = [];
		this.scrollElement = null;
		this.targetWindow = null;
		this.isScrolling = false;
		this.currentScrollToIndex = null;
		this.measurementsCache = [];
		this.itemSizeCache = /* @__PURE__ */ new Map();
		this.laneAssignments = /* @__PURE__ */ new Map();
		this.pendingMeasuredCacheIndexes = [];
		this.prevLanes = void 0;
		this.lanesChangedFlag = false;
		this.lanesSettling = false;
		this.scrollRect = null;
		this.scrollOffset = null;
		this.scrollDirection = null;
		this.scrollAdjustments = 0;
		this.elementsCache = /* @__PURE__ */ new Map();
		this.observer = /* @__PURE__ */ (() => {
			let _ro = null;
			const get = () => {
				if (_ro) return _ro;
				if (!this.targetWindow || !this.targetWindow.ResizeObserver) return null;
				return _ro = new this.targetWindow.ResizeObserver((entries) => {
					entries.forEach((entry) => {
						const run = () => {
							this._measureElement(entry.target, entry);
						};
						this.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
					});
				});
			};
			return {
				disconnect: () => {
					var _a;
					(_a = get()) == null || _a.disconnect();
					_ro = null;
				},
				observe: (target) => {
					var _a;
					return (_a = get()) == null ? void 0 : _a.observe(target, { box: "border-box" });
				},
				unobserve: (target) => {
					var _a;
					return (_a = get()) == null ? void 0 : _a.unobserve(target);
				}
			};
		})();
		this.range = null;
		this.setOptions = (opts2) => {
			Object.entries(opts2).forEach(([key, value]) => {
				if (typeof value === "undefined") delete opts2[key];
			});
			this.options = {
				debug: false,
				initialOffset: 0,
				overscan: 1,
				paddingStart: 0,
				paddingEnd: 0,
				scrollPaddingStart: 0,
				scrollPaddingEnd: 0,
				horizontal: false,
				getItemKey: defaultKeyExtractor,
				rangeExtractor: defaultRangeExtractor,
				onChange: () => {},
				measureElement,
				initialRect: {
					width: 0,
					height: 0
				},
				scrollMargin: 0,
				gap: 0,
				indexAttribute: "data-index",
				initialMeasurementsCache: [],
				lanes: 1,
				isScrollingResetDelay: 150,
				enabled: true,
				isRtl: false,
				useScrollendEvent: false,
				useAnimationFrameWithResizeObserver: false,
				...opts2
			};
		};
		this.notify = (sync) => {
			var _a, _b;
			(_b = (_a = this.options).onChange) == null || _b.call(_a, this, sync);
		};
		this.maybeNotify = memo(() => {
			this.calculateRange();
			return [
				this.isScrolling,
				this.range ? this.range.startIndex : null,
				this.range ? this.range.endIndex : null
			];
		}, (isScrolling) => {
			this.notify(isScrolling);
		}, {
			key: "maybeNotify",
			debug: () => this.options.debug,
			initialDeps: [
				this.isScrolling,
				this.range ? this.range.startIndex : null,
				this.range ? this.range.endIndex : null
			]
		});
		this.cleanup = () => {
			this.unsubs.filter(Boolean).forEach((d$1) => d$1());
			this.unsubs = [];
			this.observer.disconnect();
			this.scrollElement = null;
			this.targetWindow = null;
		};
		this._didMount = () => {
			return () => {
				this.cleanup();
			};
		};
		this._willUpdate = () => {
			var _a;
			const scrollElement = this.options.enabled ? this.options.getScrollElement() : null;
			if (this.scrollElement !== scrollElement) {
				this.cleanup();
				if (!scrollElement) {
					this.maybeNotify();
					return;
				}
				this.scrollElement = scrollElement;
				if (this.scrollElement && "ownerDocument" in this.scrollElement) this.targetWindow = this.scrollElement.ownerDocument.defaultView;
				else this.targetWindow = ((_a = this.scrollElement) == null ? void 0 : _a.window) ?? null;
				this.elementsCache.forEach((cached) => {
					this.observer.observe(cached);
				});
				this.unsubs.push(this.options.observeElementRect(this, (rect) => {
					this.scrollRect = rect;
					this.maybeNotify();
				}));
				this.unsubs.push(this.options.observeElementOffset(this, (offset, isScrolling) => {
					this.scrollAdjustments = 0;
					this.scrollDirection = isScrolling ? this.getScrollOffset() < offset ? "forward" : "backward" : null;
					this.scrollOffset = offset;
					this.isScrolling = isScrolling;
					this.maybeNotify();
				}));
				this._scrollToOffset(this.getScrollOffset(), {
					adjustments: void 0,
					behavior: void 0
				});
			}
		};
		this.getSize = () => {
			if (!this.options.enabled) {
				this.scrollRect = null;
				return 0;
			}
			this.scrollRect = this.scrollRect ?? this.options.initialRect;
			return this.scrollRect[this.options.horizontal ? "width" : "height"];
		};
		this.getScrollOffset = () => {
			if (!this.options.enabled) {
				this.scrollOffset = null;
				return 0;
			}
			this.scrollOffset = this.scrollOffset ?? (typeof this.options.initialOffset === "function" ? this.options.initialOffset() : this.options.initialOffset);
			return this.scrollOffset;
		};
		this.getFurthestMeasurement = (measurements, index) => {
			const furthestMeasurementsFound = /* @__PURE__ */ new Map();
			const furthestMeasurements = /* @__PURE__ */ new Map();
			for (let m$1 = index - 1; m$1 >= 0; m$1--) {
				const measurement = measurements[m$1];
				if (furthestMeasurementsFound.has(measurement.lane)) continue;
				const previousFurthestMeasurement = furthestMeasurements.get(measurement.lane);
				if (previousFurthestMeasurement == null || measurement.end > previousFurthestMeasurement.end) furthestMeasurements.set(measurement.lane, measurement);
				else if (measurement.end < previousFurthestMeasurement.end) furthestMeasurementsFound.set(measurement.lane, true);
				if (furthestMeasurementsFound.size === this.options.lanes) break;
			}
			return furthestMeasurements.size === this.options.lanes ? Array.from(furthestMeasurements.values()).sort((a$1, b) => {
				if (a$1.end === b.end) return a$1.index - b.index;
				return a$1.end - b.end;
			})[0] : void 0;
		};
		this.getMeasurementOptions = memo(() => [
			this.options.count,
			this.options.paddingStart,
			this.options.scrollMargin,
			this.options.getItemKey,
			this.options.enabled,
			this.options.lanes
		], (count, paddingStart, scrollMargin, getItemKey, enabled, lanes) => {
			if (this.prevLanes !== void 0 && this.prevLanes !== lanes) this.lanesChangedFlag = true;
			this.prevLanes = lanes;
			this.pendingMeasuredCacheIndexes = [];
			return {
				count,
				paddingStart,
				scrollMargin,
				getItemKey,
				enabled,
				lanes
			};
		}, { key: false });
		this.getMeasurements = memo(() => [this.getMeasurementOptions(), this.itemSizeCache], ({ count, paddingStart, scrollMargin, getItemKey, enabled, lanes }, itemSizeCache) => {
			if (!enabled) {
				this.measurementsCache = [];
				this.itemSizeCache.clear();
				this.laneAssignments.clear();
				return [];
			}
			if (this.laneAssignments.size > count) {
				for (const index of this.laneAssignments.keys()) if (index >= count) this.laneAssignments.delete(index);
			}
			if (this.lanesChangedFlag) {
				this.lanesChangedFlag = false;
				this.lanesSettling = true;
				this.measurementsCache = [];
				this.itemSizeCache.clear();
				this.laneAssignments.clear();
				this.pendingMeasuredCacheIndexes = [];
			}
			if (this.measurementsCache.length === 0 && !this.lanesSettling) {
				this.measurementsCache = this.options.initialMeasurementsCache;
				this.measurementsCache.forEach((item) => {
					this.itemSizeCache.set(item.key, item.size);
				});
			}
			const min = this.lanesSettling ? 0 : this.pendingMeasuredCacheIndexes.length > 0 ? Math.min(...this.pendingMeasuredCacheIndexes) : 0;
			this.pendingMeasuredCacheIndexes = [];
			if (this.lanesSettling && this.measurementsCache.length === count) this.lanesSettling = false;
			const measurements = this.measurementsCache.slice(0, min);
			const laneLastIndex = new Array(lanes).fill(void 0);
			for (let m$1 = 0; m$1 < min; m$1++) {
				const item = measurements[m$1];
				if (item) laneLastIndex[item.lane] = m$1;
			}
			for (let i = min; i < count; i++) {
				const key = getItemKey(i);
				const cachedLane = this.laneAssignments.get(i);
				let lane;
				let start;
				if (cachedLane !== void 0 && this.options.lanes > 1) {
					lane = cachedLane;
					const prevIndex = laneLastIndex[lane];
					const prevInLane = prevIndex !== void 0 ? measurements[prevIndex] : void 0;
					start = prevInLane ? prevInLane.end + this.options.gap : paddingStart + scrollMargin;
				} else {
					const furthestMeasurement = this.options.lanes === 1 ? measurements[i - 1] : this.getFurthestMeasurement(measurements, i);
					start = furthestMeasurement ? furthestMeasurement.end + this.options.gap : paddingStart + scrollMargin;
					lane = furthestMeasurement ? furthestMeasurement.lane : i % this.options.lanes;
					if (this.options.lanes > 1) this.laneAssignments.set(i, lane);
				}
				const measuredSize = itemSizeCache.get(key);
				const size = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i);
				const end = start + size;
				measurements[i] = {
					index: i,
					start,
					size,
					end,
					key,
					lane
				};
				laneLastIndex[lane] = i;
			}
			this.measurementsCache = measurements;
			return measurements;
		}, {
			key: "getMeasurements",
			debug: () => this.options.debug
		});
		this.calculateRange = memo(() => [
			this.getMeasurements(),
			this.getSize(),
			this.getScrollOffset(),
			this.options.lanes
		], (measurements, outerSize, scrollOffset, lanes) => {
			return this.range = measurements.length > 0 && outerSize > 0 ? calculateRange({
				measurements,
				outerSize,
				scrollOffset,
				lanes
			}) : null;
		}, {
			key: "calculateRange",
			debug: () => this.options.debug
		});
		this.getVirtualIndexes = memo(() => {
			let startIndex = null;
			let endIndex = null;
			const range = this.calculateRange();
			if (range) {
				startIndex = range.startIndex;
				endIndex = range.endIndex;
			}
			this.maybeNotify.updateDeps([
				this.isScrolling,
				startIndex,
				endIndex
			]);
			return [
				this.options.rangeExtractor,
				this.options.overscan,
				this.options.count,
				startIndex,
				endIndex
			];
		}, (rangeExtractor, overscan, count, startIndex, endIndex) => {
			return startIndex === null || endIndex === null ? [] : rangeExtractor({
				startIndex,
				endIndex,
				overscan,
				count
			});
		}, {
			key: "getVirtualIndexes",
			debug: () => this.options.debug
		});
		this.indexFromElement = (node) => {
			const attributeName = this.options.indexAttribute;
			const indexStr = node.getAttribute(attributeName);
			if (!indexStr) {
				console.warn(`Missing attribute name '${attributeName}={index}' on measured element.`);
				return -1;
			}
			return parseInt(indexStr, 10);
		};
		this._measureElement = (node, entry) => {
			const index = this.indexFromElement(node);
			const item = this.measurementsCache[index];
			if (!item) return;
			const key = item.key;
			const prevNode = this.elementsCache.get(key);
			if (prevNode !== node) {
				if (prevNode) this.observer.unobserve(prevNode);
				this.observer.observe(node);
				this.elementsCache.set(key, node);
			}
			if (node.isConnected) this.resizeItem(index, this.options.measureElement(node, entry, this));
		};
		this.resizeItem = (index, size) => {
			const item = this.measurementsCache[index];
			if (!item) return;
			const delta = size - (this.itemSizeCache.get(item.key) ?? item.size);
			if (delta !== 0) {
				if (this.shouldAdjustScrollPositionOnItemSizeChange !== void 0 ? this.shouldAdjustScrollPositionOnItemSizeChange(item, delta, this) : item.start < this.getScrollOffset() + this.scrollAdjustments) {
					if (this.options.debug) console.info("correction", delta);
					this._scrollToOffset(this.getScrollOffset(), {
						adjustments: this.scrollAdjustments += delta,
						behavior: void 0
					});
				}
				this.pendingMeasuredCacheIndexes.push(item.index);
				this.itemSizeCache = new Map(this.itemSizeCache.set(item.key, size));
				this.notify(false);
			}
		};
		this.measureElement = (node) => {
			if (!node) {
				this.elementsCache.forEach((cached, key) => {
					if (!cached.isConnected) {
						this.observer.unobserve(cached);
						this.elementsCache.delete(key);
					}
				});
				return;
			}
			this._measureElement(node, void 0);
		};
		this.getVirtualItems = memo(() => [this.getVirtualIndexes(), this.getMeasurements()], (indexes, measurements) => {
			const virtualItems = [];
			for (let k = 0, len = indexes.length; k < len; k++) {
				const measurement = measurements[indexes[k]];
				virtualItems.push(measurement);
			}
			return virtualItems;
		}, {
			key: "getVirtualItems",
			debug: () => this.options.debug
		});
		this.getVirtualItemForOffset = (offset) => {
			const measurements = this.getMeasurements();
			if (measurements.length === 0) return;
			return notUndefined(measurements[findNearestBinarySearch(0, measurements.length - 1, (index) => notUndefined(measurements[index]).start, offset)]);
		};
		this.getMaxScrollOffset = () => {
			if (!this.scrollElement) return 0;
			if ("scrollHeight" in this.scrollElement) return this.options.horizontal ? this.scrollElement.scrollWidth - this.scrollElement.clientWidth : this.scrollElement.scrollHeight - this.scrollElement.clientHeight;
			else {
				const doc = this.scrollElement.document.documentElement;
				return this.options.horizontal ? doc.scrollWidth - this.scrollElement.innerWidth : doc.scrollHeight - this.scrollElement.innerHeight;
			}
		};
		this.getOffsetForAlignment = (toOffset, align, itemSize = 0) => {
			if (!this.scrollElement) return 0;
			const size = this.getSize();
			const scrollOffset = this.getScrollOffset();
			if (align === "auto") align = toOffset >= scrollOffset + size ? "end" : "start";
			if (align === "center") toOffset += (itemSize - size) / 2;
			else if (align === "end") toOffset -= size;
			const maxOffset = this.getMaxScrollOffset();
			return Math.max(Math.min(maxOffset, toOffset), 0);
		};
		this.getOffsetForIndex = (index, align = "auto") => {
			index = Math.max(0, Math.min(index, this.options.count - 1));
			const item = this.measurementsCache[index];
			if (!item) return;
			const size = this.getSize();
			const scrollOffset = this.getScrollOffset();
			if (align === "auto") if (item.end >= scrollOffset + size - this.options.scrollPaddingEnd) align = "end";
			else if (item.start <= scrollOffset + this.options.scrollPaddingStart) align = "start";
			else return [scrollOffset, align];
			if (align === "end" && index === this.options.count - 1) return [this.getMaxScrollOffset(), align];
			const toOffset = align === "end" ? item.end + this.options.scrollPaddingEnd : item.start - this.options.scrollPaddingStart;
			return [this.getOffsetForAlignment(toOffset, align, item.size), align];
		};
		this.isDynamicMode = () => this.elementsCache.size > 0;
		this.scrollToOffset = (toOffset, { align = "start", behavior } = {}) => {
			if (behavior === "smooth" && this.isDynamicMode()) console.warn("The `smooth` scroll behavior is not fully supported with dynamic size.");
			this._scrollToOffset(this.getOffsetForAlignment(toOffset, align), {
				adjustments: void 0,
				behavior
			});
		};
		this.scrollToIndex = (index, { align: initialAlign = "auto", behavior } = {}) => {
			if (behavior === "smooth" && this.isDynamicMode()) console.warn("The `smooth` scroll behavior is not fully supported with dynamic size.");
			index = Math.max(0, Math.min(index, this.options.count - 1));
			this.currentScrollToIndex = index;
			let attempts = 0;
			const maxAttempts = 10;
			const tryScroll = (currentAlign) => {
				if (!this.targetWindow) return;
				const offsetInfo = this.getOffsetForIndex(index, currentAlign);
				if (!offsetInfo) {
					console.warn("Failed to get offset for index:", index);
					return;
				}
				const [offset, align] = offsetInfo;
				this._scrollToOffset(offset, {
					adjustments: void 0,
					behavior
				});
				this.targetWindow.requestAnimationFrame(() => {
					if (!this.targetWindow) return;
					const verify = () => {
						if (this.currentScrollToIndex !== index) return;
						const currentOffset = this.getScrollOffset();
						const afterInfo = this.getOffsetForIndex(index, align);
						if (!afterInfo) {
							console.warn("Failed to get offset for index:", index);
							return;
						}
						if (!approxEqual(afterInfo[0], currentOffset)) scheduleRetry(align);
					};
					if (this.isDynamicMode()) this.targetWindow.requestAnimationFrame(verify);
					else verify();
				});
			};
			const scheduleRetry = (align) => {
				if (!this.targetWindow) return;
				if (this.currentScrollToIndex !== index) return;
				attempts++;
				if (attempts < maxAttempts) {
					if (this.options.debug) console.info("Schedule retry", attempts, maxAttempts);
					this.targetWindow.requestAnimationFrame(() => tryScroll(align));
				} else console.warn(`Failed to scroll to index ${index} after ${maxAttempts} attempts.`);
			};
			tryScroll(initialAlign);
		};
		this.scrollBy = (delta, { behavior } = {}) => {
			if (behavior === "smooth" && this.isDynamicMode()) console.warn("The `smooth` scroll behavior is not fully supported with dynamic size.");
			this._scrollToOffset(this.getScrollOffset() + delta, {
				adjustments: void 0,
				behavior
			});
		};
		this.getTotalSize = () => {
			var _a;
			const measurements = this.getMeasurements();
			let end;
			if (measurements.length === 0) end = this.options.paddingStart;
			else if (this.options.lanes === 1) end = ((_a = measurements[measurements.length - 1]) == null ? void 0 : _a.end) ?? 0;
			else {
				const endByLane = Array(this.options.lanes).fill(null);
				let endIndex = measurements.length - 1;
				while (endIndex >= 0 && endByLane.some((val) => val === null)) {
					const item = measurements[endIndex];
					if (endByLane[item.lane] === null) endByLane[item.lane] = item.end;
					endIndex--;
				}
				end = Math.max(...endByLane.filter((val) => val !== null));
			}
			return Math.max(end - this.options.scrollMargin + this.options.paddingEnd, 0);
		};
		this._scrollToOffset = (offset, { adjustments, behavior }) => {
			this.options.scrollToFn(offset, {
				behavior,
				adjustments
			}, this);
		};
		this.measure = () => {
			this.itemSizeCache = /* @__PURE__ */ new Map();
			this.laneAssignments = /* @__PURE__ */ new Map();
			this.notify(false);
		};
		this.setOptions(opts);
	}
};
var findNearestBinarySearch = (low, high, getCurrentValue, value) => {
	while (low <= high) {
		const middle = (low + high) / 2 | 0;
		const currentValue = getCurrentValue(middle);
		if (currentValue < value) low = middle + 1;
		else if (currentValue > value) high = middle - 1;
		else return middle;
	}
	if (low > 0) return low - 1;
	else return 0;
};
function calculateRange({ measurements, outerSize, scrollOffset, lanes }) {
	const lastIndex = measurements.length - 1;
	const getOffset = (index) => measurements[index].start;
	if (measurements.length <= lanes) return {
		startIndex: 0,
		endIndex: lastIndex
	};
	let startIndex = findNearestBinarySearch(0, lastIndex, getOffset, scrollOffset);
	let endIndex = startIndex;
	if (lanes === 1) while (endIndex < lastIndex && measurements[endIndex].end < scrollOffset + outerSize) endIndex++;
	else if (lanes > 1) {
		const endPerLane = Array(lanes).fill(0);
		while (endIndex < lastIndex && endPerLane.some((pos) => pos < scrollOffset + outerSize)) {
			const item = measurements[endIndex];
			endPerLane[item.lane] = item.end;
			endIndex++;
		}
		const startPerLane = Array(lanes).fill(scrollOffset + outerSize);
		while (startIndex >= 0 && startPerLane.some((pos) => pos >= scrollOffset)) {
			const item = measurements[startIndex];
			startPerLane[item.lane] = item.start;
			startIndex--;
		}
		startIndex = Math.max(0, startIndex - startIndex % lanes);
		endIndex = Math.min(lastIndex, endIndex + (lanes - 1 - endIndex % lanes));
	}
	return {
		startIndex,
		endIndex
	};
}

//#endregion
//#region node_modules/@tanstack/react-virtual/dist/esm/index.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_react_dom = require_react_dom();
var useIsomorphicLayoutEffect = typeof document !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
function useVirtualizerBase({ useFlushSync = true, ...options }) {
	const rerender = import_react.useReducer(() => ({}), {})[1];
	const resolvedOptions = {
		...options,
		onChange: (instance2, sync) => {
			var _a;
			if (useFlushSync && sync) (0, import_react_dom.flushSync)(rerender);
			else rerender();
			(_a = options.onChange) == null || _a.call(options, instance2, sync);
		}
	};
	const [instance] = import_react.useState(() => new Virtualizer(resolvedOptions));
	instance.setOptions(resolvedOptions);
	useIsomorphicLayoutEffect(() => {
		return instance._didMount();
	}, []);
	useIsomorphicLayoutEffect(() => {
		return instance._willUpdate();
	});
	return instance;
}
function useVirtualizer(options) {
	return useVirtualizerBase({
		observeElementRect,
		observeElementOffset,
		scrollToFn: elementScroll,
		...options
	});
}

//#endregion
//#region node_modules/@privy-io/react-auth/dist/esm/ConnectWalletView-CWvNDYJ-.mjs
var import_jsx_runtime = require_jsx_runtime();
var import_lib = require_lib();
var N = {
	phantom: { mobile: {
		native: "phantom://",
		universal: "https://phantom.app/ul/"
	} },
	solflare: { mobile: {
		native: void 0,
		universal: "https://solflare.com/ul/v1/"
	} },
	metamask: { image_url: {
		sm: H,
		md: H
	} }
};
var I$1 = class I$1 {
	static normalize(e$3) {
		return e$3.replace(/[-_]wallet$/, "").replace(/[-_]extension$/, "").toLowerCase();
	}
	isEth(e$3) {
		return e$3.chains.some(((e$4) => e$4.includes("eip155:")));
	}
	isSol(e$3) {
		return e$3.chains.some(((e$4) => e$4.includes("solana:")));
	}
	inAllowList(e$3, n$3) {
		if (!this.normalizedAllowList || 0 === this.normalizedAllowList.length || "listing" === n$3 && this.includeWalletConnect) return !0;
		let t$1 = I$1.normalize(e$3);
		return this.normalizedAllowList.some(((e$4) => t$1 === I$1.normalize(e$4)));
	}
	inDenyList(e$3, n$3) {
		return "listing" === n$3 && "rabby" === e$3 || "agw" === I$1.normalize(e$3);
	}
	chainMatches(e$3) {
		return "ethereum-only" === this.chainFilter ? "ethereum" === e$3 : "solana-only" !== this.chainFilter || "solana" === e$3;
	}
	getAllowListKey(e$3, n$3, t$1, l) {
		let i = I$1.normalize(e$3);
		for (let e$4 of this.normalizedAllowList || []) if (i === I$1.normalize(e$4)) return e$4;
		if ("connector" === n$3) {
			if (("injected" === t$1 || "solana_adapter" === t$1) && "ethereum" === l && this.detectedEth) return "detected_ethereum_wallets";
			if (("injected" === t$1 || "solana_adapter" === t$1) && "solana" === l && this.detectedSol) return "detected_solana_wallets";
		}
		if ("listing" === n$3 && this.includeWalletConnect) return "wallet_connect";
	}
	connectorOk(e$3) {
		return !!("null" !== e$3.connectorType && "walletconnect_solana" !== e$3.walletBranding.id && this.chainMatches(e$3.chainType) && (this.inAllowList(e$3.walletClientType, "connector") || ("injected" === e$3.connectorType || "solana_adapter" === e$3.connectorType) && ("ethereum" === e$3.chainType && this.detectedEth || "solana" === e$3.chainType && this.detectedSol)));
	}
	listingOk(e$3) {
		if (e$3.slug.includes("coinbase")) return !1;
		if ("ethereum-only" === this.chainFilter) {
			if (!this.isEth(e$3)) return !1;
		} else if ("solana-only" === this.chainFilter && !this.isSol(e$3)) return !1;
		return !(!this.inAllowList(e$3.slug, "listing") || this.inDenyList(e$3.slug, "listing"));
	}
	getWallets(e$3, n$3) {
		let t$1 = /* @__PURE__ */ new Map(), l = (e$4) => {
			let n$4 = t$1.get(e$4.id);
			if (n$4) {
				n$4.chainType !== e$4.chainType && (n$4.chainType = "multi");
				let t$2 = new Set(n$4.chains);
				e$4.chains.forEach(((e$5) => t$2.add(e$5))), n$4.chains = Array.from(t$2), !n$4.icon && e$4.icon && (n$4.icon = e$4.icon), !n$4.url && e$4.url && (n$4.url = e$4.url), !n$4.listing && e$4.listing && (n$4.listing = e$4.listing), !n$4.allowListKey && e$4.allowListKey && (n$4.allowListKey = e$4.allowListKey);
			} else t$1.set(e$4.id, e$4);
		};
		e$3.filter(((e$4) => this.connectorOk(e$4))).forEach(((e$4) => {
			let n$4 = I$1.normalize(e$4.walletClientType);
			l({
				id: n$4,
				label: e$4.walletBranding?.name ?? n$4,
				source: "connector",
				connector: e$4,
				chainType: e$4.chainType,
				icon: e$4.walletBranding?.icon,
				url: void 0,
				chains: ["ethereum" === e$4.chainType ? "eip155" : "solana"],
				allowListKey: this.getAllowListKey(e$4.walletClientType, "connector", e$4.connectorType, e$4.chainType)
			});
		}));
		let i = e$3.find(((e$4) => "wallet_connect_v2" === e$4.connectorType)), a$1 = e$3.find(((e$4) => "walletconnect_solana" === e$4.walletBranding.id));
		n$3.filter(((e$4) => this.listingOk(e$4))).forEach(((n$4) => {
			let t$2 = [...n$4.chains].filter(((e$4) => e$4.includes("eip155:") || e$4.includes("solana:")));
			if (e$3.some(((e$4) => I$1.normalize(e$4.walletClientType) === I$1.normalize(n$4.slug) && "ethereum" === e$4.chainType && "null" !== e$4.connectorType)) || i || n$4.mobile.native || n$4.mobile.universal || X[n$4.slug]?.chainTypes.includes("ethereum") || (t$2 = t$2.filter(((e$4) => !e$4.includes("eip155:")))), e$3.some(((e$4) => I$1.normalize(e$4.walletClientType) === I$1.normalize(n$4.slug) && "solana" === e$4.chainType && "null" !== e$4.connectorType)) || a$1 || n$4.mobile.native || n$4.mobile.universal || X[n$4.slug]?.chainTypes.includes("solana") || (t$2 = t$2.filter(((e$4) => !e$4.includes("solana:")))), !t$2.length) return;
			let o$3 = I$1.normalize(n$4.slug), c$1 = N[n$4.slug]?.image_url?.sm || n$4.image_url?.sm;
			t$2.some(((e$4) => e$4.includes("eip155:"))) && l({
				id: o$3,
				label: n$4.name || o$3,
				source: "listing",
				listing: n$4,
				chainType: "ethereum",
				icon: c$1,
				url: n$4.homepage,
				chains: t$2,
				allowListKey: this.getAllowListKey(n$4.slug, "listing")
			}), t$2.some(((e$4) => e$4.includes("solana:"))) && l({
				id: o$3,
				label: n$4.name || o$3,
				source: "listing",
				listing: n$4,
				chainType: "solana",
				icon: c$1,
				url: n$4.homepage,
				chains: t$2,
				allowListKey: this.getAllowListKey(n$4.slug, "listing")
			});
		})), this.includeWalletConnectQr && i && l({
			id: "wallet_connect_qr",
			label: "WalletConnect",
			source: "connector",
			connector: i,
			chainType: "ethereum",
			icon: ye,
			url: void 0,
			chains: ["eip155"],
			allowListKey: "wallet_connect_qr"
		}), this.includeWalletConnectQrSolana && a$1 && l({
			id: "wallet_connect_qr_solana",
			label: "WalletConnect",
			source: "connector",
			connector: a$1,
			chainType: "solana",
			icon: ye,
			url: void 0,
			chains: ["solana"],
			allowListKey: "wallet_connect_qr_solana"
		});
		let o$2 = Array.from(t$1.values());
		o$2.forEach(((e$4) => {
			let n$4 = N[e$4.listing?.slug || e$4.id];
			n$4?.image_url?.sm && (e$4.icon = n$4.image_url.sm);
		}));
		let r$1 = /* @__PURE__ */ new Map();
		return this.normalizedAllowList?.forEach(((e$4, n$4) => {
			r$1.set(I$1.normalize(e$4), n$4);
		})), {
			wallets: o$2.slice().sort(((e$4, n$4) => {
				if (e$4.allowListKey && n$4.allowListKey) {
					let t$3 = this.normalizedAllowList?.findIndex(((n$5) => I$1.normalize(n$5) === I$1.normalize(e$4.allowListKey))) ?? -1, l$2 = this.normalizedAllowList?.findIndex(((e$5) => I$1.normalize(e$5) === I$1.normalize(n$4.allowListKey))) ?? -1;
					if (t$3 !== l$2 && t$3 >= 0 && l$2 >= 0) return t$3 - l$2;
				}
				if (e$4.allowListKey && !n$4.allowListKey) return -1;
				if (!e$4.allowListKey && n$4.allowListKey) return 1;
				let t$2 = I$1.normalize(e$4.id), l$1 = I$1.normalize(n$4.id);
				"binance-defi" === t$2 ? t$2 = "binance" : "universalprofiles" === t$2 ? t$2 = "universal_profile" : "cryptocom-defi" === t$2 ? t$2 = "cryptocom" : "bitkeep" === t$2 && (t$2 = "bitget_wallet"), "binance-defi" === l$1 ? l$1 = "binance" : "universalprofiles" === l$1 ? l$1 = "universal_profile" : "cryptocom-defi" === l$1 ? l$1 = "cryptocom" : "bitkeep" === l$1 && (l$1 = "bitget_wallet");
				let i$1 = r$1.has(t$2), a$2 = r$1.has(l$1);
				return i$1 && a$2 ? r$1.get(t$2) - r$1.get(l$1) : i$1 ? -1 : a$2 ? 1 : "connector" === e$4.source && "listing" === n$4.source ? -1 : "listing" === e$4.source && "connector" === n$4.source ? 1 : e$4.label.toLowerCase().localeCompare(n$4.label.toLowerCase());
			})),
			walletCount: o$2.length
		};
	}
	constructor(e$3, n$3) {
		if (this.chainFilter = e$3, n$3 && n$3.length > 0) {
			if (this.normalizedAllowList = n$3.map(String), this.normalizedAllowList.includes("binance")) {
				let e$4 = this.normalizedAllowList.indexOf("binance");
				this.normalizedAllowList.splice(e$4 + 1, 0, "binance-defi-wallet");
			}
			if (this.normalizedAllowList.includes("bitget_wallet")) {
				let e$4 = this.normalizedAllowList.indexOf("bitget_wallet");
				this.normalizedAllowList.splice(e$4 + 1, 0, "bitkeep");
			}
		}
		this.detectedEth = this.normalizedAllowList?.includes("detected_ethereum_wallets") ?? !1, this.detectedSol = this.normalizedAllowList?.includes("detected_solana_wallets") ?? !1, this.includeWalletConnect = this.normalizedAllowList?.includes("wallet_connect") ?? !1, this.includeWalletConnectQr = this.normalizedAllowList?.includes("wallet_connect_qr") ?? !1, this.includeWalletConnectQrSolana = this.normalizedAllowList?.includes("wallet_connect_qr_solana") ?? !1;
	}
};
var U$1 = (t$1) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
	viewBox: "0 0 32 32",
	xmlns: "http://www.w3.org/2000/svg",
	...t$1,
	children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m0 0h32v32h-32z",
			fill: "#5469d4"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m15.997 5.333-.143.486v14.106l.143.143 6.548-3.87z",
			fill: "#c2ccf4"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m15.996 5.333-6.548 10.865 6.548 3.87z",
			fill: "#fff"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m15.997 21.306-.08.098v5.025l.08.236 6.552-9.227z",
			fill: "#c2ccf4"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m15.996 26.665v-5.36l-6.548-3.867z",
			fill: "#fff"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m15.995 20.07 6.548-3.87-6.548-2.976v6.847z",
			fill: "#8698e8"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m9.448 16.2 6.548 3.87v-6.846z",
			fill: "#c2ccf4"
		})
	]
}), $ = (t$1) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("svg", {
	viewBox: "0 0 32 32",
	xmlns: "http://www.w3.org/2000/svg",
	...t$1,
	children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("linearGradient", {
			id: "a",
			gradientUnits: "userSpaceOnUse",
			x1: "7.233",
			x2: "24.766",
			y1: "24.766",
			y2: "7.234",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("stop", {
					offset: "0",
					stopColor: "#9945ff"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("stop", {
					offset: ".2",
					stopColor: "#7962e7"
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("stop", {
					offset: "1",
					stopColor: "#00d18c"
				})
			]
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			d: "m0 0h32v32h-32z",
			fill: "#10111a"
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", {
			clipRule: "evenodd",
			d: "m9.873 20.41a.645.645 0 0 1 .476-.21l14.662.012a.323.323 0 0 1 .238.54l-3.123 3.438a.643.643 0 0 1 -.475.21l-14.662-.012a.323.323 0 0 1 -.238-.54zm15.376-2.862a.322.322 0 0 1 -.238.54l-14.662.012a.642.642 0 0 1 -.476-.21l-3.122-3.44a.323.323 0 0 1 .238-.54l14.662-.012a.644.644 0 0 1 .475.21zm-15.376-9.738a.644.644 0 0 1 .476-.21l14.662.012a.322.322 0 0 1 .238.54l-3.123 3.438a.643.643 0 0 1 -.475.21l-14.662-.012a.323.323 0 0 1 -.238-.54z",
			fill: "url(#a)",
			fillRule: "evenodd"
		})
	]
});
var F = ut.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`, D = ut.button`
  padding: 0.25rem;
  height: 30px;
  width: 30px;

  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--privy-border-radius-full);
  background: var(--privy-color-background-2);
`, M$1 = ut.div`
  position: relative;
  display: inline-flex;
  align-items: center;

  &::after {
    content: ' ';
    border-radius: var(--privy-border-radius-full);
    height: 6px;
    width: 6px;
    background-color: var(--privy-color-icon-success);
    position: absolute;
    right: -3px;
    top: -3px;
  }
`, P = ut.img`
  width: 32px;
  height: 32px;
  border-radius: 0.25rem;
  object-fit: contain;
`, Q = ut.span`
  display: flex;
  gap: 0.25rem;
  align-items: center;
  padding: 0.25rem 0.5rem;
  font-size: 0.75rem;
  font-weight: 500;
  line-height: 1.125rem; /* 150% */
  border-radius: var(--privy-border-radius-sm);
  background-color: var(--privy-color-background-2);

  svg {
    width: 100%;
    max-width: 1rem;
    max-height: 1rem;
    stroke-width: 2;
  }
`, H$1 = ut.div`
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  max-height: 24rem;
  overflow-y: scroll;

  &::-webkit-scrollbar {
    display: none;
  }

  scrollbar-gutter: stable both-edges;
  scrollbar-width: none;
  -ms-overflow-style: none;

  ${(e$3) => "light" === e$3.$colorScheme ? "background: linear-gradient(var(--privy-color-background), var(--privy-color-background) 70%) bottom, linear-gradient(rgba(0, 0, 0, 0) 20%, rgba(0, 0, 0, 0.06)) bottom;" : "dark" === e$3.$colorScheme ? "background: linear-gradient(var(--privy-color-background), var(--privy-color-background) 70%) bottom, linear-gradient(rgba(255, 255, 255, 0) 20%, rgba(255, 255, 255, 0.06)) bottom;" : void 0}

  background-repeat: no-repeat;
  background-size:
    100% 32px,
    100% 16px;
  background-attachment: local, scroll;
`;
function V({ enabled: e$3 = !0, walletList: n$3, walletChainType: t$1 }) {
	let l = re(), { connectors: i } = I(), { listings: r$1, loading: c$1 } = U(e$3), s$1 = t$1 ?? l.appearance.walletChainType, d$1 = n$3 ?? l.appearance?.walletList, h$1 = (0, import_react.useMemo)((() => new I$1(s$1, d$1)), [s$1, d$1]), { wallets: m$1, walletCount: u } = (0, import_react.useMemo)((() => h$1.getWallets(i, r$1)), [
		h$1,
		i,
		r$1
	]), [p, g$1] = (0, import_react.useState)(""), w$1 = (0, import_react.useMemo)((() => p ? m$1.filter(((e$4) => e$4.label.toLowerCase().includes(p.toLowerCase()))) : m$1), [p, m$1]), [f, v] = (0, import_react.useState)();
	return {
		selected: f,
		setSelected: v,
		search: p,
		setSearch: g$1,
		loadingListings: c$1,
		wallets: w$1,
		walletCount: u
	};
}
var R = (e$3) => !e$3 || "string" != typeof e$3 && (e$3 instanceof G || e$3 instanceof Z);
var Y = ({ index: l, style: i, data: a$1, recent: o$2 }) => {
	let r$1 = a$1.wallets[l], { walletChainType: c$1, handleWalletClick: s$1 } = a$1, { t: d$1 } = n$2(), h$1 = {
		...i,
		boxSizing: "border-box"
	};
	return r$1 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Z$1, {
		style: h$1,
		onClick: () => s$1(r$1),
		children: [
			r$1.icon && (r$1.connector && !R(r$1.connector) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(M$1, { children: "string" == typeof r$1.icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(P, { src: r$1.icon }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(r$1.icon, { style: {
				width: "32px",
				height: "32px"
			} }) }) : "string" == typeof r$1.icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(P, { src: r$1.icon }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(r$1.icon, { style: {
				width: "32px",
				height: "32px"
			} })),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(te, { children: r$1.label }),
			o$2 ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Q, { children: d$1("connectWallet.lastUsed") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ee, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: ["ethereum-only" === c$1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(U$1, {}), "solana-only" === c$1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)($, {})] }) })] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ee, { children: !("ethereum-only" === c$1 || "solana-only" === c$1) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [r$1.chains?.some(((e$3) => e$3.startsWith("eip155"))) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(U$1, {}), r$1.chains?.some(((e$3) => e$3.startsWith("solana"))) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)($, {})] }) })
		]
	}) : null;
};
var G$1 = ({ className: h$1, customDescription: p, connectOnly: C$1, preSelectedWalletId: T, hideHeader: S, ...F$1 }) => {
	let D$1 = re(), { t: M$2 } = n$2(), { connectors: Q$1 } = I(), H$2 = F$1.walletChainType || D$1.appearance.walletChainType, G$2 = F$1.walletList || D$1.appearance?.walletList, { onBack: ae$1, onClose: oe, app: re$1 } = F$1, { selected: ce, setSelected: se, qrUrl: de$1, setQrUrl: he, connecting: me, uiState: ue, errorCode: pe, wallets: ge, walletCount: we, handleConnect: fe, handleBack: ye$1, showSearchBar: ve, isInitialConnectView: be, title: Ce$1, search: xe, setSearch: Te } = function({ onConnect: e$3, onBack: n$3, onClose: t$1, onConnectError: l, walletList: i, walletChainType: c$1, app: s$1 }) {
		let h$2 = re(), { connectors: m$1 } = I(), { t: u } = n$2(), { wallets: p$1, walletCount: g$1, search: w$1, setSearch: f, selected: C$2, setSelected: x } = V({
			enabled: M(i ?? []),
			walletList: i,
			walletChainType: c$1
		}), [T$1, L] = (0, import_react.useState)(), [k, z] = (0, import_react.useState)(), [S$1, E$1] = (0, import_react.useState)(), [K, O] = (0, import_react.useState)(), q = !C$2 && !S$1 && !K, N$1 = q && (g$1 > 6 || w$1.length > 0), I$2 = m$1.find(((e$4) => "wallet_connect_v2" === e$4.connectorType)), U$2 = (0, import_react.useCallback)((async (n$4, t$2) => {
			if (!n$4) return;
			let i$1 = t$2?.name ?? "Wallet";
			if (K?.connector !== n$4 || "loading" !== T$1) {
				if (L("loading"), "string" == typeof n$4) return ae.debug("Connecting wallet via deeplink", {
					wallet: i$1,
					url: n$4.length > 80 ? `${n$4.slice(0, 80)}...` : n$4
				}), O({
					connector: n$4,
					name: i$1,
					icon: t$2?.icon,
					id: t$2?.id,
					url: t$2?.url
				}), void window.open(n$4, "_blank");
				ae.debug("Connecting wallet via connector", {
					wallet: i$1,
					connectorType: n$4.connectorType
				}), O({
					connector: n$4,
					name: t$2?.name ?? n$4.walletBranding.name ?? "Wallet",
					icon: t$2?.icon ?? n$4.walletBranding.icon,
					id: t$2?.id,
					url: t$2?.url
				});
				try {
					let t$3 = await n$4.connect({ showPrompt: !0 });
					if (!t$3) return ae.warn("Wallet connection returned null", {
						wallet: i$1,
						connectorType: n$4.connectorType
					}), L("error"), z(void 0), void l?.(new o$1("Unable to connect wallet"));
					ae.debug("Wallet connection successful", {
						wallet: i$1,
						connectorType: n$4.connectorType
					}), L("success"), z(void 0), Ue({
						address: t$3.address,
						client: t$3.walletClientType,
						appId: h$2.id
					}), setTimeout((() => {
						e$3({
							connector: n$4,
							wallet: t$3
						});
					}), g);
				} catch (e$4) {
					if (e$4?.message?.includes("already pending for origin") || e$4?.message?.includes("wallet_requestPermissions")) return void ae.debug("Connection request already pending, maintaining loading state", { wallet: i$1 });
					let t$3 = e$4 instanceof Error ? e$4.message : String(e$4?.message || "Unknown error");
					ae.error("Wallet connection failed", e$4, {
						wallet: i$1,
						connectorType: n$4.connectorType,
						errorCode: e$4?.privyErrorCode
					}), L("error"), z(e$4?.privyErrorCode), l?.(e$4 instanceof Error ? e$4 : new o$1(t$3 || "Unable to connect wallet"));
				}
			} else ae.debug("Duplicate connection attempt prevented", { wallet: i$1 });
		}), [
			h$2.id,
			e$3,
			K,
			T$1
		]), $$1 = (0, import_react.useCallback)((() => S$1 ? (L(void 0), z(void 0), O(void 0), void E$1(void 0)) : K ? (L(void 0), z(void 0), void O(void 0)) : C$2 ? (L(void 0), z(void 0), O(void 0), void x(void 0)) : "error" === T$1 || "loading" === T$1 ? (L(void 0), z(void 0), void O(void 0)) : void n$3?.()), [
			S$1,
			K,
			C$2,
			T$1,
			n$3
		]);
		return {
			selected: C$2,
			setSelected: x,
			qrUrl: S$1,
			setQrUrl: E$1,
			connecting: K,
			uiState: T$1,
			errorCode: k,
			search: w$1,
			setSearch: f,
			wallets: p$1,
			walletCount: g$1,
			wc: I$2,
			isInitialConnectView: q,
			showSearchBar: N$1,
			title: (0, import_react.useMemo)((() => K?.connector === I$2 && S$1 && import_lib.isMobile && K?.name ? u("connectWallet.goToWallet", { walletName: K.name }) : K?.connector === I$2 && S$1 && K?.name ? u("connectWallet.scanToConnect", { walletName: K.name }) : S$1 && K?.name ? u(import_lib.isMobile ? "connectWallet.goToWallet" : "connectWallet.scanToConnect", { walletName: K.name }) : "string" == typeof K?.connector ? u("connectWallet.openOrInstall", { walletName: K.name }) : C$2 && !K ? u("connectWallet.selectNetwork") : K ? null : u("connectWallet.selectYourWallet")), [
				K,
				S$1,
				C$2,
				I$2,
				u
			]),
			handleConnect: U$2,
			handleBack: $$1,
			onClose: t$1,
			onConnect: e$3,
			app: s$1
		};
	}({
		...F$1,
		walletList: G$2,
		walletChainType: H$2
	}), _e = Q$1.find(((e$3) => "wallet_connect_v2" === e$3.connectorType)), We = Q$1.find(((e$3) => "walletconnect_solana" === e$3.walletBranding.id)), Le = (0, import_react.useRef)(null), ke = useVirtualizer({
		count: ge.length,
		getScrollElement: () => Le.current,
		estimateSize: () => 56,
		overscan: 6,
		gap: 5
	}), ze = (0, import_react.useCallback)((async (e$3) => {
		let n$3 = "solana-only" !== H$2 && e$3.chains?.some(((e$4) => e$4.startsWith("eip155"))), t$1 = "ethereum-only" !== H$2 && e$3.chains?.some(((e$4) => e$4.startsWith("solana"))), l = () => {
			let n$4 = e$3.id;
			return X[n$4] || X[`${n$4}_wallet`];
		}, i = (n$4) => {
			let t$2 = I$1.normalize(e$3.id);
			return Q$1.find(((e$4) => I$1.normalize(e$4.walletClientType) === t$2 && e$4.chainType === n$4 && "wallet_connect_v2" !== e$4.connectorType && !("ethereum" === e$4.chainType && e$4 instanceof G || "solana" === e$4.chainType && e$4 instanceof Z)));
		}, a$1 = async () => {
			if (!_e || !e$3.listing) return !1;
			let n$4 = N[e$3.listing.slug] ? {
				...e$3.listing,
				...N[e$3.listing.slug]
			} : e$3.listing;
			return _e.setWalletEntry(n$4, he), await _e.resetConnection(e$3.id), await fe(_e, {
				name: e$3.label,
				icon: e$3.icon,
				id: e$3.id,
				url: e$3.url
			}), !0;
		}, o$2 = async () => !!We && !!e$3.listing && (await We.disconnect(), We.wallet.setWalletEntry(e$3.listing, he), await new Promise(((e$4) => setTimeout(e$4, 100))), await fe(We, {
			name: e$3.label,
			icon: e$3.icon,
			id: e$3.id,
			url: e$3.url
		}), !0), r$1 = async (n$4) => {
			let t$2 = ((e$4) => {
				let n$5 = l();
				if (n$5) return n$5.getMobileRedirect({
					isSolana: e$4,
					connectOnly: !!C$1,
					useUniversalLink: !1
				});
			})(n$4);
			return !!t$2 && (await fe(t$2, {
				name: e$3.label,
				icon: e$3.icon,
				id: e$3.id,
				url: e$3.url
			}), !0);
		};
		if (n$3 && t$1) se(e$3);
		else {
			if (n$3 && !t$1) {
				let n$4 = i("ethereum");
				if (n$4 && !R(n$4)) return ae.debug("Attempting injected EVM connection", {
					wallet: e$3.id,
					connectorType: n$4.connectorType
				}), void await fe(n$4, {
					name: e$3.label,
					icon: e$3.icon,
					id: e$3.id,
					url: e$3.url
				});
				if (import_lib.isMobile && l()) {
					if (await r$1(!1) || await a$1()) return;
				} else if (await a$1() || await r$1(!1)) return;
			}
			if (t$1 && !n$3) {
				let n$4 = i("solana");
				if (n$4 && !R(n$4)) return ae.debug("Attempting injected Solana connection", {
					wallet: e$3.id,
					connectorType: n$4.connectorType
				}), void await fe(n$4, {
					name: e$3.label,
					icon: e$3.icon,
					id: e$3.id,
					url: e$3.url
				});
				if (import_lib.isMobile) {
					if (await r$1(!0) || await o$2()) return;
				} else if (await o$2() || await r$1(!0)) return;
			}
			if (!R(e$3.connector)) {
				if (ae.debug("Using fallback direct connector", {
					wallet: e$3.id,
					connectorType: e$3.connector?.connectorType
				}), _e && "wallet_connect_v2" === e$3.connector?.connectorType) if (await _e.resetConnection(e$3.id), "wallet_connect_qr" !== e$3.id && e$3.listing) {
					let n$4 = N[e$3.listing.slug] ? {
						...e$3.listing,
						...N[e$3.listing.slug]
					} : e$3.listing;
					_e.setWalletEntry(n$4, he);
				} else _e.setWalletEntry({
					id: "wallet_connect_qr",
					name: "WalletConnect",
					rdns: "",
					slug: "wallet-connect",
					homepage: "",
					chains: ["eip155"],
					mobile: {
						native: "",
						universal: void 0
					}
				}, he);
				We && "walletconnect_solana" === e$3.connector?.walletBranding.id && (await We.disconnect(), "wallet_connect_qr_solana" !== e$3.id && e$3.listing ? We.wallet.setWalletEntry(e$3.listing, he) : We.wallet.setWalletEntry({
					id: "wallet_connect_solana_qr",
					name: "WalletConnect",
					rdns: "",
					slug: "wallet-connect-solana",
					homepage: "",
					chains: ["solana"],
					mobile: {
						native: "",
						universal: void 0
					}
				}, he), await new Promise(((e$4) => setTimeout(e$4, 100)))), await fe(e$3.connector, {
					name: e$3.label,
					icon: e$3.icon,
					id: e$3.id,
					url: e$3.url
				});
				return;
			}
			e$3.url ? await fe(e$3.url, {
				name: e$3.label,
				icon: e$3.icon,
				id: e$3.id,
				url: e$3.url
			}) : ae.warn("No available connection method for wallet", { wallet: e$3.id });
		}
	}), [
		_e,
		We,
		fe,
		se,
		he,
		H$2,
		C$1,
		Q$1
	]);
	return (0, import_react.useEffect)((() => {
		if (!T) return;
		let e$3 = ge.find((({ id: e$4 }) => e$4 === T));
		e$3 && ze(e$3).catch(console.error);
	}), [T]), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(w, {
		className: h$1,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(w.Header, {
				icon: S && be ? void 0 : me && !de$1 || de$1 && import_lib.isMobile && me?.icon ? me.icon : me ? void 0 : e$2,
				iconVariant: me && !de$1 || de$1 && import_lib.isMobile ? "loading" : void 0,
				iconLoadingStatus: me && !de$1 || de$1 && import_lib.isMobile ? {
					success: "success" === ue,
					fail: "error" === ue
				} : void 0,
				title: S && be ? void 0 : me && !de$1 ? M$2("connectWallet.waitingForWallet", { walletName: me.name }) : de$1 && import_lib.isMobile ? M$2("connectWallet.waitingForWallet", { walletName: me?.name ?? "connection" }) : Ce$1,
				subtitle: S && be ? void 0 : me && !de$1 && "string" == typeof me.connector ? M$2("connectWallet.installAndConnect", { walletName: me.name }) : me && !de$1 && "string" != typeof me.connector ? "error" === ue ? pe === E.NO_SOLANA_ACCOUNTS ? `The connected wallet has no Solana accounts. Please add a Solana account in ${me.name} and try again.` : M$2("connectWallet.tryConnectingAgain") : M$2("connectionStatus.connectOneWallet") : be ? p ?? (re$1 ? M$2("connectWallet.connectToAccount", { appName: re$1.name }) : null) : null,
				showBack: !!ae$1 || !be,
				showClose: !0,
				onBack: ae$1 || ye$1,
				onClose: oe
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(w.Body, {
				ref: Le,
				$colorScheme: D$1.appearance.palette.colorScheme,
				style: { marginBottom: de$1 ? "0.5rem" : void 0 },
				children: [
					ve && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(J, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(e$1, {
						style: { background: "transparent" },
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(de, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MagnifyingGlassIcon_default, {}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
							className: "login-method-button",
							type: "text",
							placeholder: M$2("connectWallet.searchPlaceholder", { count: String(we) }),
							onChange: (e$3) => Te(e$3.target.value),
							value: xe
						})]
					}) }),
					de$1 && import_lib.isMobile && "loading" === ue && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						style: {
							display: "flex",
							flexDirection: "column",
							alignItems: "center",
							gap: "1rem"
						},
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(y, {
							variant: "primary",
							onClick: () => window.open(de$1.universal ?? de$1.native, "_blank"),
							style: { width: "100%" },
							children: M$2("connectWallet.openInApp")
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(le, {
							value: de$1.universal ?? de$1.native,
							iconOnly: !0,
							children: "Copy link"
						})]
					}),
					de$1 && !import_lib.isMobile && "loading" === ue && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						style: {
							display: "flex",
							flexDirection: "column",
							alignItems: "center",
							gap: "1rem"
						},
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(le, {
							value: de$1.universal ?? de$1.native,
							iconOnly: !0,
							children: M$2("connectWallet.copyLink")
						})
					}),
					de$1 && !import_lib.isMobile && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(C, {
						size: 280,
						url: de$1.universal ?? de$1.native,
						squareLogoElement: me?.icon ? "string" == typeof me.icon ? (e$3) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
							...e$3,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("image", {
								href: me.icon,
								height: e$3.height,
								width: e$3.width
							})
						}) : me.icon : Ce
					}),
					de$1 && !import_lib.isMobile && me?.url && ("binance" === me.id || "binanceus" === me.id || "binance-defi" === me.id) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ie, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [
						"Don't have ",
						me.name,
						"? "
					] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(n$1, {
						href: me.url,
						target: "_blank",
						size: "sm",
						children: "Download here"
					})] }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(X$1, { children: [
						me && !de$1 && "string" == typeof me.connector && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Z$1, {
							onClick: () => window.open(me.connector, "_blank"),
							children: [me.icon && ("string" == typeof me.icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(P, { src: me.icon }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(me.icon, {})), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(te, { children: me.name })]
						}),
						ce?.chains.some(((e$3) => e$3.startsWith("eip155"))) && !me && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Z$1, {
							onClick: () => ze({
								...ce,
								chains: ce.chains.filter(((e$3) => e$3.startsWith("eip155")))
							}),
							children: [
								ce.icon && ("string" == typeof ce.icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(P, { src: ce.icon }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ce.icon, {})),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(te, { children: ce.label }),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ee, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(U$1, {}) })
							]
						}),
						ce?.chains.some(((e$3) => e$3.startsWith("solana"))) && !me && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Z$1, {
							onClick: () => ze({
								...ce,
								chains: ce.chains.filter(((e$3) => e$3.startsWith("solana")))
							}),
							children: [
								ce.icon && ("string" == typeof ce.icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(P, { src: ce.icon }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ce.icon, {})),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(te, { children: ce.label }),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ee, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)($, {}) })
							]
						}),
						be && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [!(we > 0) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ne, { children: M$2("connectWallet.noWalletsFound") }), we > 0 && !de$1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							style: {
								maxHeight: 56 * Math.min(ge.length, 5) + 5,
								width: "100%"
							},
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								style: {
									height: `${ke.getTotalSize()}px`,
									width: "100%",
									position: "relative"
								},
								children: ke.getVirtualItems().map(((e$3) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Y, {
									index: e$3.index,
									style: {
										position: "absolute",
										top: 0,
										left: 0,
										height: `${e$3.size}px`,
										transform: `translateY(${e$3.start}px)`
									},
									data: {
										wallets: ge,
										walletChainType: H$2,
										handleWalletClick: ze
									}
								}, e$3.key)))
							})
						})] })
					] })
				]
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(w.Footer, { children: [
				me && !de$1 && "string" != typeof me.connector && "error" === ue && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(w.Actions, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(y, {
					style: {
						width: "100%",
						alignItems: "center"
					},
					variant: "error",
					onClick: () => fe(me.connector, {
						name: me.name,
						icon: me.icon,
						id: me.id,
						url: me.url
					}),
					children: M$2("connectWallet.retry")
				}) }),
				!!(re$1 && re$1.legal.privacyPolicyUrl && re$1.legal.termsAndConditionsUrl) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(h, {
					app: re$1,
					alwaysShowImplicitConsent: !0
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(w.Watermark, {})
			] })
		]
	});
};
var J = ut.div`
  position: sticky;
  // Offset by negative margin to account for focus outline
  margin-top: -3px;
  padding-top: 3px;
  top: -3px;
  z-index: 1;
  background: var(--privy-color-background);
  padding-bottom: calc(var(--screen-space) / 2);
`, X$1 = ut.div`
  display: flex;
  flex-direction: column;
  gap: ${5}px;
`, Z$1 = ut.button`
  && {
    gap: 0.5rem;
    align-items: center;
    display: flex;
    position: relative;
    text-align: left;
    font-weight: 500;
    transition: background 200ms ease-in;
    width: calc(100% - 4px);
    border-radius: var(--privy-border-radius-md);
    padding: 0.75em;
    border: 1px solid var(--privy-color-foreground-4);
    justify-content: space-between;
  }

  &:hover {
    background: var(--privy-color-background-2);
  }
`, ee = ut.span`
  display: flex;
  align-items: center;
  justify-content: end;
  position: relative;

  & > svg {
    border-radius: var(--privy-border-radius-full);
    stroke-width: 2.5;
    width: 100%;
    max-height: 1rem;
    max-width: 1rem;
    flex-shrink: 0;
  }

  & > svg:not(:last-child) {
    border-radius: var(--privy-border-radius-full);
    margin-right: -0.375rem;
  }
`, ne = ut.div`
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
`, te = ut.span`
  text-overflow: ellipsis;
  white-space: nowrap;
  color: var(--privy-color-foreground);
  font-weight: 400;
  flex: 1;
`, le = ut(m)`
  && {
    margin: 0.5rem auto 0 auto;
  }
`, ie = ut.div`
  text-align: center;
  margin-top: 1rem;
  font-size: 0.875rem;
  font-weight: 400;
  color: var(--privy-color-foreground-3);
`;

//#endregion
export { Y as i, I$1 as n, V as r, G$1 as t };
//# sourceMappingURL=ConnectWalletView-CWvNDYJ--v84Ei_Hj.js.map